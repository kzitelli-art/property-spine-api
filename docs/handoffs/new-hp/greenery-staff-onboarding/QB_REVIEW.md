# QB review of staff onboarding — September 12, 2026

Fable's `e0abebd78b8eef13d2f7cfbd67d38be9bcf3f26b` was independently fetched and its full configured CI run `34658921279` (run number 466) confirmed successful at that SHA. This successor is tested on the currently released API `992a8db4750722cb64ef116a8e6ba47c19872351` plus Fable's patch. No app change is required.

The repaired phone conflict target made an existing organization-authority defect reachable for phone-only accounts: the organization invite could update an existing account from another organization. The email branch already had the same gap. Both conflict updates now require the existing account to belong to the acting organization and exclude platform super-admin accounts. The condition is checked by PostgreSQL under the conflicting row's lock. An existing account outside that scope receives 409 `existing_account_authority_conflict`, without changing the account or creating a team assignment. User provisioning and assignment now share a transaction, so an assignment error cannot leave a partially provisioned account.

An existing account with no organization also does not become organization-owned through this admin door. Existing staff should use the governed Team invite and person-confirmation path. Same-organization phone-only provisioning and Fable's omitted-platform-role preservation continue to work. The super-admin door retains its explicit platform authority.

The expanded `tests/proofs/greenery_staff_onboarding.db.js` passed **52 checks, zero failures**, on real PostgreSQL and the real HTTP server, with the canonical nonce boundary and fake message transport. Schema ceiling 194 / 182 applied migrations. Coverage includes organization separation for phone and email, privileged account preservation, no duplicate identity, same-organization provisioning, property adoption and refusal to reparent, Team person confirmation, OTP acceptance, Leasing access at both properties, repeat invitations, and the no-line delivery branch. The no-line branch now requires a successful property selection rather than silently skipping when a previously revoked session is used.

The existing inactive assignment editor also works: super-admin `PATCH /admin/users/:userId/assignments/:assignmentId` reactivates the same row, the property selector issues a new session, and that session can issue a governed Team invite. This corrects the earlier receipt's overbroad statement that the two admin invite doors are the only way to obtain initial property access. When a valid inactive assignment already exists, reactivation is an existing alternative. No new onboarding workflow was added.

Proof source uses the actual selector's session rotation; a return to the original property uses its newly issued token. Local evidence: workspace `tmp/fable-review-successor-qb.log`. Raw local logs contain synthetic credentials and are not committed. The original Fable witness and 39-check evidence remain historical receipts for their recorded source and fixture; the expanded successor is not a claim of replaying that older fixture unchanged.

All 56 source-governance gates passed; modified JavaScript syntax and `git diff --check` passed. No production identity, property adoption, assignment, invitation, provider action or migration was performed. This is owned DB/HTTP evidence, not Mike's production acceptance. The Greenery's first organization adoption still requires the correct organization to be established.
