# QB review: nonlocking projections and explicit staff authority

September 12. Claude cfdd445ed1ea4216422ff2c7a847a99e1cedbbe1 is based directly on released API1320c2c. Exact CI34702747189/run482 completed successfully. Its original99/4 witness and103/0 successor are retained historical evidence, not the counts for this strengthened review.

## Correction and independent challenge

The Leasing desk composes application review inside a read-only transaction. Its shared offer reader took FOR UPDATE, which PostgreSQL refuses; one bound application could make the property desk return503. Claude added an internal nonlocking-read option and kept the default writer locks.

Independent QB review found the acknowledged-offer plus pending-successor branch dropped that option. It also found the standing projection still defaulted to locking. Owned real PostgreSQL witnessed both: SQLSTATE25006 in the historical branch, and a returned standing object containing read_failed plus aborted-transaction failures. The successor passes the option through the historical branch and explicitly opts the standing projection into nonlocking reads. Writer authority, row locks, currentness, target identity and acknowledged economics are unchanged.

The historical regression uses an explicitly synthetic retained-state fixture on its own unit/space, outside the full journey's standard BedB. Its six DB/HTTP checks pass: acknowledged1200 and pending1350 remain separate, standing has no swallowed read failures, desk200 and applicant acknowledgement remains on its original version. It is a reader regression, not proof that this fixture was authored through public writer doors.

## Combined owned journey

Root combined portable fixes f3c2155 and assertion successor e4c08f1 as tested source6293f23. Integration src and the new/strengthened proof files byte-match that tested tree; later differences are registration/docs. On the same fresh owned database after the separate historical fixture, the real HTTP no-consent journey passed **155 checks,0 failures,3 observations**. Mike joins through governed invite/OTP. It completes through an executed pending lease on the exact bed, without claiming activation, possession or a real message.

- Mike's valid terms confirmation explicitly returns403/not_authorized_for_terms; refusal snapshots show no confirmation, event or economic changes. KZ's override cannot confirm unacknowledged economics either.
- KZ's valid confirmation explicitly reports managed_role_override and the canonical actor. Mike generates the packet and issues signing links directly; no fallback actor rescues failures. The same person/application/property/offer/confirmation identities are checked.
- Desk and queue are reread at ten material transitions. Scope and application identity are checked. The final selector must return200 before absence is interpreted. Person Card must carry the exact pending-bed tenancy and no hidden read_failed uncertainty.
- Lead-source and occupied-sibling fixture writes now precede the first invite/OTP action. Later direct SQL is read-only. The original fixture prelude still cancels prior nonterminal leases on standard BedB, so this is not a twice-executed same-bed history-preservation claim.

The unit branch/mode test and existing offer/read tests are now registered in the ordinary full runner. The historical DB regression is registered before the no-consent journey. Source gates passed56/56 on the correction lane; full combined CI is the next gate and is not claimed by this local receipt.

## Cleanup and scope

The earlier nonce85f867e1903ad1ab090dd927 was canonically dropped before preparing the distinct-target fixture. Root's combined run used fresh nonce d0f13826c88afe4821417626, real182 applied migrations/194 ceiling, instrument fixture and fenced fake transports. Root canonically dropped that owned database after success, stopped its own PostgreSQL17 cluster55449 and verified no listeners on55449/3350. Other clusters and shared databases were untouched. Local console evidence is workspace handoffs/new-hp/NO_CONSENT_QB_LOCAL_20260912.log; the final completed nonce manifest is workspace tmp/proposed-terms-manifest.completed.json.

This source does not grant Mike new authority or accept real KZ signer/configuration. It does not prove the production Tour times setup path, email delivery or actual-property launch readiness. The person/role names are a synthetic authority shape. Approval and terms authority derive from obligations/assignments, not a hardcoded personal requirement that KZ always act.

No new Greenery-only-blocker conclusion follows. Existing retireInventoryUnits is a canonical retirement primitive; the staff-facing door and approved physical-identity reconciliation are separate. Greenery still needs its actual reviewed mapping/custody/classification and operating setup. Automatic inventory mapping, real-source activation and launch acceptance remain outside this desk correction.
