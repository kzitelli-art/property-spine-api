# Expected-ready association correction, September 11

Baseline: released API `24f4482140bed554185fe4fdcbb7c2366a5d3c07`, paired app `b00cf4993e4f699cd174a7ef5c8a27ca4b5507c4`. Isolated branch `codex/expected-ready-20260911`. No schema change, push or deployment in this lane.

Intention: an expected-ready date must apply to the exact home it describes, without changing possession, lease rights or confirmed readiness. Existing owner: `turnovers` retains the date; `availabilityRead` projects it into canonical leasing availability, application target reads/authority, turn priority and Unit Turn. History: `PRE_MOVEOUT_REVIEW.md` documents why actual `openTurnover` cannot serve as prospective planning: it writes move-out and starts the turn. The current service still does that. No historical Claude expected-ready harness was recovered by this lane.

First observed red: a synthetic Bed A outgoing lease with an active unit turnover dated October 1 caused Bed B's canonical `available_from` to return `2026-10-01`, where the proof required null. The shared reader attached the unit's turn to every position. This narrower observed defect precedes design of a new planning lifecycle.

Correction: retain the outgoing lease's space identity when loading the turn and apply its date only to that space. The join requires property and unit compatibility via `spaces.unit_id`. When the turn has no outgoing lease, only an unambiguous single whole-unit position retains the old unit-grain meaning. One remaining bed is not a whole-unit plan. An unresolved outgoing lease does not fall back to whole-unit scope. The classifier's conservative unit-wide turning/readiness state remains unchanged; this correction removes unsupported dates and provenance, not physical blockers.

Proof: owned loopback PostgreSQL 17 built from the real migration/precondition chain, ceiling 194 / 182 ledger rows. New `tests/proofs/availability_turn_date_scope.db.js` passed 14 domain assertions and seven staff-session HTTP/canonical row reconciliations, including exact bed, sibling noninheritance, undated shared scope, date replacement, closure, wrong-unit lease and one remaining bed. The real operator route was `/operator/leasing/availability-canonical`; the final server was restarted after source edits. The new proof is registered in `tests/e2e/verify_all.sh` beside the existing work-target HTTP proof.

Additional controls passed: availability readiness axis (13), triage work scope HTTP, availability occupancy-basis unit cases, and application target dates (15). No browser clicks or model/provider conversation proof in this lane. The UI reads the corrected operator route; other consumers share the canonical function. That source wiring is not a new Ask Spine/browser proof.

Limits: no prospective expected-ready writer, governed estimate correction lifecycle, notice-to-plan adoption or cancellation added. No task deadline became readiness; no estimate became certification; no notice became move-out. Future planning still needs the existing turnover owner extended with attributed exact-home scope, revision history, adoption and retirement rules. No second planner or speculative date arithmetic is introduced.

Reproduction: provision an owned disposable database using `tests/e2e/proof_boundary.js`, apply the real migration/precondition chain, launch the actual server under `proof_fence_preload.js` and fake provider preloads, then run `node tests/proofs/availability_turn_date_scope.db.js` with the matching `E2E_PROOF_MANIFEST`, `E2E_DATABASE_URL` and `E2E_API_BASE`. Never substitute a shared database.
