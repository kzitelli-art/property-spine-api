# QB successor: Author and Execute

September 13. This supersedes the original candidate receipt's statements about signature bypass protection, rollback coverage, reader attribution and rollback compatibility. It records development evidence, not a production release or acceptance of Skyline or The Greenery.

## What changes for the operator

An authorized owner authors the exact offer. Staff handles application delivery and package preparation; the applicant and required guarantor sign. The authorized owner then makes one Execute decision, which records application approval and company signing as distinct canonical facts. Execution creates the existing pending tenancy; it does not establish possession or economic activation. No actual Mike permission was changed.

## Independently found and repaired

- **Withdrawn application signed through the retained door.** On unmodified Claude source, a real invited signer-only actor received HTTP 201 from company-sign after the application was withdrawn. The application stayed withdrawn and unapproved while the company signature and executed packet were committed. Shared company signing now refuses terminal and unapproved applications before writing.
- **Finished resident review stayed open.** Resident signatures followed by the retained approve door and then Execute left terms_review open. Both signing doors now consume the current packet's matching frozen tenant acknowledgment for the actual current gate. Earlier approval remains an earlier act.
- **Concurrent packet commands used inconsistent locks.** Generation, issuance and company decisions now acquire application before packet and recheck identities. The regression observes Execute and a delayed issue retry waiting on the owned application transaction while canonical generation completes; issue refuses the signed packet without minting another link.
- **Preparation was presented as human confirmation.** Current-record reads retain separate author/preparer attribution. Execute history comes only from the current packet's actual audit, including the distinction between newly approved and already approved. Legacy individual acts are not relabeled as Execute.
- **The move-in panel read the wrong response level.** The app unwraps the canonical envelope and rejects late results from an older lease, request, property or actor. A pending tenancy no longer receives a Lease active heading.
- **New attribution references reached model context.** The first integrated CI run failed the existing Ask Spine no-database-IDs check. The existing sanitizer now handles attribution actor references and plural IDs while preserving the full canonical record for entitled readers. Its regression includes both derived and legacy confirmation.

## Proof and corrections to proof claims

Root independently ran Claude's original HTTP journey: 409 passed. Two additional HTTP challenges reproduced the withdrawn-signature and open-review defects. Scrubbed first-red records are retained in the workstation handoff directory.

The successor HTTP journey passes 435 checks with the full-browser handoff enabled. It retains two separate completed ordinary journeys, a withdrawal, a separately approved coexistence journey and the browser prospect on distinct beds. A real approval, gate completion and company signature are observed inside the transaction before an injected execution-service failure; rollback restores all scoped canonical rows. The subsequent normal Execute succeeds.

The expanded Chromium run passes 50 checks: native Author, application preparation, Mike's explicit synthetic email attestation, package preparation, signing-link issue and Execute against the same owned HTTP server/database. Applicant submission and resident/guarantor signatures in this run use HTTP. It is not an all-browser applicant proof. Actual email delivery is not claimed; fake provider logs remain unchanged. Mike's separate session explicitly replaces the initially prepared link through the existing recovery control before recording the synthetic email send.

The first browser attempt correctly stopped because the fixture omitted the recorded next move after the tour. The original outcome now explicitly records send_application. A later assertion was corrected to accept the rendered uppercase Possession pending text; the assertion was not removed. Canonical attribution passed 31 read-only checks after the complete fixture.

An attempted standalone older Ask suite after the two-step fixture stopped because it requires its original Mike Grivna fixture and fixed port 3000. That suite belongs on the configured runner's fresh database, not after a fixture that renames the original actor. Its older fixture cleanup is isolated to the disposable database; earlier successful evidence is retained. Final configured CI must pass this existing suite.

## Migration and release boundary

Migration 195 introduces no new table; it expands confirmation vocabularies and requires offer identity/hash for derived preparation. Owned database verifiers establish both directions: at 194 the baseline verifies and candidate refuses pending 195; at 195 the candidate verifies and baseline refuses the unknown ledger version. All checks leave the ledger unchanged. The owned applied count moves from 182 to 183, ceiling 194 to 195.

Consequently, **dab19b6 is not a valid API rollback after 195**. Do not delete the ledger row, narrow constraints, erase derived history or run the old startup verifier as recovery. A release must coordinate migration and API deployment and retain a forward-compatible recovery build that can read and finish existing two-step packets. Neither ordinary CI nor a schema verification alone establishes this operational release step.

The last production release verified before this work was API dab19b6 / app e23a36a on September 12. No migration, provider action, actual-property setup or deployment was performed by this review. Skyline's real operating configuration, website connections and Mike acceptance remain open; Greenery also retains its source-to-home identity, custody, classification and lease-configuration work. Greenery's deliberately incomplete fixture is a truthful refusal test, not an accepted property.

## Closing checkpoint

- API candidate docs tip: `7c4c5fe` records source `04f5e369c7fdce965ba2112f4a9d374c18a4da8f` on `codex/hp-two-step-review-20260913`; configured GitHub verify run **491 / 34754931870 succeeded** at that exact source commit (docs-only follow-up).
- App candidate: `e8fe8e9c9db9df7edcf50b935cd1fb81e173eed1` on the matching branch. The final move-in display correction is `eab7a4cb355e61509ad1c04580812bffc7de084d`; its focused proof is 38/0 and the sanctioned app suite is 68 harnesses / 2,291 assertions / 0 failures.
- Owned runtime: HTTP 435/0 and native staff Chromium 50/0 passed on a fresh nonce database, including native Author and Execute. Attribution read is 31/0. The standalone 194 migration compatibility proof is 3/3; both owned databases were dropped and their completed manifests retained under the workstation `tmp` records. No production database or provider was touched.
- The full browser proof uses HTTP for applicant submission and signatures while native staff controls cover Author, preparation, attestation, issue and Execute. It is not an all-browser applicant claim.
- Release custody remains with QB. Before deployment, pin a forward-compatible recovery build and run migration 195 and API/app deployment as one coordinated release. Do not treat the prior live API `dab19b6a84493093911427dfe2de29ee91451ac9` as a post-195 rollback.
