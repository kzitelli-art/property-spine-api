# Room identity: bounded O1/O2 candidate

The operator should see each actual rentable room, and confirming an empty room must not claim that its occupied neighbor is empty. Fable's source-reviewed O1/O2 counterexamples were reproduced against unchanged API018e6d621ef7e2e3bd4c074f6c7d6f97e5811061 through the owned local HTTP/API path before product edits. Both source orders returned one unit-wide availability row, with current/vacant determined by the first row. An unnamed vacancy confirmed200 despite a neighboring operative lease. Canonical availability independently retained the occupied room.

The existing owners survive: activation_service confirms proposals, import_source_rows retains lineage, snapshot_loader reads source evidence, space_position supplies the dated overlay, availability_read owns marketing eligibility, and tenancy_position_read supplies Ask Spine standing. No new route, table, domain service or migration is introduced. The forbidden second path is a new browser-owned availability calculation or unit-wide person join beside the canonical space read.

Changes:

- Vacancy and occupied confirmation share existing explicit space selection. Unknown room names and ambiguous multiple positions refuse; named vacant rooms retain the competing-lease guard. A missing current unit refuses vacancy rather than recreating inventory. Successful vacancy confirmation closes the existing durable source-to-space lineage.
- Rent Roll availability groups by resolved space. A room's future lease/readiness/possession no longer closes or overlays its neighbor. Source links that were explicitly unresolved remain unresolved even if inventory later shrinks. Historical promoted rows lacking a current linked space remain visible, with their historical proposal status preserved and current publication held for review.
- The actual signed-in app already uses canonical availability through Market & Pricing. Browser execution disproved the proposed app cutover: its target renderer is dormant on that live path. The unnecessary product changes were removed; final app index.html is byte-identical to7f4878e. Only the new browser proof remains.

Executable boundaries: philosophy sections4–7,12,29,33,40,41 map to tests/unit/rent_roll_space_identity.test.js and tests/proofs/onboarding_space_availability.db.js. The former runs in verify_all.sh. The latter runs in onboarding_review_local.js against an owned disposable database/server after its explicit pending claim-index step. The numbered chain remains192; this is not migration allocation or deployment authority.

Observed successor evidence: both source orders preserve two identified rooms; the vacant room inherits no neighboring person/lease; unnamed multi-position vacancy refuses422; named empty neighbor confirms200; occupied room refuses409; unknown label refuses422; renamed unit refuses422 without inventory recreation; a legitimate single-position vacancy still confirms and retains its source-space link. A synthetically replayed historical ambiguous promotion retains its row and promoted status but is held out of current Rent Roll publication. Actual-source confirmations: zero.

The same HTTP proof verifies property-query retargeting cannot override the session and a management-only assignment receives403 at canonical Leasing availability. Real Ask tenancy fact gathering retains two positions, one occupied and one open, and omits unentitled tenancy facts. This is fact gathering, not Ask HTTP/model-answer or causal-explanation proof.

Run the bounded proof with the normal private local wrapper inputs, ONBOARDING_SPACE_PROOF_ONLY=1 and PROOF_SPACE_EXPECT_DEFECT=0. PROOF_SPACE_BROWSER=1 additionally invokes the paired app spaces phase. To replay the parent witness, provide ONBOARDING_SPACE_PARENT_ROOT at the exact clean018e6d6 parent and PROOF_SPACE_EXPECT_DEFECT=1; the harness verifies inspected product files are unchanged. The parent JSON here was captured before these product edits. Private paths, tokens, rows and runtime identifiers are excluded from these aggregate receipts.

Accepted bounded proof: clean APIaef0d10372e110a86f28c0fb321ac4504642b44c and app0785cf8ea3e3344e53dcf3a77765021ca383a99b passed the full owned local run. Source32, ledger16, lifecycle10, snapshot15 and Deal Setup HTTP31 passed, alongside the new space HTTP/Ask assertions. The actual visible Market & Pricing -> full Availability browser path showed Room2/Room3 separately, excluded occupied Room1 from marketing, showed no asking prices, and presented unavailable/Retry after a locally injected503. Its screenshot was inspected. July/Skyline fixed workbook controls, seven-status and negative-count checks, byte/hash retention, lineage, restart, Ask fact gathering and mixed Add All (one added/one refused) passed. Actual-source confirmations remained zero. Owned database and cluster cleanup completed; cluster data absent and listener closed; SMS, model and egress sentinels empty. The existing44 app harnesses/1609 assertions passed after restoring the unchanged product. API CI34049719757 passed at exactaef0d10; raw logs inspected.

Prior API CI34049459235 failed at0414e52 because the snapshot fixture manually promoted a vacancy without its actual source-space link. aef0d10 supplies that synthetic link, retaining all15 original assertions and the separate required refusal of historical unlinked claims; API product code is unchanged. The strengthened explicit wrong-person/lease parent witness also passed against clean018e6d6. The first browser attempt targeted the dormant renderer and failed; it is not acceptance evidence. Fable's independent challenge of historical claims in other canonical readers, inventory changes and configured-but-unestablished empty rooms remains open. The narrower Rent Roll guard does not establish those other readers are repaired. No production/provider action, deployment, merge or actual-source confirmation occurred.


Proof scope: these receipts establish the bounded API/local-browser rehearsal, not whole-product or deployment acceptance. In the synthetic UI, Ready appeared without a newly created readiness certification; Fable is specifically asked to adjudicate the existing canonical readiness basis, not told that the label is correct merely because the browser check passed.
