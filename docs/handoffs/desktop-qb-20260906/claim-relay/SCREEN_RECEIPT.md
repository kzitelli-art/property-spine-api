# Retained source entries on the existing Rent Roll

Kameron's question: while onboarding the next deal, which source entries still
need attention? The existing canonical `datedPropertyPositions` read already
served their counts/list to the unit Rent Roll and Ask standing. The app's
`psLiveUnitRentRoll` did not display that information, and its early empty return
hid it completely when inventory had been retired. This was a partial existing
path, not a missing domain. No new writer, route, store or matching rule is needed.

QB reread CURRENT_STATE and the complete PHILOSOPHY at API94298bc before building.
The app worker read the same unchanged philosophy; QB retained integration
judgment. Existing reader/display repairs are Class 1; the owned proof additions
are Class 3. The forbidden second path is a UI-owned reconciliation calculation,
identity join, confirmation workflow or second source store.

## Changes and falsification

The app adds one expandable section, **Source entries needing review**, using
the same Rent Roll payload. Confirmed and held counts remain separate; source
keys and unknown labels are escaped; the 50-row bound says how many of the total
are shown. Quiet zero counts produce no notice. No source key becomes a room,
person or action target. Existing Deal Setup remains the place to review source.
One helper serves both populated and zero-inventory rendering.

QB exercised the actual old and new `psLiveUnitRentRoll` functions with the same
zero-inventory payload: parent8baf740 rendered the empty inventory message and
hid the retained entry; successor retained the empty message and showed the entry.
The worker's earlier missing-helper test was only an absence check, not this
behavioral witness. Real browser entry through Management → Rent Roll now covers
zero/one/51 retained entries, including the one-entry/zero-inventory case.

The API simplification is one condition: decide whether zero counts are known by
the successful baseline read (`read === 'ok'`), not count truthiness. A positive
owned-DB witness showed94298bc losing both known zeros; the successor preserves
them while retaining NOT_ESTABLISHED inventory. No baseline still means unknown.

Philosophy §§4–7, 29, 33, 40.7 map to assertions for quiet zero, absent baseline,
no invented inventory, consistent standing/Ask counts, displayed source keys,
partial-list disclosure, unavailable state without stale claims, and real Retry
recovery. Existing scope, entitlement, conflict and history assertions remain.

## Observed proof

- Sanctioned app command: `C:/Program Files/Git/bin/bash.exe ./run_harnesses.sh`:
  44 harnesses, 1648 passed, zero failed/red. Plain `bash` selected an unavailable
  WSL installation first; no suite result was claimed from that launcher failure.
- API occupancy-basis unit tests: 4 passed. Relay DB/HTTP: 21 passed, including
  the new known-zero/unknown distinction; identity17, unattached14, readiness13
  and existing historical/room controls passed.
- Full owned Windows wrapper exited zero: parent source controls, pending-index
  negative and positive, source32/ledger16/lifecycle10/snapshot15/HTTP31;
  browser spaces, retained entries, July/Skyline review and restart, lineage,
  Ask fact gathering, and mixed Add All all passed.
- Browser retained entries: quiet zero, visible one with zero positions, 51 total
  with 50 listed, forced503 removes stale data, Retry restores the real read.
  QB inspected the private section and viewport screenshots.
- Real-source confirmations: zero. July164 rows/proposals, Skyline262, both with
  zero persons, leases and established positions. Original bytes/hashes preserved.
- Owned DB dropped, cluster stopped, data absent; SMS/model/egress logs zero bytes.

Two earlier full attempts stopped in proof navigation: reloading a prior desk hash
while expecting Home, then dispatching a second change after filling the date had
already triggered a read and removed the input. The proof now starts independent
fixtures at the actual Home URL and performs the date action once. Product router
behavior and the separately recorded desk repaint issue were not changed. Both
failed attempts cleaned up. The final full run used frozen product and proof files.

Command: the existing `tests/e2e/onboarding_review_local.ps1`, with retained private
workbook paths, the existing PostgreSQL/Chrome executables and unchanged retirement
parent root; `PROOF_SPACE_EXPECT_DEFECT=0`, `PROOF_SPACE_BROWSER=1`,
`PROOF_RELAY_BROWSER=1`, `PROOF_HISTORICAL_READER_CHALLENGE=successor`.
No `ONBOARDING_SPACE_PROOF_ONLY` flag. Private run locations remain outside Git.
[Final aggregate/custody](screen-db-receipt.json) names the exact base heads and
SHA256 of every changed file exercised. Only receipt/docs/evidence were added
afterward. [Browser aggregate](screen-browser-receipt.json) contains no runtime IDs.

## Limits

This is the bounded review/restart milestone, not production onboarding or a
completed occupancy establishment. Ask fact gathering passed; Ask HTTP/model
answers and cross-domain composition are not claimed. The retained list is bounded
and has no new pagination/detail contract. No deployment, migration allocation,
provider action or actual-source confirmation occurred.

Fable retains the independent identity/readiness challenge: null lineage history,
retirement reversal/replacement, named placeholders, and readiness across marketing,
touring, application and delivery. This slice does not decide those policies.
