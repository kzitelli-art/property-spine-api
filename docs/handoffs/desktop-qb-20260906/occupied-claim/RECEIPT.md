# Occupied opening claim — QB integration and browser receipt

Bases: API f73e69eddd1382fb7d14396a0962b7e999730ae3;
app6c8346609bf373fea4b8e495b4c18a1a92e98d39.
Reviewed availability candidate b01499b33925c7bfdbda27f506d538a720bbd2b1.
Root reread current state and all philosophy before building. No branch merge
or rebase; selected reviewed files incorporated into QB-owned candidate.

An operator must not offer a bed whose established opening position says it is
occupied, nor mistake missing lease evidence for nobody occupying it. Existing
owners are dated_positions, availability_read, unitRentRoll and the app's
psRruDetail. The same accepted claim already reaches standing; this repair does
not introduce a writer, state, lookup, truth store or new person relationship.

Root verified all three requested candidate repairs: HTTP proof is inside the
owned-server branch, full ordered lease rows are compared before/after, and the
product comment points at its delivered proof. Exact candidate CI34177227649
succeeded, with the availability proof PASS after server UP. The earlier CI
first failure was CURRENT_STATE numbering, before the also-real misplaced step.

Root reproduced classifier4pass/1fail on unchanged f73 availability, then5pass
on the reviewed successor. Owned18/18 DB/HTTP checks passed, including selector
exclusion, vacancy control, native lease, pending future commitment, contested,
down precedence, readiness, unknown basis, entitlement and byte-equivalent lease
row sets. Direct promoted-row fixture is retained-data shape; the current confirm
writer classification creates a lease and is tested separately. No claim that a
fresh confirmation produces this shape.

## Actual first browser red

Root clicked Leasing / Market & Pricing / Full availability: occupied claim
withheld, vacancy control visible. Then Management / Rent Roll / Room2 detail:
'Resident None on this date', with no server explanation. The browser failed
CLAIM_BASIS_NOT_EXPLAINED_IN_RENT_ROLL. Root inspected its actual screenshot.
The owned database dropped, cluster stopped and data removed.

The missing piece was a dropped server explanation. psRruDetail now uses the
existing OPENING_OCCUPANCY_ACCEPTED_TERMS_UNKNOWN reason code to render the
server sentence and show resident identity as Not established. No lease dates,
contractual rent or person link is invented. Confirmed vacancy and native lease
rendering remain in their existing paths. Six local assertions first yielded
three failures, then all73 classification tests passed; row semantics37,
cutover152 and zero-count contract51 stayed green.

## Combined successor acceptance

The frozen combined successor passed all nine real browser phases: zero, claim,
source-auth, holds, spaces, relay, stage, restart and mixed. Root inspected the
claim detail screenshot: Resident Not established and the exact server sentence
are visible. Claim HTTP/service/DB18 and zero HTTP10 passed. July164 rows and
Skyline262 rows retained their hashes and lineage, with unchanged restart and
mixed Add All assertions. Actual-source confirmations0. Provider sentinel logs
(SMS, Anthropic and egress) were all empty. The runner reported owned database
drop; root verified its cluster process ended and its data directory was removed.
The final-status file is a pre-cleanup status capture, not the final process state.

Application-target exclusion is HTTP/service proven; the application picker was
not browser tested. Source inspection suggests its conversation entry lacks a
required conversion_id; that is a separate unproven UI concern, not a reason to
invent fixture session state or expand this onboarding repair. Ask fact gathering
is covered; Ask HTTP/model answers, deployment, production migrations and actual
source confirmation are not claimed.

Commands: the owned onboarding_review_local.ps1 wrapper ran with CLAIM, ZERO,
SPACE, RELAY, SOURCE_AUTH and HOLDS browser flags enabled, focused name absent,
SPACE_PROOF_ONLY=0 and both defect flags0. Exact executable hashes are preserved
in tested-custody.json. Receipt files omit private runtime identifiers and rows.
CI for the committed successor is pending; the local browser result is complete.
Local source governance: all50 gates exited0, including CURRENT_STATE8/8. App successor a2dac2b pushed; 73 classification assertions passed.
