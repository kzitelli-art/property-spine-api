# Fable review: QB disposition

Fable returned an independent source review of retirement, onboarding, the pending index and desktop successors. Fable executed nothing; the original source review is preserved in the user's QB attachment. QB owns the following dispositions and any subsequent runtime claims.

## Governance

Kameron directly confirmed retirement authorization in QB task 01a076e5 on September 6: "yes it my understanding they were not being used and i love improving through simplicfication of code". This supersedes the uncertainty about whether authorization occurred. It does not prove external non-use; deployment remains held.

## Findings

- F1 accepted as release coupling, with a precision correction: source establishes the repository's ceiling192 index, not a live production schema. The new owned-DB witness must encounter raw23505 on uq_proposed_natural, zero committed stage batches/proposals and retained original bytes before pending DDL, then two distinct source-linked current/future proposals after it. The same witness is wired into local orchestration and CI. Migration allocation/application remains held; no migration193 is invented. Revise the temporary ceiling192 witness when that DDL enters the numbered chain.
- F2 reproduced by executing the unchanged proof guard: total2/conflicted2 throws STATUS_TOTAL_MISMATCH. The successor totals the seven explicit statuses. The original guard is frozen as a regression witness; missing known statuses still mean zero only within that vocabulary, and invalid counts/independent vacancy checks remain enforced.
- F3 reproduced through the actual unchanged source adapter and extracted API reducer: one named current row without a unit survives parsing, but total1 has assignment buckets summing to0. The successor adds unassigned_current to the canonical review read and the paired app reads canonical section/assignment counts. Missing counts display unknown. This is an incomplete summary and proof-contract finding; the old app did display the row, so it is not evidence that the product refused the workbook at upload.
- F4 accepted as a dormant display gap. Lease confirmation promotes directly; no confirmed-state behavior change is included.
- F5 accepted as a classified surviving writer and documentation correction. CURRENT_STATE now names the key-only leasing-basis setter and marks the old rows39/40 ingestion-proof descriptions as historical after retirement. No route contract is changed here.

The synthetic duplicate-room and current-without-unit workbooks use the same stageSource browser upload/read/review/download path as the originals. Fixed July/Skyline controls, absence-only status semantics, vacancy checks, retained hashes, restart, zero actual-source confirmations and synthetic mixed-confirmation assertions remain mandatory. The first-room overlay concern is still separate and not freshly reproduced.

## Execution status at repair commit

Pure app contract checks passed29; renderer checks passed4. The sanctioned app suite exited0:44 harnesses,1609 passed, zero failed/red. The unchanged F2/F3 discriminators above were executed before the repairs. The full successor database/browser run and exact custody are pending a follow-up receipt; source review and pure checks alone are not their acceptance.

## Superseding executed acceptance

Full owned wrapper exited0 with clean API8fd9e9ba2ef73900debc25551449922d1b29be5d and app7f4878e830cfe0dcad4a6a6ee24cdc7384066152. PostgreSQL17.11, Node24.16.0 and local Chrome; exact custody is in [the database receipt](fable-followup/db-receipt.json). Reviewed aggregate receipts: [stage](fable-followup/stage.json), [restart](fable-followup/restart.json), [mixed](fable-followup/mixed.json).

The ceiling192 witness produced raw23505 on the natural-key index, zero committed stage batches/proposals and unchanged retained bytes. After the separately applied pending SQL, a fresh activation retained both source-linked current/future claims. Source successor checks passed32; other required lifecycle/ledger/snapshot/HTTP checks passed. CI verify [34039624425](https://github.com/kzitelli-art/property-spine-api/actions/runs/34039624425) completed successfully on exact8fd9e9b using PostgreSQL16; the raw log was inspected for both index witnesses, source32 and final required-assertions success.

Real browser stage retained and visibly reconciled two conflicted current claims, and one assigned vacancy plus one blocked current unassigned resident. July164 and Skyline262 retained rows/claims, fixed status and vacancy controls, review UI, exact original bytes and zero actual-source confirmations all passed. Restart preserved both actual-source reviews and database lineage. Ask fact gathering remained NOT_ESTABLISHED with zero established/occupied/open/known-next positions; no Ask HTTP/model-answer proof is claimed. The synthetic mixed case added1/refused1, and both actual-source database fingerprints remained unchanged afterward. SMS/model/egress sentinels stayed empty. The successful run dropped its owned DB, stopped the cluster and removed its data directory.

The first follow-up run at ec5378a/37f781a exposed a fixture mistake after proving F1: the ledger correctly refuses a file with no assigned inventory. The revised F3 fixture includes an assigned structural row alongside the unassigned claim. No ledger guard was relaxed. A separate redirected PowerShell invocation stalled before loading application code; its cluster was explicitly stopped, but automatic approval review rejected temporary-data deletion with only "blocked by policy". That stopped data directory remains local. It is not part of the successful-run cleanup claim.

Codex labor independently checked the changes. Its proposed current-row section-label defect was refuted by dsSection's current-first branch and explicitly retracted, then the actual browser fixture passed. No separate first-room overlay reproduction or new Fable first-room finding is claimed.
