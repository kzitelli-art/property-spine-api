# Whole-file rollback and correction through the same setup

API product43d30ed and app product28df16d are unchanged. Two bounded workers
extended existing proofs; QB reviewed their assertions, owned the runtime and
inspected the resulting browser screenshot. Both workers reread paired current
state and complete philosophy. No new product component or workflow was added.

The API proof now checks a valid earlier unit followed by a contradictory later
unit. Real HTTP409 leaves units, spaces, lineage, proposals, leases, baselines
and import batches unchanged, with the activation status/source/batch links
unchanged. The retained artifact hash and size survive. A separate direct call
to the older loadSnapshot function refuses and rolls back through its existing
load_failed contract; this is service proof, not /snapshot HTTP proof.

Two deliberately wrong helper inputs prove the comparator rejects a batch-only
or activation-link-only mutation. These are helper-level negative controls, not
observed database faults. The full mixed-grain proof passed35 assertions, including
those two controls. New composite/legacy parent-mode branches were added but not
run in this follow-up; the original parent27 witness remains the observed parent
evidence. Do not describe all new cases as independently reproduced parent defects.

The browser used the existing Upload and read action after refusal. Retrying the
same file/date returned200 with the same artifact, then409 again. A corrected
file removed only the whole-unit row, preserving vacant Room1 and unknown actual
rent. It returned201 and reached the same activation's visible single-row review,
with the corrected source hash and no opening position or confirmation. QB caught
and removed an initial test design that also changed the resident/rent; the final
proof isolates the inventory correction.

The observed failed-source screen had no button/link labeled Download, View source
or Open source. This bounded observation agrees with the source review, but does
not establish absence of every possible application path. The separately retained
file remains downloadable by its existing API route. Operator access to rejected
retained files is a separate unresolved UI reachability question.

## Exact run and verification

Owned Windows run81d0533129864d7985f435ab6fb1e83a used the unchanged existing
onboarding_review_local.ps1 wrapper, paired app, retained July/Skyline inputs,
owned PG17.11 and Chrome. PROOF_SPACE_BROWSER=1, PROOF_RELAY_BROWSER=1,
PROOF_SPACE_EXPECT_DEFECT=0, PROOF_HISTORICAL_READER_CHALLENGE=successor.
The full parent source/lifecycle/snapshot, index negative/successor, canonical
source/ledger/lifecycle/snapshot/HTTP, identity/readiness/relay/space controls and
browser spaces/relay/stage/restart/mixed passed. July164 rows/64units/105spaces;
Skyline262rows/72units/160spaces. Retained bytes/hashes and lineage survived restart.
Actual-source confirmations0; no actual-source people, leases or established
opening positions. Ask fact gathering only, not an HTTP/model answer.

Owned database dropped; cluster stopped; data removed; SMS/model/egress logs0.
Adjacent recovery-browser and recovery-db JSON hold safe aggregates and custody.
Both executable changed-file hashes matched the final run before commit. The
untracked worker navigation document in custody was later annotated with results;
it is not runtime code. Static node checks and diff checks passed. No product
change, production/provider action, deployment, merge/rebase or migration authority.
