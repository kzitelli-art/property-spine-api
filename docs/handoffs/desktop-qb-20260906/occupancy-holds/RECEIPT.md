# Canonical occupancy under physical holds

Status: successor service, HTTP, actual Formal Schedule/browser CSV and complete
local onboarding rehearsal passed.

Inspected API edceeb5ebb8a8e4041f63c3057876329015f548d and paired app 8d1da84.
CURRENT_STATE and the complete PHILOSOPHY were reread. Fable 5f94fdb supplied
reported 30/34 observations, independently source-reviewed by QB. Its screen
labels were source inspection; its all-down literal-true assertion was not a
negative-denominator guard. QB's focused proof asserts exact results and actual
HTTP CSV rather than constructing a screen label from JSON.

The operator needs an honest fraction over leasable, uncontested positions,
while a physical hold must not cancel tenancy or rent (§§5, 7, 29, 33). The
existing owner is currentRentRoll; institutionalRentRoll and CSV reshape its
totals. History 0d159ae introduced the independent-axis summaries and leasable
denominator. Both the denominator overlap and numerator membership were missed.

The intended smallest correction is one eligible population for both sides of
the fraction. Preserve tenancy_summary, lease rows, contractual rent, all
exception counts, and unresolved positions in the existing denominator. A down
contested position must be excluded once. No replacement status, domain owner,
hold service, reporting workflow, or separate stored total is allowed.

The new proof is Class 3, owned disposable infrastructure. Native synthetic
leases exercise A without requiring B's directly promoted historical claims.
Hold/resolve use the existing HTTP owner. Canonical service, institutional
service, both JSON endpoints and real CSV are checked before, during and after
holds. All-down must return occupied 0, denominator 0, pct null; tenancy remains
two occupied positions and trusted rent remains 1750.

Defect B remains separate: current writers create a lease for confirmed occupied
rows, while synthetic historical residue can present an accepted occupied claim
without one. Availability already has its canonical classification and can
guard offerability without another read owner. Fable's bounded follow-up is
prepared separately; it is not claimed fixed here.

No production/provider operation, deployment, migration authority, actual-source
confirmation or whole-domain acceptance is claimed.

## Root-observed first failure

On unchanged API edceeb5, the strict successor proof returned 65 passed and
16 failed. The failures were the three held fractions through canonical and
institutional services, both HTTP JSON reads, real CSV, and the overlapping
exclusion count. All hold/resolve HTTP operations, scope/refusal controls,
baseline/restoration, unchanged leases, rent 1750 and independent axis counts
passed. Parent proof SHA256:
`4798076BF350610D87BD176DE9D8BF76E73B7B2B42E598CD2CD0F2556D0819B6`.
Owned database dropped, cluster stopped, and cluster data removed.

## Candidate correction

currentRentRoll now builds one non-down, non-contested population and counts
contractually occupied members within it. Its excluded contested count refers
only to contested positions not already excluded as down. Full contested
exceptions, full tenancy counts and rent retain their existing meanings.
The legacy read-only proof's obsolete subtraction invariant was corrected;
it was not run against its ambient/private target. The new owned proof is
wired into the existing local and CI runners.

All 50 source-governance gates passed after the product correction. Syntax and
diff checks passed. Independent source review found no numerator/denominator
asymmetry; naming and visible exclusion explanation remain separate follow-up
questions. A hold still does not establish vacancy or cancel an operative lease.

The browser extension follows the actual Management → Rent Roll → Full schedule
→ Formal schedule path. It checks scoped canonical/institutional responses,
the visible all-down total, and the existing client Export CSV action. It adds
no product UI. Its optional private fixture emitter is proof infrastructure.

## Final local evidence

- `PROOF_FOCUSED_NAME=canonical_occupancy_holds`, `PROOF_EXPECT_DEFECT=0`,
  `ONBOARDING_SPACE_PROOF_ONLY=1`, through the existing local PowerShell wrapper:
  unchanged parent 65 passed / 16 failed; complete owned cleanup.
- Full wrapper with no focused proof, `ONBOARDING_SPACE_PROOF_ONLY=0`, and
  `PROOF_SPACE_BROWSER=1`, `PROOF_RELAY_BROWSER=1`,
  `PROOF_SOURCE_AUTH_BROWSER=1`, `PROOF_HOLDS_BROWSER=1`: 86 passed / 0 failed
  for the new proof (81 behavioral checks plus five private browser fixture
  checks). Retirement 41, source authority 77, mixed-grain writer 35 all passed.
- All seven browser phases passed: holds plus the existing source-auth, spaces,
  relay, stage, restart, and mixed. Root visually inspected the private Formal
  Schedule screenshot: eight total/down positions, 0 of 0 eligible occupancy,
  trusted monthly rent 1750. Actual browser Export CSV carried 0 of 0. This is
  not a browser proof of every intermediate hold case; those are service/HTTP.
- July/Skyline review, retained hashes and counts, real-source DB lineage and
  restart, and synthetic mixed Add All assertions passed. Actual-source
  confirmations remained zero. Provider/egress logs were empty; the owned DB
  was dropped, cluster stopped, and cluster data removed.
- `node tests/verify_source_governance.js`: all 50 gates passed. Syntax and diff
  checks passed. No ambient-target legacy proof was executed.

`tested-custody.json` records the seven frozen code/proof hashes. The only
product change is in the canonical aggregation owner. App changes are browser
proof infrastructure; its product HTML is unchanged. The displayed denominator
still needs clearer explanation, and Defect B offerability is not fixed here.

Paired browser-proof commit: `22d705d8d5f35408fc5ae3abd7f2d1bbcd05fee5`.
Its product index remains byte-identical to the inspected base.
