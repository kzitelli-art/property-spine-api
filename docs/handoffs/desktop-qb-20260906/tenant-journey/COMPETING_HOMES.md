# Competing homes and maintenance priority — 2026-09-08

API9f2acff97af311e6ebdcd5e2219f8cf873894748 / appc05ae68f6f8bc49ca4b5bd752adc928e1f4e0d88 plus local dirty candidate. Root reread CURRENT_STATE and full PHILOSOPHY; tour_handoff_gap independently reviewed existing maintenance owners/history, no edits. This checkpoint adds Class3 proof only; product source unchanged.

Intention: an offer must respect the exact home's outgoing rights and feasible turn dates, and maintenance must use the same canonical lease commitment truth. Existing owners: application target authority -> canonical interval and availability, lease_offers, rankTurnPriority. History4b34775 explicitly removed application-status priority; current maintenance and operator routes both call rankTurnPriority. The app's live turnover panel already reads that route. No new turn writer, ranking store, invented scheduling margin or application reservation.

Owned run spine-onboarding-proof-e2964ce6aba843d2833f4021fd0632bb exited0. The existing focused application_turn_window wrapper ran real migrations and proof fences, service/HTTP date checks, actual post-tour controller/shared picker browser read and the new application_turn_offer_http helper. Database dropped, cluster stopped and verified data removed. No production/provider actions, actual-source confirmations or sends.

Real HTTP assertions: missing canonical staff assignment refuses; occupied outgoing lease spanning requested term refuses and rolls back prepared offer; unknown readiness refuses; bare inventory label without occupancy basis refuses; explicit synthetic competing tenancy with an ending lease and recorded turn-ready date permits exact terms; same offer retry returns same ID; same-key changed terms refuses; foreign space plus forged property body refuses; anonymous request refuses. Corrected outgoing rights permit the originally blocked home. Literal zero deposit remains0.00.

Maintenance proof: compare full existing priority projection before/after preparing an offer on an actual in-progress turn — no priority, deadline or commitment changes. Real operator turn-priority HTTP then ranks executed/funded lease above pending future lease above a turn carrying only an application offer. Offer-only turn has null commitment date and no contributors. This is a proof of the existing connection; no automatic maintenance assignment or new prospective turn is claimed.

Fixture failures were not product defects: ae1799c lacked assignments identity; 97b47fd tried offering the fixture named A-marketable with no established occupancy basis; 60bbab added a spanning lease while leaving its vacancy label contradictory. Each was cleaned up. Successor explicitly supplies synthetic identity and consistent outgoing tenancy/notice/turn evidence. Naming a fixture marketable did not establish it. Existing negative assertions are retained; no product refusal was relaxed.

Proof limits: offer writes traverse real HTTP, browser picker read traverses real HTTP, but they are separate observations. No full-shell browser offer/write sequence, tour birth, provider dispatch, actual property rent/terms or production proof. Existing visibility flake remains open. This is feasible-home selection, not preference or NOI optimization. An application's lifecycle still must not stand in for a lease commitment. Future turn planning before actual move-out remains the next separate operating seam, with current possession preserved.

Next local build: carry attributed prospect preferences through existing person_facts/read owners into selection and define supported comparison basis before recommending a home. Investigate full-shell visibility and prove changed-date refusal inside the full review UI. Do not call turnover_service.startTurn solely to plan an upcoming turn; it records actual move-out/possession consequences.
