# Preserve recorded sibling possession during move-out

2026-09-10. Inspected API48a85ab810269ce5fab13002fd61166112832f80, paired appf2eda58544b1650f123735f4105c750a47eba081. Workspace instructions, CURRENT_STATE, operating board and complete PHILOSOPHY read. Relevant owner history: f54bb91 introduced canonical turnover from move-out.

Intention: moving out one bed cannot label a still-possessed sibling's unit vacant. Existing owner is turnover_service.openTurnover; canonical recordEffectivePossession already ends only the selected lease's space. The unconditional compatibility-cache update was the observed break. Reuse spacePosition's existing as-of possession read after the write, within the same transaction. If any position in the unit is still possessed, retain occupied. No new event, possession classifier, schema or turn-planning path.

First red: new tests/proofs/turnover_sibling_cache.db.js established two bed move-ins through the canonical service, called the real HTTP move-out door with Bed A's lease, verified exactly Bed A's move-out and Bed B's live possession/active lease, then failed because the unit cache was vacant. Successor passed the same owned DB/HTTP path with the unit cache occupied. Both runs applied the wrapper's real migration chain and verified owned database drop and cluster-data removal. Pending index DDL remained confined to the owned proof.

Reproduce with the existing onboarding_review_local.ps1 wrapper and approved local runtime/source paths, PROOF_FOCUSED_NAME=turnover_sibling_cache and ONBOARDING_SPACE_PROOF_ONLY=1; no tenant journey flag. No external rebuild script or shared database. Unit setup is synthetic; possession setup is DB/service, move-out is HTTP. The operator route bridge unit test passed13; all54 source-governance gates passed after runtime cleanup. No product edits followed the successor.

Boundaries: this closes the demonstrated live-possession sibling case. It does not make the compatibility cache canonical occupancy. Imported lease-only siblings without recorded possession, unknown sibling evidence, last-bed release, and concurrent economic activation require adjacent coverage before claiming general cache reconciliation. The existing no-live-possession behavior is unchanged. Canonical readers still own occupancy and availability; no browser/provider/deployment or complete turnover acceptance claim. The read currently loads the property's positions through its established loader; no separate SQL classifier was introduced for convenience.

Next: strengthen adjacent occupancy cases, then finish durable bed identity for existing prospective work. Work deadlines remain distinct from expected home readiness. Claude's reported79/79 harness has not been supplied, so its new result remains reported rather than independently replayed.
