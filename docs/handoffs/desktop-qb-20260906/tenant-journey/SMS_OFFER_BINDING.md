# Confirm the same application offer

2026-09-09. API9f2acff97af311e6ebdcd5e2219f8cf873894748 and appc05ae68f6f8bc49ca4b5bd752adc928e1f4e0d88 plus local working changes. CURRENT_STATE and board inspected; complete PHILOSOPHY reread at owner request. Governing intention (§§4–9,28–34,40–41): confirming a proposal must not authorize different terms selected later.

Existing mechanism: conversational confirmation introduced at2526bc8, restored after falsification atce13dc7; opaque authenticated claims bind staff/property/conversion/home/date/nonce/expiry. The newer canonical application-offer owner already retains and hashes terms, selects one current offer and checks currency. Invitation birth accepts an explicit applicationOfferId through the existing send command. These owners survive; a new confirmation database, second terms writer or SMS-specific pricing policy is forbidden.

First red owned1f1816e70de8481caf7eaa16308d77cc: mint with original1025 offer, create a2500 successor retaining home and dates, redeem through staff SMS. The reply said Application sent; the fresh-review assertion failed. Replacement was created through the canonical prepareApplicationOffer service in an owned transaction, because the pre-invitation HTTP revision route requires an invitation. Therefore this is an HTTP redemption failure for a service-established state, not proof of pre-invitation revision through the operator UI. The first-red assertion stops before invitation economics are requeried; source identifies the unbound selection, but the observed failure is the send instead of review. Database dropped and cluster data removed.

Smallest repair: extend existing sealed claims with selected offer ID and retained terms hash. Redeem reuses resolveApplicationOffer inside the staging transaction, compares both and passes that exact ID to the existing send command. Changed selection yields APPLICATION_TERMS_REVIEW_REQUIRED and a fresh-review receipt. Ambiguity still uses the same sole-offer rule. No new business writer or schema. This is a Class1 extension to the permanent confirmation primitive; test mode is Class3. Old tokens lacking bound terms are rejected, requiring a fresh proposal; no compatibility fallback authorizes unbound terms.

Service refusal regression passes after its stub supplies the canonical offer read. All54 source-governance gates pass. First successorc3105eb7 stopped before application exercise on EADDRINUSE; owned DB/data cleanup passed. It is an infrastructure stop, not a product result.

Successor a8f699f290f3421ba5efc8d93ee93387 passes45 assertions through the existing owned runner. The original SMS token now returns the changed-terms review receipt, with zero invitation/intent and unchanged original/successor snapshots/statuses. Owned database dropped and cluster data removed. Command: established onboarding_review_local.ps1 inputs with PROOF_TENANT_JOURNEY=1, ONBOARDING_SPACE_PROOF_ONLY=1, PROOF_SMS_OFFER_REFUSAL=1 and PROOF_SMS_OFFER_REPLACED=1. Valid full-path control follows separately with both offer-test flags removed.

Valid full-path successor4a37c305de604e41883a60fdcf3cc865 passes151 assertions through application sending, replay/actor/expiry controls, applicant and company signing, exact-bed tenancy and reconciled signing reads. Both test-only offer flags removed. Owned database dropped and cluster data removed. No real SMS sent; models locally refused. This is fresh successful-send evidence on the same product source as the changed-offer refusal.

Limits: binding does not itself display all terms to staff or implement phone-only offer authoring. Those remain next product seams. No browser, production or concurrent-writer proof is claimed by this receipt. No deployment, push, live send or real signing.
