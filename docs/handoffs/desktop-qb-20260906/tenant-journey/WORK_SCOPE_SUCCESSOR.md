# Explicit work location: tested successor

September 10, 2026. API base 17a3944b7e5cbc3a3e75528dadf7096cb4c954f5; app base f2eda58544b1650f123735f4105c750a47eba081. Product source and complete PHILOSOPHY inspected; philosophy hash remains 977B30A4C41B0F8C1511DA3521D030E031AB13B9D1897EEA5970F011F487FC28. The commits containing this receipt and the paired app changes identify the successor. This is development evidence, not deployment.

## Operating result and preserved owners

An operator can select the whole unit or an exact space for each proposed work item in the existing triage door. Leaving it unresolved records `unspecified`; mentioning a bed in prose never selects it. Migration194 adds scope_kind and space_id to the existing unit_triage_required_work identity. It preserves legacy rows as unspecified and prevents silent target changes. The canonical triage service validates membership before any writes. Corrections must reference a confirmation for the same unit/property.

Work state, work-flow HTTP, Unit Turn and the existing work displays carry the same recorded scope and canonical label. Acceptance and proof continue to inherit the work identity. The existing work service also exposes a compact required-work standing to entitled Ask gathering; raw work/unit/space IDs do not enter its model facts. Pending work, quiet, read failure and timeout remain distinct. No second work table, parser, completion writer or readiness calculation was added.

Scope selection changes neither sibling possession nor actual turnover/readiness. Work due dates remain commitments, not expected-ready dates. Existing conservative readiness gates remain unchanged. An observation correction does not silently rewrite or withdraw existing required work; its existing attributed work lifecycle remains authoritative.

## Falsification and verification

- Original owned HTTP first red: a successful staff-session confirmation discarded the explicit bed id. See WORK_SCOPE_FIRST_RED.md.
- Expanded read reconciliation found work-flow HTTP still discarded the target despite the service returning it. The route now forwards it.
- Independent reviewer identified cross-unit correction history risk. Owned HTTP reproduced an invalid correction returning201; successor returns400 before creating an observation, while a same-unit attributed correction remains available.
- Ask composite first red: outstanding required work could yield QUIET. The canonical standing now declares attention and the composer carries count/read-state grounding. `node tests/unit/required_work_standing.test.js` passes, including quiet, failed and timed-out cases.
- `triage_work_scope.db.js` passed through real Postgres and staff-session HTTP: exact target; unknown prose; explicit unit-wide scope; wrong-unit/property, malformed and contradictory scope refusal without partial writes; immutable target; work, flow and Unit Turn reads; proposal target containment; Ask canonical gathering and entitlement/failure controls; unchanged recorded sibling possession; no actual turnover, readiness walk or certification created; and preserved correction history.
- With `PROOF_WORK_SCOPE_BROWSER=1`, the actual app triage component was clicked at390x844 against the real owned API through a test bridge implementing the app's existing __psLive interface. Selecting Bed A and confirming produced a durable exact-bed work row. Browser closed; owned database dropped and cluster data removed. This is component-plus-HTTP evidence, not the full signed-in app shell or visual-design acceptance.
- App `work_scope_identity_app.test.js` executes Chrome interactions with controlled API replies. It rejects parent f2eda585 (zero location controls), and passes the successor: explicit choices, unspecified prose, escaped labels, removing an earlier work item without shifting the selected target, and no space id on unit-wide/unspecified requests. The worker's initial regex checks were replaced with this behavioral test.
- All54 source-governance gates passed after final product changes and after proof runtime cleanup. The required-work unit and HTTP proofs are registered in tests/e2e/verify_all.sh. Browser tests require installed Playwright/Chrome and are separately recorded, not claimed as remote CI execution.

The earlier browser attempt stopped because the test assumed app/node_modules/playwright existed. The final proof uses the existing API dependency resolution. That harness problem is not a product failure. No provider/model call or live user interaction was required.

## Commands and remaining boundary

Owned wrapper: PROOF_FOCUSED_NAME=triage_work_scope, ONBOARDING_SPACE_PROOF_ONLY=1, PROOF_WORK_SCOPE_BROWSER=1, then tests/e2e/onboarding_review_local.ps1 with local private workbook and installed tool paths. Do not copy desktop paths blindly onto HP. Unit: node tests/unit/required_work_standing.test.js. App: set CHROME to the installed executable, then node work_scope_identity_app.test.js. Source gates: node tests/verify_source_governance.js with all owned servers stopped.

Migration194 was applied only to disposable proof databases. Production ledger/rollout is not established. Ask gathering is service/DB-proven; a provider-generated Ask answer was not exercised. Full-shell browser acceptance, exact-space pricing matching and prospective bed-ready scheduling remain separate unfinished work. This identity slice does not claim the full automatic leasing/turn algorithm is complete.

Desktop QB retains integration ownership. The new HP task may set up and review in parallel; it should not make overlapping product edits until a subsequent tested handoff.
