# CURRENT STATE — what exists, and at what proof level

**September 16, 21:26 UTC — production property navigation and address presentation:** unchanged API `cd7279ebb23865f4e3e7f9c827dd4b5cc52d475f` remains live at schema 187/199; app `610439db2c7df6a1aad3a2e9dcf1fb26752b843d` is live at Render deployment `dep-dalgimbl550s73b6up1g`. The current property name and an explicit `All properties` action now open the existing server-authorized picker from Home and every desk; Home also exposes `Switch property`. Chooser addresses use one display formatter, so retained Skyline shorthand `1417 n 15 phily` presents as `1417 N 15th St, Philadelphia` and Greenery presents as `1325 N 15th St, Philadelphia`; the underlying property records are unchanged. The sanctioned app suite passed 72 harnesses / 2,427 assertions / 0 failures, the focused real-Chromium navigation proof passed 31/31, Render checked out the exact commit, and every served first-party asset byte-matched it. No property identity, access, inventory, pricing, lease, knowledge, provider, API or schema write occurred. See [release receipt](handoffs/new-hp/property-navigation-polish/RECEIPT.md).

**September 16, 12:39 UTC — production property identity cleanup:** API `cd7279ebb23865f4e3e7f9c827dd4b5cc52d475f` is live at Render deployment `dep-dal8l9tg1s2s73eju130`; unchanged app `3607b3d5891a4a96b6b5183ead875b974863aef9` remains live. Exact full CI `35095230370` succeeded, `/health` identifies `cd7279e`, and production has 187 applied migrations through ceiling 199. Migration 199 adds the governed, immutable display-name command. The production chooser now distinguishes `Property Spine Demo — Solo Shape` from the one real `Solo on Chestnut`, and also shows `Skyline Apartments`, `Greenery Apartments`, and `Uno on Chestnut`. Skyline canonical identity is `1417`; Greenery is `1325-N-15`; registry model remains bed for both. Greenery operating `leasing_basis` remains honestly `unknown`. Real Chromium switched Skyline → Management and Greenery → Leasing through the server-authorized picker with no page errors; screenshots and element hit tests are retained. Existing KZ/Mike/John access was not changed. Whether KZ's demo assignment should remain visible is still an owner decision. See [production receipt](handoffs/new-hp/property-identity-cleanup/RECEIPT.md).

**September 16, 11:04 UTC — live Temple access and operator knowledge packet:** using the signed-in production app on unchanged API `c8c5176bd556d6ea9e57af08bc31ca98fbb4c190` / app `3607b3d5891a4a96b6b5183ead875b974863aef9`, KZ preserved SUPER ADMIN while gaining Greenery Property Admin access and verified Greenery in the authorized picker. John Franco is now an active OneFive member with organization-level Property Manager assignments for Skyline and Greenery; Mike's existing two-property assignments remain. No Team invite or human OTP acceptance was performed, so organization-level assignments are not yet full person-level staff-bridge acceptance. Skyline remains five of ten live shelves: `leasing_highlights`, `amenities`, and `leasing_faq` were corrected and read back on September 16, removing unsupported free/paid labels and conflicted exact Saturday package hours. Greenery remains zero of ten published. A 20-page Solo-shaped combined review packet is versioned at [the Temple knowledge review](content/temple/SKYLINE_GREENERY_KNOWLEDGE_REVIEW_20260916.docx); it separates descriptive shelves from governed pricing, availability, readiness, policy, lease, and maintenance owners and requires source/effective date, canonical media mapping, separate operator review, final approval, and property stewardship. No customer message, source-data correction, pricing, inventory, lease, migration, provider, or deploy action occurred. See [live access and review receipt](handoffs/new-hp/property-knowledge-onboarding/LIVE_PROPERTY_ACCESS_AND_REVIEW_20260916.md).

**September 16, 02:58 UTC — live governed prospect replies and reusable property-knowledge onboarding:** API `c8c5176bd556d6ea9e57af08bc31ca98fbb4c190` passed exact full CI run `35048455828` and is live at Render deployment `dep-dal05omk1f9s73d7tud0`; `/health` identifies `c8c5176`, and startup verified 186 applied migrations through ceiling 198. App `3607b3d5891a4a96b6b5183ead875b974863aef9` is live at `dep-dal06gek1f9s73d8017g`; the sanctioned suite passed 72 harnesses / 2,427 assertions / 0 failures and all served assets match. Prospect opening answers now select relevant descriptive shelves, route prices through governed pricing, avoid availability claims without an availability read, preserve numeric ranges and share the established legal/fair-housing/action safety floor. Existing platform roles are preserved when property access is added. The reusable onboarding packet now defines the same ten descriptive shelves plus canonical owners for staff, inventory, pricing, readiness, policy, lease and maintenance. Production content remains five live Skyline shelves and zero Greenery shelves; two safer Skyline successors and all remaining Temple cards are prepared but not published. The canonical Greenery is adopted into OneFive, while KZ/Mike person-level identity and John Franco's confirmed mobile/invite remain pending. No customer message, Team invite, OTP, inventory correction or pricing change occurred. See [release receipt](handoffs/new-hp/property-knowledge-onboarding/RELEASE.md).

**September 15, 20:04 UTC — live Ask Spine conversational grounding refinement:** API `6dfc3b31bde56f0c87bbb03d6b2148700e0a168d` passed CI run `35016506384` and is live at Render deployment `dep-dakq7se7bikc73daccv0`; app `ac6f837e8679a500305a083d804549c0f216ce69` is live at `dep-dakq5q61egvs73ar1j8g`, with all served assets matching. Approved leasing knowledge answers now open with natural topic-specific language, and the app renders grounding labels for people while retaining the underlying metadata keys. Live Skyline checks covered answered, mixed answered/missing, missing-only, and exact-home caveat responses; browser errors/warnings were absent. This is a wording/readability release only: no property data, inventory, pricing, lease configuration, access, Greenery custody or migration change.

**September 15, 22:11 UTC — live Ask Spine label polish:** app `2feed58b0bd25e481596051c4aa1541ed7fac0a4` is live at Render deployment `dep-daks45nf3r2c738fka30`; served assets byte-match the commit. Grounding now reads in operator language such as “About · Amenities” and “Still to confirm · Floor plans,” while durable topic keys remain in the DOM for audit and tests. A cache-fresh signed-in Skyline session confirmed the updated answer and label; no browser errors or warnings. This remains a presentation-only change with no property-data or authority changes.

**September 15, 22:40 UTC — live prospect-opening correction:** API `2a5a7486d6674c3d2a71af042d6ce59c0510c5e7` passed exact CI run `35031633254` and is live at Render deployment `dep-daksicajnfac73fq2jvg`; `/health` reports `commit_short: 2a5a748` and schema ceiling 198. The canonical opening response now receives the prospect's original question and current descriptive `agent_facts` through the shared Leasing Knowledge reader, so the first turn can answer the question before offering a tour. Rent, availability, readiness, fees, dates, exact-home identity and tour times remain governed by their existing readers. App remains `2feed58`. This is a code-level release; a real prospect message and full live lead-to-lease acceptance are still pending. No property data, Greenery custody, pricing or customer message changed. See `handoffs/new-hp/prospect-first-response/RECEIPT.md`.

**September 15 prospect conversation correction, source candidate:** the existing canonical first-response service now passes the prospect's original inquiry into its opening model turn and supplies current property-wide `agent_facts` through the shared Leasing Knowledge reader. A question such as “Does this building have a gym?” can be answered from approved property knowledge before the open tour invitation; rent, availability, readiness, fees, dates, exact-home identity and tour times remain governed by their existing readers. The ongoing `/agent/inbound` SMS loop and human takeover boundary are unchanged. Focused question-aware opening and bare-rent routing tests pass, plus all56 source-governance gates locally. Exact CI/deployment and live prospect acceptance remain open. No property data, Greenery custody, pricing, availability or customer message changed. See `handoffs/new-hp/prospect-first-response/RECEIPT.md`.

**September 13, 21:48 UTC — final integrated candidate, not released:** live remains API `d15c968` / app `336c82f`, schema 182/194, and Skyline occupancy is unreconciled. This tree carries the accepted bed-kind mapping, source-index 198 and CI repair, the full 194–198 release/recovery wrapper, and Claude’s current-rent-roll delivery plus QB review corrections. Product `9800489` passes 63 real HTTP/PostgreSQL18.6 checks: competing rights stay held, dated contractual conflicts remain reviewable, and current observations do not attach to future-only leases. Those are fixture results, not actual Skyline occupancy. The complete release recovery matrix passed 52/0 independently on PostgreSQL17 and18.6; cached app336 against owned schema198 passed19/0. Final exact-commit CI and combined app context/browser acceptance precede release. No production migration, source confirmation, classification, Greenery adoption or customer action occurred. See `docs/handoffs/new-hp/combined-194-198-release/QB_INTEGRATION.md` for component pins and precise limits.

**September 13 QB integration successor — not released:** this tree combines source-home review `f5dbc7e` (exact CI 504 succeeded), inventory correction `a0177f6` (exact CI 505 succeeded), the existing two-step leasing candidate, and the live rent-reader safeguard from `03ec80e` (exact CI 508 succeeded). Source-lineage policy covers migration 196's selected unit/space references. First combined CI 506 at `e88de2d` failed because its inventory hardening fixture skipped the new source review door: initial read-source returned `source_review_stale`, followed by 16 downstream failures. The reviewed test-only successor from `b0254a3` uses preview, token and explicit decisions; different raw/canonical parent labels prove durable selected identity and retirement protection. It passed 105/0 on a fresh owned database through 197; cleanup and 56 source gates were verified. Final combined CI and coupled app/browser acceptance remain open. Paired app `44e0e4` is pushed with 70 harnesses / 2340 passed / 0 failed and separate Chromium 13/0. Migrations 195–197 remain unreleased; no actual source confirmation, inventory correction or property acceptance occurred.
**September 13 institutional rent projection candidate:** the formal report now reads the canonical economics state before presenting a recorded amount as monthly contractual rent. Unavailable amounts serialize blank in both JSON and CSV; the status explains why, and source evidence remains in the operating detail. QB witnessed the defect in the live owner session on API03ec80e: rows labeled "Occupied — rent unavailable" still displayed $0 or $-375. Product5cbe40f extends the existing registered negative-rent DB/HTTP proof to17 checks; pure projection6/0 passed locally and is registered in the shared runner. Full CI and deployment are pending. The paired sortable ledger uses existing dated reads and reporting doors; no schema or business data change.
**September 13 canonical rent safeguard candidate — not released:** product commit `97cc552` withholds negative/nonfinite amounts from trusted contractual economics through the existing dated-position classifier, preserves original source/lease values, and keeps tenancy and possession unchanged. Canonical/institutional totals and CSV report unavailable rather than zero when no position contributes trusted rent. Ask's existing tenancy standing adds `occupied_positions_with_unavailable_contract_economics`; the separate null-only no-recorded-rent count keeps its meaning. The delivered candidate passed 15 owned DB/loopback-HTTP checks, 43 tenancy Ask assertions and all 56 source gates. QB then bound the registered proof to its nonce-owned manifest and unique per-run fixture identities; full CI of that successor is pending. This does not interpret or correct Skyline's retained Actual Rent source values. No production, schema, import or writer changes occurred.

**Current live correction, September 13 15:31 UTC:** API `03ec80ea8744fa78380407740baaa86991ba89da` and app `1efef934443125809aa7141d8aa43075302625fa` are verified Render Live. API exact CI 508 succeeded; all 24 public app assets matched and API health identified 03ec80e. The owner session was reloaded: Management and full Rent Roll agree at 160 positions across 72 units. App full suite 69 harnesses / 2293 passed / 0 failed, plus separate Chromium 13/0. Late Management responses cannot update a changed screen or property. Zero/negative/nonfinite source amounts do not contribute trusted contractual economics; raw source amounts remain inspectable. Production schema remains 182 applied / ceiling 194. This is signed-in read acceptance, not Mike's login or full property launch. Workspace receipt: `handoffs/new-hp/MANAGEMENT_GUARD_AND_RENT_RELEASE_20260913.md`.

**September 12, 16:17 UTC — verified live nonlocking projections:** API `dab19b6a84493093911427dfe2de29ee91451ac9` / unchanged app `e23a36a4588cdeda10eb3d902cea4b917f68dbe9` are live. Exact full CI34704388244/run483 passed, including no-consent journey155/0, historical read6/0,56 source gates and owned cleanup. The desk, historical bound/pending offer branch and standing projection now use nonlocking reads; writer locks and authority remain. Explicit Mike/KZ assertions and desk/queue rereads replace actor fallbacks. Render dep-dainlj67bikc739d9sd0 succeeded; public health and24/24 app files matched, fresh schema182/194. No property configuration, source activation or production journey acceptance. [Release receipt](handoffs/new-hp/no-consent-two-person-journey/RELEASE.md). Later receipt-only commits are not a new deployed SHA.

**September 13 source-to-home identity review candidate, local only:** retained rent-roll bytes now preview grouped source identities before inventory changes; explicit reviewed existing/new/existing-parent decisions bind lease proposals to durable canonical IDs in the existing proposal history. Greenery shape proves105 beds under64 existing parents while preserving171 units,212 total spaces,64 consumed-placeholder IDs and107 untouched unknown identities. Focused mounted HTTP54/54, ordinary Deal Setup HTTP32/32, shipped-app Chromium11/11 and all56 source gates pass. Migration196 is isolated and unapplied outside the proof DB; no deployment or real-property acceptance. [Candidate receipt](handoffs/new-hp/source-home-review-20260913/RECEIPT.md).

**September 12, 15:55 UTC — app-only reusable onboarding release:** API1320c2c remains live; app `e23a36a4588cdeda10eb3d902cea4b917f68dbe9` is live. Sanctioned app suite67 harnesses/2253 assertions/0 failures/exit0; Render dep-dainbnrm8hqs73dimq8g succeeded and24/24 served app assets matched. Leasing setup reads existing knowledge/tour/lease owners; Deal Setup exposes the existing authorized-property picker and verified retained-source download, with mobile layout repair. Initial packaging failure was contained; only leasing-setup.js was added to the static build copy list. No API product/data/schema change or real-property acceptance. [Release receipt](handoffs/new-hp/reusable-onboarding-release/RECEIPT.md). At this historical app-only checkpoint the API correction was pending; the16:17 release above supersedes that status.

**September 12, 13:37 UTC — verified live staff release:** API `1320c2c5a6e52caa15de967670ed826ea1d10a84` / app `4fd0af8f006fd2a926dd422604696e8312501686` are LIVE. Exact API full CI34696419417 passed; the final integrated app sanctioned runner passed65 harnesses/2156 assertions/zero failures/exit0. Both exact Render deployments succeeded; public health identifies1320c2c and23/23 served app files byte-match4fd0af8. Fresh schema remains182 applied/194 ceiling. This releases capture-only authenticated website intake, explicit external inquiry-email attestation through communications/UI/Ask, authorized unsent-offer correction and restricted-Mike manual application preparation/recovery. Actual owned HTTP/Chrome slices passed34 application and36 initial-email checks. Website cutover, real staff-session eligible-home/tour acceptance and Greenery organization/source reconciliation remain open; no new property-data/configuration, customer message or production leasing acceptance. Immediate recovery is b4a494c/993d408. [Portable release receipt](handoffs/new-hp/day-one-staff-release/RECEIPT.md). Any later receipt-only commit is documentation, not a claim that its SHA was deployed.

## Historical checkpoints — release/status language below is as observed then

**September 12 combined API accepted, release preparation:** exact `da04cb8b3ceccd1a4712955369f1dbd83759af86` passed full CI34695989569: external email36, capture-only26, delivery replay37, Greenery legacy42, Skyline88, empty Greenery34, all56 source gates and owned cleanup. The initial combined run stopped on a proof-only foreign-fixture name mismatch; its successor requires the canonical runner's property ID. Existing conversation reply records an attested external email through the interactions owner; queue, Person and Ask share that event. Waiting-on can become prospect while human takeover custody remains in progress until explicit handback; later inbound returns attention to the same owner. Actual sibling-app Chrome passed36/36 on API f179ad2 (same product as da04cb8) and app5b14fe6, covering email-only and phone-without-consent capture, exact retry, reload, attributed history and later question. The broader app suite remains under test-only repair; no full app-suite or production acceptance claim yet. Greenery's42 checks preserve171 unit IDs and171 space-to-parent IDs and require exactly46 extra units under the wrong export convention. Its84/21/80% remain synthetic fixture values. Live remains b4a494c/993d408; fresh read-only schema182/194 and Render live source were rechecked. See [external reply](handoffs/new-hp/website-reply/EXTERNAL_EMAIL_REPLY.md), [actual browser](handoffs/new-hp/external-email-ui/RECEIPT.md) and [Greenery review](handoffs/new-hp/greenery-legacy-inventory/QB_REVIEW.md).

**September 12 no-consent two-person journey (local, corrected candidate):** on the released pair `1320c2c` / `4fd0af8` one prospect walked website inquiry with no consent → Mike's ownership and recorded external email → native tour → KZ-authored and corrected terms → Mike's manual-email preparation with retries and regeneration → attestation → application with guarantor → KZ approval, terms and execution → one pending lease on the exact bed, 103 checks 0 failures after a three-file correction. Witness on unmodified source 99/4: the Leasing desk returned 503 for the property once a submitted application was bound to an offer (row lock inside a read-only transaction; present in every release since Sept 9). Browser slices on the pair 34/34 and 36/36. See row 69 and [receipt](handoffs/new-hp/no-consent-two-person-journey/RECEIPT.md).


**September 12 capture-only intake accepted, not released:** exact `4dafc97abbbee6f7e1b0d4d24096cdbd7fdab898` passed full CI34695386313. Explicit authenticated `attempt_sms:false` captures without a model response or unsent outbound text. Omitted/true attempts and the public demo retain their contracts; historical replay retains its original receipt. Logs confirm focused HTTP26, delivery replay37 including worker death in both response modes, unchanged inquiry-state42, all required proofs and owned cleanup. Initial-inquiry email recording is the separate successor above; application invitation attestation does not answer that gap. See [capture-only correction](handoffs/new-hp/website-reply/CAPTURE_ONLY.md).

**September 12 application integration accepted, not released:** full configured CI34694595189 succeeded at exact `fd8fe0b72f670cb0e0be55b73930d51a81d61747`: draft correction54/0, canonical draft read4/0, manual-email HTTP20/0, Skyline journey88/0, deliberately empty Greenery34/0, all56 source gates and owned cleanup. Actual sibling-app Chrome/owned HTTP then passed34/34 with a restricted Mike-shaped actor and a separate authorized HTTP offer author: native edit403, authorized correction/reload, unchanged offer preparation with no reauthoring, lost-response replay, link replacement/reload, copy and explicit synthetic email-send attestation on the same exact bed. App product `e5c6d8b` matches the tested `7ebbd3a` after LF normalization. This bounded browser slice uses HTTP for authoring and tour preconditions; it is not deployed Mike acceptance or the entire no-consent applicant-to-lease journey. The broader app harness has failures under investigation against the live baseline; do not call it green. Live remains b4a494c/993d408. See [paired browser receipt](handoffs/new-hp/staff-shell-application/RECEIPT.md), [manual preparation](handoffs/new-hp/MANUAL_EMAIL_APPLICATION_20260912.md) and [draft recovery](handoffs/new-hp/draft-offer-correction/READ_RECOVERY.md).

**September 12 empty Greenery CI registration:** full configured CI34692918961 succeeded at exact8cf5078752e62785d239f5721e735fcadb64ec37. Logs verify Skyline89/0 through pending lease, empty Greenery34/0 through its intended exact-home stop, all56 source gates and owned database cleanup. The Greenery journey runs in a sequential server phase with a fresh synthetic property ID. This deliberately empty fixture does not rehearse production's171 legacy unit/space records. No product, migration, provider or production change in this registration. See [registration and isolation](handoffs/new-hp/staff-assisted-journey/GREENERY_CI_REGISTRATION.md).

**September 12 current unsent-draft recovery, local owner read:** `readCurrentApplicationDrafts` shares canonical offer currentness and retained-term validation, returns exact draft IDs/home/terms in a batch read-only query, and reports historical ambiguity without selecting a winner. Owned correction HTTP proof now passes 54/0, with four additional recovery reads; separate historical-state READ ONLY proof passes 4/0. Unit and 56 source gates pass. Existing capability/app composition remains a separate integration step; no browser, full CI or production claim. See [read recovery receipt](handoffs/new-hp/draft-offer-correction/READ_RECOVERY.md).

**September 12 unsent application-offer correction, local API candidate:** observed on unmodified `6cc9c7f`: manager saves 1250 before any invitation, correction to 1025 returns 409; new-key duplicate creates leave multiple current drafts. Existing staff route now permits an explicitly named uncommunicated draft successor before any invitation/application, retains immutable terms and actor lineage, serializes creation/correction and refuses unnamed duplicates. Owned HTTP 50/0 proves retries, competing creates/revisions, authority/property/exact-home scope, old confirmation refusal, fresh corrected send and retained post-invitation applicant review. Canonical offer unit test and 56 source gates pass. No app change, CI, deployment or production acceptance claim. See [receipt](handoffs/new-hp/draft-offer-correction/RECEIPT.md).

**September 12 Greenery legacy-inventory establishment rehearsal (local, proof-only):** on unmodified `8cf5078` the 171-row legacy shape (64 parents, 107 other rows, placeholders, no use, no lineage, no organization) was adopted, provisioned through the super-admin door, and established through Deal Setup with the bed basis on the retained real labels, 105/105 rows, every legacy identity preserved, 40 checks 0 failures. Findings: the 107 untouched legacy rows stay in the rentable denominator (39.6% reported vs 80% over source positions); no door reconciles or excludes a legacy row; the tracker's unprefixed labels read as-is create 46 new units and land 18 rows on legacy records, so the upload must carry the `1325-` parent; the super-admin door writes the account's organization. No product change. See row 68 and [receipt](handoffs/new-hp/greenery-legacy-inventory/RECEIPT.md).


**September12 QB review of Claude staff-assisted journey:** exact5cf3258 and full CI34665396220/run470 verified. Historical Skyline84/0 completes an explicit staff-assisted exact-bed chain through pending lease; deliberately empty Greenery30/0 stops at configuration and is not production inventory evidence. Resident browser151 is separate; staff operator shell remains unexercised. Tests/docs integrated above live b4a494c; successor makes early Skyline stop fail, enforces exact tenancy/read agreement and execution/activation/possession separation, and includes current website-question capture. Fresh production read finds Skyline price/lease inputs already present; Greenery171 legacy unit/space records require source-linked establishment/classification, not blind replacement. No product change or new deployment. See [QB review and limits](handoffs/new-hp/staff-assisted-journey/QB_REVIEW.md).

**September 12 website inquiry visibility (local successor):** authenticated website questions now reach the existing inbound communication owner, preserving source-touch/event provenance, exact replay, thread version and human/review control. Conversation, Person Card, agent context and entitled Ask read that same capture. Unhandled website questions route to staff; email-only cards expose recorded contact without an unusable SMS composer. Owned HTTP visibility29/state42/classification18/replay33, real-agent confirmation12 and actual sibling-app browser16 pass. No migration, historical backfill, provider send, production journey or website activation claim. See [mechanism and evidence](handoffs/new-hp/WEBSITE_INQUIRY_VISIBILITY_20260912.md).

**September 12 Greenery essential content preparation (data-only candidate):** existing Temple packet now selects six Greenery source-attributed drafts through the unchanged offline staging tool, bound to retained addressed Greenery UUID. Official pages and representative media links were rechecked; prior drafts/dates preserved. Current manager contact, exact-home media associations, measurements and operating policies remain unestablished; stale website manager details excluded. Offline payload assertions only, no fresh Greenery DB/HTTP/Ask or production publication. See [load recipe and remaining sources](content/temple/GREENERY_ESSENTIAL_LOAD.md).

**September 11 evening Skyline essential content preparation:** integration `fdb01c6` reuses the existing 20-card Temple packet and selects two cards from dated owner-confirmed furnishing/amenity facts. Offline staging is bound to the verified Skyline UUID and emits only existing authenticated knowledge writer requests; no parallel store or runtime writer. Isolated full-server HTTP proved exact writer/workspace/Ask/prospect-context agreement, unchanged repeat, scope, expiry and replacement-history behavior. Original drafts and other 18 cards preserved. No production publication, new owner approval or browser claim. See [load recipe and proof](content/temple/SKYLINE_ESSENTIAL_LOAD.md). Live API remains `992a8db`, paired app `2c57e71`; prepared content does not fill production shelves by itself.

**September12 QB staff onboarding successor, local DB/HTTP:** Fable `e0abebd` is verified at full CI run34658921279, then tested atop released992a8db. Organization invites now refuse existing foreign, unaffiliated and super-admin identities without mutating them; account and assignment writes are atomic. Same-org phone-only provisioning, preserved platform role, existing inactive-assignment reactivation, Team confirmation/OTP and both-property Leasing access pass52 checks, zero failures. Schema194 unchanged. No production onboarding or deployment claim. See [QB review](handoffs/new-hp/greenery-staff-onboarding/QB_REVIEW.md); earlier Fable39/0 evidence remains historical.

**September 12 overnight staff-assisted journey, local DB/HTTP (not deployed):** on unmodified API `d45d768` one prospect walked website inquiry → staff ownership → native tour → actual outcome → exact bed → manager offer → Ask Spine send with confirmation → offer revision with applicant review → application with guarantor → approval → packet → resident and guarantor signatures → company execution → one pending lease on the exact bed, 84 checks, 0 failures; the Greenery shape passes the same staff steps and stops at exact home with no established position (configuration, not code). Resident-facing pages driven in real Chromium by the existing suite (151). Operator shell not driven. No product source changed. See row 67 and [receipt](handoffs/new-hp/staff-assisted-journey/RECEIPT.md).

**September 12 authenticated intake delivery replay (local candidate):** optional `Idempotency-Key` on existing `POST /leasing/intake` binds a delivery to property + original source label and stable JSON payload. Reuses immutable `lead_received.metadata` and transaction advisory locking; no new table/route or migration. Exact retries read the captured receipt before person/touch/draft/send; changed payload conflicts. `source_lead_id` remains provider provenance, and headerless arrivals keep prior repeated-touch behavior. Incomplete post-capture response returns `first_response_sent:null`, no new draft/send, and an explicit unestablished completion receipt. Owned full-server HTTP/Postgres proof covers sequential/concurrent retries, conflicts, legacy behavior, source/property scope and post-capture response failure; registered in the existing activated intake CI phase. No production/provider action or remote CI claim. See [receipt](handoffs/new-hp/INTAKE_DELIVERY_REPLAY_20260912.md).

**September 11 staff conversation booking HTTP acceptance (local candidate):** committed adapterdf49a3c passes27/27 owned real HTTP/Postgres controls: authenticated inquiry → staff claim → exact native future slot → existing actual-outcome capture → named canonical follow-up work. Scope/replay/competing requests, current host and staff eligibility, stale/foreign/missing slots, no confirmation send and preserved inquiry work are covered. Required full-runner step registered; harness isolation8/8 and proof syntax pass. No browser, remote CI or production claim from this proof lane. See [receipt](handoffs/new-hp/CONVERSATION_TOUR_BOOKING_20260911.md).

**September11 staff conversation native booking (local candidate):** installed Person Card lacked future booking while the canonical service existed behind a key-only adapter. New staff-session/leasing-entitled conversation adapter derives property/person/lead/actor, validates scope before namespaced replay, refuses changed-slot key reuse, and delegates existing bookTourIntoSlot. App reads existing native slots and books explicitly, with no SMS/completion claim. Installed render/async controls and20native source gates pass; real HTTP/browser pending separate acceptance. No schema/provider/deploy. See [receipt](handoffs/new-hp/CONVERSATION_TOUR_BOOKING_20260911.md).

**September 11 captured inquiry staff ownership (local candidate, not deployed):** two scoped staff sessions reproduced takeover recording only a human mode without its actor; the existing thread-to-obligation link now persists one accountable staff owner. Detail, scoped work and Ask read that same obligation. Competing takeover/handback/draft approval refuse; completed prior ownership is preserved; wrong-property/person links are not displayed or mutated. Attention now includes the existing active states open/in_progress/blocked/escalated so claimed work does not disappear; explicit collection status filtering remains exact.35 owned HTTP/Postgres checks and all56 source-governance gates pass locally, including consent-free recorded-not-delivered replies and opted-in fake-transport positive control. CI registration uses the existing activated intake phase. No migration, real send, production or browser claim from this lane. See [receipt](handoffs/new-hp/STAFF_INQUIRY_OWNERSHIP_20260911.md); QB records actual-app browser evidence separately.

**September11 explicit application target preserved (candidate, not deployed):** QB integrated workerad3f31e as126cc9e after review. Fable's occupied Bed A→free Bed B proposal substitution independently reproduced through owned HTTP; successor clarifies with no substituted target. QB reran staff-action68 and prospect-confirmation42 controls; worker56 source gates pass. Same independent HTTP challenge moved126/3→127/2; remaining two assertions require review of shared-apartment physical readiness scope, not a claim of readiness completion. No schema/app/provider change. See [receipt](handoffs/new-hp/EXACT_TARGET_REFUSAL_20260911.md). Existing whole-unit/sole-space selection, authority and immutable terms remain owners.

**September 11 post-release continuation (local, not deployed):** isolated workers reproduced and corrected historical prospect false unit confirmations and sibling expected-date inheritance. QB integrated worker81b7069 and951c2c4 into the released-source continuation. Matcher42 controls and worker real agent/DB12 pass; readiness14 domain +7 staff HTTP reconciliations independently rerun by QB. Runtime cleanup verified. No schema/app change; exact-space selection and prospective planning remain open. Knowledge stewardship review found no established review-task creation/delegation contract; no authority was invented. Fresh read-only Temple identity counts show addressed Greenery has inventory but no organization/team assignment, and both addressed properties have0 current agent facts. See [confirmation](handoffs/new-hp/PROSPECT_CONFIRMATION_20260911.md), [readiness](handoffs/new-hp/EXPECTED_READY_SCOPE_20260911.md), [stewardship](handoffs/new-hp/KNOWLEDGE_STEWARDSHIP_PREBUILD_20260911.md), and [identity read](handoffs/new-hp/TEMPLE_IDENTITY_READ_20260911.md). QB combined source verification passed all56 gates; focused matcher42 and inventory-date units also passed. Both new regressions are now registered in the full CI runner. This is not a full combined CI/DB/browser/deployment receipt.

**September 11 combined release is live:** API `24f4482140bed554185fe4fdcbb7c2366a5d3c07` paired with app `b00cf4993e4f699cd174a7ef5c8a27ca4b5507c4` pushed on `codex/hp-release-integration-20260911`; full API CI run34627440787 succeeded at the exact source. Both Render deployments succeeded. Fresh API health identifies24f4482; verify-only startup confirms182 migration entries through194; all23 served app files match appb00cf49. Public browser sign-in smoke passed. No new migration, content publication or signed-in production journey acceptance. This supersedes earlier upload-blocked/local-only claims for this pair. See [release receipt](handoffs/new-hp/COMBINED_RELEASE_20260911.md). Selection/readiness/stewardship continuation workers use isolated branches; no later product change is claimed here.

**September 11 combined successor verified:** API506651e + scope correction7ff043c paired with appb00cf49 passed56 source gates, owned schema194/full-server knowledge HTTP/SMS, sibling and triage/detailed-scope browser-to-HTTP, exact-space informational matching/date proofs, real knowledge-editor browser and151 tour-to-exact-bed-tenancy assertions. Representative resources now explicitly leave exact-home association unestablished. Owned runtime cleaned. Git upload blocked on workstation write credentials; this combined candidate is local-only, not deployed. See [receipt](handoffs/new-hp/COMBINED_INTEGRATION_20260911.md). Earlier in-progress entry below is historical.

**September 11 combined candidate:** HP knowledge/coverage and informational matching at 12bb184 are combined with deployed desktop 7629b63, paired with app 1a7a192 + bf86a77. Both histories and canonical owners are preserved; only the dated entries in this file conflicted. Source governance56, focused knowledge/matching/Ask/SMS/required-work units, app authorization35/live-first9 and both work-scope component tests passed. Owned full-server/browser proofs are in progress; this candidate is not deployed. Desktop pair 7629b63/bf86a77 was deployed September10 with schema194; older no-deploy statements below describe historical slices. HP QB owns continuation.

**2026-09-10 maintained knowledge workspace:** existing agent-facts GET now projects a reusable ten-topic checklist with current/missing/expired/retired coverage from one scoped read. Paired app adds guided questions, visible history, unsaved-change protection, outage retry and Leasing-desk entry. Coverage/routing units, extended owned HTTP/SMS and Chromium proof pass; all56 source gates and app authorization35/live-first9 pass. No schema change or deployment. Drafts, designated review ownership, reminders and formal onboarding completion remain open. See [receipt](handoffs/new-hp/KNOWLEDGE_WORKSPACE_20260910.md).

**QB final local checkpoint, 2026-09-10:** merge b773784 integrates informational exact-space matching with possession and knowledge work, paired app f6dadcc. Product source equals worker c20701f, whose 56 governance gates passed; QB final matching DB/service, sibling HTTP, knowledge HTTP/SMS and app browser all pass. Owned database drop and runtime shutdown verified. See [receipt](handoffs/new-hp/QB_INTEGRATION_20260910.md). Next: durable exact-space selection and [Temple identity investigation](handoffs/new-hp/TEMPLE_IDENTITY_NEXT.md). No production identity/content changes or deployment.

**2026-09-10 QB combined candidate:** isolated integration a3e1421 combines HP dec88db possession changes with b095728 knowledge/content, paired app f6dadcc. Both dated evidence sets retained. Fresh 55 source gates, possession/knowledge/phone-focused units, owned migration chain193, sibling HTTP, knowledge HTTP/SMS and paired app browser pass. See [integration receipt](handoffs/new-hp/QB_INTEGRATION_20260910.md). No main merge, deployment or Temple content publication. This initial integration preceded the informational matching successor below; exact selection remains open.
**Exact-space informational prospect matching, 2026-09-10 (candidate only):** QB observed the owned real-agent first red after canonical fixture preconditions passed: free sibling omitted, legacy-rent budget errors, pricing term/space/provenance missing. The prospect tool now requests explicit pricing months and uses exact-space mode in existing `leasing_inventory`, composing `leaseableApplicationTargets` and `resolveSpaceEconomics`. Budget/sort/limit follow published new-lease rent; bed/unit rent basis and whole-unit dimensions remain explicit. Unknown pricing and failed reads remain distinct. Durable offered evidence includes exact space and pricing authority; model context excludes raw IDs. Selection remains disabled. QB successor and date proof passed with real Postgres plus scripted in-process model; no HTTP/provider/browser/deployment claim. Worker unit controls and 56 source-governance checks passed. Legacy direct inventory mode remains a named compatibility adapter. See `docs/handoffs/desktop-qb-20260906/tenant-journey/PROSPECT_MATCHING_FIRST_RED.md` for limits and receipts.
**Detailed-scope correction containment, September10:** owned HTTP first red showed another unit's scope could be named as the correction predecessor. Existing service now checks unit/property before writing; successor refuses without partial scope or work withdrawal and preserves valid correction history with original and corrected bed identities. Owned cleanup verified. See TURN_SCOPE_TARGET.md adjacent correction receipt. App bf86a77 unchanged; no deployment.

**Detailed planning retains work location, September10:** owned HTTP first red reproduced the detailed turn-scope writer dropping explicit bed identity. It now shares triage target validation, persists the same work fields and forwards them through planning/inherited/sequence reads. App detailed-scope selector and sequence display tested; combined component-browser-to-real-HTTP successor and sibling possession controls pass, owned cleanup verified. Read tenant-journey/TURN_SCOPE_TARGET.md. Expected-ready scheduling remains open; no deployment or new schema for this extension.

**Explicit maintenance work location, September10:** existing triage capture now retains an explicitly chosen whole-unit or rentable-space target; absent scope stays unspecified. Canonical work/flow/Unit Turn and entitled Ask gathering retain it. Owned first reds for discarded identity, dropped HTTP fields and cross-unit correction history were repaired; successor DB/HTTP and app-component-to-real-HTTP browser passed and cleaned. Ask attention/quiet/failure tests and all54 source gates pass. Migration194 is disposable-proof tested only; no deployment. Read tenant-journey/WORK_SCOPE_SUCCESSOR.md for limits. Desktop remains QB; HP setup/review runs in parallel. Expected readiness and automatic pricing matching are not completed by this identity slice.

**Sibling conflict and competing move-out coverage, 2026-09-10:** owned HTTP first red reproduced a false vacant cache under overlapping sibling rights. The cache now respects the existing classifier's conflict verdict. Seven owned HTTP cases pass, including simultaneous move-outs yielding one201/one409 and one turn; cleanup and source gates54 pass. Proof registered in full CI. See tenant-journey/SIBLING_CONFLICT_AND_REPLAY.md. Next implementation is explicit prospective-work scope identity per WORK_SCOPE_IDENTITY_PREBUILD.md; readiness is not derived from task dates. No deployment.

**Shared-unit vacancy uncertainty, 2026-09-10:** owned HTTP first red showed a lease-only sibling incorrectly becoming vacant in the unit cache after the other bed's move-out. Existing canonical read now carries ended-possession provenance; shared-unit cache distinguishes recorded ends from missing evidence. Five HTTP scenarios pass, including lease-only unknown and last-bed vacant; dates/provenance8 and source gates54 pass, both runtimes cleaned. See tenant-journey/SIBLING_VACANCY_UNCERTAINTY.md. Actual-import, overlapping-rights, concurrency and browser acceptance remain open; no deployment.

**Overnight possession date correction, 2026-09-10:** the shared classifier applied asOf to leases but included future possession events. Unit first red reproduced tomorrow's arrival as current. Existing classifier now filters possession events by effective date. Date6 and interval38 pass; owned HTTP sibling proof now passes live sibling, future arrival, future departure and last-bed cases, with exact event identity preserved and cleanup verified. Source gates54 pass. See tenant-journey/POSSESSION_EFFECTIVE_DATE.md. Imported-lease-only/unknown cache cases and concurrency remain open; no deployment.

**Overnight live-possession sibling repair, 2026-09-10:** independent owned HTTP first red reproduced Bed A's move-out relabelling Bed B's still-possessed unit vacant. Existing turnover writer now reuses the canonical as-of position reader before updating its compatibility cache. Successor preserves Bed B's possession/lease and occupied unit label; both runtimes cleaned, route bridge13 and source gates54 pass. See tenant-journey/TURNOVER_SIBLING_CACHE.md. Imported lease-only/unknown sibling and last-bed/concurrency cases remain open; no general occupancy reconciliation or deployment claim.
**2026-09-10 Temple content fill:** Prepared 20 sourced topic cards (10 per property) in [content package](content/temple/LEASING_CONTENT.md) and matching JSON for the existing fact contract. Fresh official pages checked; historical/contradictory policies excluded, two dimension gaps explicit. Fresh read-only runtime reports d55dae9; Neon production ledger 192, six Skyline/Greenery-named identities, zero active agent facts across them. No production import: live writer lacks new topic keys and target identity must be resolved. Content validates against topic/source/length constraints; this is content preparation, not DB/browser proof or approved policy.

**2026-09-09 leasing knowledge candidate:** Existing agent facts now feed a live editor, prospect context, in-app Ask Spine and staff SMS knowledge retrieval. Property-wide approved wording/links, expiry, history and leasing entitlement preserved; Solo-only follow-up tour constants removed. Owned full-server HTTP/SMS and real app browser proof pass; all 55 source-governance gates pass. No deployment or real property content import. See [receipt](LEASING_KNOWLEDGE_RECEIPT.md) for paired branches, proof boundaries and remaining content/layout verification.

**Phone terms use the requested dates, 2026-09-09:** first red proved that a text requesting move-in before recorded turn readiness could save an offer. Phone now supplies the full dates to the existing target reader, explains excluded-bed reasons, and revalidates through the existing target authority inside preparation's transaction. Focused HTTP 50/50 and full phone-origin application/signing/exact-bed tenancy 162/162 passed; all owned runtimes cleaned and 54 source gates passed. See tenant-journey/PHONE_TERMS_DATES.md. No deployment or prospective-turn-planning claim; exact-space published-price matching remains open.

**Prospect inventory date connection, 2026-09-09:** the existing agent tool now carries explicitly stated start/end dates to its existing inventory reader; invalid dates are distinct from missing dates. Real agent-service/Postgres first red and successor passed with a scripted local model; informational results cannot become selected through yes. No provider/HTTP/deployment or complete matching claim. All54 source gates passed. HP CI's notice response mismatch reproduced and test corrected; combined notice54/37/18 passed. See tenant-journey/PROSPECT_INVENTORY_DATES.md. Exact-space published-price matching remains the next cutover.



**New HP handoff preparation, 2026-09-09:** excluded application homes now retain canonical dated refusal reasons in the API and shared picker. Unit and owned DB first reds reproduced; successor DB/HTTP/post-tour picker browser path passed and cleaned. See tenant-journey/EXCLUDED_TARGETS.md and docs/handoffs/new-hp/README.md. Matching, prospective turn planning and production acceptance remain open. GitHub checkpoint custody will be recorded separately; earlier entries are historical.



**Combined phone-origin integration:** owned57230e9d passes159 assertions from phone-authored terms and phone confirmation through tenant application, approval/signing, exact-bed tenancy and reads. Owned cleanup verified. This advances the prior separate47/151 evidence; scope/fixture correction in tenant-journey/PHONE_TERMS.md. Local only; browser/provider/release acceptance and documented text limitations remain open.

**Local phone terms checkpoint:** explicit terms can arrive in parts, show complete economics and advance via Confirm application. Existing canonical owners reused;47 owned HTTP checks plus separate151 full-path control and54 source gates pass, cleanup verified. See tenant-journey/PHONE_TERMS.md for exact custody, first red and limits. Structured fees/concessions and revisions by text, broad language and production acceptance remain open. Working changes only, not deployed; automatic heartbeat remains paused.

**Desktop local checkpoint2026-09-09:** confirmation now binds the selected application offer identity/hash. Root firstred1f1816e7 demonstrated a send after service-established replacement; successora8f699f2 rejects it through staff SMS. Full successor4a37c305 passes151 assertions through exact-bed tenancy,54 source gates pass, owned cleanup verified. Includes the earlier ambiguous-offer refusal receipt repair. These are working changes above9f2acff, not deployed. Phone-only complete terms review/authoring, browser acceptance and release remain open. See docs/handoffs/desktop-qb-20260906/tenant-journey/SMS_OFFER_BINDING.md. Automatic tenant-phone heartbeat is PAUSED; older entries below are dated history.

**Shared Leasing standing now reads recorded tour history:** exact SMS notes/standing/actor survive into the canonical reader Ask uses; history explicitly distinguished from current readiness. Owned9d90c376 first red, successor20508189 passes with property separation and failed/timed-out controls;54 source gates pass. Both runtimes cleaned. Ask HTTP/model answer and broader correction history remain OPEN. See tenant-journey/SMS_LEASING_STANDING.md. No deployment/live sends.

**Person Card mobile history now displays recorded outcome notes:** actual pcLiveHistoryHtml browser first red missing notes; successor passes at390x844 with escaping, wrapping and absent-note controls. Existing signal tests24/24 pass. Separate owned HTTP proof already passed; combined full-shell browser/HTTP and Ask remain OPEN. See tenant-journey/SMS_PERSON_CARD.md. No deployment/live sends.

**SMS outcome now reaches Person Card HTTP:** exact original notes, explicit standing and staff actor reconcile from immutable event into one existing history entry. First red2d5b2b13; successord3cc9d92 passes and both runtimes cleaned;54 source gates pass after final source change. Existing tour_outcome vocabulary reused, no new store. Browser and Ask still OPEN. See tenant-journey/SMS_PERSON_CARD.md. No deployment/live sends.

**SMS immutable outcome notes proven through owned HTTP:** existing completeTour now records notes independently of legacy v2 outcome fields. First red39f34645; successorf7dcb69a preserves exact text, explicit standing and staff actor on one completed event;54 source gates pass. Both proof runtimes cleaned. Person Card/Ask v3 readers remain OPEN; worker sms_reader_trace finished. See tenant-journey/SMS_OUTCOME_NOTES.md. No deployment/live sends.

**Fable notice repairs integrated locally and independently HTTP-proven:** root reproduced sibling failure on current candidate, applied both hash-matched patches, then grain37/37 and challenge18/18 passed through owned Postgres/HTTP with full migration preconditions (087/110 applied). Source gates54 pass; both runs cleaned. API board labels preserved, browser and shared-key property authorization remain OPEN. See tenant-journey/NOTICE_QB_ACCEPTANCE.md. No deployment/push.

**SMS original feedback preserved through clarification, owned HTTP proven:** completeTour now receives the original prompt-linked body through its existing feedback.notes input. Conversion notes first red41053307, successor1f083a03 passed and cleaned;54 source gates pass. No live sends/deployment. Partial pre-standing visibility and full phone application/terms remain OPEN. See tenant-journey/SMS_OUTCOME_NOTES.md.

**Fable F1 bounded repair, local service proof:** executed-lease recording and admission now detect sibling-bed disagreement and retain application_space_id. First red 76/79; successor81/81, overlap8/8, source gates54 pass. Existing recording-versus-activation separation preserved. Owned DB/HTTP and browser picker proof remain OPEN. See tenant-journey/EXECUTED_PREMISES.md. No deployment or push.

**Overnight SMS continuation proven locally:** same outcome prompt now retains original send-app request through the standing reply. Real owned7b778bbb run passed;54 source gates pass. Exact actor/property/thread scope, replay, preferences and no-invitation gates preserved. Bare unbound replies no longer complete a different tour. Partial tour/app/Ask visibility, freeform notes and full terms/matching/phone confirmation remain OPEN. See tenant-journey/SMS_PROMPT_CONTINUATION.md. No live send/deployment; heartbeat stays active.

**Overnight SMS preference seam proven through owned HTTP.** Exact owner wording now saves asserted home/timing preferences before clarification; later outcome reply preserves them. Real preference-write failure returns an honest retry receipt and recovery passes. Final owned9924aa39 run passed/cleaned;53 source gates pass. Full original-send-intent continuation and partial tour outcome/Ask reconciliation remain OPEN. Existing tour_outcome_prompts is the next reuse candidate; worker idle. See tenant-journey/SMS_PARTIAL.md. No live sends/deployment.

**Overnight SMS first repair, local only:** owner text now retains send-app intent and bedroom no longer counts as an exact bed target. Three first-red cases repaired; action59/router40 checks pass. Partial facts/intent carry-forward is STILL OPEN. Lower-cost sms_partial_trace owns proof-only helper tests/e2e/staff_sms_partial_capture.js; root owns implementation. See tenant-journey/SMS_PARTIAL.md.

**Fable review received; contributor omission repaired locally.** Fable verified the frozen1056-file source package. Live Turn priority now exposes canonical bed/space contributors with lease state and known/unknown start dates. Browser component red/green passed; backend unchanged. Legacy renderer fallback reachability remains under review. See tenant-journey/FABLE_CONTRIBUTORS.md. No deployment.

**Review access repaired; prospective planning remains scoped.** Frozen API/app working-source ZIP in Downloads contains1056 hash-verified source files for Fable. Not sent externally. Existing maintenance deadlines are connected; source review finds no reachable pre-move-out expected-completion writer. No lifecycle build accepted until exact-bed/sibling and correction boundaries are proved. See tenant-journey/PRE_MOVEOUT_REVIEW.md.

**Latest local checkpoint: recorded prospect context and mobile review.** Both shared selection entries reuse Person Card preferences, preserving zero, raw text and failed-versus-empty reads. Populated mobile review exposed an off-screen Cancel; existing sheet now scrolls. Focused owned DB/HTTP/mobile browser chain passed and cleaned up. See tenant-journey/PROSPECT_CONTEXT.md. No automatic recommendation or deployment claim.

**Latest local repair: prospect-needs read failure.** Existing prospectVitals returns503 on failed attribute lookup instead of healthy fallback/nulls. Canonical capture/correction, conversation/Card reconciliation and failure/recovery proven through owned HTTP;51 source gates pass. Next: attributed context in shared home selection. See tenant-journey/PROSPECT_VITALS_FAILURE.md. No deployment.

**Latest local repair: turn-date meaning.** Removed inferred vacancy-window arithmetic; both shared picker doors display canonical lease-end/expected-ready dates. Red/green DOM, owned HTTP/browser and source gates50 passed. Next: existing prospectVitals swallows read failures; preserve failure and provenance before using it for recommendations. See tenant-journey/TURN_DATE_LABELS.md.

**Latest proof: competing homes and maintenance, local only.** Real offer HTTP rejects overlapping outgoing rights/unknown evidence and accepts a feasible competing home; refusals roll back. Existing maintenance HTTP ranks committed > pending > offer-only, with no deadline from an application offer. See tenant-journey/COMPETING_HOMES.md. Product source unchanged this checkpoint; no deployment.

**Latest checkpoint: shared application review, local only.** Conversation and post-tour now mount one existing date-first/complete-terms component. Cancel/Back late-response and changed-offer retry defects have red/green controls. Owned post-tour HTTP/browser read passed; full-shell visibility reliability remains open. See `handoffs/desktop-qb-20260906/tenant-journey/SHARED_REVIEW.md`. Older dated entries below are history, not the current post-tour gap. No deployment.

**2026-09-08 date-first conversation picker, local only:** requested start/end now
reach the existing leaseable-units API and canonical full-term check. The conversation
picker asks dates before homes, carries them into review and preserves them on Back.
First red admitted a home ready after the requested date; unit successor7 and owned
Postgres/HTTP/Chromium picker-function proof passed. This is actual extracted app
functions on the owned API, not full-shell navigation or a send. Post-tour
followups-door.js still has the older picker/direct-send path; consolidation onto
existing terms review remains. See TURN_SELECTION.md for proof limits/custody.

**2026-09-08 forward selection / turn-window first repair, local only:**
Owner requires home choice and maintenance planning to account for future turns.
Owned Postgres reproduced a conflicting answer: an early turn-ready estimate made
an application target offerable while the canonical contractual interval was blocked
by the outgoing active lease. Existing application target authority now composes
the exact-space interval read with physical readiness; complete offer dates are
checked before invitation/acceptance and at the staff offer door. Seven focused DB
assertions and the full163 mobile tenant journey passed; source governance50 passed
sequentially. New authenticated HTTP menu challenge passed11 assertions on a fresh
owned database. No automatic preference optimizer,
turn creation or production deployment is claimed. See
docs/handoffs/desktop-qb-20260906/tenant-journey/TURN_SELECTION.md.

**2026-09-08 terms-first owner ruling and local candidate (supersedes trial notes below):**
Owner completed staff sign-in, explicitly authorized closing the old test case and
a labelled simulated tour, received a new Demo whole-unit application, and submitted
it. Owner then identified missing lease terms and required agreement FIRST.
Live application remains unchanged; lease approval/generation/delivery is held.
API9f2acff and appc05ae68 plus local changes now bind prepared application proposals
in existing lease_offers to invitations, applicant acknowledgement, management
confirmation and lease packet. Migration193 is owned-disposable only, not deployed.
Mobile submission, mismatch refusals, offer-bound lease generation, both resident
signers, company execution, move-in and rent roll passed 163 full-path assertions
through owned real HTTP/Postgres and Chromium. Providers were fenced; owned DB
and cluster data were removed. This is a synthetic by-bed fixture. See
docs/handoffs/desktop-qb-20260906/tenant-journey/TERMS_FIRST.md for current limits.
Successor offers now use the same invitation and application. An append-only
acknowledgement history preserves prior acceptance; new terms require explicit
review both before and after submission. Stale acceptance, duplicate acceptance,
acknowledgement rewrites and revisions after a packet exists are tested refusals.
Already-submitted applications without an offer now enter the same terms review
through an explicitly selected application, without invented prior agreement or
another application. The 163 checks include legacy migration-shape and Ask reconciliation checks;
local mobile acceptance preserves answers, zero deposit and one first acceptance.
All50 source-governance gates pass, followed by an isolated163-check journey.
Application terms/fees now reach Ask through canonical Leasing standing. Both
Ask HTTP doors agree with application detail on missing, pending and accepted
terms. More than one application requires clarification unless latest is explicit.
Older-application selection and causal explanations are not claimed. Conditional/unit-assessed
catalog charges and structured concessions refuse rather than inventing terms.
This is not whole-product acceptance or a production deployment.

**2026-09-08 bounded live tenant trial:** owner authorized texts to his own phone
and subsequently application and test-lease delivery. Live health identifies
d55dae9. Existing Demo intake through the authenticated door and inbound SMS
conversation worked; exact outgoing messages were independently carrier-delivered.
Public website intake remains403 with DEMO_MODE=false. Requested3pm tour did not
book: no future open slots, no tour, and no takeover-queue row for this trial.
AI promised human follow-up despite that absent queue evidence. Provider delivery
was not reflected in comm_events.provider_status on the observed reads.
Owner's existing active staff account has Demo access. Canonical sign-in code
sent; verification pending. Prior invitations are consumed/revoked. No fresh
application or lease has been sent; no tour outcome or approval fabricated.
Local scheduler-response repair is under review, not deployed. The scheduled QB
follow-up is test coordination, not a replacement product workflow. See the
tenant journey receipt and QB_OPERATING_BOARD for current custody.

**2026-09-08 local tenant journey:** existing lead/tour/outcome/application/signing
proof passed106; extension using real mobile application and separate signer
pages, then charges, activation, readiness, keys and canonical rent roll passed118.
Rent roll names the same Person, executed lease and Bed B, with rent counted once.
One local CSS repair hides an empty unknown Target date row; browser red/green,
render24/24 and source gates50 passed. Final repaired journey118/118, owned cleanup
complete. Not deployed. Model calls refused and SMS captured locally: live AI,
carrier delivery and personal tenant trial remain unproved. See
[tenant journey receipt](handoffs/desktop-qb-20260906/tenant-journey/RECEIPT.md).

**2026-09-07 application picker:** the existing Leasing Work route is now browser
verified through canonical intake, walk-in tour, conversion and explicit next
action.25 assertions passed; exact server target identities matched the visible
picker, the occupied claim stayed excluded, and Cancel sent nothing. This is a
focused synthetic proof, with one locally refused intake draft, no further model
attempts, SMS0, egress0 and owned cleanup. Product code unchanged. Conversation
shortcut and actual dispatch are not claimed. See
[picker receipt](handoffs/desktop-qb-20260906/application-picker/RECEIPT.md).
**2026-09-07 occupied opening claim:** availability now honors the existing
accepted occupied claim when no operative lease exists, withholding an offer
without inventing lease terms. Rent Roll detail shows the existing server reason
and resident identity as Not established. Root reproduced classifier and browser
failures before repair; successor18 DB/HTTP checks and all nine combined browser
phases passed, including July/Skyline restart and mixed Add All. Zero actual-source
confirmations, provider logs empty, owned cleanup verified. Application targeting
is HTTP/service proven; picker UI and Ask model answers are not claimed. See
[occupied claim receipt](handoffs/desktop-qb-20260906/occupied-claim/RECEIPT.md).
**2026-09-07 Management reconciled zero:** the app preserves explicit zero in
six existing count selections. Owned HTTP import/read10 passed; unchanged parent
browser showed100% despite source occupied0. Successor Management visibly showed
0 of2 and the nonzero control1 of2. Full July/Skyline lineage/restart, mixed Add All,
and all eight browser phases passed, actual-source confirmations0, owned cleanup
complete. API product unchanged; source-versus-contractual measure provenance is
still separate. See [zero receipt](handoffs/desktop-qb-20260906/management-zero/RECEIPT.md).

**2026-09-07 canonical occupancy under holds:** the current rent-roll fraction
now counts occupied positions within the same non-down, non-contested population
as its denominator, excluding overlaps once. Full tenancy and rent totals remain
unchanged. Parent 65/16 reproduced impossible fractions; successor 81 behavioral
checks plus five browser-fixture checks passed. Formal Schedule and its client
CSV showed the all-down 0 of 0 while canonical pct stayed null and rent stayed
1750. All six existing onboarding browser phases, July/Skyline lineage/restart,
and mixed Add All passed with zero actual-source confirmations and owned cleanup.
Availability for historical occupied claims is covered by the later receipt above. No deployment. See [hold receipt](handoffs/desktop-qb-20260906/occupancy-holds/RECEIPT.md).

**2026-09-07 leasing retirement exclusion:** the shared leasing occupancy reader
and leasing desk availability now reuse the canonical inventory retirement
predicate. Root observed 8 retirement-specific failures before editing; all 41
behavioral checks pass afterward, preserving the leased sibling, label rules,
unknown basis, write refusals, and reinstatement. Full July/Skyline review,
restart, lineage, mixed Add All and six existing browser phases passed with
zero actual-source confirmations and owned cleanup. The current authenticated
Leasing home fetches but does not paint its condition strip; visible occupancy
presentation and hold-denominator meaning remain separate. No app change or
deployment. See [retirement receipt](handoffs/desktop-qb-20260906/occupancy-retirement/RECEIPT.md).

**2026-09-07 retained-source authority:** members now need a live target-property
Leasing/Management assignment for rent-roll setup/read; existing administrator
authority is preserved. Downloads and opening-position composition reuse that
scope owner. Utility/Contracted Services downloads enforce their existing kinds,
and Deal Setup download requires rent_roll. Corrected parent refusals,77 successor
HTTP checks, five browser actors and full July/Skyline review/restart/mixed passed.
Actual-source confirmations0; owned runtime cleanup complete. Local candidate
proof only; retained-file discovery and historical mixed inventory remain separate.
See [authority receipt](handoffs/desktop-qb-20260906/source-authority/RECEIPT.md).

**2026-09-07 source-refusal recovery proof:** unchanged API43d30ed/app28df16d
product passed stronger whole-file rollback and existing-browser recovery checks.
Mixed-grain35 includes empty-batch/activation linkage, a valid earlier unit and
direct older-loader refusal. Browser same-file retry deduplicates; a corrected
file reaches review in the same setup without confirmation. Full July/Skyline
restart/lineage/mixed passed; cleanup complete. Rejected retained-file access
from the screen remains a separate question. See
[recovery receipt](handoffs/desktop-qb-20260906/mixed-grain-writer/RECOVERY_RECEIPT.md).

**2026-09-07 contradictory new inventory guard:** a retained source naming the
whole-unit sentinel beside a named room on one by-bed unit previously created
and offered both positions. Parent27 assertions reproduced this through owned
HTTP before editing. The existing materializer now refuses that combination;
successor25 covers both orders, rollback and valid-layout controls. Both browser
refusals and full July/Skyline stage/restart/lineage/mixed passed on frozen
API01fac5f/appa60550f plus recorded changes. Cleanup complete, actual-source
confirmations0; existing mixed historical inventory remains a separate question.
See [writer receipt](handoffs/desktop-qb-20260906/mixed-grain-writer/RECEIPT.md).

**2026-09-07 retained-entry screen/restart follow-up:** the existing unit Rent Roll
now displays unmatched source entries in one expandable review section, including
when no current inventory exists, and discloses a bounded list. Standing preserves
known zero counts after a successful baseline read. Full owned Windows July/Skyline
review/restart/lineage/mixed and browser spaces/retained-entry/failure/retry passed
on frozen API94298bc/app8baf740 plus the recorded changes. App44/1648 passed;
cleanup complete; actual-source confirmations0. This is browser-verified rehearsal,
not deployment or established occupancy. Exact source custody and limits:
[screen receipt](handoffs/desktop-qb-20260906/claim-relay/SCREEN_RECEIPT.md).

**2026-09-07 retained-claim reader repair:** based on Fable API `6a2d46f`,
`codex/claim-relay-20260907` preserves every agreeing eligible source reference,
keeps retained claims in zero-inventory standing/Ask facts without establishing
occupancy, and relays bounded-list truncation. Three parent defects reproduced
before repair; successor owned DB/HTTP checks: 20 passed, plus identity17,
unattached14, readiness13, historical/space and index controls. Cleanup passed.
No new app panel, Ask model answer, deployment or actual-source confirmation.
Full July/Skyline browser/restart proof was green at API6a2d46f/app8baf740;
it has not been repeated on this reader successor. See
[receipt](handoffs/desktop-qb-20260906/claim-relay/RECEIPT.md).

**2026-09-06 availability basis candidate (Fable acting lead):** the canonical
availability classifier offered any position with no lease, no possession and
no turn as `marketable_now`, whether or not Spine held an established
occupancy basis for it; the Rent Roll refused to bucket the same position and
Ask Spine counted it as not established. Reproduced through real HTTP on an
owned disposable database at unchanged77d6156 (two rooms under a historical
bare-unit vacancy: Ask open0, marketable_now2). Repaired on
`claude/read-side-basis-protection-20260906` (API81a8ec5/586da32,
app9bf6ea3): `occupancy_unknown` and `evidence_unreconciled` are refused
below the lease, commitment, possession and turnover guards and above triage
and use type; no date, a named blocking fact, exhaustive counts, basis carried
on the row. The app lists both in "Contested or unresolved" with the server's
reason, and now also lists the two triage readiness states it used to drop.
Unit, database, HTTP and Chromium rungs observed locally on Linux; not run
through the desktop wrapper, not deployed. Expect any never-onboarded
position to read *Occupancy not established* instead of *Marketable now*.
See [receipt](handoffs/desktop-qb-20260906/availability-basis/RECEIPT.md).
Same branch, API857cf34: the canonical space reader now answers an opening
claim using `import_source_rows` lineage where present. Legacy no-lineage claims
still use text subject to active retirement protection, and a unit-level claim
can answer its unit's sole position under the ledger compatibility rule; a claim
against retired inventory no longer follows its number onto the replacement
unit. Row 63. API87ae10a: a confirmed source row that attaches to no
position is now named on the tenancy standing projection, the Rent Roll unit
view and Ask Spine by the key the source gave it, never inferred onto a bed;
a whole-unit placeholder beside real beds no longer absorbs a bare-unit
claim, though it is still counted as a position. Row 64. API7c0fc48 /
app8baf740: the availability row's readiness axis now says what the
readiness owners say (certification, triage, assigned walk, turn in
progress, or unknown) and names which one answered; no offer changed, and
the ruling on offering never-walked, revoked and completed-but-uncertified
units is recorded for Kameron. Row 65. **CI:** `verify` runs 417–423 on this
branch were red and were this candidate's own doing (rows 61 and 63); runs
424 (`e86b746`) and 425 (`7c0fc48`) are green, so rows 61–65 read CI-green
at `7c0fc48`.

**2026-09-06 room identity candidate:** Fable O1/O2 were positively reproduced
through local HTTP on unchanged018e6d6. Vacancy confirmation now shares explicit
space selection and preserves source-space lineage; Rent Roll projection and
person overlays retain per-room identity. Historical promoted claims without a
current linked space remain visible but held out of current Rent Roll publication.
The bounded successor HTTP/database proof passes, including named/ambiguous/
contested/single-position cases, scoped reads and Ask tenancy fact gathering.
Full frozen local browser/July/Skyline stage, restart, lineage and mixed proof
passed at APIaef0d10/app0785cf8; CI34049719757 passed exactaef0d10. Actual-source
confirmations remain zero. The signed-in app already used canonical availability;
an unnecessary proposed cutover was removed, retaining only browser proof. Broader
historical-claim behavior in other canonical readers remains open for independent
Fable review. See [proof scope and remaining gaps](handoffs/desktop-qb-20260906/space-identity/RECEIPT.md).

**2026-09-06 Fable review and owner clarification:** Kameron directly confirmed
retirement authorization in QB task 01a076e5: "yes it my understanding they were
not being used and i love improving through simplicfication of code". This
resolves the authorization question. It does not establish external non-use;
external traffic remains unverified and production deployment remains held.

**Onboarding release coupling (F1):** the candidate's source-row conflict target
requires the index predicate in migrations/pending/proposed_source_claim_identity.sql.
The numbered chain ends at192 and retains the broader natural-key uniqueness.
Do not deploy this candidate on that chain without separately allocating,
reviewing and applying the index migration. No migration193 is allocated here;
no live production schema was inspected. The new owned-DB coupling witness
must show raw23505 with retained bytes before the pending step, then both
current/future source claims afterward. Its ceiling192 expectation must be
revised when the migration becomes part of the numbered chain.

**Fable follow-up candidate:** readActivation now includes unassigned_current
alongside unassigned_future; the paired app reads those canonical counts and
keeps unavailable counts unknown. The browser proof totals all seven statuses.
Synthetic conflicting-current and current-without-unit witnesses are added to
the existing retained-source path. Full owned browser/database review, restart,
lineage and mixed proof passed at API8fd9e9b/app7f4878e; CI34039624425 passed
on exact API8fd9e9b. The F1 ceiling192 failure and pending-index success both
executed. Actual-source confirmations remained zero. Execution/acceptance is recorded separately
in docs/handoffs/desktop-qb-20260906/FABLE_DISPOSITION.md.

**Residual boundaries (F4/F5):** confirmed is an admitted database status but
currently unreachable for lease confirmation (which promotes directly); its
visible label is not distinct. No new transition/display behavior is added.
POST /properties/:id/leasing-basis remains a body-driven writer behind the
operator-key gate, without staff actor attribution or property-scoped session
authority. It was explicitly excluded from the nine retired paths; its future
governance remains a separate decision. Rows39/40 below describe pre-retirement
proofs: the successor's retired ingestion paths now assert authenticated410,
not their former actor/property checks. POST /properties is separately retained.


**2026-09-05 subsequent owner ruling:** Kameron authorized retirement of the
nine legacy ingestion HTTP entry points in favor of canonical Deal Setup.
The candidate returns an explicit 410 after the unchanged operator-key gate;
retained history and shared services are preserved. This supersedes earlier
"legacy access undecided/proposal only" notes for candidate implementation.
External usage is still unverified, and production deployment remains held.
See [the route list and before/after proof contract](LEGACY_INGESTION_RETIREMENT_2026-09-05.md).
Check CI for the exact new candidate SHA; older green runs do not prove this change.

**2026-09-05 candidate navigation update:** `codex/qb-proof-checkpoint-20260905`
descends from the unchanged prior successor `f953449` (not deployed).
Code checkpoint `2a31892594c707c49cfd863cfaed97e2b6a1d6da` passed
[verify run 394](https://github.com/kzitelli-art/property-spine-api/actions/runs/33965050698).
Its raw job log was inspected: the unchanged f953449 server positively exhibits
the notice correction/retarget, deposit serialization and comparison defects;
the candidate passes the corresponding repair proofs, Deal Setup's 31 real-HTTP
assertions including restart, both browser rungs, and owned-database cleanup.
Notice corrections preserve original bed/lease identity; attribution locks the
shared deposit; cross-property shadow requests are explicitly unsupported (400).
See [the bounded contract and limitations](QB_CHECKPOINT_2026-09-05.md).
This does not resolve partial-attribution cash-proof semantics, the unit-wide
notice guard, legacy ingestion access, or real Greenery workbook acceptance.
Production d55/app484 and Mike's session counts remain dated observations; no
production/provider work or invitation resend occurred. Older production claims
below are historical navigation. Run 391 at f953449 also passed, including the
approval-timestamp proof in its descendant tree; row 60's unread-CI note is stale.

The subsequent documentation-only commit `a5feb48` failed
[verify run 395](https://github.com/kzitelli-art/property-spine-api/actions/runs/33965246309)
in the existing named-person reconciliation gate (`NO_SUBJECT`). All three
parent defect witnesses, three candidate repair stages and Deal Setup's 31
assertions passed before that failure; owned-database cleanup also passed.
The earlier green checkpoint does not make this failed run green. Consult the
workflow for the exact candidate HEAD before accepting the final checkpoint.
Review identified a fixture hazard: packet setup randomly reused a small phone
range even though intake resolves an existing person by phone. A collision can
retain an earlier person's name. The exact failed-run identity was not retained,
so this is a strong explanation, not recovered database evidence. The fixture
now selects an unclaimed canonical/legacy phone; product identity is unchanged.

> # ⛔ BEFORE YOU BUILD ANYTHING, SEARCH THIS FILE.
>
> This file exists for one reason: **threads keep rebuilding things that already
> exist.** Renewals, turnovers, an obligations queue, a follow-up ladder and a
> person-correction path were each built, then later described as missing.
>
> **Search this file for the thing you are about to build.** If it appears here,
> your job is almost certainly to *extend or connect* it — not to write it again.
>
> ```bash
> grep -in "renewal\|turnover\|notice" docs/CURRENT_STATE.md
> ```

**THIS DOCUMENT DESCRIBES CURRENTLY OBSERVED CAPABILITY, NOT PRODUCT INTENT.**

| File | Answers |
|---|---|
| **`CURRENT_STATE.md`** (this) | **What exists now, and at what proof level** |
| `PHILOSOPHY.md` | What the product should mean · how to decide if a feature belongs |
| `THREAD_HANDOFF.md` | **History.** What happened, and why. Not current-state authority. |

Resolution order for any present-tense question:

```text
CURRENT SOURCE / RUNTIME  →  CURRENT_STATE.md  →  PHILOSOPHY  →  THREAD_HANDOFF
   (always wins)              (this file)         (meaning)      (history only)
```

> ### ⚠ ABSENCE FROM THIS MAP IS NOT ABSENCE FROM THE PRODUCT
> **The survey is complete as of 2026-08-20** — three waves plus four completeness
> critics covered every `src/` directory, all 176 migrations, all 292 test files, CI,
> the app repo, `server.js`'s inline routes, and `tools/`. That means gaps here are now
> *depth*, not *breadth*: ~350 capabilities are recorded, but only the decision-changing
> ones are promoted into the index below. Full detail lives in
> `docs/current-state-build/03_`, `05_` and `06_WAVE3_RESULTS.md`.
>
> **Still: if something is not listed, its state has not been established here — that is
> not evidence it does not exist.** Search the source before concluding anything is
> missing, and add what you find. Four waves each found things the previous ones missed,
> including a full CI pipeline nobody had recorded.

---

## STATE SNAPSHOT

```text
API verified against    ecfc9af4    2026-09-17 13:38 UTC — RELEASE-LANE RECORD
                        (row 117). NOT re-read from this session: no production
                        credential here, and the proxy refuses the Render host.
                        Supersedes the b3e4407 / 2026-09-14 stamp below.
                        /health as recorded: commit_short ecfc9af,
                        resolved_from render_env, ok true.
LEDGER AFTER DEPLOY     188 rows, ceiling 200  2026-09-17 (row 117; five facts in
                        one query: ceiling 200 · ledger_rows 188 · column 1 ·
                        constraint 1 · index 1). MIGRATIONS 199 AND 200 ARE
                        RELEASED. Supersedes the "186 rows, ceiling 198" stamp
                        below, which was true on 2026-09-14 and is now history.
                        ⚠ 200 was applied as SQL in the Neon console, NOT through
                        migrations/migrate.js, so EXPECTED_LEDGER_CEILING is not
                        what held — the ledger was read in the same editor first.
                        The service starting is itself independent proof the
                        schema agrees with the code: prestart verifies and
                        refuses to boot on a pending file.
APP deployed            2e8199a     2026-09-17 — OWNER-REPORTED, not verified from
                        any session: a static site exposes no build identity.
                        This is also the API's app pin as of this merge
                        (tests/e2e/app_pin.txt), moved forward from 312a999,
                        which is its ancestor — the pin adopted two commits and
                        dropped none.
                        2026-09-19: the API's pin moved AHEAD of the deployed
                        app, to 475b3e1 (claude/app-convergence-20260919, a
                        descendant of 2e8199a — row 154). The pin says what CI
                        exercises; deployed stays 2e8199a until someone ships.
THIS BRANCH vs LIVE     the integration line CONTAINS production (ecfc9af4 is an
                        ancestor) and is ahead of it: deployed line + graded
                        board + the dated-read and lender-surface repairs.
                        github main (ed66d65a) does NOT contain production — the
                        deployed line was never merged there. Nothing on this
                        branch is live until a human merges and runs deploy.sh,
                        and A DEPLOY DOES NOT MIGRATE (see CLAUDE.md).
API verified against    b3e4407     2026-09-14  (owner report, later same day)
API verified against    b3e4407     2026-09-14  (owner report, later same day)
                        SUPERSEDES d15c968, which this file carried earlier today.
                        Production moved twice in one day; do not quote a sha
                        from this file without checking its date.
APP deployed            6f92b50     2026-09-14  (owner report) — resolves the
                        d45344d / 336c82f disagreement recorded earlier today.
LEDGER AFTER DEPLOY     186 rows, ceiling 198  2026-09-14 (owner deploy log:
                        "RELEASE VERIFIED: 186 ledger rows, ceiling 198").
                        MIGRATIONS 195-198 ARE RELEASED. 182 -> 186 is exactly the
                        four. The rent-roll reconciliation door and two-step Execute
                        plumbing are LIVE. Supersedes the UNKNOWN this file carried
                        earlier today, which was over-cautious: the QB wrote that a
                        serving API proves nothing about 195-198, and that holds only
                        if the deployed tree LACKS those files. b3e4407 carries all
                        four, and prestart refuses to boot on a pending migration, so
                        serving b3e4407 DOES prove the ledger holds them.
DEPLOYED LINEAGE        6fa4638 is an ANCESTOR of b3e4407 (verified), so every graded
                        lane is in production. b3e4407 = 6fa4638 + 2 codex commits
                        (migrations byte-identical); app 6f92b50 = pin b0be9f4 + 2
                        codex commits. THE FOUR CODEX COMMITS WERE NOT GRADED.
SKYLINE, REAL DATA      2026-09-14 owner read of the live app, NOT the fixture:
                        160 positions / 72 units; 31 confirmed occupied; 9 pending
                        activation; 20 need review; 14 overlapping lease claims; 1
                        conflicting occupancy evidence; 13 commenced awaiting
                        move-in funds; ONLY 4 positions with trusted rent economics.
                        ⚠ The $26,350 / 31-position / $78,200 figures in rows 80
                        and 82 are SYNTHETIC fixture values in Skyline's SHAPE.
                        They are not Skyline's and must never be quoted as its
                        economics. Real trusted-economics count is 4.
                        Tracker reads ~90.6% preleased (145 signed/pending, ~148
                        occupancy claims). Both readings are valid and unreconciled;
                        "31 occupied" is NOT the building's business occupancy.
PRODUCTION DEPLOYED     d15c968     2026-09-14  ← owner /health read. SUPERSEDES the
                        61f99bf/2026-08-22 stamp below, which stood unchallenged
                        for three weeks and was WRONG. Dated rows keep 61f99bf as
                        history; it is not the running build.
Production ledger       182 rows, ceiling 194  2026-09-14 (owner read of Neon)
                        Rows ≠ ceiling: numbers are skipped. 189 was the stale claim.
APP deployed            NOT RE-READ 2026-09-14. Two records disagree and neither is
                        measured: d45344d (2026-08-21, here) and 336c82f (QB handoff).
                        Unresolved — needs a read before it is quoted anywhere.
RELEASE CANDIDATE       API 6fa4638 · app b0be9f4 · migrations 195-198 at pin 03550a8.
                        NOT RELEASED. 2026-09-14 attempt stopped SAFELY before any
                        production change on the owner's precondition: live
                        conversations, leads and lease activity (tenant_signed,
                        submitted) were in flight. Neon ledger and all 56 source
                        governance gates passed. Not a code failure — a window problem.
Migrations on github main     187  → production is ahead; see defect #29
Surveyed / verified     2026-08-19 (wave 1) · 2026-08-20 (Codex PR review, AM
                        domains) · 2026-08-20 (wave 2, 148 capabilities:
                        teams/access, management door, onboarding intake,
                        money/pricing, app repo, server.js inline, tools/)
                        · 2026-08-20 (pricing fix deployed, PR #128, live)
                        · 2026-08-21 (live Skyline pricing + tour walk;
                        authority-review precondition; native scheduler ruling)
                        · 2026-08-21 (native scheduler + session-only Team
                        onboarding deployed; ledger 189)
                        · 2026-08-21 (post-tour → exact-bed application →
                        resident/company lease execution HTTP-proven; code
                        deployed inactive for Skyline)
                        · 2026-08-21 (OneFive staff-line activation door
                        deployed inactive; Super Admin mobile layout proven)
                        · 2026-08-21 (dashboard/staff-SMS personal-attention
                        question converged on one recorded-accountability read)
                        · 2026-08-22 (post-tour conversion ownership projected
                        into that same canonical personal-attention read)
                        · 2026-08-22 (staff SMS post-tour capture and exact-bed
                        application send converged on the canonical services)
                        · 2026-08-22 (541-305-8509 atomically transferred from
                        Demo ORG to OneFive Management; old history preserved)
                        · 2026-08-22 (authenticated production Asset Management
                        + Ask Spine read audit; duplicate Solo identity found)
                        · 2026-08-20 (wave 3 FINAL: migrations 001-119,
                        src/shared + governance, all 292 tests, CI reality —
                        58 findings; full record retained in 06_WAVE3_RESULTS.md)

SKYLINE PRICING         PUBLISHED IN PRODUCTION  2026-08-20
                        2BR $850 · 3BR-1BA $750 · 3BR-1.5BA $775, per bed,
                        12-month term only, effective 2026-08-20.
                        Verified through quotablePricing — the adapter the
                        agent calls — not the publisher's own report.

SKYLINE CONVERSATION    INTEGRATION BRANCH DEPLOYED DIRECTLY, NOT MERGED
                        codex/skyline-conversation-integration-20260820
                        Production runtime: 61f99bf, 2026-08-22.
                        Based on claude/property-spine-orientation-cso2ao;
                        adds the one-reader Ask Spine obligation fix and a
                        canonical published-economics read for Ask Spine.
                        Clear staff SMS reads defer to the same Ask Spine answer;
                        post-tour and application actions defer to the existing
                        tour, target, conversion, and dispatch services.
```

✔ **RESOLVED 2026-08-20: production now runs `main`'s lineage.** Was: the
deployed commit lived only on a separate branch, 39+ commits ahead, carrying
migrations `main` lacked. PR #128 landed that branch on `main`; the owner then
deployed `main`'s head (`bcd3089`) manually from the Render dashboard and
confirmed it live directly. See defect #1 for what shipped in that deploy, and
defect #7's row for the full resolution history — kept as a record, not deleted.

**Staleness check — run this, do not trust the SHA above.** Naming a SHA in a file
is self-defeating: committing the file changes the SHA. So the guarantee is stated
as something checkable instead:

```bash
git diff --name-only 77f93f5..HEAD -- src/ migrations/ server.js
```

**Empty → every row below is current.** Non-empty → rows touching those paths are
*navigation only* until reverified. Reverify the row, then update it here.

---

## THE VOCABULARY — say the rung, never "done"

| Rung | Means | How it is earned |
|---|---|---|
| `REPORTED` | A document claims it; no code evidence found | — |
| `LOCALLY_EXERCISED` | Logic tested, **no real database** | a `.test.js` with no `pg` |
| `BUILT_BUT_DORMANT` | Code exists, **nothing calls it** | grep finds no `src/` or `server.js` caller |
| `HTTP_PROVEN` | **One** test: real Postgres **and** real router | `require("pg")` + `listen()` in the same file |
| `BROWSER_VERIFIED` | Real Chromium drives the real surface | a `*.browser.js` — *check it isn't a fake pool* |
| `DEPLOYED` | Confirmed present in the deployed build | `git merge-base --is-ancestor <sha> <deployed>` |
| `PRODUCTION_PROVEN` | **Observed working in production** | a recorded live run |

Never write *done*, *built*, *working*, *live*, *mostly done*. Those blend intent
with evidence — which is the failure this file exists to end.

**Three traps this repo has actually hit:**
- A file named `*.db.js` proves nothing. **Open it.** **Ten** files pass a hand-built
  fake pool to a real router — not the three previously named. See defect #19.
- A browser proof can run against a simulated database. **Check for `fakePool()`.**
- **A rung earned once is not a rung held.** Most `HTTP_PROVEN` and `BROWSER_VERIFIED`
  rungs in this file rest on the 68 `.db.js` proofs and 94 `*_proof.js` files — **none
  of which any automation runs** (defect #17). They passed when a human ran them. Nothing
  re-runs them, so nothing would notice if they broke. Read every rung below as *"was
  demonstrated at least once,"* not *"is continuously verified."*

---

## STRUCTURE THREAD — 2026-08-27 (file renames, server.js decomposition, tests reorg)

A structure-only thread. **No route path changed, no mount order changed, no
behavior changed by intent.** Proof: route-registration inventory extracted from
`git show HEAD:server.js` vs the new structure is set-identical (36 moved
registrations, 1:1, only `app.*` → `router.*` prefixes differ), and
`./tests/e2e/verify_all.sh` ran **ALL PROOFS PASSED** against a real local
Postgres 16 (schema built from the real migration chain via
`tests/e2e/apply_migrations.sh`) — twice, before and after the test reorg.
Browser rung SKIPPED locally (no Chromium) — same honest skip CI reports.

What moved:

| Change | Detail |
|---|---|
| 20 `src/` files renamed | fused/camelCase → snake_case: `leasingleads.js` → `leasing_leads.js`, `teamaccess.js` → `team_access.js`, `leasingShadowImport.js` → `leasing_shadow_import.js`, etc. Full list in git history. Two **pinned git-history references** inside tests (`git show <sha>:src/comms/tenantlink.js` in the conversation gates) initially corrupted by the sweep — restored; pinned SHAs keep the pre-rename path on purpose |
| `server.js` 3,648 → 836 lines | Inline blocks extracted **verbatim** per the organ pattern (docs/architecture.md): Release-0 baseline routes → `src/baseline/baseline_routes.js` (NOT `src/release0/` — that dir is the dormant activation boundary, enforced by `gate_activation_dormant.js`); lease lifecycle → `src/tenancy/lease_lifecycle_routes.js`; AI ingest pipeline + routes → `src/agent/document_ingest.js` + `document_ingest_routes.js`; space-position read → `src/tenancy/space_position_routes.js`; sms-proof → `src/comms/sms_proof_route.js`. Each mount sits at the exact line position its inline block occupied |
| `tests/` reorganized | 290 flat files moved by their existing naming conventions: `gates/` (14) · `proofs/` (68 `*.db.js` + 89 `*_proof.js`) · `unit/` (90 `*.test.js`) · `arcs/` (4) · `scenarios/` (25) · `e2e/`, `fixtures/`, `support/`, `_engine.js`, `_run_receipt.js`, `verify_source_governance.js` unchanged. Runner resolves bare names from `gates/` and slash entries from `tests/` root. CI and `verify_all.sh` needed no changes (they reference only `e2e/` + the runner) |

Rung reality check, stated honestly: the 12 e2e proofs exercise the server and a
slice of the moved routes end-to-end; the other moved routes are proven by the
inventory-identity argument plus the 38 gates, not by individual HTTP proofs.
`gate_property_creation_paths.js` caught a real extraction miss (the baseline
block's `propertyCreation` dependency) — the gate discipline is load-bearing.

**Docs-cleanup follow-on (2026-08-27, on top of the structure work):** 128
dated receipts/close-outs/candidates moved to `docs/archive/` via `git mv`;
13 living docs + all subdirectories stay at `docs/` root; `docs/README.md`
added as the index (living vs historical, with the search-first rule).
Every citation to archived files was rewritten to `docs/archive/…` — cross-repo
citations (`property-spine-app/docs/…`) were checked and left intact. Defect
#2 closed: `migrations/README.md` no longer instructs hand-run production
migrations. No doc content was rewritten or deleted; receipts are kept.

**Wave-3 audit findings retained as historical inputs, not silently discarded:**
the final survey measured the then-current CI coverage and branch-protection posture,
ten fake-pool HTTP harnesses, 47 demo-UUID-pinned tests, two dead historical proofs,
the from-scratch migration failure at 083, the app CI gap, four orphaned baseline
tables, environment-variable documentation gaps, and the DEMO_MODE boot writer.
The exact evidence and counts remain in
`docs/current-state-build/06_WAVE3_RESULTS.md`; later RC1 proof rows below supersede
only the findings they explicitly re-prove.

---

## ⛔ KNOWN LIVE DEFECTS

| # | Defect | Evidence |
|---|---|---|
| 65 | **RESOLVED ON CANDIDATE (label) · RULING OWED (offer), 2026-09-06 — every availability row said physical_readiness "ready", from the absence of an open turn.** The classifier's axis is `turning` when a turn is in progress and `ready` otherwise; availability relayed it, so a unit nobody walked, a unit with a walk assigned and not done, a triaged unit with no blocker, a triaged unit in severe condition, a certified-then-revoked unit and a completed-turn unit all read "ready" — including the one the same read held `not_ready_confirmed` and the two it held `readiness_unknown` — and the page rendered "Ready" for all of them. Observed on an owned database with seven populated shapes, each with an established vacancy basis (`tests/proofs/availability_readiness_axis.db.js`, `PROOF_EXPECT_DEFECT=1` 13/13 against an `e86b746` worktree). Fix, read only: the row relays the readiness owners the read already loads per unit — live certification → `ready`, confirmed triage → its `deriveReadiness` state, open `initial_unit_walk` obligation → `unknown`, turn in progress → `turning`, none → `unknown` — and `readiness_basis` names which answered; the app renders it as served (Ready · certified / Not ready / Turn in progress / Readiness unknown). Successor 13/13; `marketing_state` unchanged for every shape; browser spaces phase PASS on both fixtures. **Ruling owed (recorded, not taken):** never-walked, revoked and completed-but-uncertified units are still offered `marketable_now` while every readiness owner says unknown; requiring a certification (the BUILD 4 rule) would empty marketable inventory on a property with no certified walks, which is what the earlier reverted blanket guard did by accident. Options and the revocation question are in the receipt. Rung: `PROVEN` (real Postgres) and Chromium locally; CI `verify` run 425 green on `7c0fc48`. | `src/surfaces/availability_read.js` (`readinessAxis`); `tests/proofs/availability_readiness_axis.db.js`; `tests/e2e/verify_all.sh`; app `index.html` (`psAvReadiness`), `availability_cutover_app.test.js`; `docs/handoffs/desktop-qb-20260906/availability-basis/RECEIPT.md` |
| 66 | **RESOLVED IN SOURCE (provisioning doors) · GOVERNED PATH PROVEN LOCALLY, 2026-09-12 — an existing human_staff account can be brought into a newly adopted property through the existing invite and OTP path, and the two admin provisioning doors could not attach a phone-only account.** On `0f5dccca` an owned server and owned database walked: super-admin adoption → first team assignment through an admin door → org admin opens the property through the selector → governed `POST /properties/:id/team-invites` for the existing phone (existing person confirmation required, wrong person refused) → OTP accepted → session at the new property → `resolveStaffIdentity` `resolved` (`bridge_plus_active_assignment_plus_authority`), staff contexts and person-level assignments at both properties, leasing module reported and served, Skyline session and access untouched, platform role and phone-only shape preserved, no duplicate user or person, repeat invitation idempotent, used and superseded invites refuse reuse, cross-property invite refused, wrong-client provisioning refused, reparenting refused. Witnessed on the unmodified source: `POST /admin/organizations/:id/invite` and `POST /org/users/invite` answer 500 for a phone-only account because `ON CONFLICT (phone)` names a constraint `users` does not have (only the partial expression index `uq_users_phone_normalized`), and the super-admin door reset an omitted `platform_role` to member (org_admin → member). Fix: both doors now target the normalized-phone expression with the index's predicate, and the super-admin door treats `platform_role` as optional (new account → member; existing account → unchanged) and reports the effective role. Successor 39 passed / 0 failed; witness 37 passed / 0 failed (defects observed as expected). Recorded, not changed: `users.organization_id` is never written by the governed invite path; `/auth/sms/start` returns `dev_code` only when `NODE_ENV` is not production; an email-bearing invite naming another account's phone still fails at the phone index. First-property access order: adoption, then an admin door for the first assignment, then the selector, then governed invites. Registered in `verify_all.sh` after "legacy decision writes closed". Nothing here is production acceptance. | `src/identity/super_admin.js` `/admin/organizations/:orgId/invite`; `src/identity/org_admin.js` `/org/users/invite`; `tests/proofs/greenery_staff_onboarding.db.js` (`PROOF_EXPECT_DEFECT=1` witness); `docs/handoffs/new-hp/greenery-staff-onboarding/RECEIPT.md`; `tests/e2e/verify_all.sh` |
| 67 | **HTTP-PROVEN LOCALLY (Skyline shape, chain complete) · CONFIGURATION STOP OBSERVED (Greenery shape), 2026-09-12 — one staff-assisted lead-to-lease journey per property shape on the unmodified `d45d768`.** Skyline shape on an owned server and owned database: authenticated website inquiry (idempotent relay, consent recorded, nothing sent) → agent takes ownership (second staff 409) → native slot booked from the conversation (replay) → check-in → actual outcome (replay) → exact bed from leaseable-units → manager offer (agent 403; leased sibling `not_offerable`; ungoverned ready date refused) → Ask Spine send proposal (occupied sibling and bare apartment clarify, never re-aim) → one text, one link (used receipt on replay; wrong actor 403) → offer revised on the invitation (no message; superseded hash cannot submit) → application with guarantor (idempotent) → approve (agent 403, foreign manager 403) → terms → packet (blocks further offer change) → resident and guarantor links (re-issue replays; cross-role refused; company blocked until resident) → company execution → one pending lease on the exact bed at the acknowledged rent; activation and possession not claimed; removed assignment revokes the live session. 84/0. Greenery shape through adoption, admin and governed invites, inquiry, ownership, tour and outcome 30/0, then stops at exact-home: no established, use-configured position — configuration, not code. Resident-facing pages separately driven in real Chromium by the existing suite (151). **Not exercised:** the operator shell for staff steps; any production identity. No product source changed. Observations for QB: no door changes a draft offer before it is sent; Ask reads `applicant_followup` for an executed, unactivated tenancy. | `tests/e2e/staff_assisted_journey.e2e.js`; `tests/e2e/verify_all.sh`; `docs/handoffs/new-hp/staff-assisted-journey/RECEIPT.md` + `evidence.*.json` |
| 68 | **HTTP-PROVEN LOCALLY (owned DB + owned server), 2026-09-12 — establishing a bed-basis opening position on the Greenery legacy shape (171 unit rows, one placeholder each, no use, no lineage, no organization) through the existing owners keeps every legacy identity and leaves the untouched legacy rows in the leasable denominator.** Unmodified tests-only `8cf5078` (product = live `b4a494c`): super-admin adoption → super-admin provisioning of an organization-less existing account (org-admin door refuses 409) → Deal Setup → read-source with bed basis → 106/106 rows confirmed → established. Retained production, tracker and workpaper labels (no ids). 171 units before and after; 212 spaces (105 beds, 107 legacy placeholders); 84 leases; availability 84 occupied / 21 use_not_configured / 107 occupancy_unknown / 0 marketable; selector empty; classification tool refuses the three workpaper codes without a Greenery ruling; repeats refused or idempotent. **Findings:** occupancy by basis and the canonical rent roll count the 107 untouched legacy rows as rentable (84 of 212 = 39.6% reported vs 80% over source positions); no product door reconciles, retires or excludes a legacy row; the tracker's unprefixed labels read as-is create 46 new units and land 18 rows on legacy records, so the upload must carry the `1325-` parent; the super-admin door sets `users.organization_id` on the attached account; QB's five unmatched labels and three uncovered rooms reproduce. 40/0, 17 observations. No product change. | `tests/proofs/greenery_legacy_inventory.db.js`; `tests/e2e/verify_all.sh`; `docs/handoffs/new-hp/greenery-legacy-inventory/RECEIPT.md` |
| 69 | **RESOLVED IN SOURCE (Leasing desk read) · HTTP-PROVEN LOCALLY, 2026-09-12 — the no-consent, two-person journey completes on the released pair, and the Leasing desk went 503 for the whole property once any submitted application was bound to an application offer.** On unmodified `1320c2c` (app `4fd0af8`) an owned server walked: website inquiry with no consent and `attempt_sms:false` (captured once, no model, no text, no phantom outbound, exact retry replays, `needs_you`) → Mike takes ownership and records the email he sent outside Spine (recorded, not verified, replay, wrong-claim/recipient refused; a later question is unanswered again and still his; Ask reads both) → native tour → KZ authors and corrects the unsent draft (Mike 403) → Mike prepares the link for email (SMS refused without consent; lost-response 409 names the invitation; changed expiry conflicts; regeneration revokes the old link) → Mike attests the send (manually sent, idempotent) → applicant submits with guarantor → KZ approves and confirms terms (Mike refused), Mike generates the packet and links, both sign, Mike is not the signer, KZ executes → one pending lease on the exact bed, no activation, no possession, no text to anyone. Witness 99/4: every failure is `GET /operator/leasing/desk` 503 `LEASING_DESK_UNAVAILABLE` (`SELECT FOR UPDATE` inside the desk's read-only transaction via `readBoundApplicationOffer` → `readApplicationOffer`; in every release since `ec9e774`). Corrected: the review projection reads without a row lock (`lock:false`); writers unchanged. Successor 103/0; browser slices on the pair 34/34 and 36/36; units and 56 gates green. Recorded: terms confirmation needs KZ; timezone is a hard tour prerequisite; the older tour browser proof is stale against app `4fd0af8`. | `src/money/application_offer_terms.js`; `src/applications/proposed_terms_service.js`; `src/applications/application_review.js`; `tests/e2e/no_consent_two_person_journey.e2e.js`; `tests/unit/application_offer_terms.test.js`; `tests/unit/application_review_offer.test.js`; `tests/e2e/verify_all.sh`; `docs/handoffs/new-hp/no-consent-two-person-journey/RECEIPT.md` |
| 70 | **CANDIDATE (not released) · HTTP-PROVEN LOCALLY + BROWSER (staff shell), 2026-09-13 — two-step leasing: the ordinary lease carries exactly two commercial decisions, AUTHOR and EXECUTE.** On branch `claude/two-step-leasing-20260913` over `24007eb` (app `e23a36a`): `assessLeasePacketEligibility` admits a `submitted` application bound to the applicant's acknowledged, current authored offer (`preparation_basis: authored_offer`); generation writes a SYSTEM-DERIVED lineage record (`application_proposed_terms_confirmations.source='authored_offer_acknowledged'`, `authority_basis='authored_offer'`, actor = preparer, economics + hash = the offer's; migration 195 widens the 085 CHECKs) so packet ↔ confirmation ↔ offer lineage, issue checks and admission are unchanged; the generate/issue perimeters admit `submitted`; the resident may sign a two-step packet (evidence frozen on the `tenant_submitted` audit); **`POST /operator/leasing/lease-packets/:id/execute` `{application_decision:"approve"}`** resolves BOTH authorities before any write (approval obligation owner/role or override, AND configured signer), then in one transaction runs the released `approveApplication` (event, closed approval gate, terms-review obligation satisfied from the packet's frozen evidence, `approved_at`), the released company-signature core (refactored out of the route) and `executeSpineLease`, and audits `executed_by_decision` with both decisions. The released `company-sign` door refuses an unapproved application (`application_not_approved`). `applicationNext`/`execution_primary_action` author `prepare_lease_packet` → `issue_terms_review_link` → `await_resident_acknowledgment` → `execute_lease`; the app carries an `executeLeasePacket` adapter, an Execute panel/button with a two-consequence confirmation, and a progress ladder without a confirmation step. Witness on unmodified `24007eb` 134/32 (every failure the released rule: review says approve, packet 403 at the perimeter, execute 404). Successor 409/0 on a fresh owned database in CI order (after the no-consent journey leased the fixture's Bed B): four journeys on four beds the proof establishes itself, first lease preserved; actor matrix (Mike preparation only; signer-only cannot decide; approver-only cannot sign; foreign property); withdrawal before Execute refused; three concurrent Executes → one decision, one lease; lost-response and other-key retries replay; rollback of an injected boundary failure leaves no partial write; rereads (desk, queue, review, Person Card, Ask) with no concealed read failure. Browser 17/17 on the real shell (Mike refused through the real door with the server's copy visible; KZ executes once). Existing leasing suites, no-consent journey 155/0, gates and app harnesses 67/2,253/0 green. **Not exercised:** a full Greenery-shaped journey (second shape only to the setup refusals); stale-packet and sibling-bed guards (unreachable through released doors). Not released, not deployed; QB owns integration. | `migrations/195_two_step_leasing_authored_offer_basis.sql`; `src/applications/lease_packet_eligibility.js`; `src/applications/proposed_terms_service.js` (`deriveConfirmationFromAuthoredOffer`); `src/applications/lease_packets.js` (`executeLeasePacketDecision`, `companySignCore`, `/execute`); `src/applications/applications.js` (`applicationNext`); `src/applications/application_review.js`; `src/identity/operator.js` (perimeters); `server.js`; `tests/e2e/two_step_leasing.e2e.js`; `tests/e2e/two_step_execute.browser.js`; `tests/unit/two_step_packet_eligibility.test.js`; `tests/e2e/verify_all.sh`; `docs/handoffs/new-hp/two-step-leasing/RECEIPT.md` |
| 72 | **CANDIDATE (not released) · HTTP-PROVEN LOCALLY + BROWSER (staff shell), 2026-09-13 — inventory correction HARDENED: the wall, the race, identity, command identity, one explanation.** Successor to row 71 on the same branch (API `e65dfb2`, app `0d035c8`); the row-71 candidate's gaps were shown red first (`hardening.witness.json` on `5be5c1c`: the work-order door attached work + obligation to a retired unit, new child position / reopen / re-target accepted, the authority race and actor suspension wrote retirements, reinstatement double-counted a covered position, no command identity, conflicts unnamed). New: `src/tenancy/inventory_relationship_policy.js` (61 entries, every column referencing units/spaces classified: blocks-while-operative with the table's own status vocabulary, retained history, interest, lineage, ledger); migration **197** (24 `trg_retired_inventory_*` triggers from the policy refusing new attachment / re-target / reopen while history, audit and closing stay possible; `inventory_correction_commands`); `tests/gates/gate_inventory_relationship_policy.db.js` (live catalog both ways, in `verify_all.sh`); `inventory_correction.js` re-cut on the policy — locks then authority (`FOR SHARE` on assignment + user row; `actor_disabled`), retry identity before stale/already-retired checks (replay 200 `idempotent` + `current_state`, `command_payload_conflict`, retire/reinstate independent, concurrent duplicates → one decision, refused → no receipt), identity read (`covered` refuses, `unresolved` needs `identity_decision: position_not_covered_by_current_representation` + reason, recorded), bounded list/history with whole-property totals, `correctionStanding` carried by tenancy standing as `inventory_correction` and by the history as `explanation` (labels only, byte-identical); internal-application and work-order doors map the wall to 409. App: reread table + counts + list + review + history after a decision, generation/sequence guard drops late responses, server paging, frozen command keys with drafts kept across a lost request, identity decision controls. CI run 500 (`87be4bd`, docs over `e65dfb2`) green with the gate and the hardening proof executed. Proven on a fresh owned DB at `e65dfb2`: hardening 101/0 (both concurrency orders for application and work/obligation writers, authority race + suspension through the governed doors, Deal Setup occupied + vacant shapes both `promoted`, covered/unresolved/clear, replay/conflict/duplicates, exact reader deltas, Ask gather/answer with a deterministic stub — no model call), delivered proof 85/0, gate PASS, browser 29/29, contract, governance gates, app 68/2,291/0. **Not done here:** other writer doors still 500 on the wall (recorded); no production migration; authority still coarse. | `src/tenancy/inventory_relationship_policy.js`; `migrations/197_inventory_correction_hardening.sql`; `src/tenancy/inventory_correction.js`; `src/tenancy/inventory_retirement.js`; `src/tenancy/tenancy_position_read.js`; `src/identity/operator.js`; `src/applications/application_submission.js`; `src/maintenance/maintenance.js`; `tests/gates/gate_inventory_relationship_policy.db.js`; `tests/e2e/inventory_correction_hardening.e2e.js`; `tests/e2e/inventory_correction.e2e.js`; `tests/e2e/inventory_correction.browser.js`; `tests/unit/inventory_correction_contract.test.js`; `tests/e2e/verify_all.sh`; `docs/handoffs/new-hp/inventory-correction/SUCCESSOR_RECEIPT.md`; `docs/handoffs/new-hp/inventory-correction/MIGRATION_197.md` |
| 73 | **CANDIDATE (not released) · HTTP-PROVEN LOCALLY + BROWSER (staff shell), 2026-09-13 — a CURRENT external rent roll reconciles into an already-onboarded property through the existing Deal Setup doors.** Branch `claude/current-rent-roll-reconciliation-20260913` (API `be9ae71`, app `4558460`) over unreleased candidates API `5765357` / app `cc5a100`. No new writer, no onboarding product: preview-source → read-source (explicit reviewed mapping) → confirm → establish. First reds on the baseline (`current_rent_roll_reconciliation.witness.json`, 45/8): the actual Skyline shape (160 rooms `position_kind='unit'`) is refused by the bed-grain rule with `inventory_target_changed` — **the rule is right, the classification is wrong; correction is the governed mapping tool (Luna), applied in the proof as a labelled stand-in**; a dateless signed row manufactured an ACTIVE lease with null dates; no candidate for a name-only row on a bed with a lease in force (duplicate person, refused duplicate lease); the unit-basis placeholder with no kind aborted the second shape. Now: `confirmProposal` treats a dateless row as a dated observation (compared from the as-of date forward) and accepts it as **current occupancy with contractual terms unknown** (`opening_claim_occupied`, evidence row names person + exact home, no lease, rent retained as evidence); right-holders on the home ride into the identity review as candidates (`person_ingress` `home_tenant`); a recognised resident is **tied to the lease in force or pending** (no second lease, pending not activated, no possession); a different claimant is held; blank rows never vacancy; `Pending` status is not signed; unit basis accepts the unclassified placeholder; tracker columns (Semester, Key Pickup, New/Renewal) are read and never dates. App (five isolated blocks): the source-home picker read a field the preview never sends and offered no existing home; the review counter and retained-claims notice had no CSS rule (display:none); a refused Add did not reload; candidate buttons were gated on `staged` and overwritten by Add/Leave out — no candidate reachable. Proven on a fresh owned DB at `be9ae71`: proof 59/0 (160 rows accounted, conflicts distinct, stale/wrong-property/replay/revoked/suspended refused, establish supersedes July, dated/unit/standing/Ask agree: occupied 122 · pending 12 · open 0 · needs review 26, availability offers none, same-bytes reuse, `source_rows_mismatch` / `source_date_mismatch`, unit shape), browser 13/13 with `elementFromPoint` visibility, app suite 70/2,395/0, governance PARENT EXIT 0, onboarding regressions green; pre-existing baseline failures identical (opening_tenancy 4, operative_overlap 34, person_ingress_hostile 4, gate_person_ingress 2). Registered in `verify_all.sh`; **CI run 519** (`e067069`, docs over `be9ae71`) passed the full `verify_all.sh` with the proof executed (59/0) — runs 517/518 were red on the coverage gate only (a table-row numbering gap), no proof ran. **No schema change; 199 not used; harness ran ledger 197 + the pending source-claim index (Terra), never claimed deployed.** Owner decisions recorded in the receipt: 160 explicit picks (labels differ, exact-text-only suggestion), "Approve all as new homes" duplicate-unit risk, the two identity conflicts, blank rows, pending stays pending, held claimants, Luna dependency, occupancy basis (122 by claim vs 31 by lease), per-user authority. **Coupled browser acceptance 2026-09-14:** the app's pinned proof at `1a5f257` (combined app, live property picker, context guard) ran unedited against API `e808199` on a fresh owned DB at ledger 198 through the sealed-origin transport (TLS front, `route.continue`, non-loopback aborted) — **14/14**, visible property selection, real multipart upload observed, no duplicate homes or leases; receipt `docs/handoffs/new-hp/current-rent-roll-reconciliation/COUPLED_BROWSER_ACCEPTANCE_20260914.md`. | `src/onboarding/activation_service.js`; `src/identity/person_ingress.js`; `src/onboarding/source_home_identity_review.js`; `src/onboarding/rent_roll_field_map.js`; `tests/proofs/current_rent_roll_reconciliation.db.js`; `tests/e2e/verify_all.sh`; app `index.html`, `canonical_onboarding_review_app.test.js`, `current_rent_roll_reconciliation.browser.js`; `docs/handoffs/new-hp/current-rent-roll-reconciliation/RECEIPT.md`; `docs/handoffs/new-hp/current-rent-roll-reconciliation/COUPLED_BROWSER_ACCEPTANCE_20260914.md` |
| 74 | **RELEASE BLOCKER FOUND AND CORRECTED ON A CANDIDATE, 2026-09-14 — the 194→198 release wrapper at `e808199` refuses on every Linux host.** `tools/release/migration_194_198_predeploy.js` pins migrations 195, 196 and 197 to the SHA-256 of a CRLF Windows checkout while 198 is pinned to the LF git blob; `.gitattributes` holds all four at LF, so on Render, GitHub CI and this container the wrapper stops at 195 with `differs from the reviewed source` before applying anything. Proof of cause: the LF blobs' hashes after LF→CRLF conversion equal the three pins exactly; the migration bytes are identical on the release lane `50c4275`, every integration commit and `e808199`, so no migration content is in question. Invisible to CI because `verify_all.sh` applies migrations with raw psql, the release contract test does not hash files, and the recovery matrix is not in CI; Windows runs passed because their checkouts carried CRLF. Witnessed on an owned exact-194 database built from `d15c968` (182 rows): recovery matrix 3/52 then refused, release command refused, ledger untouched. Corrected on `claude/release-hash-pin-20260914` (`03550a8`, one commit over `e808199`): pins re-set to the LF blob hashes, `tests/unit/migration_194_198_reviewed_hashes.test.js` recomputes every pin from the tracked file and the HEAD blob (fails on the old pins), registered in `verify_all.sh`; CI run 525 green with that step executed. Rehearsal at `03550a8` on owned PG16: recovery 52/0, the exact human sequence 194→198 (`186 ledger rows, ceiling 198`), verify-only paths, `npm start` `/health` at the pinned build, the old build's prestart refusal witnessed, reconciliation 63/0, contract 23/0, governance 56/0. Runbook `docs/handoffs/new-hp/combined-194-198-release/RELEASE_RUNBOOK_20260914.md`. **Owner decision:** the release pin moves from `e808199` to `03550a8`, or Terra re-pins on the codex lane; nothing released, production untouched. | `tools/release/migration_194_198_predeploy.js`; `tests/unit/migration_194_198_reviewed_hashes.test.js`; `tests/e2e/verify_all.sh`; `docs/handoffs/new-hp/combined-194-198-release/RELEASE_RUNBOOK_20260914.md`; `docs/handoffs/new-hp/combined-194-198-release/rehearsal-20260914/` |
| 75 | **CI NOW EXECUTES THE COMBINED-APP BROWSER RUNGS, AND THE FIRST EXECUTION FOUND A DEFECT, 2026-09-14.** Lane `claude-opus/ci-browser-rungs-20260914` over `89b2d50a` (API only; the app was read at a pinned commit and not edited). First red on the baseline, in its own CI log (run 533, job `103948326783`): `── browser: inventory correction      SKIPPED (needs Chromium and E2E_APP_ROOT=<operator app checkout>)` and the same for `browser: two-step execute`, followed by `ALL REQUIRED ASSERTIONS PASSED` — neither skip set `FAILED` and neither was named in `SKIPPED`, so the `⚠ NOT RUN` line never printed; the coupled rent-roll rung was not in `verify_all.sh` at all and had only ever been run by hand. New `tests/e2e/app_pin.txt` (class 1) is the one declared app commit; `verify_all.sh` compares it to `E2E_APP_ROOT`'s HEAD (matched runs · absent skips by name · **drifted and unreadable FAIL and print both commits**, all four states exercised locally) and every app rung label carries the app sha; `verify.yml` reads the same file and checks `kzitelli-art/property-spine-app` out at that exact sha, read-only, `persist-credentials:false`, into `operator-app-pinned/` — **not** `property-spine-app/`, which is an existing TRACKED FILE at the repo root (`0f4aadc`) that a checkout there would have deleted in every run (caught locally as a `T` type-change). New coupled rung runs the app's `current_rent_roll_reconciliation.browser.js` unedited through the app's own `tools/coupled_browser_runner.cjs` (`node --require`, TLS_PORT derived from the nonce proof port); `tests/e2e/coupled_runner_receipt.js` (class 3, removed when the repos merge) asserts the transport receipt rather than trusting its comment. **CI run 536** (`50c49a92`) executed all of it: `── browser: coupled rent-roll (app 595e865) PASS`, `── coupled transport receipt (app 595e865) PASS`, artifact `coupled-browser-evidence` 829,713 bytes, **zero SKIPPED lines** — and `── browser: inventory correction (app 595e865) FAIL`, `locator.click: Timeout 30000ms exceeded … waiting for locator('.crumb-back') … element is not visible` at `inventory_correction.browser.js:65`. Cause: the combined app puts "Choose a property" above the shell at z-index 3900, so no desk is reachable until a property is chosen; that file held zero references to `livePropertyLayer` while `two_step_execute.browser.js` already handled it. Corrected with the same visible step as one isolated block, asking the document with `elementFromPoint` before clicking. **Not done here:** no product code, no migration, no deployment; the app repo was not edited; `app_pin.txt` is a development pin, never a release claim. | `tests/e2e/app_pin.txt`; `tests/e2e/verify_all.sh`; `.github/workflows/verify.yml`; `tests/e2e/coupled_runner_receipt.js`; `tests/e2e/inventory_correction.browser.js`; `.gitignore`; `docs/handoffs/new-hp/ci-browser-rungs/RECEIPT.md` |
| 76 | **CANDIDATE (not released) · HTTP-PROVEN LOCALLY + ASK SPINE REGISTERED, 2026-09-14 — prospect-to-home matching is RETRIEVAL ON A DECLARED BASIS.** Lane `claude-opus/matching-basis-20260914` over `0e10ef84` (app `b0be9f4` unchanged, pin not moved). Built to `docs/handoffs/new-hp/rulings/MATCHING_BASIS_RULING_20260914.md` MB-1..MB-9. First red on the baseline over HTTP and through Ask Spine (`matching-basis/first_red.witness.txt`, 3/6): no read on the leasing owner, `GET /operator/leasing/prospect-match` **404**, no `prospect_match` registry entry, `ask_spine_answer` never gathered it — and the substantive one, which PASSED as a red: with a recorded budget of 900 against a governed price of 1025 the existing `exact_spaces` seam returns **zero homes** and says "No priced home matched", dropping the one eligible home with `continue` so nothing names it or says by how much. Now `matchProspectHomes` + `readProspectMatchStanding` extend the `availableUnits` seam in the same owner: every home carries per constraint the recorded prospect fact (key/value/provenance/as-of) and the governed home fact (read/value/as-of/authority) in three states, with **violated homes returned, never filtered**; sources are the published pricing version, the application-target read and the three `recordPersonFact` keys only, so a legacy `units` row with bedrooms and a vacant flag never appears; ordering is one named deterministic rule with no weight or score; coverage is reported per family so an unpriced property says so instead of "no matches"; no term inherits the seam's existing refusal, with `term_required` and `pricing_term_required` distinct; the compact projection carries counts and unknowns and **no record ids**, registered and gathered before any screen. `gate_ask_spine_readers.js` gained an optional declared `owner` for the gathering detector (a domain exposed from an existing owner cannot be found by its name in a require path) plus three detector self-tests; entries without `owner` are unchanged. First CI run of the proof went red where local was green — it had borrowed the shared Skyline fixture, which the rent-roll proof registered just before it consumes (30/10, every failure `{"violated":0,"satisfying":0}`); it now establishes its own governed inventory through the same objects a confirmation writes. Proven on an owned nonce database at ledger 198/186 rows through the real staff door: **63/0** after the QB correction round. That round fixed four things, each red first: the door had **no person wall** — a leasing session at one property got HTTP 200 with another property's prospect's recorded budget, fact keys and homes, because `person_attributes` rows may carry a NULL `property_id`; the person-card predicate is now one shared helper (`src/identity/person_property_presence.js`) called by both doors. The composer branch keyed on `subject === "leasing" || "match"`, which `questionSubject` never yields, so `prospect_match` was registered, gate-green and **never gathered by any question** — measured by calling `gatherFacts`, not by grepping; it now keys on `leasing_person`, and a term-less gather is `attention_state: null` so a missing caller input never manufactures attention (§40.7). Readiness is pinned in both directions including the boundary month. The Ask gate now scans `src/leasing`: 115/115, 14 domains, 8 registered, 6 pending; the gate's own falsification scenario anchors on that line by exact text and was retracked (24/24). Proof 63/0. A second red was found by re-reading the delivered code: the readiness constraint named `move_month` as its prospect fact while deciding the state from whether a ready date existed at all, so a home ready AFTER the recorded month read `satisfied` and `violated` was unreachable — it now compares against the last day of the recorded month, and an unparseable month stays `not_established` rather than satisfied, every acceptance case in the ruling falsified; gate 87/87 (10 domains, 8 registered, 2 pending); all 56 source gates `PARENT EXIT 0`; nine nearest leasing/prospect regressions pass. **Not done here:** no screen and no browser rung — done as a domain and a door, **not as a screen**; no provider-generated Ask answer; the ordering rule's later tiebreaks are exercised over a small owned fixture, not many homes; **no schema (MB-9)**, no deployment. **QB integration note (board, 2026-09-14):** through the composer the projection is only ever the term-less refusal — `gatherFacts` supplies neither a term nor a person (`facts.person` is never assigned anywhere in `ask_spine_answer.js`), so today Ask Spine can say "matching needs dates" and nothing more; a term-carrying, person-naming question is the successor that makes the projection real. The `61f99bf` production stamp is untouched: re-stamping needs a production `/health` read, which the QB does not perform. | `src/leasing/leasing_inventory.js`; `src/identity/operator.js`; `src/agent/ask_spine_answer.js`; `tests/gates/gate_ask_spine_readers.js`; `tests/proofs/prospect_match_basis.db.js`; `tests/e2e/verify_all.sh`; `docs/handoffs/new-hp/matching-basis/RECEIPT.md` |
| 77 | **HARNESS ONLY · GATE STRENGTHENED, FIRST RED WITNESSED, 2026-09-14 — the Ask Spine reader gate proved a domain was ASSIGNED, never that a question REACHES it.** Lane `claude-opus/ask-gate-reachability-20260914` over board `7dbfd5a` (app pin `b0be9f4` unchanged; no `src/`, no migration, no deployment, no production read). First red, captured on a throwaway mutation that was reverted and never committed (`witness/dead_branch.first_red.txt`): with the composer branch keyed back to the dead `subject === "leasing" || subject === "match"` — the exact shape row 76 had to correct, which `questionSubject` never yields for any phrasing — the **unchanged** gate printed `ok prospect_match declared registered AND gathered`, `115 run · 115 passed · 0 failed`, `GATE EXIT = 0`. The gate read that `facts.prospect_match` was assigned inside `gatherFacts`; nothing asked whether an operator's sentence could arrive at that assignment, so a domain registered, gate-green and unreachable by every question was indistinguishable from a working one. The detector now requires each registered entry to declare `reached_by` — at least two sentences an entitled operator would actually type — calls the **live** `questionSubject` on each one, statically extracts the enclosing `if (subject === …)` guard around that domain's `facts` assignment, and fails when the produced subject is not among the guard's literals, naming both. On the same mutation it goes loud: `FAIL prospect_match is reached by "which homes fit this prospect" … questionSubject produced "leasing_person", but the branch is guarded by ["leasing","match"] — nothing an operator types this way reaches prospect_match.` All **8** registered domains (prospect_match, maintenance, compliance, utility, contracted_service, debt, equity, tenancy) declare two questions each and every one reaches; **none had to move to `pending`**, and nothing was fixed in the composer to make that true. A real fragility surfaced while building it: requiring the composer at gate load made the **gate itself die** when the falsification scenario deletes a standing read (29/30, failing for the wrong reason), so the producer is loaded lazily behind a guard and its absence is reported as a named assertion failure rather than a crash. Counts before → after: assertions **115 → 154**, domains 14, registered 8, pending 6, waived 0 (unchanged — this lane strengthened what a green means, it did not move a rung). Falsification extended from 6 mutations to **8** (dead-branch guard; `reached_by` renamed away) — 30/30, the gate goes red 8 ways and green again. Source governance run **bare**, never through a pipe: `node tests/verify_source_governance.js; echo EXIT=$?` → `✓ PASS — all 56 source-governance gates exited 0`, `EXIT=0`. **FOUND, not fixed (product, for the board):** `"when does the debt mature"`, `"when does the loan mature"` and `"what is the outstanding principal"` all resolve to subject `work`, because `DEBT_TERMS` requires the literal `loan maturity` / `maturity date` / `principal balance` — a vocabulary gap in `questionSubject`, not a reachability failure, and out of scope for a harness-only lane; the declared sentence was chosen around it and the gap recorded. **What this would miss:** `reached_by` is a declared sample, not a search of the phrasing space — a domain reachable by two declared sentences and nothing else still passes; the guard extractor takes the nearest enclosing `if (subject === …)`, so a differently-shaped branch reads as undeclared rather than as reached; module-level entitlement (`asset_management` on utility, equity and debt) is not evaluated, so reaching the branch is not proof an entitled answer comes back; and nothing here asserts the projection is **correct** once reached. | `tests/gates/gate_ask_spine_readers.js`; `tests/scenarios/ask_spine_reader_gate_falsification.js`; `docs/handoffs/new-hp/ask-gate-reachability/RECEIPT.md`; `docs/handoffs/new-hp/ask-gate-reachability/witness/dead_branch.first_red.txt` |
| 78 | **RESOLVED IN SOURCE · PROVEN THROUGH THE REACHABILITY GATE, 2026-09-14 — two morphological gaps sent lender-facing debt questions to `work`.** Lane `claude-opus/debt-vocabulary-20260914` over board `169175da` (app pin `b0be9f4` unchanged; one product file, one constant; no schema, no deployment, no production read). `DEBT_TERMS` (`src/agent/ask_spine_answer.js`) held only the literal `loan maturity`, `maturity date` and `principal balance`, so `"when does the loan mature"`, `"when does the debt mature"` and `"what is the outstanding principal"` fell through every named domain to `work` — Debt registered, gathered and gate-green under its two declared sentences, and unreachable by the question actually asked. Found by row 77's detector, which calls `questionSubject` on declared sentences rather than trusting the branch exists. **The defect was NARROWER than the packet first read it:** the assignment named three sentences as broken and four more as additions to `reached_by`; **the four additions already reached `debt`** (`what do we owe on the mortgage`, `who is the lender`, `what is the interest rate on the loan`, `is there an extension option` — `mortgage`, `lender`, `interest rate` and `extension option` were already in the constant), as did `"when does the mortgage mature"`; the real gap was two morphological shapes, not six missing sentences, and the witness records the produced subject for all eleven tested so the four that already worked are visible as already working. First red at `witness/first_red.txt` with nine declared sentences: **161 run · 158 passed · 3 failed · GATE EXIT 1**, each failure naming the produced subject (`questionSubject produced "work", but the branch is guarded by ["debt"]`). Corrected by **morphology, not a sentence table**: `(?:loan|debt|mortgage)s?\s+matur(?:e|es|ed|ing|ity)`, `matur(…)\s+(?:date\s+)?(?:of|on)\s+(?:the|our|its\s+)?(?:loan|debt|mortgage)`, `outstanding principal` and `principal (?:balance|outstanding)`. **The maturity verb is bound to a debt noun on purpose** — a bare `matur(?:e|es|ed|ity)` steals `"when does the lease mature"`, which is tenancy's, and answering a lease question from the Debt reader is a confident wrong answer about the wrong instrument (§5). No other `TERMS` constant touched; the order of checks in `questionSubject` unchanged. New `tests/unit/debt_vocabulary_subject.test.js` (class 1, registered in `verify_all.sh`) is the wall the gate cannot be: **22/22** over gained sentences (including inflections nobody enumerated — `"when did the loan matured"`, `"what is the maturity of the loan"` — which is the evidence this is morphology), the six that already worked, and eight **not stolen** (`lease mature`→tenancy, `work is outstanding`→work, `work orders`→work, `rent roll`→tenancy, `beds`→tenancy, `licenses`→compliance, `water bill`→utility, `common equity`→equity) — plus one the assignment did not ask for: `"how many beds are open, and when does the loan mature"` still returns **`composition_unavailable`**, so a wider debt vocabulary does not let debt WIN a two-domain sentence past the §40.8 guard. Gate **161/161, EXIT 0** (14 domains / 8 registered / 6 pending / 0 waived, counts unchanged); reachability falsification **30/30**, red 8 ways and green again; `node tests/verify_source_governance.js; echo EXIT=$?` → `all 56 source-governance gates exited 0`, `EXIT=0`, run bare and never through a pipe. Nearest regressions found by grep and run at head: eleven non-DB tests touching `questionSubject` or the composer all EXIT 0, including `skyline_ask_spine_sms_matrix` **157/157** across every domain. `debt_ask_spine`, `equity_ask_spine` and `utility_ask_spine` exit 1 at head **and identically on a clean `169175da` checkout with zero assertion failures on both sides** — they die on the database connection before asserting and need Postgres, which CI supplies; `leasing_ask_spine.e2e.js` needs a live server and is likewise CI's. **What this would miss:** the declared set is still a sample (`what is our LTV`, `when is the rate cap up`, `are we in covenant` were not tested and are not claimed); module entitlement (`asset_management` on debt) is still unevaluated, so reaching the reader is not proof of an entitled answer; capability class unchanged at retrieval only, so *why* debt service moved is not promised (§40.10); and the bound maturity pattern is positional — `"when does the loan we took out in 2019 mature"` does not match, which needs a proximity rule rather than a longer alternation and was not attempted. | `src/agent/ask_spine_answer.js`; `tests/unit/debt_vocabulary_subject.test.js`; `tests/gates/gate_ask_spine_readers.js`; `tests/e2e/verify_all.sh`; `docs/handoffs/new-hp/debt-vocabulary/RECEIPT.md`; `docs/handoffs/new-hp/debt-vocabulary/witness/first_red.txt` |
| 79 | **PROOF ONLY · §40.8 UNENTITLED PATH PROVEN PER DOMAIN · ONE GAP FOUND, DECLARED, NOT FIXED, 2026-09-14.** Lane `claude-opus/ask-entitlement-proof-20260914` over board `0ae2602d` (app pin `b0be9f4` unchanged; `git status --porcelain -- src/ migrations/ server.js` empty and `git diff -- src/` against the board empty; no schema, no deployment, no production read). Row 77's detector proves a question *reaches* a gather branch; nothing proved what an **unentitled** session gets once it arrives — that lane's own stated weakness. New `tests/proofs/ask_spine_entitlement_matrix.test.js` (class 1, registered in `verify_all.sh`) crosses the 8 registered domains against six module sets (`[]` · leasing · management · maintenance · asset_management · all five), one sentence per domain **taken from that domain's own declared `reached_by`** so the two proofs cannot drift, calling `gatherFacts` directly with a db whose every use throws so a read needing the database announces itself as `READ_FAILED` instead of quietly succeeding against a fixture. Each REGISTRY entry gained `entitled_by`. **134/134, EXIT 0. 47 of 48 cells behave:** every unentitled cell is `absent`, no unentitled cell produced a fact, and no unentitled absence is counted by `composite_silence` as pending or unread (§40.7 — not knowing because you may not ask is not the property being unreadable). **FOUND, not fixed:** `gatherFacts` holds **no module guard on the compliance branch at all** — `if (subject === "compliance") {` with no `allowed_modules` check, where every other registered domain guards in two places. Called directly with **zero modules** it returns the full compliance projection; proven with a complete synthetic standing shape, not inferred (`readerReached=1`, the synthetic licence label present in the envelope). **The blast radius was checked, not assumed, and is bounded:** `answer()` refuses first for `compliance|utility|contracted_service|debt|equity` without `asset_management`, and through the door a zero-module compliance question returns `not_authorized`, discloses nothing, and says *"Compliance is not available in your current access for this property."* So it is a **defence-in-depth gap, not a live leak through the door** — and it matters because this module's own comment states the rule it breaks: *"The inner NOT_AUTHORIZED envelope in gatherFacts is KEPT. It is not redundant: gatherFacts is exported and independently callable."* That reasoning was applied to leasing and not to compliance. The gap is **tracked rather than hidden**: `compliance` declares `entitled_by: ["asset_management"]` (taken from the door, which is the real contract) plus a `composer_divergence` block naming the four unguarded cells, and the proof pins today's behaviour — deleting the declaration turns it red, and **fixing the composer also turns it red**, which is how a fix announces itself and forces the declaration to be retired with it. **Judgment call flagged:** the two composer layers disagree with each other, so "declare what the composer does today" has two answers; declaring from `gatherFacts` literally would have written a defect into the registry as though it were the contract. First red at `witness/first_red.txt` — the debt module guard removed as a throwaway mutation (working tree only, reverted, `git diff -- src/` byte-identical): **125/134, 9 failures across 4 module sets**, EXIT 1, the silence assertion failing beside the disclosure one because a leaked domain also turns an unentitled session BLIND. **Asserted apart on purpose:** the unentitled debt cell touches no database (`queries === 0`) but the unentitled maintenance cell **does** — subject `work` also gathers the module-scoped attention queue — so "the db was untouched" is not the §40.8 property and is not claimed as one. Gate 161/161 EXIT 0 (14 domains / 8 registered / 6 pending / 0 waived, unchanged); reachability falsification 30/30, red 8 ways and green again, its `tenancy` anchor preserved by declaring after the `state:` line rather than before it; `node tests/verify_source_governance.js; echo EXIT=$?` → `all 56 source-governance gates exited 0`, `EXIT=0`, run bare. **Out of scope and said so:** cross-domain composition authorization stays declared UNSOLVED and is not tested; the HTTP `/ask` door and its session resolution are the transport, not the composer's own gate, and are exercised only where the two layers disagree. **What this would miss:** one sentence per domain; module sets taken as given rather than derived from a real session (§21); no **fact-level** entitlement (§40.4 rides entitlement on each fact — this proves the branch gate, so a domain that correctly refuses unentitled callers could still over-disclose within an entitled one); and the hostile db flattens most entitled cells to `READ_FAILED`, proving the branch ran but not that the projection is correct or minimal. | `tests/proofs/ask_spine_entitlement_matrix.test.js`; `tests/gates/gate_ask_spine_readers.js`; `tests/e2e/verify_all.sh`; `docs/handoffs/new-hp/ask-entitlement/RECEIPT.md`; `docs/handoffs/new-hp/ask-entitlement/witness/first_red.txt` |
| 80 | **FOUND ON THE CANONICAL RENT ROLL READ, 2026-09-14 — three lender-facing gaps in `currentRentRoll`, witnessed on an established Skyline-shaped position; reader only, not fixed.** Board `571892d`, owned nonce database at ledger 198, the Skyline-shaped rehearsal tracker established through the real Deal Setup doors (`current_rent_roll_reconciliation.db.js` 63/0), then `currentRentRoll` read as of today. (1) `tenancy_summary` does not balance: `contractually_occupied 31 · vacant 0 · unresolved 102 · contested 13` sum to 146 of `total 160`; the 14 `activation_pending` positions have no bucket, against the header's own "each summary balances within its own axis". (2) Unverified revenue has no magnitude: the 92 accepted-occupied positions with terms unknown read `economics_state not_applicable`, `current_rent null`, `imported_claim "unknown"`; the tracker's asserted rent is retained as evidence on the proposal and never projected, so the read cannot say "$X claimed, $0 verified" the way `contested_claims.implicated_rent` already shows contested money visible and uncounted. (3) The contractual axis calls an accepted occupancy `unresolved` while the row carries `OPENING_OCCUPANCY_ACCEPTED_TERMS_UNKNOWN`; and `contested_claims.claims[].start_date` is `String(date).slice(0,10)` over a JS Date (no pg type parser anywhere in `src/` or `server.js:147`), so the live payload renders weekday strings. Lines that are lender-ready as they stand: trusted rent $26,350 from 31 leases, denominator 147 with exclusions named, contested $26,250 visible and uncounted, the proof axis. All figures are synthetic in the real shape. **Not decided here:** the tracker cutover date (Kameron); the wording of the new contractual state. | `src/surfaces/rent_roll_canonical.js`; `src/tenancy/dated_positions.js`; `docs/handoffs/new-hp/lender-read/SKYLINE_LENDER_READ_20260914.md` |
| 81 | **RESOLVED IN SOURCE, 2026-09-14 — the compliance gather branch now carries the module guard its four sibling domains have.** Lane `claude-opus/compliance-guard-20260914` over board `571892df` (app pin `b0be9f4` unchanged; **one product change, one condition, one place**; no schema, no deployment, no production read). `gatherFacts` held no `allowed_modules` check on `if (subject === "compliance")` while utility, contracted_service, debt and equity — the four domains sharing the same `asset_management` entitlement — each carry one. **Defence-in-depth, not a live leak, and the receipt says so:** `answer()` has always refused a compliance question without `asset_management` with a sayable refusal, so nothing was disclosed through the door; what was wrong is that `gatherFacts` is exported and independently callable, which is the exact rule this module already states about the leasing guard (*"The inner NOT_AUTHORIZED envelope in gatherFacts is KEPT. It is not redundant: gatherFacts is exported and independently callable"*) and which had been applied to leasing and not to compliance. First red was already standing on the board as row 79's `composer_divergence` pin (134/134 with four cells recorded as an open gap); applying the guard turned that pin red — `the gap no longer reproduces`, 137/138, EXIT 1 — which is how the fix announced itself and forced the registry declaration to be retired in the same commit. The assertion was **inverted rather than deleted**, so it is now the wall that stops the guard being removed. Corrected to `if (subject === "compliance" && (allowed_modules || []).includes("asset_management"))`; `answer()`'s pre-gate, every other branch and the reader untouched. The compliance row moves from `READ_FAILED` in all six cells to `absent · absent · absent · absent · E READ_FAILED · E READ_FAILED` — **identical to utility's row**. **⚠ DELIBERATE DEVIATION, MEASURED NOT ASSUMED:** the assignment asked both for leasing_person's inner `{ read_state: "NOT_AUTHORIZED", note }` envelope AND for that envelope not to be counted by `composite_silence`; those conflict. `composite_silence` classifies every fact whose `read_state !== "OK"` as BLIND, so on the board a session merely lacking an entitlement is told `{"state":"BLIND",…,"why":"at least one required reader did not return, so silence cannot mean health"}` about a property where **nothing is unknown** — the §40.7 collapse, since a reader you MAY NOT read is not a reader that DID NOT RETURN. Copying that envelope would have propagated the defect to a second domain and failed the silence requirement in the same stroke, so compliance uses **absence** (which is also what "matching the shape of the utility/debt/equity branches exactly" requires, and what `composite_silence` reads correctly) and the leasing_person defect is **PINNED in the matrix proof (§4b), not propagated and not silently skipped** — three assertions recording that the envelope exists, that it still reads BLIND, and that the stated reason is false for an entitlement refusal; deleting the block makes the defect quiet again, fixing the silence computation makes the block go red. **FOUND, not fixed (owner decision):** `composite_silence` must exclude `NOT_AUTHORIZED` from `blind` — outside a lane scoped to one branch and a behaviour change to a domain this lane was not given. **FOUND (process):** `compliance_ask_spine` was named as a regression target and **does not exist in this repository** — `MODULE_NOT_FOUND` at head and at the board sha alike; the seven `tests/unit/compliance_*.test.js` that do exist all pass at head, and the db-backed compliance proofs need Postgres and run in CI. Entitlement matrix **143/143 EXIT 0**; gate 161/161 EXIT 0 (14 domains / 8 registered / 6 pending / 0 waived, unchanged); falsification 30/30 red 8 ways and green again; `node tests/verify_source_governance.js; echo EXIT=$?` → `all 56 source-governance gates exited 0`, `EXIT=0`, run bare. Row 79's receipt was given a **dated pointer only** — its matrix table is left exactly as measured, because it is the evidence that found the defect. **What this would miss:** an entitled caller's projection is still only proven to have RUN, not to be correct or minimal (the hostile db flattens entitled cells to `READ_FAILED`); fact-level entitlement (§40.4) remains unproven since both layers gate the BRANCH; `facts.attention`, `facts.person` and `facts.tour_schedule` are outside the 8 registered domains and were never crossed against module sets; and cross-domain composition authorization stays declared UNSOLVED. | `src/agent/ask_spine_answer.js`; `tests/proofs/ask_spine_entitlement_matrix.test.js`; `tests/gates/gate_ask_spine_readers.js`; `docs/handoffs/new-hp/compliance-guard/RECEIPT.md`; `docs/handoffs/new-hp/compliance-guard/witness/first_red.txt` |
| 82 | **RESOLVED IN SOURCE · HTTP-WITNESSED ON AN OWNED RUNTIME, 2026-09-14 — the three lender-facing lines row 80 found are closed; reader only.** Lane `claude-opus/rent-roll-lender-lines-20260914` over board `bf99e5c8` (app pin `b0be9f4` unchanged; no schema, no deployment, no production read). Red reproduced the same way first — owned nonce database at ledger 198, labelled fixtures before any business action, `current_rent_roll_reconciliation.db.js` **63/0**, then `currentRentRoll` as of today: `tenancy_summary` summed **146 of 160**, `claimed_rent_unverified` **undefined**, `unresolved: 102`, and the first contested claim read `{"start":"Sat Aug 01","end":"Mon Jul 26"}`. **(1)** `tenancy_summary` gains **two** buckets, not one — `activation_pending` (14, the missing positions) and `occupied_terms_not_established` (92) — and now sums **160 of 160**, and **3 of 3** on the unit-basis "Other Shape" property, with no property-specific branch. **(2)** `totals.claimed_rent_unverified` = **$78,200 over 92 positions** beside `contractual_rent_trusted` = **$26,350 over 31, unchanged**; sourced from the accepted claim's proposal via `basis_ref.proposal_id` and projected onto the row as `claimed_rent`, mirroring `contested_claims.implicated_rent` — **visible without being counted**: every one of the 92 rows keeps `current_rent === null` and `contributes_trusted_rent === false`, and the occupancy numerator is still 31. The proof recomputes the total from the proposals directly rather than checking the read against itself. A claim with no rent reads **null, never 0**, and is counted separately (`positions_claimed_without_rent`), proved by removing one claim's rent, re-reading, and restoring it. **(3a)** New tenancy state `occupied_terms_not_established`; **`unresolved` keeps its meaning** for the 10 genuinely unreconciled positions, every one of which is asserted to carry `evidence_state === "unreconciled"`. It is carried exactly as `unresolved` was — inside the occupancy denominator (147), never vacant, reported beside — and the operating bucket is untouched because `rentRollBucketOf` keys on `basis_type`. **⚠ The predicate is deliberately NOT literally `bucket_reason_code`:** measured on the fixture, **104** rows carry `OPENING_OCCUPANCY_ACCEPTED_TERMS_UNKNOWN` but only **92** are accepted occupancies — the other **12 are `activation_pending`**, beds holding a commenced lease, and keying on the code literally would have reclassified them and **re-broken the very balance finding 1 exists to fix**. The state keys on the code's accepted branch condition (an accepted `occupied` claim with `uncorroborated` evidence); the proof asserts the 12-row difference and that every one is `activation_pending`. **(3b)** One `isoDate` helper used in both claim dates, **no global pg type parser** — witnessed over the real door on the owned server: `GET /operator/rent-roll/canonical` → 200, `{"start_date":"2026-08-01","end_date":"2027-07-26"}`, **0 of 30** claims non-ISO, summary 160 of 160. **⚠ MY FIRST CONSUMER SWEEP WAS INCOMPLETE AND CI CAUGHT IT (run 559, red):** the first grep covered `src/`, `server.js`, the app file and only `tests/unit/` + `tests/gates/` — **not `tests/proofs/`** — and I then claimed a complete list, breaking this repo's own rule that a count is a claim about a search and the whole repo must be searched, not a subfolder. `availability_uncorroborated_claim.db.js` pinned the old value in two assertions and went red. The corrected whole-repo sweep found **four** real consumers of the old value in tests, two of which the first sweep missed: `availability_uncorroborated_claim.db.js:215` and `:239` (**missed, then updated**, intent preserved — the claim supports occupancy, nothing supports an offer, no measure was forced to agree); `rent_roll_canonical_proof.js:25`, a `TENANCY` vocabulary allowlist that named four values and was **already** missing `activation_pending`, now naming all six; `rent_roll_canonical_proof.js:75`, whose subject is now the new state, checked across both so the never-vacant guarantee still covers every row it covered; and `skyline_rent_roll_model.db.js:225`, where `vacantPos` is the complement of `contractually_occupied` summed against it to 160. After the fix `availability_uncorroborated_claim` reads **14/4 identically on the baseline tree and this one** — the four remaining are a local 503 on the "down accepted" door that cascades, present on both sides and absent in CI. **A DISTINCT TENANCY VALUE WAS CHOSEN OVER A SUMMARY SUB-OBJECT, and the header's axis list is updated with it** (it listed four values while the code yielded five; it now lists six and records that `activation_pending` had been yielded while the list said four, which is how it reached `tenancy_summary` with no bucket): a per-row surface needs a per-row value — `rent_roll_institutional.js` labels each row for a lender, and a sub-object on the summary cannot drive that label, so those 92 rows would have kept reading "Unresolved occupancy evidence" while the summary said otherwise, the read disagreeing with itself one altitude apart; the board's complaint was about what a ROW says; and the row already carries the distinction durably in `basis_type`, so the axis was the one place still collapsing two facts §40.7 keeps apart. **The unit view and standing read mirror the BUCKET axis, not this one** (`occupied / activation_pending / open / needs_review`, keyed on `basis_type`; the proof asserts all 92 still bucket as `occupied`), and `tenancy_position_read.js` keys on `evidence_state` and `interval_state` — neither needed a change. **Every consumer of `tenancy_state` was then grepped across the whole repository** (`server.js` and the app file contain none): `rent_roll_canonical.js` updated; **`rent_roll_institutional.js:78` updated and this is why the grep mattered** — its `statusLabel` tested the old value, so all 92 positions would have fallen through to `return "Occupied"`, silently upgrading beds with no established rent, term or legal right to plain Occupied **on the one surface a lender reads**; it now says "Occupied — terms not established". A third file beyond the two named, and a reader. Unaffected and shown so: `rent_roll_unit_view.js` and `availability_read.js` (pass-through), `pricing_decision_packet.js` and `future_rent_roll_pricing_contract.js` (test `contractually_occupied`), `tenancy_position_read.js` (a comment; its `evidenceUnresolved` keys on `evidence_state`, its `t.unresolved` on `interval_state`), and `dated_positions.js:1016/1029/1064` which are **a different axis** — `interval_state` in the forward read, not `tenancy_state`. Proof **78 passed, 0 failed**: the original **63 unchanged**, plus 15 additive in a new section I; no existing assertion altered. Governance bare → `all 56 source-governance gates exited 0`, `EXIT=0`. Ask Spine tenancy gather, SMS matrix, `ask_spine_answer`, entitlement matrix, reader gate, `availability_occupancy_basis` all EXIT 0. **FOUND, not fixed:** `rent_roll_canonical_proof` (32/6) and `rent_roll_institutional_proof` (`FATAL: Cannot use 'in' operator…`) fail **identically on the baseline tree**, each measured on its own freshly built database from `bf99e5c8` and from this tree — pre-existing, not this lane's. **What this would miss:** one fixture shape twice (bed and unit, both synthetic); only the `rent` key is consulted, so a source carrying `actual_rent` alone reads null; `claimed_rent` is projected, never reconciled, and must never be read as a Spine figure; the institutional CSV gains the new wording but no new column; and `positions_claimed_without_rent` is 0 on this fixture, its branch proved by editing one claim rather than by a fixture that naturally contains one. **SECOND-READ FINDING, 2026-09-14, NOT FIXED — (3b) IS CLOSED ONLY WHERE THE PROCESS RUNS AT OR BEHIND UTC.** `isoDate` renders a `Date` with `toISOString().slice(0,10)`, but node-postgres decodes a `date` column to LOCAL midnight, so in any timezone AHEAD of UTC the UTC calendar date is the PREVIOUS day. Measured, not reasoned: `TZ=Europe/Berlin` turns the pinned `2026-08-01` into `2026-07-31` — the same off-by-one class of wrong value this line was opened to remove. CI and the deployed host both run UTC, so the witnessed payload is correct. **⚠ THE WORD "LATENT" IN THIS ROW WAS WRONG — SEE ROW 90.** This row concluded *"nothing observed is wrong today; the defect is latent, not live"*, which was true of the two fields it looked at and false of the same expression four files away, on the rent-roll INTAKE path, where it does not render a wrong value — it REFUSES the file. "Latent" was a conclusion about the call sites this row examined, stated as a conclusion about the defect. The helpers own comment asserts the opposite ("a local-time slice can move them a day") and so will mislead the next reader — for a `date` column carrying no time or zone it is the LOCAL calendar components that are correct. **Also found, not charged to this lane:** `tenancy_summary` has no Ask Spine consumer, so the two new buckets and `claimed_rent_unverified` are readable on the screen and **not askable** (§40.2); the underlying fact stays reachable as `evidence_state = uncorroborated`, no domain regressed, and the reader gate is correctly green. | `src/surfaces/rent_roll_canonical.js`; `src/surfaces/rent_roll_institutional.js`; `src/tenancy/dated_positions.js`; `tests/proofs/current_rent_roll_reconciliation.db.js`; `docs/handoffs/new-hp/rent-roll-lender-lines/RECEIPT.md`; `docs/handoffs/new-hp/rent-roll-lender-lines/witness/` |
| 83 | **RESOLVED IN SOURCE, 2026-09-14 — entitlement is not silence: `NOT_AUTHORIZED` no longer makes an unentitled session read BLIND (§40.7).** Lane `claude-opus/silence-not-authorized-20260914` over board `f680a365` (app pin `b0be9f4` unchanged; **one product change, one place** — the `composite_silence` computation in `src/agent/ask_spine_answer.js`; no schema, no deployment, no production read). First red was already standing on the board as row 79's matrix pin: `composite_silence` classified every fact whose `read_state !== "OK"` as BLIND, so a maintenance-only session asking a leasing question got `{"state":"BLIND","unread":[{"domain":"leasing_person","read_state":"NOT_AUTHORIZED"}],"why":"at least one required reader did not return, so silence cannot mean health"}` — about a property where **nothing was unknown**. `NOT_ESTABLISHED`, `READ_FAILED`, `READ_TIMED_OUT` and `QUIET` are facts about the property and about Spine; `NOT_AUTHORIZED` is a fact about the **caller**, and folding the fifth into the four made every restricted session read as blind, manufacturing a scary unknown out of a healthy read (§5). A withheld reader is now **not required for this caller** — excluded from the readers whose return decides health, excluded from `pending`, and reported separately as `withheld: [{ domain, reason: "not_authorized" }]`, so an answer can say *"Spine did not read X for you"* without claiming the property is unreadable. Three load-bearing details: the QUIET reason becomes *"every reader **this session may read** returned"* (the old wording would have been a quiet overstatement, and the proof asserts the wording rather than trusting it); `withheld` appears **only** when something was withheld, so with nothing refused the payload is byte-identical to before and no consumer sees a contract change; and `withheld` is spread into the BLIND branch too, so a caller both refused one domain and failed by another sees both facts. **The pin was FLIPPED, not deleted** — the three FOUND assertions became the positive wall in the same block, plus five more: not counted as pending (entitlement is not attention), an **absent** domain is not reported as withheld (absence ≠ refusal, which keeps the compliance row of the matrix honest after row 81's guard), no `withheld` key when nothing was refused, and a genuine failure is **still BLIND** so the fix narrows blindness by exactly one value. Matrix **148/148 EXIT 0** (was 143); the whole 8 × 6 grid unchanged elsewhere. **⚠ CORRECTION TO THE BRIEF:** `skyline_ask_spine_sms_matrix` was named as asserting composite_silence shapes and **asserts none** (`grep` returns nothing); it passes 157/157 unchanged and is **not** evidence about this change. **No test assertion anywhere had to be changed.** The real source-only consumer is `tests/unit/required_work_standing.test.js`, which is the one that matters: its `deepEqual(composite_silence.domains, ['maintenance'])` would have failed had a key appeared when nothing was withheld, and its `BLIND` assertions for `BROKEN` and `READ_TIMED_OUT` prove genuine failures are untouched — both pass. `triage_work_scope.db.js` and `prospect_match_basis.db.js` assert ATTENTION/pending shapes this change does not reach and run in CI. **Also in this lane: `tests/unit/compliance_ask_spine.test.js` now exists** (class 1, registered in `verify_all.sh`, **8/8**) — it had been named as a regression target twice before anyone checked the file was there, while every other registered Asset Management domain had one; the entitlement matrix covers *who* may read compliance and nothing covered *what they get when they may*. Built in debt's shape with a synthetic reading through the reader seam and no database, it asserts the contract envelope (`contract_version`, `as_of`, `capability_classes` with comparison and cause explicitly false, `composition_authorization: unsolved`, `coverage`), that every item keeps `standing / why / unresolved / next / attention`, that `not_established` does not read as fine, that evidence reaches the model as **role and label only**, and that **no record id, source id or opener token escapes** — with those ids seeded into the fixture so their absence is a measurement rather than an empty-fixture artefact. Governance bare → `all 56 source-governance gates exited 0`, `EXIT=0`. **What this would miss:** BLIND-plus-withheld together is **unreachable today and therefore not proven** — `leasing_person` is the only writer of a `NOT_AUTHORIZED` envelope and nothing else gathers in its subject, so that path is written for the next writer, not witnessed; `withheld` is a new key with **no consumer yet**, so the answer *can* now say "Spine did not read X for you" and nothing makes it; and the instruction block was deliberately not changed, so the model is told how to read `composite_silence` but not yet what `withheld` means. | `src/agent/ask_spine_answer.js`; `tests/proofs/ask_spine_entitlement_matrix.test.js`; `tests/unit/compliance_ask_spine.test.js`; `tests/e2e/verify_all.sh`; `docs/handoffs/new-hp/silence-not-authorized/RECEIPT.md`; `docs/handoffs/new-hp/silence-not-authorized/witness/` |
| 84 | **RESOLVED IN SOURCE · DB-PROVEN ON AN OWNED RUNTIME · FALSIFIED BOTH DIRECTIONS, 2026-09-15 (found 2026-09-14) — an UNGRADED codex commit on the deployed API reverted the one protection row 82 exists to provide, on the surface a lender reads.** Deployed API `b3e4407` = graded board `6fa4638` + `2b5c47e` "Align formal rent roll with operating buckets" + `b3e4407` (proof update); migrations byte-identical; `6fa4638` is an ancestor, so every graded lane IS in production. **What the codex commit fixes is real:** the formal schedule and the operating table disagreed, and a needs-review or activation-pending position could read "Occupied" through the old axis-only fallback. It also ADDS four honest totals (`positions_activation_pending`, `_open`, `_needs_review`, `_occupancy_unconfirmed`) and returns `"Occupancy Unconfirmed"` when `bucket == null`. **What it loses is bigger.** (1) `statusLabel` now returns `r.bucket_label` FIRST, and `RENT_ROLL_LABELS.occupied` is the bare string `"Occupied"`. `rentRollBucketOf` buckets BOTH `contractually_occupied` AND `basis_type = opening_claim_occupied` as `occupied` — its own comment says *"Occupied" must never secretly mean "a canonical lease exists"* — so every accepted-occupancy-with-terms-unknown position reverts from `"Occupied — terms not established"` to plain **"Occupied"**. Row 82 added that exact line to stop exactly this, and its `⚠ WITHOUT THIS LINE...` warning comment now sits above code that can no longer run: the new early return precedes it for every row carrying a bucket. The graded fix is dead code. (2) `confirmed_contractual_occupancy` is now `buckets.occupied` — a field whose NAME says confirmed contractual, populated by a bucket defined never to imply a lease. The CSV emits `"Confirmed contractual occupancy," <n> " of " <total>`. On real Skyline, where **31** positions are confirmed occupied and only **4** carry trusted rent economics, this reports the claim-backed population as contractually confirmed to a lender. The denominator also moved from `of_leasable_resolved` (excludes down and contested) to `buckets.total` (everything) — two changes in opposite directions on one ratio. **Not a bad commit; a narrow, high-consequence loss.** Minimal fix, both halves needed: special-case the one bucket that collapses two facts (`if (r.bucket === "occupied" && r.tenancy_state === "occupied_terms_not_established") return "Occupied — terms not established";` ABOVE the `bucket_label` return), and either rename `confirmed_contractual_occupancy` to what it now counts or restore it to the contractual count and add `occupied_all_bases` beside it. **Demo bearing:** the operating Rent Roll table is UNAFFECTED and honest; this is the formal/print/CSV schedule, which is the artefact that travels. **── RESOLVED 2026-09-15, AND THE ROW UNDERSTATED IT BY TWO. ──** Reading the diff rather than this row found that `if (r.bucket_label) return r.bucket_label;` as the FIRST line made **every** branch beneath it unreachable, not just the one this row named — and this schedule has no other column for any of them (`COLUMNS` is the whole list). Also lost: **a DOWN unit's vacant bed printed as a bare "Open"** — `rentRollBucketOf` does not read `is_down` at all, so a bed under a physical hold was offered as available to lease on the lender surface; **overlapping lease claims** flattened into an unqualified "Needs Review"; and **a spanning lease whose amount cannot support a contractual claim** printed "Occupied" beside an empty rent cell with nothing saying which it was. **The fix keeps BOTH rules rather than one.** The bucket supplies the base word — the alignment commit was right about that — and each fact the bucket cannot express is APPENDED, so the base still matches the operating table character for character; a refinement is not a disagreement, and a proof asserts every status either IS the bucket label or is that label followed by `" — "`. **The terms split uses `contractual_terms_state`, not the `tenancy_state` this row prescribed:** both separate the same rows today, but only that one is derived from the SAME `current_lease_position` that decides the bucket's first branch, so the split is exact by construction rather than two independently-computed sets happening to coincide. The headline is passed through from the canonical reader again, with `positions_occupied_all_bases`, `positions_occupied_terms_not_established` and the two denominator exclusions reported beside it, and the CSV emits every total the JSON carries. **MEASURED, BOTH DIRECTIONS, ON A REAL FIXTURE** (12 bed positions, three occupied by claim with no lease, one unit down): before `Confirmed contractual occupancy,4 of 12` · after `1 of 9`. And on the holds fixture the pre-fix headline read **`2 of 8` in EVERY state — including all-units-down, where canonical occupancy is `0 of 0`.** A ratio that cannot move when the property goes down is not measuring the property; that is worse than the naming error this row recorded, and no prose found it — running the proof against both versions did. **THE SECOND UNGRADED COMMIT TAUGHT A PROOF THE DEFECT.** `b3e4407` changed 15 assertions in `canonical_occupancy_holds.db.js` to compare against the buckets, leaving a comment that asserting the contractual projection *"was the reason this proof stayed red after the production-facing consistency fix"*. The `expected` table three lines above was left untouched and still encoded the contractual contract. Restored, with the bucket asserted beside it under its own name. **Evidence:** owned nonce database at ledger ceiling 198, real HTTP against an owned server — `canonical_occupancy_holds` **116/0** (30 red against the pre-fix source), `availability_uncorroborated_claim` **27/0** with nine new label assertions green on first run and written from the doctrine before being run (5 red against the pre-fix source), all 56 governance gates `PARENT EXIT 0`. **NOT browser-verified in this thread** — the coupled rent-roll browser rung was not run here. **App side:** `psIrTotalRows` is now the one definition both the printed table and the CSV export read (they had already drifted — the table showed `Positions down`, the export did not), every qualifying row optional so the deployed API's older response still renders exactly as before; `rent_roll_cutover_app.test.js` **163/0**, falsified. | `src/surfaces/rent_roll_institutional.js`; `src/tenancy/dated_positions.js` (`rentRollBucketOf`, `RENT_ROLL_LABELS`, `contractualTermsState`); `tests/proofs/canonical_occupancy_holds.db.js`; `tests/proofs/availability_uncorroborated_claim.db.js`; `tests/proofs/rent_roll_institutional_proof.js`; app `index.html` (`psIrTotalRows`, `psIrHas`); app `rent_roll_cutover_app.test.js` |
| 85 | **RESOLVED IN SOURCE · FIRST RED OBSERVED LOCALLY · DB RUNG IN CI, 2026-09-15 — "what can I show this person?" was deleted by a refusal that belongs to the contractual-offer decision, and the matcher was never told who was being asked about.** Lane `claude/institutional-bucket-truth-20260914` over board `2aab0db` (this lane also carries the deployed codex commits `2b5c47e`/`b3e4407`, which were in production and NOT on the board). **Two defects, one question.** (1) `matchProspectHomes` (`src/leasing/leasing_inventory.js`) inherits its refusal vocabulary from `availableUnits(exact_spaces)` (MB-7) and treated EVERY inherited qualification as fatal. The composer always calls with no term, so `term_required` returned zero homes on every showing question. **Showing and offering are different decisions with different requirements**; new named predicate `src/leasing/match_decision_strength.js` says which refusals bound which. `term_required`/`pricing_term_required` now cap the answer at `likely_fit`; invalid caller input, read failures (§40.7 — a read that failed is not an empty inventory) and `no_property` still block everything, and an **unrecognised qualification fails closed** so a refusal this predicate was never taught does not become a showing answer. **The offer decision is unchanged: everything that blocked a contractual offer still blocks one.** Each home carries `decision_strength` (showable · likely_fit · offerable), capped by the answer ceiling; `committed` is deliberately unreachable here because it is established by execution, not by matching. `needs_for_offer` names the ONE fact standing between the answer and an offer, so a caller asks for that and nothing else. **The `term` basis entry no longer reads SATISFIED when no term was chosen** — it was hardcoded satisfied, which manufactured the agreement the read exists to establish (§5); it is now `not_established` with `why: no_term_chosen`, and `constraint_coverage.term` says `term_not_chosen` rather than claiming a comparison that never happened. (2) `src/agent/ask_spine_answer.js` passed `person_id: (facts.person && facts.person.id) || null` — **`facts.person` is assigned nowhere in that file**, so it was always null and the matcher never learned the subject. The person WAS already resolved, by `resolveLeasingSubject` ~300 lines BELOW the matcher call, in the same `leasing_person` branch. Resolved once now and memoised, read by both (§7); resolving twice would let one question answer about two people on one pass. A subject that will not resolve is not a matcher failure — the leasing_person branch still owns that outcome and still distinguishes the four silences. **Proven here, bare:** first red observed (module absent), then `match_decision_strength` 18/18; Ask gate **161/161**; falsification **30/30**; entitlement matrix, debt vocabulary, compliance Ask Spine, prospect inventory dates, prospect confirmation, availability occupancy basis all EXIT 0; **all 56 source-governance gates exited 0, PARENT EXIT 0** (the repository was unshallowed first — shallow history had left 36 gates NOT RUN behind the `conversation_intent_extraction` HEAD~1 check). **NOT proven here, said plainly:** the DB-backed matcher proofs (`prospect_match_basis.db.js`) were NOT re-run — this container has no IPv6 and `proof_boundary.js` binds `::` to check the port, so the owned-database harness cannot start. That rung is CI's. **⚠ CORRECTED AFTER REVIEW, 2026-09-15 — THIS ROW ORIGINALLY OVERSTATED THE RESULT, AND CI WAS RED AT `2331a94` WHEN IT WAS WRITTEN.** The slice was reported before its CI returned; run 573 failed. Three real defects in the first cut. **(a) THE OPERATOR GOT COUNTS, NOT HOMES.** The standing projection reduced every use of `r.homes` to `.length`, so the backend held an answer to "WHICH home?" while the conversation received an answer to "how many homes?" — the exact re-entry this work exists to remove, and "Mike gets ranked homes" was therefore untrue. It now carries `options`: top three, each with its label, `decision_strength`, what it `satisfies`, what is `unconfirmed`, what `conflicts`, plus governed ready date and price. **Labels only, never record ids** (§40.8/MB-8). **(b) THE FAIL-CLOSED CLAIM WAS FALSE IN INTEGRATION.** The predicate denylisted refusals while the caller filtered the qualification against its own list and passed `null` for anything unrecognised — so an unknown refusal arrived as "no refusal" and proceeded. Correct in isolation, defeated by its caller, and claimed as a safeguard regardless. Inverted: the predicate now ALLOWLISTS the two success qualifications (`exact_space_matches_informational`, `matching_incomplete_pricing_unresolved`) and the caller passes the RAW qualification, so neither layer can mistake an unknown value for success. **(c) `likely_fit` OVERCLAIMED.** It was assigned whenever nothing was violated — including a home where nothing at all was known. "Nothing rules it out" is not "probably a good fit"; it now requires at least one recorded need actually satisfied. **THE PROOF CONTRACT WAS STALE AND IS REPLACED, NOT RESTORED:** five assertions in `prospect_match_basis.db.js` still demanded a term-less refusal. They now assert both halves — a term-less match ANSWERS with candidate homes, AND the offer restriction holds (ceiling `likely_fit`, no home reads `offerable`, the term constraint is `not_established` rather than satisfied, coverage reads `term_not_chosen`, and the missing fact is named). The HTTP assertion is on `/operator/leasing/prospect-match` — the MATCHING door, where no contractual authority is exercised and none was weakened. **✅ BUILD CLOSED 2026-09-15 — PROVEN THROUGH THE REAL CALLER, NOT A HELPER.** `prospect_match_basis.db.js` **80 run / 80 passed / 0 failed** on an owned nonce database at ledger ceiling 198, built through `apply_migrations.sh` from the real chain, with the owned server booted through `boot.sh`. **THE HARNESS COULD NOT RUN WITHOUT IPv6 AT ALL:** `proof_boundary.js` `portFree` binds `::` with `ipv6Only:false` — a deliberate DUAL-STACK reservation that detects a conflict on either family — and a kernel with IPv6 disabled cannot bind it, so every db-backed proof was unrunnable and three rounds of local green could not see a missing import. Only `EAFNOSUPPORT`/`EADDRNOTAVAIL` now fall back to the IPv4 wildcard; `EADDRINUSE` still propagates, verified by occupying a port. **ONE GATE INTERPRETATION:** `interpretGate()` returns blocks / ceiling / dates_established / pricing_term_established / missing / refusal_kind; `ceilingFor` and `blocksEverything` are deleted, not wrapped, and null / undefined / `""` / unrecognised all BLOCK — the null-success hole is closed. **MISSING DATES ≠ UNRESOLVED PRICING TERM** (the db proof caught this): `pricing_term_required` keeps dates ESTABLISHED and names `pricing_term`; only `term_required` names `requested_dates`. **ORDERING:** the lead key was `all_recorded_constraints_satisfied`, which requires zero unknowns — so with no term chosen it separated nothing and a home known over-budget AND wrong-type outranked a home nothing ruled out. Now conflict-first → fewer unknowns → readiness → price → unit → **space_label**, extracted as the named rule MB-5 already required and falsified against the old key. **THE BASIS REACHES THE CONVERSATION:** three lists of constraint NAMES replaced by one compact basis row per constraint carrying the recorded prospect value and the governed home value, so a tradeoff can be explained without refetching. **A READ FAILURE IS NO LONGER FLATTENED** into `read_state: OK` / NOT_ESTABLISHED; the refusal kind is classified once and the projection projects it (§40.7). **NOT DONE, one accepted item:** evaluating governed inventory once instead of twice on the complete-input path — it touches the shared availability/pricing read, not this module alone. **Not fixed:** the composer still passes null dates and term (`src/agent/ask_spine_answer.js:733` — `requested_start: null, requested_end: null, lease_term_months: null`), so the ceiling is `likely_fit` on every Ask Spine question. **⚠ THIS ROW'S ORIGINAL NEXT STEP WAS WRONG, CORRECTED 2026-09-15.** It said *"supplying recorded timing from prospect facts is the next step and is what raises it"*. It does not raise it, and reading the seam says why. `availableUnits` returns `term_required` when **either** date is missing, and a recorded `move_month` yields at best a START; supply both dates and it returns `pricing_term_required`, whose own note reads *"Ask which published pricing term in months the prospect wants; **do not infer it by rounding the dates**"*. Both qualifications cap at `likely_fit`. So recorded timing moves the answer from one capped state to another capped state and changes no ceiling — and a thread that implemented this note would find the ceiling unmoved and be one step from inferring a term, which the seam explicitly forbids. **What actually raises it:** a chosen PRICING TERM, and row 14 already built the governed way to get one — one published term is quoted and stated, two or more return the published menu for the prospect to choose. `needs_from_caller` already names which of the two facts is missing, so the mechanism is in place and what is absent is the term itself, not a read. | `src/leasing/match_decision_strength.js`; `src/leasing/leasing_inventory.js`; `src/agent/ask_spine_answer.js`; `tests/unit/match_decision_strength.test.js`; `tests/e2e/verify_all.sh` |
| 86 | **MERGED ONTO THIS BRANCH, NOT RE-PROVEN AT CEILING 198, 2026-09-15 — `main` had a Docker local-dev path and an SSL fix this lineage never received, and merging them produced code that could not parse.** `main` at `ed66d65a` (28 Aug, human-authored) carried work no September thread had: a `docker-compose` local-dev stack (`db` + one-shot `migrate` + `api` + the sibling app on :8080), `tools/dev_migration_release.js`, `tools/dev_static_server.js`, `database_ssl.js` honoring an explicit `sslmode=disable`, and the same SSL hardcode removed from `src/onboarding/import_rent_roll_truth.js`. **The merge hid a SyntaxError behind a clean auto-merge.** Both lineages had independently fixed the migrate.js SSL defect — main by importing `src/shared/database_ssl`, this lineage (commit `7151a0ce`, 21 Aug) by defining a local `function databaseSsl`. Git kept both, in one scope. `git merge` called the file auto-merged; `node --check` called it *"Identifier 'databaseSsl' has already been declared"*. **Only two files were touched on both sides** (this one and `migrate.js`), so the collision surface was exactly the conflict surface plus one silent auto-merge — a sweep of all **963** tracked `.js` files found no second instance. Resolved to the import: the local copy recognised only loopback hosts, so the compose service name `db` still read as remote and still attempted SSL against a server that does not speak it. **The local copy's stated reason was true and load-bearing** — this runner is COPIED into isolated directories where `../src/shared/…` does not resolve. `tests/unit/migration_release_gate.test.js` already stages the dependency; `tools/release0/prove_out_of_order_release.js` did not, and put `migrate.js` at the temp ROOT so the require escaped the fixture to `/tmp/src/shared/…`. It now stages a repo-shaped tree and returns the `migrations/` dir, leaving every caller's `path.join(dir, …)` unchanged. **Falsified both directions:** old layout `Error: Cannot find module '../src/shared/database_ssl'`; new layout reaches the database (`ECONNREFUSED` against a deliberately dead port). **What this row does NOT claim.** The Aug-28 thread proved the compose path against a fresh volume at **ledger ceiling 187**. This tree's ceiling is **198**, eleven migrations further on, and that proof has **not** been re-run here — no Docker daemon and no database were used in this thread. `docker-compose` on this branch is therefore `BUILT_BUT_DORMANT` *as of ceiling 198*, not `PROVEN`. What was observed here: **963/963** `.js` syntax-clean, **all 56 source-governance gates `PARENT EXIT 0`**, and `src/leasing/agent_capability.js` removed with **zero referrers** anywhere in the merged tree (this lineage's `server.js` had already dropped the `require`; `main`'s still has it at `server.js:689`). | `migrations/migrate.js`; `tools/release0/prove_out_of_order_release.js`; `src/shared/database_ssl.js`; `docker-compose.yml`; `tools/dev_migration_release.js`; `tools/dev_static_server.js`; merge commit on `claude/determined-dirac-qcvj5v` |
| 87 | **FOUND 2026-09-15, NOT FIXED — `main` and the deployed line have forked, and THIS FILE forked with them: two documents assign DIFFERENT defects to the SAME numbers.** Three measurements, each re-run rather than recalled. (1) **The deployed commit is not on `main`.** `git merge-base --is-ancestor b3e4407 main` exits **1**. Production is served from the board lineage, not from `main`. (2) **`main` is eleven migrations behind the released schema.** Its highest is `187_lease_security_deposit.sql`; this branch and the production ledger reach **198**. Migrations **188–198** exist on no `main` commit. (3) **The numbering collided.** `main`'s rows 22 / 28 / 29 are the from-scratch schema build, the migrate.js SSL hardcode and the docker-compose path; this lineage's 22 / 28 / 29 are three unrelated August rows. `main` also carries resolutions this lineage never saw (row 2, docs-cleanup 27 Aug) while this lineage carries rows 30–85 `main` has never seen. **The existing gate cannot catch this, and that is the point.** `gate_current_state.js` §4 checks that numbering is sequential with no gaps or duplicates **inside one file**. Both lineages satisfy it independently — `main` at 1..29, this branch at 1..87. A gate that scans one document cannot see two documents. Its own comment says *"More than one thread edits this file. A collision shows up here first"*; that held while the threads shared a branch, and stopped holding when they did not. **Not automatically a defect in the branching model** — `CLAUDE.md` states deploys are manual and *"merging to `main` deploys nothing"*, so `main` being a non-deploy branch is by design. What is not by design is that the repo's single state-of-truth document now has two incompatible copies, and that anyone reading `main` today gets a description of a codebase three weeks and eleven migrations behind what is serving. **This merge resolved the collision in one direction only** — onto the designated branch, by taking this lineage whole and re-stating `main`'s content as row 86. `main` itself is untouched and still carries the other numbering. **What would resolve it:** an owner ruling on whether `main` is meant to be the integration line. If yes, the board merges to `main` and this collision has to be resolved once, in that direction, by someone who reads both tables. If no, `main` should say so at the top of its own `CURRENT_STATE.md` rather than presenting a stale table as current. **Owner:** `UNASSIGNED` pending that ruling. | `git merge-base --is-ancestor b3e4407 main` → exit 1; `git ls-tree main -- migrations/` highest = 187; `tests/gates/gate_current_state.js:101-118` (§4 numbering check); `CLAUDE.md` ("Deploys to Render are MANUAL") |
| 88 | **MEASURED 2026-09-15, NOT CHASED — 96 files hardcode `ssl: { rejectUnauthorized: false }`, which is why so much of this harness cannot run against a local Postgres.** Counted by area: `src/` **2** (`identity/enroll_qa.js`, `leasing/demo_preflight.js` — both QA/demo paths), `server.js` **0**, `tests/` **66**, `tools/` **28**, plus one each in `migrations/` and `seeds/`. **Production is unaffected and this is not a security finding** — Neon speaks SSL and `rejectUnauthorized:false` is the working answer there. What it costs is REACH: a file with the hardcoded object dies on a local non-SSL server with `FATAL: The server does not support SSL connections` before it runs a single assertion, so two thirds of the proof population can only ever be exercised against a remote database. That is a direct multiplier on defect #17 (what CI actually covers) and #19 (fake pools): a proof nobody can run locally is checked by nobody between CI runs. **The fix per file is one line** — `databaseSsl(url)` from `src/shared/database_ssl.js`, which owns the answer and also honors an explicit `sslmode=disable`. Three are already converted (`migrations/migrate.js` and `src/onboarding/import_rent_roll_truth.js` by the 28 Aug docker thread, `tests/proofs/rent_roll_institutional_proof.js` by this one, each because the thread was already in the file). **Deliberately recorded rather than swept:** a 96-file identifier sweep is exactly the move that renamed an API response key and not the app that read it. If it is done it should be one lane with its own proof, not a side effect. | `grep -rl 'ssl: { rejectUnauthorized: false }' src/ tests/ tools/ server.js` = 96; `src/shared/database_ssl.js` |
| 89 | **RUNG MOVED BY EXECUTION, 2026-09-15 — the app's browser proofs DO run in this environment, and all 71 app test files pass.** Defect #23 records that the app repo has no CI and that its browser proofs *"run only when a human runs them"*. That is still true of CI. What is no longer true is that they are hard to run: `npm ci` in the app repo (two packages) plus `CHROMIUM=/opt/pw-browsers/chromium` — the runtime already reads `CHROMIUM`/`CHROME` for `executablePath`, so **no file needed changing** — takes the suite from **16 red / 55 green to 71 green**. The 16 were all `Cannot find module 'playwright'`, then all `Executable doesn't exist at .../chromium_headless_shell-1234` because the pinned Playwright 1.62.1 wants a browser revision this image does not carry; the preinstalled 1194 build serves fine through the existing override. **What this does NOT claim:** the 16 include four `*.browser.test.js` component proofs with test-provided adapters, not the coupled full-stack rung — `verify_all.sh`'s coupled rent-roll browser step was **not** run here, and no rung in this file is upgraded on the strength of this row. It records that the door is open, and how. | `property-spine-app/tests/browser_runtime.js` (`process.env.CHROMIUM` override, already present); `ls /opt/pw-browsers` = chromium-1194; 71/71 app test files exit 0 |
| 90 | **RESOLVED IN SOURCE · DB-PROVEN ON AN OWNED RUNTIME IN TWO TIMEZONES · FALSIFIED BOTH DIRECTIONS, 2026-09-15 — the rent-roll INTAKE door refused valid files on any host east of UTC, and no proof could ever have seen it.** Row 82 recorded an `isoDate` off-by-one and called it latent. Making it *provable* disproved the word. **Why nothing could see it:** `tests/e2e/boot.sh` built the owned server with `env -i` and a whitelist that did not include `TZ`, while `proof_boundary.js`'s `serverEnvironment()` already whitelists it — so the owned server always ran in the container's zone and **no proof in this repo could exercise a host ahead of UTC.** Passing `TZ` through is one token; unset, `env -i` still yields UTC, so no existing run changes. **What that immediately exposed:** with the server in `Europe/Berlin`, `availability_uncorroborated_claim` did not fail an assertion — it **died** on `409 source_date_mismatch · "The requested date, retained source date and date inside the file disagree. Nothing was changed."* There was no disagreement: the requested date and the date inside the file are strings; the retained artifact's date is a `date` column, which node-pg hands back as a Date at **LOCAL midnight**, and `toISOString()` read it back a day early. **Five sites, one path** — `source_home_identity_review.js:23` (the refusal above), `source_artifact_service.js:382` (the same false refusal one door earlier, on the retained bytes), `activation_service.js:839` (decides whether an undated lease may take the source date), `activation_service.js:1262` (**renders the date into a receipt a person reads as fact** — its own comment explains fixing the locale-string bug here, and that fix carried the off-by-one), `rent_roll_field_map.js:177` (the fallback parse of a date a human typed into their own rent roll). The rule now lives in `src/shared/date_column.js`, as `database_ssl.js` owns SSL: for a `date` column the LOCAL components ARE the recorded day, and the conversion must not happen — **the opposite of the rule for a `timestamptz`**, which is why the comment it replaces asserted the reverse in good faith. **MEASURED, BOTH DIRECTIONS:** the pre-fix reader with the server in Berlin returned contested claims as `2025-12-31/2027-12-30` and `2026-02-28/2027-02-27` — **5 red**; the SAME pre-fix reader with the server in UTC — **121 passed, 0 failed.** That pair is the whole story: the wrong code is right in UTC, so a UTC-only harness could only ever agree with it. After the fix, server and client both in Berlin: `canonical_occupancy_holds` **121/0**, `availability_uncorroborated_claim` **27/0**, identical under UTC; all 57 governance gates `PARENT EXIT 0` in both zones. `date_column_timezone.test.js` (**15/0**) runs in child processes with `TZ` set — because `TZ` is read at process start, so setting `process.env.TZ` inside a test proves nothing about a host in Berlin — and carries its own falsification, asserting the old expression really does yield `2026-07-31` in Berlin, Auckland and Kolkata and really does agree in UTC and Los Angeles. **NOT claimed:** ~20 further `toISOString().slice(0,10)` call sites exist outside this path (money, identity, snapshot_loader, compliance); they are untouched and unmeasured. The institutional helper was changed too and **nothing observed moved there** — those dates arrive as strings and take the slice branch; that change disarms a trap rather than repairing a wrong value, and the comment says so. | `src/shared/date_column.js`; `tests/unit/date_column_timezone.test.js`; `tests/e2e/boot.sh` (TZ passthrough); `src/onboarding/source_home_identity_review.js`, `source_artifact_service.js`, `activation_service.js`, `rent_roll_field_map.js`; `src/surfaces/rent_roll_canonical.js`, `rent_roll_institutional.js`; `tests/proofs/canonical_occupancy_holds.db.js` |
| 91 | **METHOD FINDING, 2026-09-15 — running the governance suite covers 57 of the 112 test files CI runs, and this thread shipped a CI red through the 68-file gap.** `node tests/verify_source_governance.js` prints *"all 57 source-governance gates exited 0"* and that sentence is precise and true. It is **not** the claim a thread needs before pushing. `tests/e2e/verify_all.sh` invokes **112** distinct test files; 68 of them are in no gate list — 23 `tests/unit/*`, 44 `tests/proofs/*.db.js`, and `gate_inventory_relationship_policy.db.js`. Run 581 went red on `tests/unit/rent_roll_institutional_projection.test.js`, one of the 23, on a source change whose gates were green. **The repo already warned about exactly this** — *"'Every source-governance gate passes' means those gates, not 'the tree is clean.' Say which"* — and saying which is necessary but not sufficient: the 23 unit files cost seconds and should simply be run. **The reproducible list:** `grep -oE 'node tests/[a-z0-9_/.]+\.(js|test\.js)' tests/e2e/verify_all.sh | sed 's|^node ||' | sort -u` against the `{ file: ... }` entries in `verify_source_governance.js`. **Also learned, and not a defect:** the 44 `.db.js` proofs need `HARNESS_DATABASE_URL` and refuse deliberately without it (`docs/DB_HARNESS_ISOLATION.md`) — running them without it produces a red that is a harness message, not a finding; and three of them (`availability_turn_date_scope`, `current_rent_roll_reconciliation` 77/1, `onboarding_claim_index_dependency`) fail on a shared already-migrated proof database and fail **identically** from a worktree at `591b48ca`, before any of this thread's changes — attribute before reporting. | `tests/e2e/verify_all.sh` (112 files); `tests/verify_source_governance.js` (57); CI run 581 |
| 92 | **RESOLVED IN SOURCE · UNIT-PROVEN WITH INLINE FALSIFICATION · DB-PROVEN THROUGH THE REAL DOOR, 2026-09-15 — a recorded budget of "up to $1.4k" was read as 1.4, and `showable` was being handed to homes that conflict.** Three source-level claims from an external review, each checked against the source and each true. **(1) The budget reader took the first number anywhere in the string.** `"up to $1.4k"` → **1.4**, after which every priced home falls outside the budget, is dropped with `continue`, and the prospect is told *"No priced home matched these criteria"* — a confident **wrong negative**, not an honest blank. `"$1,200–$1,400"` → **1200**, silently discarding the top of a stated range. **Not a hypothetical shape:** `src/comms/prospect_capture.js` instructs the model to store a budget as *"short verbatim-ish text (e.g. '$1,400/mo', 'under $1,600', '$800 per person')"* and validates only length ≤ 60 — the reader is handed human phrasing **by design** and was treating it as a number. `moveMonthEnd` ten lines below already held the right line (*"No clever parser … honest blank beats confident wrong"*); money did not have it. A magnitude suffix and a range now each refuse with their own reason. **(2) A named day was discarded** — `2026-10-01` and `2026-10-15` both became `2026-10-31`, so a home ready on the 25th passed a prospect who said the 1st, which is the exact failure that comment names. The governed capture path only writes `YYYY-MM`, so this branch is reached from the free-text writers where a stated day means a stated day. **(3) `showable` carried two opposite facts, and this QB put it there yesterday.** `match_decision_strength.js` defines it as *"worth walking to; nothing known contradicts it"*; `leasing_inventory.js` assigned it when `counts.violated > 0`. **The same defect as a physically DOWN unit printing as "Open" on the formal rent roll (row 84), one layer up, in the same week.** The projection repeated it at the count level — `showable` was `filter((h) => h.decision_strength)`, i.e. every matched home with any strength, under the name of the weakest specific claim; both reached Ask Spine. A home with a recorded conflict now makes **no strength claim**, and Spine declines to rank it rather than inventing a fourth rung, because **the ladder itself is under review** (a home can fit and be unshowable, or be offerable and a poor fit). **Evidence:** `prospect_fact_reading.test.js` **32/0** first run, every refusal case running the replaced expression beside it so the test SHOWS the defect; `prospect_match_basis.db.js` **82 passed** on an owned nonce database at ledger ceiling 198 through the real HTTP door, with the three new strength assertions **red against the pre-fix source (79/4)**; all **58** source-governance gates `PARENT EXIT 0`; the 23 unit tests outside the gate list 0 red. Its one remaining failure (MB-9 ledger row count) fails identically from a worktree before any of this thread's changes. `budgetAmount`/`moveMonthEnd` moved to module scope and exported — both are pure, both decide whether a person hears "nothing fits", and a predicate with its own test and a caller nobody drove is how a missing import shipped twice in this same file. **STILL NOT SOLVED, and not hidden:** `"$800 per person"` parses to 800 with nothing recording per-bed vs per-unit — on a property whose own answer has to explain that bed rent is per bed; a bare `"1.4"` is still accepted as $1.40; and `flexible`, a value capture writes on purpose, is unusable by the matcher (now asserted so it stays visible). All three are **meaning questions that belong at capture**, not to a reader downstream of it. **MB-5, MB-7 and MB-8 are now marked superseded where they live** — MB-5 says *"Changing the rule is a ruling, not a tweak"* and the overnight build changed it, so that record was owed; a well-written obsolete instruction is how a future agent reinstalls a fixed defect. | `src/leasing/leasing_inventory.js` (`budgetAmount`, `moveMonthEnd`, per-home strength, standing counts); `tests/unit/prospect_fact_reading.test.js`; `tests/proofs/prospect_match_basis.db.js`; `docs/handoffs/new-hp/rulings/MATCHING_BASIS_RULING_20260914.md`; `docs/handoffs/new-hp/matching-basis/RECEIPT.md`; `src/comms/prospect_capture.js` (the writer this reader must match) |
| 93 | **SUPERSEDED BY ROW 94 FOR THE BOUNDARY AND THE DELIVERY VOCABULARY — the authority ruling named here was GRANTED (993efa4a) and delivery landed (8c095ea0); what this row still describes accurately is the durable-obligation mechanism. · BUILT · DB-PROVEN ON AN OWNED RUNTIME, 2026-09-15 — the application-to-lease handoff is now owed DURABLY with submission, and the step it cannot take by itself is attributed work by design.** Before this, `generateLeasePacket` had exactly ONE caller in the product (`src/identity/operator.js`), a staff route: the applicant finished and the next step waited on a person opening a screen. **Built from what exists — no second sender, no second workflow engine, no second eligibility rule, and NO MIGRATION** (`obligations.type` carries no vocabulary CHECK). `recordHandoffOwed` runs inside the submission transaction so the statement that the handoff is owed commits with the application or not at all; `runOwedHandoffs` reads the owed work from the DATABASE rather than a callback and discharges each in its own transaction. Every guard it leans on was already proven: eligibility refuses an issued packet, `generateLeasePacket` locks the application and runs the one predicate itself, `readBoundApplicationOffer` is read fresh inside that call and THROWS on a superseded offer, and `issueLeasePacketLink` returns `already_issued` rather than minting a second token. **⚠ THE BOUNDARY THIS FOUND IS THE RESULT.** Preparing a package on the two-step (authored offer) basis writes a derived lineage record ATTRIBUTED to a named staff actor — `deriveConfirmationFromAuthoredOffer` refuses without one: *"the key-gated legacy door cannot author this record."* A system runner cannot discharge it without inventing an actor, which is the authority manufacture this codebase exists to prevent. So the runner stops there, and what it does when it stops is what is proven. **13/13 on an owned nonce database at ledger ceiling 198:** the handoff is recorded owed and system-owned in the submission transaction; **a SEPARATE pool with no shared memory still finds it on disk** (restart survival measured, not simulated with a flag); recording twice does not owe twice; the runner refuses BY NAME (`preparation_actor_required`); no packet without an attributed preparer; the work stays OWED — a refusal is not a completion; the completed application is untouched; re-running creates no packet and no second obligation; and a **superseded offer is not sent**, with the work still owed and the application untouched. **Two silent-drop defects fixed while building, same class:** `completeObligation` takes `{ obligation_id, completed_by }` and nothing else — an earlier draft passed `resolution_code` and a note, both of which would have been dropped while reading as a record that was written; and both submission routes build their own response shapes and would have dropped `handoff_obligation_id`, so the after-commit step would never have fired. **OWED: one authority ruling.** Who is the attributed preparer when no person pressed the button? The offer's AUTHOR already made the commercial decision and is recorded on the offer (`authority_basis_snapshot`); attributing preparation to them is a one-line change and an owner decision, not a QB one. | `src/applications/lease_handoff.js`; `src/applications/application_submission.js`; `src/applications/lease_packets.js` (service surface + `resolveSignerAccess`); `server.js`; `tests/proofs/lease_handoff_durable.db.js`; `tests/e2e/verify_all.sh`; `src/applications/proposed_terms_service.js:374` (the boundary) |
| 94 | **BUILT · DB-PROVEN ON AN OWNED RUNTIME WITH FALSIFICATION, 2026-09-15 — the automatic lease handoff now begins at APPLICATION COMPLETION, not application submission, and the completion boundary is expressed in the obligation engine's own `required_inputs` rather than in a second concept of completeness.** Row 93 wired `recordHandoffOwed` into the submission transaction. With a background recovery sweep running, that owed a lease for every application the moment it arrived — **including one still missing something** — so an unattended sweep would have turned unfinished applications into delivered signing links because nobody was watching. **No application-requirements owner exists in this repo and none was added** (searched `src/`, `migrations/`, `server.js`: `documents` is orphaned per defect #24; `lease_config.application_options` declares two optional FORM QUESTIONS — parking and utility preference — and no requirement set). **So completion is `obligations.required_inputs` being empty, and the inputs are OBSERVED rather than invented** — each one a refusal the canonical packet writer already raises, hoisted from 3am inside a sweep to submission where a person can act on it: `resident_identity` and `guarantor_contact` (`lease_packets.establishPacketSigners` → `resident_identity_not_established` / `guarantor_contact_not_established`, *"Correct the application before generating the governing packet"*) and `applicant_contact` (`dispatchSigningLinks` → `no_contact_for_this_signer`). A property that later declares a document policy appends to the SAME array through the same engine; the boundary does not move. **New in `src/applications/lease_handoff.js`:** `recordCompletionOwed` (called by `application_submission.js` in place of `recordHandoffOwed`; human-owned, `leasing_agent` → `leasing_manager`, `due_at` = submitted_at + 30 days — ruling 2's first clock, carried on the obligation so the board escalates it rather than a private timer); `completeApplicationRequirement` (**the only writer that owes a handoff after submission** — runs the engine's `satisfyObligation` and reads the `remaining` IT returns, then completes and owes the handoff in the same transaction); `reconcileApplicationCompletion` / `reconcileOwedCompletions` (**the FACT closes the input, not a button** — re-observes open completion work against current facts and satisfies only what observation confirms, so the sweep READS rather than requiring six upstream writers to notify it, §7); `expireStaleCompletions` (the 30-day clock lapses the application through `application_lifecycle.markTerminal` — the one writer of `lease_applications.status` — and closes the obligation `resolution_code='expired'`, a value migration 084 already constrains, **never as satisfied**); `readApplicationHandoffStanding` (§40.6 compact projection; returns `NOT_ESTABLISHED` for applications predating this boundary rather than calling them complete). **Four guards the owner named, all proven:** (a) `runOwedHandoffs` refuses while a completion obligation is open **even if the writer is bypassed** — the guard is not one edit from being gone; (b) both sweep passes CLAIM with `for update skip locked`, so two workers cannot both regenerate a packet version and hand one applicant two bearer links to two different agreements; (c) the sweep takes an EXPLICIT property allowlist (`LEASE_HANDOFF_RECOVERY_PROPERTIES`) and **refuses to start when enabled with no scope** rather than reading that as "all", and excludes `source='import'` — Spine did not run a historical application and does not now finish it; (d) **`accepted` ≠ `delivered`**: `sendPropertySms` returns `sent:true` when the TRANSPORT took the message (Twilio queued/accepted), so the receipt says `accepted`/`not_accepted`, per-signer `transport_state`, and the source names where each later rung actually lives (issued = `lease_packet_signers.token_hash` · delivered = `comm_events.provider_status` via `recordProviderStatus` · opened = signer access · signed = `submitted_at`). Tenant and guarantor are dispatched INDEPENDENTLY: a reachable tenant is sent to while the guarantor's own line records why nothing went to them. **58/58 on an owned nonce database at ledger ceiling 198** (`tests/proofs/lease_handoff_durable.db.js`, was 34), including: an incomplete application owes applicant work and names what is missing; no handoff exists; an unattended sweep prepares NOTHING; a handoff owed by any other route is deferred by name with the outstanding item; re-observation cannot satisfy an input the facts still find missing; the guarantor's contact arriving closes it through the engine and leaves its `input_satisfied:` event; only then is the handoff owed; a 40-day-old application lapses to `expired` and the obligation closes `expired` with its inputs still outstanding; a fully sendable 400-day-old IMPORTED application is passed over entirely. **Falsified in four directions** — removing the runner guard sends a signing link for an incomplete application (2 red); dropping the import exclusion texts the historical applicant (2 red); closing expiry as `satisfied` (1 red); the 20-day early-clock check is green only because the clock does not fire early. 58 source-governance gates green (*those gates*, not the tree). **Recovery stays OFF for live properties** — two switches, both off — until the inventory hold and approver follow-through (rulings 3–4) are connected. **Not done here:** the applicant-signature inventory hold, immediate approver follow-through, the 60-day lease-signing clock, the continuous journey through the real public submission door, and an Ask Spine registration for the standing read (§40.2 — this is a service read, not yet a registered domain reader). |
| 95 | **BUILT · DB-PROVEN ON AN OWNED RUNTIME THROUGH THE REAL PUBLIC SIGNER ROUTE, 2026-09-15 — a home an applicant has SIGNED FOR now stops being offered, in every canonical availability reader, with no new schema.** Before this an applicant could sign the package Spine prepared and nothing changed: the bed still read `marketable_now`, the prospect matcher still offered it, the property headline still counted it open, and a second application could still be aimed at it. **Two people could sign for one bed and the first anyone learned of it was at move-in.** **NO TABLE, NO COLUMN, NO MIGRATION.** `lease_packets.tenant_submitted_at` is already stamped by the ONE signer-submission writer on both packet shapes (a governing instrument → `tenant_in_progress`; a terms acknowledgment → `submitted`). That column IS the applicant's signature. New `src/applications/application_inventory_hold.js` is a READ over it — one predicate (`tenant_submitted_at is not null` · `superseded_at is null` · `status <> 'void'` · application not terminal), in one place, consumed rather than re-derived. **The hold reaches every reader through ONE change.** `availability_read.js` overlays it exactly as it overlays operating_use and triage, and `marketingState` consults it **LAST** — a commitment is not an occupancy fact, so contested / down / occupied / unturned / `occupancy_unknown` all keep the description their own evidence earned, and the hold only ever converts a bed that would otherwise read `marketable_now`. Because `application_target_authority` and `application_target_read` (the prospect matcher's and Ask Spine's shared authority) both already consult `availabilityRead`, they inherit it: **proven, not assumed** — the matcher stops offering the held home and still offers its roommate's. New refusal `application_target_held_for_signed_applicant`, kept distinct from `not_offerable` so staff learn the difference between *this home has a problem* and *this home is taken*, and **kept at submission-time revalidation too** rather than collapsing into "no longer available", which is false of a home that is physically fine. **The refusal names nobody** — it is shown to a different prospect, and disclosing who signed to the person being refused is one applicant's decision leaking to another; the attribution (`application_hold`: who, when, packet, `contested_by`) travels on the entitled availability row and in the headline label *"Signed for by an applicant — held"*. `for_application_id` exempts the holder's own application (so a correction to their own terms is not refused because they signed) and is **server-derived at every call site** — the loaded application or invitation row, never a request body (§21); a wrong id is still refused. Threaded at `operator.js` successor-offer correction, `acceptApplicationTerms`, and public-submission revalidation. **Nothing is reserved, so nothing has to be released**: a withdrawn or declined application frees its bed through the same read that held it — no release step, no stale reservation. The held state is named in BOTH the headline and `states` so it is never a quiet remainder. **22/22** (`tests/proofs/application_inventory_hold.db.js`) on an owned nonce database at ledger ceiling 198, with the API on real HTTP: the signature is made by **POST /t/lease/:token/fields/:id/complete then POST /t/lease/:token/submit**, using the token extracted from the message the product actually sent — nothing stamps `tenant_submitted_at` by hand. Two beds in one unit, both established vacant through the real opening-position chain (an earlier fixture read `occupancy_unknown` and would have measured nothing). **Falsified:** disabling the one guard turns 8 assertions red, including *a SECOND application cannot be aimed at the held bed* → `ok:true`. 58 source-governance gates green. **Also repaired:** `tests/unit/application_future_target.test.js` pinned the submission-recheck call shape by requiring its argument object to CLOSE immediately after `requested_end`, so any further server-derived argument broke it — a pin that fails on correct changes teaches people to loosen pins. It now asserts the two values it cares about, plus that the hold exemption is server-derived. **Not done here:** an operator surface rendering the hold, and the app-side copy for the new refusal (API-side only; the deployed app falls through to showing the sentence verbatim, which is honest). |
| 96 | **BUILT · DB-PROVEN ON AN OWNED RUNTIME WITH FALSIFICATION · ONE DOCTRINE WALL REPORTED, NOT WORKED AROUND, 2026-09-15 — the approver's decision goes live at COMPLETION, and the 60-day lease-signing clock runs on the same machinery as the 30-day one.** **Ruling 3 (approver follow-through).** `application_approval` (leasing_manager) is spawned by `application_submission` at SUBMIT with the rail's 48-hour window — the right birth, the wrong moment for its clock: at submission the application may still be missing things, so a decision window started there expires on the APPLICANT'S delay rather than the approver's. `raiseApprovalDecision` **raises the existing gate, never creates one** (a second decision obligation is a second decision; with no gate on file it refuses and says `no_open_approval_gate` rather than inventing one for an imported application): clock restarted from completion, `priority='high'`, the engine's own `escalation_interval_minutes` set so it climbs on its own, and a durable `application_decision_became_live` event so *"how long has this waited on me?"* is a read. Fires from BOTH completion entry points, in the completion transaction — the package Spine owes and the decision the approver owes go live together, which is the two-step design (195), not one after the other. **⚠ THE OUTBOUND TEXT TO THE APPROVER IS NOT BUILT, AND THAT IS A DOCTRINE WALL, NOT AN OMISSION.** Migration 132's `ck_cl_outbound_policy_by_type` permits an `operations` line only `disabled` or `reply_only`, and states in its own words that *"assignment pushes, reminder campaigns and staff broadcasts are not features that were left unbuilt — they are rows that cannot exist."* **Spine cannot proactively text a staff member today, by construction.** The two purposes that can reach a phone (`staff_otp`, `staff_invite`) are named credential transport; routing a decision summary through one would be exactly the informal exception that list exists to refuse. Not worked around. **What it would take is the owner's call: a proactive staff consent rail and a line policy that can express it — a migration and a doctrine decision, not a widened purpose.** Until then the standing read **states** `approver_notified: false` with `notification_blocked_by: proactive_staff_messaging_unexpressable` rather than staying silent, because a read that omits it would say somebody had been told (§5). No second decision summary was built either — `application_review.js` already composes it. **Ruling 4 (60-day lease-signing clock).** `lease_signing` obligation, spawned in the SAME transaction that closes the handoff — i.e. when the link actually goes out, not at completion and not at generation; a window in which a lease must be signed cannot start before the link is on its way. `required_inputs` are `signature:tenant` / `signature:guarantor`, so **tenant and guarantor are independent** and a half-signed package reads as exactly that with the outstanding list naming WHO. The signature closes its own input through `reconcileOwedSignatures` — the public signer route has no business knowing a clock is waiting on it (§7). **⚠ THIS IS NOT THE LINK'S EXPIRY.** `lease_packet_signers.token_expires_at` is 14 days and is a BEARER-TOKEN security window; this is the DEAL window. Collapsing them would give a signing link a 60-day life or expire a live deal in a fortnight — pinned by an assertion that the two differ. **One expiry rule, two thin wrappers.** `expireStaleCompletions` and `expireStaleSignings` are both `expireStaleClock`, so lapse-through-`markTerminal`, close-as-`expired`-never-`satisfied`, claim-with-`skip locked` and skip-imports cannot drift between the clocks. The recovery sweep runs both, under the same explicit property allowlist. **74/74** (`tests/proofs/lease_handoff_durable.db.js`, was 58) on an owned nonce database at ledger ceiling 198. **Falsified:** not raising the decision at completion turns 2 red; setting the signing window to 14 days makes it collide exactly with the token expiry and the collision assertion catches it. **Also hardened:** two window assertions read the module constant, which proves the window is APPLIED but cannot prove it is 30 or 60 — the literal values are now asserted separately, and the labels no longer claim a number they were reading from the thing under test. 58 source-governance gates green; the hold proof re-run 22/22. **Not done here:** the continuous journey through the real public submission door, an operator surface for the raised decision, and an Ask Spine registration for the standing read (§40.2). |
| 97 | **BUILT · DB+HTTP-PROVEN ON AN OWNED RUNTIME THROUGH THE REAL PUBLIC DOORS · ONE DEFECT FOUND AND FIXED BY WALKING IT, 2026-09-15 — the application-to-lease chain is proven END TO END, and the rejection and failed-delivery branches with it.** Rows 94–96 each proved one link. `tests/proofs/application_to_lease_journey.db.js` (**25/25**) is the only place the links are exercised AS A CHAIN and the only place the applicant's two acts go through the doors a real applicant touches: invitation prepared + attested sent through the canonical service (the raw token exists only in that call's return, so a proof cannot INSERT a submittable invitation) → **GET /t/application/:token/context** for the terms hash — acknowledged from the page the applicant is shown, not reached out of the database → **POST /applications/submit-public** → handoff runner with no person acting → **POST /t/lease/:token/fields/:id/complete** → **POST /t/lease/:token/submit** → **POST /applications/:id/deny** through the real operator door. **THE DEFECT THE JOURNEY FOUND, IN THIS BUILD'S OWN WORK.** The deny route already released the conversion rail's `lease_signature_followup` for one stated reason — *"so the team is not told to chase a signature on a dead application"* — and knew nothing about the three obligations rows 94–96 added. **A declined applicant kept a 60-day signing clock, an open completion chase and an owed signing package**, all pointed at a decision already made. `releaseOnTerminal` now closes all three in the deny transaction and on both expiry clocks, as **`resolution_code = 'revoked'`** — migration 084's own word — never `satisfied` (nothing was supplied) and never `expired` (no window ran out); the work was called off. It refuses a conversion-linked obligation rather than closing one outside the rail. **Falsified: disabling it leaves `lease_signing` OPEN on a declined application** (2 red). **The happy path, measured:** submission's own response reports `completion_obligation_id` and returns `status: submitted` — it owes the applicant's remaining work and does not return a lease; the standing read says the package is owed with nothing outstanding AND the approver's decision is live with `approver_notified: false`; the package is dispatched with no person acting; the applicant's signature holds their bed at the moment they sign and closes its own clock input; the chain ends in one sentence — package sent, signed, decision still the approver's. **Rejection:** the denied application is terminal, **its home returns to the market by the same read that held it** (no release step), nothing further is prepared, and nothing is owed. **Failed delivery:** an applicant with no number gets `not_accepted` / `failure_class: system` — a SYSTEM failure, not applicant inaction — the packet is still committed so nothing has to be rebuilt, the work stays owed, the standing read still says *package owed, not sent*, and **no signing clock starts: a link that never went out starts no deadline.** Three beds in one unit, all established vacant through the real opening-position chain. Re-verified together: journey 25/25 · hold 22/22 · durable handoff 74/74 · guarantor signing 45/45 · governing lease execution 16/16 · Spine lease execution 25/25 · current rent-roll reconciliation 78/78 · 58 source-governance gates. **Still not done:** an operator surface for the raised decision and the hold, the app-side copy for the new refusal, the proactive staff notification (blocked by the row-96 doctrine wall), and Ask Spine registration for the standing read (§40.2 — this is a service read, not a registered domain reader). |
| 98 | **CI FIRST RED, FOUND AND FIXED, 2026-09-15 — two regressions from rows 95 and the delivery-wiring commit, both in one rung (`tests/e2e/tour_application_lease.e2e.js`).** Run 598 on `6adca317` named its own first red: *"FIRST RED: Spine retains the tour receipt and requests complete terms before send confirmation"* (assertion 32). Run 589 on `993efa4a` had named a different one on the same rung: *"intentional successor offer returned HTTP 409: A lease packet already exists."* **(1) THE HOLD READ FAILED CLOSED ON A MISSING RELATION.** Row 95's `availability_read` overlay called `heldSpacesForProperty` with no error tolerance, on the deliberate reasoning that a hold read that cannot run would offer a signed-for bed to a second person. That is right for a timeout or a permission error and **wrong for 42P01**: if `lease_applications` does not exist, no application exists, so no applicant CAN have signed — an empty hold map is the truth there, not a guess. The CI Postgres log shows the exact query erroring `relation "lease_applications" does not exist`, which took `availabilityRead` down, and with it `resolveApplicationTarget` — so a staff post-tour reply that should have asked for complete terms said *"I couldn't read the application targets just now"* instead. Now tolerated for **42P01 only**, with the same error code, the same `try/catch` shape and the same written reasoning as the triage overlay ten lines above it; every other failure still propagates. **(2) NARROWING THE CORRECTION REFUSAL TO STATUS ALONE DROPPED A CONTRACT.** `8c095ea0` replaced *"any packet row blocks a successor offer"* with `FROZEN_PACKET_STATES = submitted/resident_executed/executed`, which fixed 589's red (automatic preparation blocking every legitimate correction) and silently broke the assertion *"a prepared lease packet blocks further application-offer revision"* — a staff-generated packet is `draft` the moment it is made. **Status was never the whole discriminator, and the record already carried the right one**: `application_proposed_terms_confirmations.source` (migration 195). `operator_proposed_terms` means A PERSON confirmed these terms and generated a package from them — a commitment, so revision is a governed correction and is refused. `authored_offer_acknowledged` means SPINE prepared it from the applicant's acknowledged authored offer — nobody chose that moment, so it must not block a correction. Frozen states still block either way. Third refusal sentence added for the new case; the two existing sentences are unchanged. **Proven by reproduction, not by reasoning alone.** The rung was reproduced locally on an owned nonce database at ledger ceiling 198 with the real server, the real fixtures and the pinned app checkout: **it now PASSES**, where before these two fixes it died at assertion 120 (*"a prepared lease packet blocks further application-offer revision"*). Re-verified: durable handoff 74/74 · inventory hold 22/22 · application-to-lease journey 25/25 · guarantor signing 45/45 · governing lease execution 16/16 · Spine lease execution 25/25 · 58 source-governance gates. **Method note worth keeping:** the container running this session has no IPv6, so `proof_boundary.test.js` cannot bind `::` and the full local `verify_all.sh` cannot start — the rung had to be reproduced by standing up its own prerequisites (owned database, migration chain, property/pricing/instrument fixtures, server, two Skyline steps). CI is the authority for the whole suite; this reproduction is the authority for this rung. |
| 99 | **CI FIRST RED, DIAGNOSED BY REPRODUCTION AFTER ONE WRONG DIAGNOSIS, 2026-09-15 — row 95's inventory hold is correct and the SHARED E2E FIXTURE was not ready for it.** Row 98 fixed two real defects and **did not move this red**: run 600 on `73cd2762` failed at the same assertion as run 598. **The measurement that settled it** (and should have come first): stand up the rung's prerequisites, leave on the fixture bed exactly what the preceding rung leaves — an application whose packet a resident signed — then ask the two readers directly. `availabilityRead` on Bed B: `held_for_signed_applicant`, `more_than_one_applicant_has_signed_for_this_home`, `contested_by` naming a second signed application. `resolveApplicationTarget`: `application_target_held_for_signed_applicant` — *"Someone has already signed for this home, so it is being held."* That sentence carries no *"Unit 3B, Bed B"* and no *"offer/terms"*, which is exactly the four-part assertion at `tour_application_lease.e2e.js:582`. **The rung was refused because the product correctly refused to aim a second application at a bed somebody had already signed for.** **Every leasing scenario shares ONE bed** (`Bed B` on the Skyline E2E unit, via `ctx()` in `tests/e2e/leasing_e2e_lib.js`), and each leaves behind a signed package. `ctx({wipe:true})` exists to return that fixture to a clean state and already deleted its leases and executed-lease records — those leftovers used to be inert once the lease rows were gone, **and are not any more**. **Fixed in two places, one mechanism.** `ctx({wipe})` now also supersedes the property's current packages, so every consumer starts clean; and `resident_signing.browser.js` supersedes its OWN package at the end, because it signs *after* its wipe and the next rung (`tour_application_lease.e2e.js`) does not use `ctx` at all. **⚠ SUPERSEDED, NEVER DELETED** — migration 192's mutation guard refused a delete outright (*"lease packet signer identity is frozen after issue"*), and `POST /applications/:id/deny` refused a second attempt with 409 because `toPacket` had already closed the approval gate. Both walls were obeyed, not worked around; `superseded_at` is the product's own statement that a package is no longer current, and it is what `resolveSignerAccess` and the hold read both already honour. **Falsified in the same harness:** leaving the signed package reproduces CI's first red verbatim; superseding it returns Bed B to `marketable_now` with no hold and the rung passes. Every shared-fixture consumer re-run green — leasing clean path · hostile falsifications · cross-surface reconciliation · standing vs review · ask spine facts · invite-to-guarantor lease — plus durable handoff 74/74, inventory hold 22/22, journey 25/25 and 58 source-governance gates. **Row 98's two fixes are kept and were not wasted:** the 42P01 tolerance is right on its own terms, and the operator-committed correction refusal is what let this rung get past assertion 120 at all. **The method lesson is the cost of the wrong diagnosis:** row 98 reasoned from a Postgres error in the CI log to a plausible cause and shipped it without reproducing the failing assertion. The reproduction took one more round and named the cause exactly. *Reproduce the assertion, then fix it.* |
| 100 | **THE NEXT CI FIRST RED WAS MY OWN PROOF, AND IT HAD NEVER PASSED IN CI, 2026-09-15.** Row 99 cleared `invite-to-guarantor lease`; run 602 then stopped at **`application-to-lease handoff is owed durably and safe to retry`** (`tests/proofs/lease_handoff_durable.db.js`, registered at `verify_all.sh:374`). **`sendPropertySms REFUSED … reason=send_mode_disabled`.** `SMS_SEND_MODE` defaults to `disabled` and the boundary fails closed before any transport — correct behaviour. `verify_all.sh` does not set it for this step, so **every acceptance assertion in that proof has failed in CI since it landed**; it passed locally only because the shell running it happened to export `SMS_SEND_MODE=customer_care`. **A proof that depends on its operator's environment is exactly the class of thing this file exists to catch, and I introduced one.** Fixed the way the repo already does it — the proof sets `process.env.SMS_SEND_MODE` itself, as `agent_booking_xturn_proof.js` and `inbound_prospect_resolution_proof.js` do. Every other boundary gate (property line, recorded consent, stop controls) still runs in front of the fake transport; only the mode they are evaluated under is opened. Re-verified with the variable **unset** in the shell: 74/74. **⚠ AND A CLAIM THAT NEEDS CORRECTING.** `tests/proofs/application_inventory_hold.db.js` (22/22) and `tests/proofs/application_to_lease_journey.db.js` (25/25) are **NOT registered in `verify_all.sh`**, so CI has never run either of them. Rows 95 and 97 reported their counts as evidence; those counts are real and were measured on an owned nonce database at ledger ceiling 198, but they are **locally exercised, not CI-enforced**, and the proof ladder rung claimed for them should be read that way until they are registered. Both now set their own send mode and both re-pass with it unset (22/22, 25/25), so registering them is a one-line change per proof — deliberately **not** made here, because the instruction for this build is to fix the current first red and freeze, and registering two new proofs into the suite is new surface, not a fix. **It is the first thing to do when this reopens.** |
| 101 | **A REAL DEFECT IN ROW 95's HOLD PREDICATE, FOUND BY THE NEXT CI RED: THE HOLD DID NOT END WHEN TENANCY BEGAN, 2026-09-15.** Row 100 cleared the handoff proof; run 603 then stopped at **`unsent application draft correction`** (`tests/e2e/draft_offer_correction.e2e.js`) with both concurrent creates refused `application_target_held_for_signed_applicant`. **This one is not a fixture problem.** `application_inventory_hold` released a bed only on `declined / withdrawn / expired` — right for an application that **STOPPED**, wrong for one that **FINISHED**. Once an application reaches `accepted_term_required` or `active` the lease exists and governs the bed; a reader still saying *"held for a signed applicant"* is asserting a commitment that has already been honoured. A probe on the fixture bed showed exactly that: the hold naming an `active` application whose packet was `executed`. `RELEASED_STATUSES` now covers both endings — stopped (`declined`, `withdrawn`, `expired`) and finished (`accepted_term_required`, `active`) — and is exported so the proof asserts the boundary rather than restating it. **Why this was worth fixing even though production would usually shadow it:** the hold is consulted LAST, so an occupied or committed position normally never reaches it and the lease decides first. **A guard that is only correct because something upstream normally shadows it is not a guard** — and the moment the fixture's lease rows were wiped, the stale hold surfaced and refused a legitimate application. Pinned in `application_inventory_hold.db.js` (now **26/26**): a signed application at `approved` and `lease_ready` still holds its home; at `accepted_term_required` and `active` the hold ends. Re-verified: durable handoff 74/74 · journey 25/25 · 58 source-governance gates. **Stated plainly about method:** this is the fourth CI round on one freeze. Rounds one and two shipped without reproducing the failing assertion; rounds three and four reproduced first and each named its cause exactly. The reproduction harness (own database, migration chain, fixtures, server, the rung's predecessors) cost about one round to build and has paid for itself twice. |
| 102 | **THE EXCEPTION I CARVED TWICE WAS NEVER NEEDED — ROW 94 HAD ALREADY REMOVED ITS CAUSE, 2026-09-15.** Row 101 cleared `unsent application draft correction`; run 605 then stopped at **`two-step leasing: author and execute`** (380 passed, 19 failed). Reproduced locally: **one root cause, eighteen cascades.** `two_step_leasing.e2e.js` asserts *"a prepared packet blocks a further offer change (changed terms need a new package, never a silent swap)"* — 409, on a packet whose lineage is `authored_offer_acknowledged`. That is **precisely the case row 98 made permissible**, so the offer change returned 200, a new offer superseded the terms, and every later assertion in that journey failed against terms that had moved (`regenerating before issue returns the same draft` → 409, `exactly ONE of three concurrent Execute requests decides` → three `APPLICATION_TERMS_REVIEW_REQUIRED`, `both journeys executed` → no leases). **The third case that appeared to contradict the two suites is GONE.** `8c095ea0` narrowed this refusal because the tour journey's legitimate successor offers were being refused with *"A lease packet already exists"* — and they were, because submission itself owed AND dispatched the handoff, so a package existed before anyone tried to correct anything. **Row 94 moved the handoff from submission to COMPLETION and removed the after-commit dispatch**, so no package exists at those revision points at all. Two attempts were made to carve an exception for a condition that had already stopped existing — frozen states only, then frozen states plus operator-confirmed lineage — and **both let a real silent swap through**. The rule is now plain and matches both suites: **a CURRENT package blocks a successor offer.** Superseded and void packages do not — a superseded version is history and its access is already dead, because `resolveSignerAccess` refuses it. The package's state only chooses which sentence the operator reads; a third sentence covers a package prepared but not yet acknowledged. Reproduced in CI order (`draft correction` 446 → `two-step` 452): the two-step rung goes **0 failures** and `invite-to-guarantor lease` stays green. Re-verified: durable handoff 74/74 · inventory hold 26/26 · journey 25/25 · **guarantor signing 45/45** (the proof `8c095ea0` said the narrowing was needed for — it does not need it) · governing lease execution 16/16 · Spine lease execution 25/25 · 58 source-governance gates. **The lesson, and it is the same one twice:** a hole carved into a rule to accommodate one failing test outlives the condition that justified it. When the condition is removed by a later change, the hole stays — and the next thing it lets through is not a test failure, it is a silent swap of agreed terms. **Check whether the reason for an exception still exists before defending the exception.** |
| 103 | **FROZEN — FULL CI GREEN ON THE EXACT PAIR, 2026-09-15.** Run 608, `verify_all.sh`'s own verdict: *"operator app: **matched** at `312a999285c60d3d13d12690e5ae7a6d407fb566`"* and *"**ALL REQUIRED ASSERTIONS PASSED** — cleanup must also succeed"*, with **no `⚠ NOT RUN:` line**, which is the suite's way of saying the browser rung actually ran rather than being skipped into a false green. **THE PINNED PAIR:** API `d9b170c5569a3ebf8498852d1387850de0b4c29c` · app `312a999285c60d3d13d12690e5ae7a6d407fb566`, and that app SHA is exactly what `tests/e2e/app_pin.txt` declares, so CI verified against the commit this repository claimed. **NO SCHEMA DELTA FROM THIS BUILD:** `git diff 993efa4a..HEAD -- migrations/` is empty — rows 94–102 added no table, column, constraint or trigger. 187 migration files on disk, highest `198_proposed_source_claim_identity.sql`. **⚠ Separately and pre-existing: this branch carries 11 migration files `main` does not (188–198), so merging to main is a deliberate schema release of those eleven, not a no-op.** **AUTOMATION OFF, TWICE OVER:** `LEASE_HANDOFF_RECOVERY_ENABLED` unset, `LEASE_HANDOFF_RECOVERY_PROPERTIES` unset, **zero property UUIDs hardcoded anywhere in the handoff module**, and enabled-with-no-allowlist **refuses to sweep** (`no_property_scope`) rather than reading an empty scope as "all". **THE RED COST FIVE ROUNDS AND SIX COMMITS, AND THE SHAPE IS WORTH KEEPING.** First red moved forward every round and none recurred: `invite-to-guarantor lease` → my own handoff proof → `unsent application draft correction` → `two-step leasing` → green. **Rounds 1–2 shipped without reproducing the failing assertion and were both wrong. Rounds 3–5 reproduced first and each named its cause exactly.** Two of the round-1/2 fixes were later found wrong by the rounds that followed. The reproduction harness — own database, migration chain, fixtures, server, the rung's predecessors in CI's order — cost about one round to build and paid for itself three times. **DEFERRED, RECORDED, NOT STARTED:** proactive approver texting and the staff-consent doctrine change (migration 132's wall, row 96); operator presentation of the raised decision and the inventory hold; app-side copy for `application_target_held_for_signed_applicant`; Ask Spine exposure of the standing read (§40.2); Skyline/Greenery production verification (no production access from this session); **and registering `application_inventory_hold.db.js` and `application_to_lease_journey.db.js` in `verify_all.sh` — one line each, and the first thing to do when this reopens (row 100).** |
| 104 | **THE INVENTORY-HOLD GUARANTEE, REPAIRED — EXTERNAL REVIEW FOUND TWO REAL DEFECTS IN ROW 95, 2026-09-16.** Both verified in source before touching anything; both were defects, not architecture. **(1) A VOIDED PACKAGE WENT ON HOLDING ITS BED.** The read tested `coalesce(lp.status,'') <> 'void'` — a value the packet lifecycle never produces. Migration 034 declares **`'voided'`**, and `lease_packets.js` checks `pk.voided_at || pk.status === 'voided'` in **six** places. So the guard matched nothing while its own comment said *"a voided package holds nothing"*. **A predicate naming a value its schema cannot hold is not a weaker guard, it is an absent one — and it reads as present, which is worse.** Now `lp.voided_at is null and coalesce(lp.status,'') <> 'voided'`, both conditions, the way the packet service checks both. **(2) TWO ALREADY-ISSUED PACKAGES COULD BOTH BE SIGNED.** Row 95 stopped a new application TARGETING a held bed — true, and not the dangerous window. Two packages may legitimately be **issued** before either applicant signs, and the hold is DERIVED from `tenant_submitted_at`, so until the signature path consulted it, both could sign. `resolveSignerAccess` locks `for update of pk, s` — **each applicant's OWN packet and OWN signer row**, which never contend. `contested_by` in the hold module was a detector for a condition that should have been unreachable: **I built the detector and not the guard.** **Fix, without a second state machine:** at the tenant's final submit, `select id from spaces where id=$1 for update` — the canonical row for that exact home — then re-read `holdForSpace` INSIDE the lock and either stamp or refuse `home_already_signed_for` (409). Both submissions contend on the one object they are actually competing for; the loser blocks, then sees the winner's signature. **One predicate, not two:** the guard asks the same read every availability surface asks, so guard and reader cannot disagree. **Tenant only** — `tenant_submitted_at` is the tenant's alone, and a guarantor signing their own side claims nothing. **No space_id, no serialization** — a legacy unit-grain application has no bed to contend on, and refusing it would break historical records to guard a race they cannot enter. The refusal **names nobody**: it is read by the applicant who lost. **`application_inventory_hold.db.js` is now 40/40** (was 26) with both hostile cases, and **BOTH FALSIFIED**: reverting `'voided'` turns 4 red with the detail line reading `packet_status: "voided"` still holding the bed; disabling the space guard turns 5 red with **`[200, 200]` — two concurrent signatures both succeeding, `signatures: 2`, and `contested_by` naming the second.** The race is real and the fix closes it. Both racers are built through the REAL handoff and sign through the REAL public signer route concurrently — a hand-built packet cannot even be issued (`issueLeasePacketLink` refuses without a current terms confirmation, the product declining to mint bearer access to terms nobody authored). **REGISTERED AT LAST:** `application_inventory_hold.db.js` and `application_to_lease_journey.db.js` are now in `verify_all.sh` (row 100's open item). Evidence that exists but is not ENFORCED protects nothing from the next change. **Two fixture consequences, both the row-99 pattern at a new scope.** `leasing_hostile.e2e.js` calls `ctx()` once and then runs **seven** independent scenarios that each sign the SAME `C.bedB`; scenario two was correctly refused. Fixed in the shared helper rather than seven places: `toPacket` now supersedes whatever was current on the bed it is about to reuse — `ctx({wipe})` says this at the scope of the property, this says it at the scope of the bed. Superseded, never deleted (migration 192). **And my own source-scan assertion went red against the fixed file**, because the comment above the fixed predicate quotes the old spelling while explaining it — *a mention is not a guard*, exactly as this repo's method notes warn; the scan now strips comments first. **One more environment leak, the SECOND this session.** Registering the journey proof immediately exposed it: it sent `process.env.OPERATOR_KEY || "proof-operator-key"` — an invented default that matched only because the shell running it locally exported the same string. CI sent a key the server had never heard of, got **401**, and five assertions failed off that one refusal (the application never went terminal, so the home stayed held and its work stayed owed). `tests/e2e/boot.sh` sets `PROOF_OPERATOR_KEY="e2e-key"` for the server these proofs talk to; that is now the default. **Registration is what found it — the proof had been "passing" against a server only I could produce.** Re-verified with `OPERATOR_KEY` and `SMS_SEND_MODE` both UNSET in the shell: journey 25/25 · hold 40/40 · handoff 74/74. |
| 105 | **FROZEN — FULL CI GREEN ON THE REPAIRED PAIR, 2026-09-16.** Run **633** (`35090146866`) on API `2a26755041a12db41e754fad03a3d29008c419b5`, app `312a999285c60d3d13d12690e5ae7a6d407fb566`: run `success`, job `success`, step *"Run every proof"* `success`. **THE PINNED PAIR:** API `2a267550` · app `312a999` — and that app SHA is exactly what `tests/e2e/app_pin.txt` declares. **WHAT THE GREEN EXIT CODE PROVES, READ OUT OF THE SCRIPT AND NOT OUT OF MEMORY.** `verify_all.sh` ends `exit $FAILED` (line 507), so a `success` step conclusion is `FAILED=0`, and three things follow from the source rather than from a printed line: (a) **no rung failed** — every `FAILED=1` site is a step failure; (b) **the browser rungs RAN** — a skip sets `SKIPPED` *and* `FAILED=1` (lines 467–469, 481–483), so a skipped browser rung cannot reach a green exit; (c) **the app pin MATCHED** — `drifted`, `unreadable` and `no_pin_declared` each `exit 1` outright at lines 134–148, before a single proof runs, and `.github/workflows/verify.yml:70–105` checks the app out at `app_pin.txt`'s sha and refuses to guess when none is declared. ⚠ **The printed verdict line itself was NOT read this time.** Row 103 quoted run 608's *"ALL REQUIRED ASSERTIONS PASSED"* from the log; this session's egress proxy refused the Actions log archive (`connect_rejected`), and a 300-line tail read only interleaved Postgres output. **The exit code is the stronger evidence and is what is claimed here** — it is what the script DID, where the line is what the script SAYS — but the distinction is recorded rather than papered over. **NO SCHEMA DELTA, TWICE OVER:** `git diff 5bfb93fc..HEAD -- migrations/` is empty (the repair pass), and `git diff 993efa4a..HEAD -- migrations/` is still empty (the whole build). 187 migration files, highest `198_proposed_source_claim_identity.sql`. **⚠ Unchanged and pre-existing: this branch carries 11 migration files `main` does not (188–198), so merging to main remains a deliberate schema release of those eleven.** **AUTOMATION STILL OFF:** `LEASE_HANDOFF_RECOVERY_ENABLED` unset, `LEASE_HANDOFF_RECOVERY_PROPERTIES` unset, zero property UUIDs hardcoded in the handoff module, enabled-with-no-allowlist still refuses to sweep. **THE REPAIR PASS CHANGED SEVEN FILES AND COST TWO ROUNDS** (run 629 red → run 633 green), and the red was the newly registered journey proof's own invented operator-key default — *registration is what found it*. **SIX EDITS WERE ASKED FOR; A SEVENTH FILE MOVED AND IS FLAGGED, NOT SMUGGLED:** `tests/e2e/leasing_e2e_lib.js` (`toPacket`) was forced by the product change, because `leasing_hostile.e2e.js` signs one shared bed seven times and the new guard correctly refused the second. **DEFERRED, UNCHANGED FROM ROW 103 AND NOT STARTED:** proactive approver texting and the staff-consent doctrine change (migration 132's wall, row 96); operator presentation of the raised decision and the inventory hold; app-side copy for `application_target_held_for_signed_applicant`; Ask Spine exposure of the standing read (§40.2); Skyline/Greenery production verification. **EXPLICITLY NOT REOPENED, BY DECISION:** the signed-but-never-countersigned bed has **no automatic expiry** — silence must not release someone's signed-for home; the human bottleneck is to be solved with staff follow-up, never with an automatic inventory release. |
| 106 | **FOUND IN PRODUCTION, NOT FIXED, 2026-09-14 — an UNGRADED codex commit on the deployed API reverts the one protection row 82 exists to provide, on the surface a lender reads.** Deployed API `b3e4407` = graded board `6fa4638` + `2b5c47e` "Align formal rent roll with operating buckets" + `b3e4407` (proof update); migrations byte-identical; `6fa4638` is an ancestor, so every graded lane IS in production. **What the codex commit fixes is real:** the formal schedule and the operating table disagreed, and a needs-review or activation-pending position could read "Occupied" through the old axis-only fallback. It also ADDS four honest totals (`positions_activation_pending`, `_open`, `_needs_review`, `_occupancy_unconfirmed`) and returns `"Occupancy Unconfirmed"` when `bucket == null`. **What it loses is bigger.** (1) `statusLabel` now returns `r.bucket_label` FIRST, and `RENT_ROLL_LABELS.occupied` is the bare string `"Occupied"`. `rentRollBucketOf` buckets BOTH `contractually_occupied` AND `basis_type = opening_claim_occupied` as `occupied` — its own comment says *"Occupied" must never secretly mean "a canonical lease exists"* — so every accepted-occupancy-with-terms-unknown position reverts from `"Occupied — terms not established"` to plain **"Occupied"**. Row 82 added that exact line to stop exactly this, and its `⚠ WITHOUT THIS LINE...` warning comment now sits above code that can no longer run: the new early return precedes it for every row carrying a bucket. The graded fix is dead code. (2) `confirmed_contractual_occupancy` is now `buckets.occupied` — a field whose NAME says confirmed contractual, populated by a bucket defined never to imply a lease. The CSV emits `"Confirmed contractual occupancy," <n> " of " <total>`. On real Skyline, where **31** positions are confirmed occupied and only **4** carry trusted rent economics, this reports the claim-backed population as contractually confirmed to a lender. The denominator also moved from `of_leasable_resolved` (excludes down and contested) to `buckets.total` (everything) — two changes in opposite directions on one ratio. **Not a bad commit; a narrow, high-consequence loss.** Minimal fix, both halves needed: special-case the one bucket that collapses two facts (`if (r.bucket === "occupied" && r.tenancy_state === "occupied_terms_not_established") return "Occupied — terms not established";` ABOVE the `bucket_label` return), and either rename `confirmed_contractual_occupancy` to what it now counts or restore it to the contractual count and add `occupied_all_bases` beside it. **Demo bearing:** the operating Rent Roll table is UNAFFECTED and honest; this is the formal/print/CSV schedule, which is the artefact that travels. **REPAIRED BY THE `determined-dirac` MERGE, 2026-09-18 — and the diagnosis needed one correction.** The codex change was not a plain revert: it inserted `if (r.bucket_label) return r.bucket_label;` as the FIRST line of `statusLabel`, which left row 82's line present in the file but unreachable for every row carrying a bucket. `RENT_ROLL_LABELS.occupied` is the bare string `"Occupied"`, so an accepted-claim bed with no lease, no established rent and no term printed plain **Occupied** on the schedule that travels; `"Contested — overlapping leases"` collapsed to `"Needs Review"` and `"Occupied — rent unavailable"` to `"Occupied"` the same way. The codex intent was itself correct and is kept — before it, `activation_pending` and `needs_review` rows had no branch at all and fell through to `"Occupied"`. `determined-dirac` resolves both: `bucket_label` is the BASE and the axis facts return as appended qualifiers, discriminating on `contractual_terms_state === "not_established"` (derived from the same `current_lease_position` that decides the bucket, so the split is exact by construction rather than two sets coinciding). Its proof's mirror assertion moved from strict equality — which had PINNED the defect — to `status === base || status.startsWith(base + " — ")`. Verified by reading both trees at this merge, not by counting occurrences: the protection line survives a grep on the deployed tree while being dead code there. _(renumbered from 84 at the determined-dirac merge: that branch's rows 84-105 cross-reference each other by number and could not move.)_ | `src/surfaces/rent_roll_institutional.js`; `src/tenancy/dated_positions.js` (`rentRollBucketOf`, `RENT_ROLL_LABELS`) |
| 107 | **RESOLVED, 2026-08-28 — docker-local-dev thread.** ~~The documented from-scratch schema build does not work.~~ Two distinct blockers, both now closed by execution: (1) the self-recording migrations 083/084 collided with the runner's own ledger insert — the runner now defers to a row the file itself recorded (`on conflict do nothing`), which also makes a fresh build's ledger shape identical to production's (normalizer accepts both spellings); (2) the data-dependent migrations 087/110 stop by design on an empty database — the canonical precondition fixtures in `tests/e2e/preconditions/` are now applied by the compose local-dev release too, not just CI's `apply_migrations.sh`. **Proven:** fresh `docker compose` volume built from empty through the governed runner — 175 migrations, ledger ceiling 187, API verified and booted against it. | `migrations/migrate.js` (ledger insert defers to self-record); `tools/dev_migration_release.js` (resume loop); migrate logs: "local schema release complete — the volume now matches this build" _(renumbered from 85 at the determined-dirac merge: that branch's rows 84-105 cross-reference each other by number and could not move.)_ | _(carried from main, where it was row 22; the board lineage numbered rows independently from 22 onward)_
| 108 | ~~`migrations/migrate.js` hardcodes SSL, so `--apply` cannot build a schema on a local non-SSL Postgres.~~ The runner now takes its answer from `databaseSsl()` (the exact fix this row prescribed), and `database_ssl.js` also honors an explicit `sslmode=disable` in the URL — the compose service name `db` is not a loopback host, so the URL itself declares no-SSL; pinned by a new case in `gate_ci_path_ssl.js`. Production (Neon, `sslmode=require`) unchanged. Adjacent same-class fix made and said so: `src/onboarding/import_rent_roll_truth.js` had the identical hardcoded object and sits in the local-dev blast radius (rent-roll import). | `migrations/migrate.js` (client uses `databaseSsl(url)`); `src/shared/database_ssl.js` (`sslmode=disable` case); `tests/gates/gate_ci_path_ssl.js` (new case); `src/onboarding/import_rent_roll_truth.js` _(renumbered from 86 at the determined-dirac merge: that branch's rows 84-105 cross-reference each other by number and could not move.)_ | _(carried from main, where it was row 28; the board lineage numbered rows independently from 22 onward)_
| 109 | **LOCAL DEV, 2026-08-28 — docker-compose path validated end to end, app repo included (was broken; see #22/#28 for the API-side blockers).** The compose stack is now `db` + `migrate` (one-shot local schema release: reads the local ledger, releases with that ceiling, applies canonical precondition fixtures on data-dependent stops, guarded to local-dev connection shapes) + `api` (`OPERATOR_APP_ORIGIN` overridden to the app's local origin — `/operator/*` CORS allows exactly that, proven by preflight) + `app` — the sibling `property-spine-app` checkout served on :8080 by `tools/dev_static_server.js`, which rewrites the app's hardcoded production API origin to `http://localhost:3000` so a local browser lands on local data; the app repo itself is untouched, and its `localStorage.ps_api_base` override still wins if previously set. Dockerfile installs from the lockfile (`npm ci --omit=dev`) instead of resolving at build time. All proven against a real fresh volume: ledger ceiling 187, `/health` ok, app served with origin rewritten (4 occurrences), traversal guarded 403, second `up` idempotent. **Class-2 scaffolding** — removal conditions stated in the two `tools/dev_*.js` files. **Recorded, not chased:** the api repo root carries a git-tracked 745KB `property-spine-app` HTML snapshot (stale since 2026-07-07, purpose unexplained — already an open question in `docs/archive/MAINTENANCE_UNIT_STATUS_SOURCE_COMPARISON.md`); compose deliberately uses the real sibling repo, never that file. | `docker-compose.yml`; `tools/dev_migration_release.js`; `tools/dev_static_server.js`; `docs/deployment.md` (Docker Compose section rewritten to match reality) _(renumbered from 87 at the determined-dirac merge: that branch's rows 84-105 cross-reference each other by number and could not move.)_ | _(carried from main, where it was row 29; the board lineage numbered rows independently from 22 onward)_
| 110 | **TWO RECORDS FROM THE 2026-09-18 INTEGRATION: ONE LANE ALREADY CLOSED (DO NOT REBUILD), ONE DEFECT FOUND AND NOT FIXED.** **(a) Closed, and it was queued as a lane that must now NOT be written.** The overnight plan carried a lane for the contested-claim date helper rendering a `date` column through `toISOString()`, which loses a day in every zone ahead of UTC. `determined-dirac` had already closed it, more completely than the lane specified: `src/shared/date_column.js` (class 1) is the single owner of the answer — the same pattern `database_ssl.js` uses — it states the rule for a `date` column (local calendar components ARE the recorded date) and the opposite rule for a `timestamptz`, it corrects the prior comment that asserted the reverse in good faith, it points at `to_char(d, 'YYYY-MM-DD')` as stronger where the query can answer, and it is wired into six consumers rather than the one the lane named. `tests/unit/date_column_timezone.test.js` passes 15/15 under `TZ=UTC` and `TZ=Europe/Berlin` on the merged tree. **(b) FOUND, NOT FIXED — the canonical dated read decides "today" in UTC while a shared module already owns the property's operating day.** `datedPropertyPositions` (`as_of` omitted) and `currentRentRoll` both compute `new Date().toISOString().slice(0, 10)`. `src/shared/property_timezone.js` exports `loadPropertyOperatingTimeZone` and six modules consult it (tours, leads, comms, operator, operating window); these two consult it **zero** times, measured by grep on this tree. For an `America/New_York` property, UTC has already rolled over between 8pm and midnight local, so an evening read resolves positions as of the next calendar day: a lease ending today reads expired, one starting tomorrow reads current. Same class as the SSL and date-column findings — a shared owner exists and the most load-bearing read bypasses it — but the fix is NOT mechanical: whether a rent roll is "as of" a property-local day or a system day is a doctrine ruling (whose day is it?) and it changes every read derived from this one. Owner decision, then a lane with its own first red. Nothing observed today is known to be wrong; no production read was taken. | `src/shared/date_column.js`; `tests/unit/date_column_timezone.test.js`; `src/tenancy/dated_positions.js` (~777); `src/surfaces/rent_roll_canonical.js` (~65); `src/shared/property_timezone.js` |
| 111 | **BUILT · UNIT-PROVEN AGAINST THE REAL `resolveContext`, 2026-09-16 — the leasing agent now retrieves the slice of property knowledge a question needs, instead of the whole shelf.** `src/agent/leasing_context_resolver.js` (Class 1, pure, no I/O, **no model call, no embeddings, no migration**) maps an inbound message to `{intents, factKeys, categories, needsPricing, needsInventory, selective, basis}`; `resolveContext` in `agent.js` takes it as an optional `selection` and filters the curated set. **IT REUSES THE TWO REGISTRIES THAT ALREADY EXIST** — `leasing_knowledge.topicsFor` verbatim for the ten descriptive shelves, and operator.js's governed `FACT_KEYS`/`CATEGORY_FOR` vocabulary (`pet_policy`·pets, `parking_rules`·parking, `tour_window`·tours, `fee_policy`·fees, `required_documents`·documents, `office_contact`/`communication_instructions`·routing) for the seven keys `topicsFor` does not reach. A competing taxonomy would have drifted from the keys operators actually write, and the drift would surface as an unanswerable question rather than as an error. **THE ONE DESIGN CONSTRAINT THAT DECIDED EVERYTHING: economic sources are NEVER narrowed.** `agent_runs.resolved_fact_snapshot_json` is what `compareEconomicSources` judges a ready draft against, over the WHOLE economic set including sources that APPEARED after review (`added_ids`). Narrowing it was falsified and the result is worse than a failing test: with the exemption removed the comparison returns **`had_economic_sources: false`**, so the pre-send guard `if (econ.had_economic_sources && !econ.match)` stops firing AT ALL — stale-draft protection silently gone. So the filter is `isEconomic(f) || selects(selection, f)`, one owner for that boundary (`draft_source_identity`), and the two stale-check call sites are **untouched**: they read live and unnarrowed, and the economic subsets are identical either way. `draft_stale_source_proof.js` 40/40 unchanged. **THREE SMALLER REFUSALS TO SAY A CONFIDENT WRONG THING.** (1) An empty narrowed fact list must not print *"no curated facts are on file for this property"* — that is false when the property has facts and none answers THIS question; there is now a second sentence for the narrowed case that tells the model not to fill the gap. (2) A skipped pricing read is marked `not_read_this_turn`, not `quotable:false` — *"PRICING IS NOT QUOTABLE"* would assert something about the property that was never checked. (3) **The unit identity row is read whenever one is linked**, selection or not: skipping it would make the prompt say *"no specific unit is linked to this inquiry yet"* about a conversation that has one, which is a confident wrong statement to save one query. Only `quotablePricing` — the governed invocation — is gated. **`needsInventory` deliberately does NOT withhold the `find_available_units` tool**: taking a tool away changes what the agent can DO, not what it retrieves, and a prospect who pivots to availability in a phrasing the table misses would meet an agent that cannot look. Retrieval narrows; capability does not. **UNCLASSIFIED LOADS EVERYTHING** — `selective:false` restores the pre-resolver behaviour exactly, so narrowing must earn each turn and the unnarrowed path stays the floor. **PROPERTY ISOLATION IS UNCHANGED AND PROVEN:** selection filters within rows `readActive` already scoped with `where property_id=$1`; it can only remove, never add, so no fallback to another property, to demo facts or to organization-wide facts is reachable. Proven with a populated demo shape present in the fixture and asserted absent. `tests/unit/leasing_context_resolver.test.js` **49/49**, registered in `verify_all.sh`, exercising the REAL `resolveContext` through `router._service` rather than a copy. **THREE FALSIFICATIONS, each red then restored:** removing the economic exemption → 4 red including the silent-guard collapse above; disabling narrowing → 2 red; making the skipped price a bare `null` → 1 red. **NO REGRESSION, MEASURED NOT ASSUMED:** the unit suite fails 21 files on this branch and **the same 21 on untouched `cd7279eb`** (DATABASE_URL and a path expectation, not this change) — identical sets, zero difference. 57 source-governance gates green. **NOT DONE HERE:** `personAttributes` is in the resolver's contract and is not fed from `agent.js` (tx1 does not load them; the inbound text already carries the budget and bedroom signals). No browser rung, no live-property run, and **the real corpus is still demo-shaped** — Solo on Chestnut 0 active facts, Greenery 0, Skyline 5, Demo Solo Shape 26 — so on a real property today this narrows a set that is mostly empty, and the honest-unknown path is what a prospect actually meets. **SIX DEFECTS FOUND BY A SECOND PASS — putting two dozen phrasings a prospect ACTUALLY texts through the table, rather than the examples in the spec.** The worst would have shipped silently: the money patterns sat inside one `\b(...)\b` wrapper, and `\b` after `\d` requires a non-word character next — so *"under $2,500"* matched **on the comma** and *"under 2000"* did not. The spec's own Example 3 passed by luck of its punctuation, and a prospect stating a plain budget would have had governed pricing skipped and been told the team would confirm. Amounts are now their own alternatives with no trailing boundary. Also missed and fixed: *"can i see it saturday"* (the object after `see` was mandatory; bare day names now count), *"wheres the office"* (no apostrophe), *"do you allow subletting"* (`\bsublet\b` cannot match `subletting`), *"how long is the lease"* (the noun last matched nothing); `renters_insurance` was riding along on *"is wifi included"*; and a bare `floor` read as an availability search, colliding with the `floor_plans` shelf. **AND ONE DEFECT THE WIDENING ITSELF INTRODUCED, caught the same way:** `lease_term` recognises the question but names no shelf, so matching it returned `selective:true` with an EMPTY `factKeys` — a narrow read to NOTHING, telling the model nothing on file answers a question the property may answer well. Selectivity is now decided by whether a shelf was actually selected, never by whether a pattern fired (`basis: intent_without_shelf` falls back to full context). Unclassified went 5/24 → 2/26. Test is now **66/66** with a regression case per finding; both falsifications re-run red (4 and 2) and restore green. |
| 112 | **BUILT · UNIT-PROVEN, 2026-09-16 — the web and SMS rails answer the same leasing question the same way, and the agent now uses what the prospect already told Spine.** **WHAT WAS ALREADY SHARED AND WAS LEFT ALONE:** the application writer (`server.js` injects the SAME `__staffLeasingAction` into `ask_spine.js` that the SMS rail calls), the leasing intent parser (`staff_sms_intent`), and the governed read (`ask_spine_answer.answer`) — and both rails already pass `mintComplianceReference` AND `applicationsService`, so the read capability was NOT divergent. The web's opaque token confirmation door is a NARROWER surface than SMS prose confirmation, not a different writer, and is kept. **THE ACTUAL DIVERGENCE, FOUND BY MEASURING 16 OPERATOR PHRASINGS THROUGH BOTH ROUTERS:** the same operator asking *"whats our pet policy"* was answered on the web and dropped on the TECHNICIAN rail over SMS. Two causes, both in shared code. **(1)** `leasing_knowledge.isSelfRead`'s question-lead regex put `\b` after bare interrogatives, so `what`/`where`/`how` matched and `whats`/`wheres`/`hows` did not — **the same trailing-`\b`-after-a-word trap that row 111 records in the pricing pattern, in a different file.** People type the contraction. **(2) ⚠ CORRECTED — THERE WAS ONLY ONE CAUSE, AND MY SECOND FIX WAS DEAD CODE.** I first recorded a second cause: that `staff_sms_router.js` required `leasing_knowledge` at line 10 and never consulted it. **The registry IS consulted** — `askSpineAnswer.questionSubject` calls `isKnowledgeRead` on its FIRST line (`ask_spine_answer.js:445`), so the contraction fix in (1) propagated to `questionSubject` and fixed both rails by itself. The `|| leasingKnowledge.isKnowledgeRead(text)` I added to the router changed the destination of **no input**; proven by removing it and re-running the corpus. Worse, the test guarding it **scanned source for the string** and passed while the code did nothing — *a mention is not a guard*, written into my own suite. The clause is removed, the assertion is now behavioural, and the dangling import is gone with a note saying the absence is deliberate, because that import is precisely what produced the wrong diagnosis. Verified unchanged: *"the dog chewed the carpet"*, *"whats broken in 3B"*, *"update the pet policy"* stay off the read path; the knowledge questions still reach it. **A SWITCH, BECAUSE OF THAT REACH:** `LEASING_KNOWLEDGE_CONTRACTIONS=off` restores the pre-fix predicate on the next message, no build — the same shape as the resolver's own switch, added once the reach below was understood. **THE REAL BLAST RADIUS IS WIDER THAN THE SMS RAIL AND WAS NOT UNDERSTOOD WHEN FIRST SHIPPED:** one line in `leasing_knowledge.isSelfRead` moves `isKnowledgeRead`, which moves `questionSubject`, which is read by the SMS router in two places AND by `ask_spine_answer.js:1694` — the subject router for every governed read on both rails. **THE PROSPECT-ATTRIBUTE INTEGRATION, WITH THE NAMES READ OUT OF THE SCHEMA.** Row 111 left `personAttributes` accepted but unfed. The resolver had GUESSED nine names from an example prompt; `person_attributes` CHECK-constrains `attr_key` to exactly **six** (`move_month·budget·unit_type·occupants·pets·reason`) and the column is `attr_key`, not `attribute_key` — **one of nine guesses was real.** Eight would have matched nothing and silently recorded a prospect as having said nothing. Now wired to the canonical Person × Property read `leasing_inventory.readProspectFacts` (exported, not reimplemented: it resolves person-level against property-level rows, keeps most-specific/most-recent, and carries `source` and `recorded_at`). **AND THE SECOND DEFECT IN THAT SAME CODE:** the old version let a stored budget set `needsPricing` outright, so a prospect who once mentioned $2,500 would have had every later question — including package handling — turned into a governed pricing search. **A recorded preference is CONTEXT for a question that depends on it, never a standing instruction.** The capability flags now come from THIS turn's words alone; recorded facts travel only when the turn is elliptical (*"what about furnished?"* → carries unit_type, move_month, budget), when this turn is itself a search, or when it is about pets. Falsified: forcing `needsPricing` off a stored budget turns 3 red including the package case. **FOUR SILENCES HELD (§40.7):** a failed attribute read is carried as `established_read_failed` and the prompt says the preferences could not be read, rather than implying the prospect never spoke. **NO MIGRATION. NO NEW READER. NO NEW IDENTITY OR MEMORY SYSTEM** — no inference from a typed name or phone. `tests/unit/shared_conversational_path.test.js` **49/49**, registered in `verify_all.sh`, covering acceptance A–E; resolver **78/78**; `draft_stale_source_proof` **40/40** unchanged; 57 source-governance gates; unit-suite failure set still **identical to untouched `cd7279eb`**. Three falsifications each red then restored. **RECORDED, NOT FIXED:** *"do we allow dogs"* and *"wheres the office"* still miss the SMS read rail because `topicsFor` covers only the ten descriptive shelves and `pet_policy`/`office_contact` are not among them — a shared-registry gap, not a channel one, and widening pattern coverage was out of scope. **NOT DONE:** no browser rung, no real conversation, no live-property run; Skyline's governed-key status is UNCONFIRMED (this session had no production access) and the approval queue at `docs/handoffs/new-hp/shared-conversational-path/SKYLINE_APPROVAL_QUEUE.md` proposes no wording and publishes nothing. **SIMPLIFICATION PASS, SAME BEHAVIOUR, FEWER MOVING PARTS.** (1) The two generation sites each read prospect facts, resolved, then bolted `established_read_failed` onto the resolver's FROZEN result with `Object.assign` — a caller mutating a contract it does not own. Collapsed into one `resolveTurnContext()`; the read state is now an INPUT the resolver reports in all four of its shapes, so nothing mutates its output. (2) `normaliseAttributes` handled three input shapes; only one had a caller. One shape. (3) **A MERGE THAT WAS MEASURED AND REJECTED:** the obvious simplification is to fold the resolver's governed-key table into `leasing_knowledge.MATCHES` so there is ONE classifier — and it would break the display contract, because `composeConversationalAnswer` does `TOPICS[key].toLowerCase()` and would throw on any key outside the ten shelves. The two classifiers answer different questions over different vocabularies and are layered, not duplicated. Recorded so the next person does not re-derive it. **AND A GUARD THAT MAKES THIS THREAD'S OWN DEFECT UNREPEATABLE (section F).** The nine guessed attribute names failed SILENTLY — a name that does not exist simply matches nothing, so a prospect appears to have said nothing. The test now parses the `attr_key` CHECK out of `migrations/061_person_attributes.sql` and asserts `ATTR_KEYS` equals it **in both directions**; asserts `PROSPECT_FACT_KEYS` stays inside it; parses `operator.js`'s `FACT_KEYS` and asserts **every fact key the resolver names is writable or explicitly declared legacy** (five are, now named in `LEGACY_FACT_KEYS` — a key no operator can write is a key that will never be answered, and leaving that implicit is how a dead entry survives); and asserts no legacy declaration outlives its key. Falsified both ways: restoring `max_rent` turns 2 red, and a `pet_polciy` typo — previously invisible — turns 1 red by name. Test 49 → 56. |
| 113 | **BUILT · UNIT-PROVEN 2026-09-16, CORRECTED 2026-09-17 — property line identity: two of the four briefed defects were ALREADY CLOSED on the deployed lineage, and saying so is the finding.** The slice brief was written against `main` (787-line `server.js`, 176 migrations, ceiling 187) and **production does not run `main`** — `/health` reports the codex lineage, 188 migrations, ceiling 199, confirmed by the owner's own Neon read of **199**. So `CURRENT_STATE`'s 187 and `THREAD_HANDOFF`'s 181 are both stale *for a different lineage*; the tiebreak the brief asked for was already performed and the answer is 199. **ALREADY CLOSED HERE, MEASURED NOT ASSUMED:** (b) the policy-blind outbound reader — `propertyLine(` has **0** occurrences in `communications_boundary.js` on this lineage and **2** on `main`; `sendPropertySms` already resolves through `resolveOutboundLine`, and the preserved chain (resolve → `resolved.line.e164` → `canSendSmsForRecord` → `sms.sendSms`) is asserted **in order**. (§3.3) the unauthenticated second inbound door — `/intake/twilio` is retired to a deterministic **410** with no form parser, and `INTAKE_ALLOWED_NUMBERS` does not exist anywhere on the lineage. Both are asserted anyway, because an assertion that a defect is ABSENT is what stops it returning on the next merge from a branch that still carries it. **REAL AND FIXED:** (a) `POST /properties/:propertyId/sms-number` **deleted**, not documented. It inserted `outbound_enabled=false` with `outbound_policy` defaulting to `'disabled'`, which satisfies migration 132's `ck_cl_outbound_flag_matches_policy` (`false = false`) while being operationally false — every other `property_facing` line is `proactive`, so the row would accept inbound and silently refuse every outbound. **Removal proof, deterministic local grep, not a code-search index:** zero callers of `sms-number` outside that file in the API, and zero in `property-spine-app` on **any** remote branch; the app touches `sms_number` twice (`index.html:30727`, `31076`) and both are display-only table cells reading the projection. ⚠ Residual stated in the tombstone: the route was `requireOperator`-gated and the shared operator key is held outside this repo, so source cannot prove no external caller existed — a 404 is the honest answer for one. **(c) THE LIVE PROSPECT PATH STOPPED GUESSING.** `leasing_leads.resolveOrCreatePerson` took `order by created_at limit 1` on `primary_phone_e164`, silently adopting the OLDEST person sharing a number — so a reassigned number attached a new prospect's messages, budget and tour to the previous holder's person card. `person_ingress.js` refuses exactly this (`disposition: "conflicted"`) and says why; **the live door was the one guessing.** Now throws `person_identity_conflicted` (409) carrying both candidates. Throws rather than returning null because the single caller dereferences `person.id` on the next line and the enclosing transaction already rolls back. **⚠ THREE CORRECTIONS, 2026-09-17, from an independent source review of `7157925d` — each was a real defect in the first pass, each is now fixed:** (i) **the sayable refusal never reached the caller.** It was set on `publicMessage` while `/leasing/intake` renders `e.publicReceipt \|\| e.message`, so what a person actually saw was `e.message` — which named the phone number and the count. Now on `publicReceipt`, and the `Error`'s own message was rewritten to be non-identifying for the same reason: **a refusal must not become the disclosure.** (ii) **only the canonical branch was fixed.** `resolveOrCreatePerson` has three lookups, and the legacy-phone (`candidates.find`) and email (`order by created_at limit 1`) siblings still adopted the first match — the same ambiguity reaching the same person card through a different door. All three now `order by created_at` with **no limit** and refuse through one `refuseAmbiguous(rows, evidence)` helper that names which key conflicted. (iii) **“nothing partial is written” was true and was not the whole story — the inquiry was LOST.** The throw precedes the lead, source-touch and message writes and the service rolls back, so a refused prospect simply vanished. The inquiry is now captured on a **fresh connection after the rollback**, as an obligation through the canonical writer `obligations.spawnObligationFromEvent` (§11) — `person_id: null`, scoped to the property, `leasing_manager`, high — never a direct `insert into obligations`; a failed capture is logged loudly and **never converts the refusal into a success**. `obligations` was chosen over `intake_events` because the latter sits behind `checkPassword` and is unscoped by property: worse disclosure than the problem it would solve. **NOT done, deliberately:** the five `insert into persons` sites are not merged and **no unique index was added to `primary_phone_e164`** (asserted at STATEMENT scope, after a first version of that assertion matched any file containing both words — a claim about a search, not about the schema). **ALSO:** `TWILIO_FROM_NUMBER` was documented in `docs/deployment.md` as settable and read by **zero** code; removed rather than wired, because a global default `from` is exactly the fallback `resolveOutboundLine` refuses. `tests/unit/property_line_identity.test.js` **33/33** (35 before four source scans were retired into the database proof recorded in the 2026-09-17 retention row), registered in `verify_all.sh`. **⚠ SUPERSEDED IN PART BY THE 2026-09-17 RETENTION ROW:** a second review found the capture described below saved a TASK and not the INQUIRY, and that its receipt claimed “saved” before the save was attempted. Both are fixed there. Read the 2026-09-17 retention row for the current state of this path. **The review's sharpest finding was about the tests, not the code:** they asserted SOURCE TEXT, which is exactly how a refusal written to the wrong field passed its own test. §5 now drives the **real `resolveOrCreatePerson`** through a client answering only its three lookups — legacy-phone, email and canonical ambiguity each refuse; `publicReceipt` is asserted as the field the endpoint actually renders; the rendered receipt is asserted to name **no phone and no candidate**; and **two controls** (a single match resolves normally, no match still creates) stop the suite passing because everything throws, which an earlier version did. The three source twins in §3 were **deleted**, not re-pointed: a source twin of a behavioural assertion only re-pins the implementation shape and re-breaks on the next honest refactor. What stayed in §3 is the one thing §5 structurally **cannot** see — its fake client answers by regex on the SQL and returns two rows whatever the query asked for, so a returning `limit 1` would leave every behavioural assertion green. All three lookups are pinned by name and **mutation-checked**: reintroducing `limit 1` in a branch turns that assertion, and only that one, red. ⚠ **Still owed, stated rather than claimed:** the end-to-end assertion through the real `/leasing/intake` HTTP endpoint needs a real database. A fake pool wide enough for its preamble is a database reimplemented badly, and the first attempt proved it — 11/11 “failed” on a missing `lead_sources` row and the control case passed for the wrong reason. Shared path 65/65 · resolver 78/78 · `draft_stale_source_proof` 40/40 · 57 gates · no regressions. **UNBLOCKED — DATABASE-VERIFIED by the owner's read-only Neon review, 2026-09-17** (project `odd-cherry-53458838`, branch `production`, both transactions `SET TRANSACTION READ ONLY`), resolved **by property ID, never by display name** — other rows carry these same names and are **not** interchangeable: **Skyline** `14e41b7c-e91c-49e8-9651-10c4908a8f6a` (key `1417`) **HAS an active property-facing line** — `f8736435-3038-41ef-b877-36b9f0d95f20`, `+12157708837`, inbound and outbound enabled, policy `proactive`, created 2026-08-18. **Greenery** `a29181cd-3ba1-461c-aead-cd989add1d11` (`1325-N-15`) and **Solo on Chestnut** `9e2bb96e-08e2-41db-81c2-91055ceb50a3` (`4233-CHESTNUT`) have **no** `communication_lines` row. ⚠ **I stated in-thread that Skyline had no line. That was wrong** — asserted from source with no database access, when the honest answer was `NOT_ESTABLISHED` **for this reader**, not absent in the world (§40.7: a read I cannot perform is not a fact about the property). Ledger ceiling **199** (`property_display_name_command`, applied 2026-09-16); 187 ledger rows. **Duplicate-phone counts, the other blocked item:** across **997** persons — **0** duplicate canonical-phone groups, **0** numbers with multiple active records, 26 persons with a canonical phone, **104 with a legacy `phone` and no canonical one**, and **0** duplicate active `communication_lines.e164` groups. **So the refusal guards a condition not currently present in production — it is a guard, not a repair**, and those 104 legacy-only rows are where it will actually bite. **THREE STATES, REPORTED SEPARATELY AND NOT COLLAPSED:** *source-tested* (35/35 above) · *database-verified* (the line configuration and person counts, read-only, by the owner) · *delivery-tested* (**NOT DONE** — a configured line is not evidence of provider account control, webhook configuration, messaging registration, consent, recipient send eligibility, or successful delivery; none were exercised). **Configuration is not delivery, and neither is code completion.** **NO production write, no number action, no republication.** Classifications: deleted route was Class 2 (temporary adapter) with its replacement condition now met by `communication_lines`; the conflicted refusal is Class 1. |
| 114 | **BUILT · PROVEN (real Postgres + real HTTP), 2026-09-17 — a refused inquiry is now RETAINED, and the receipt only says “saved” once the save committed.** The 2026-09-17 identity-refusal row's capture was reviewed and had two concrete defects. **(1) It saved a task, not the inquiry.** The obligation carried a property, a generic label, a role and a priority — no message, no submitted contact, no source, no conflict evidence. An operator opening it learned that *a* conflict happened with no way to tell WHICH inquiry they were being asked to resolve. **An alert that something was lost is not preservation of what was lost.** **(2) The receipt claimed “saved” before the save was attempted**, and kept claiming it after the save failed — the catch logged the loss and rethrew the original error carrying the same optimistic sentence. **WHAT IS RETAINED NOW:** the inquiry itself, on the record the comms boundary **already** uses for an ambiguous SMS sender — a `comm_events` row, person-less, property-scoped, `needs_human`, `classification='unknown'`. `communications_boundary.js` had solved this exact shape (*“an ambiguous SENDER still has a known property, so the claim is preserved on that property's ledger for a human”*); **the web intake door was the one losing it.** The review task then links to that evidence through the existing `obligations.related_type='comm_event'` / `related_id` linkage. No placeholder person, no second intake store. **MIGRATION 200 — one column, and why it was unavoidable.** For SMS the sender's number is recoverable from the provider via `sms_sid`; a web inquiry has no provider, so the submitted phone, email and source exist only in the request body. All **44** existing `comm_events` columns were checked against the live catalog, and every property-scoped table carrying a phone/email column (`scheduled_tours`, `tour_booking_links`, `team_invites`, `work_orders`, `bids`) models a different object; `obligations.required_inputs` is `text[]` — input NAMES, not values. `comm_events.unresolved_inquiry` (jsonb) holds the contact **as submitted**, the source, the received time, which identity key conflicted, and candidate person **ids only** — never their names. A CHECK confines it to `person_id is null and needs_human and direction='inbound'`, so it is **structurally impossible** to hang on an attributed message; both violations were mutation-tested against the real constraint. **⛔ DEPLOY CONSEQUENCE, NOT A FOOTNOTE:** this ships schema, and a deploy does NOT migrate. Verified by running `prestart`'s verify-only path against a database without 200: the service **refuses to start** and names `MIGRATION_RELEASE=1 EXPECTED_LEDGER_CEILING=199 node migrations/migrate.js --apply`. Deploying this code before releasing 200 produces a failed deploy. **RECEIPT DEPENDS ON THE COMMIT:** two constants. The refusal is thrown carrying the **pessimistic** one; it is upgraded to “it has been saved” **only after** the retained inquiry and its review task commit. A failed retention returns the honest receipt naming a next step (send it again, or call the office) and is never converted into a success. **OPERATOR DOOR:** `GET /operator/obligations/:id/retained-inquiry`, added to the module whose own header says to add a door when a real workflow needs one. Property and module come from the resolved staff session (§21); out-of-scope is **404, not 403**; the retained record's own `property_id` is re-checked against the session, so a mislinked obligation cannot pull another property's inquiry through. Contact details and candidate names are returned **only here**, and are deliberately absent from the obligation label that boards and queues render. **IDEMPOTENCY** rides the same delivery key the happy path uses, via `comm_events.correlation_key` (unique where not null) — the database decides, not a read-then-write. **THE PROOF:** `tests/proofs/leasing_identity_conflict_http.db.js` — **46/46**, registered in `verify_all.sh`, real Postgres + real Express + real HTTP over a real socket, synthetic records only, transport stubbed to throw so no message can reach anyone. It proves all seven asks: neither person got a lead · message, submitted contact, source and received time survived the rollback · an authorized operator retrieves the actual inquiry · no-session/bogus-session/other-property callers get 401/401/404 and **zero** message, contact or candidate leakage · a retry leaves exactly one inquiry and one task · a **genuinely failed** retention (a temporary CHECK makes the insert fail for real) cannot return a “saved” receipt · single-match and new-person intake still work. The first failure-injection attempt flipped a column DEFAULT and proved nothing because the insert passes the value explicitly — it went red rather than passing for the wrong reason, which is why that case exists. ⚠ **Stated, not hidden:** two collaborators are stubbed (`commBoundary.reclassify`, `leasingLifecycle.maybeReopenOnQualifyingInbound`); both are reached only on the success path AFTER the refusal throws, so nothing asserted about the conflict path runs through them, but the §7 controls do not cover classification or lifecycle writes. **EVIDENCE RELABELLED (§4 of the review):** `shared_conversational_path.test.js` calls the REAL SMS router but a LOCAL MODEL of the web door's dispatch rule — the endpoint is never invoked — and hands preference fixtures straight to the resolver with no conversation loading them. `webDestination()` is renamed `webDestinationModel()` and the header now states what it cannot see (a new branch in `ask_spine.js` would leave every assertion green while the real door diverged). These are **routing and resolver unit tests**, not end-to-end conversation proof, and a receipt must not call them one. I described them as “the real web path”; that was wrong. **Unit:** identity 33/33 · shared path 65/65 · resolver 78/78 · `draft_stale_source_proof` 40/40 · 14 of 16 source-governance gates (the 2 failures, `gate_person_ingress` and `gate_property_name_resolution`, are pre-existing and byte-identical at HEAD) · unit failure set identical to baseline. **NOT done:** no identity backfill and no unique index on `primary_phone_e164` — the owner's Neon read found **0** duplicate canonical-phone groups across 997 persons, so the refusal is a **guard, not a repair**; the 104 legacy-phone-only rows are where it will bite. Delivery remains **untested**. Classifications: migration 200 and the retention are Class 1; the operator read is Class 2 (permanent). **No production write, no number action, no message to any real prospect.** |
| 115 | **PROVEN (CI-path database + real HTTP + real browser), 2026-09-17 — the retention is verified the normal way, an operator can recover the lead in the APP, and the conversational claim is now demonstrated instead of asserted.** **(1) THE NORMAL RELEASE CHECK.** The 2026-09-17 retention row's proof ran against a hand-rolled bootstrap that SKIPPED two data-dependent migrations (087, 110). That was not equivalent to the standard run, and the repo already had the answer: `tests/e2e/apply_migrations.sh` handles both structural classes (self-recording 076/077/079/083/084; data-dependent 087/110) and resumes from the ledger. Rebuilt with it — **188 ledger rows, ceiling 200, zero skips**, 087 and 110 both applied — and the registered step passes **46/46** on that schema. ⚠ `tests/e2e/proof_boundary.js create` itself **cannot run in this container**: it binds `::` to check the port and there are no IPv6 interfaces (`EAFNOSUPPORT`, `/proc/net/if_inet6` empty). Its owned-database + nonce manifest was reproduced exactly; the port guard was not exercised. **(2) THE OPERATOR CAN REACH IT WITHOUT DEVELOPER TOOLS.** The API door alone was not enough. In `property-spine-app/index.html`: the `retainedInquiry` route, a `prospect_identity_conflict` branch in `obRow` (**Open inquiry →**, tapping opens the panel) and `openRetainedInquiry()`. **A defect found by measuring rather than assuming:** the task would have been **invisible** — `mwHighProfile()` gates the work queue on words like *countersign|concession|approv|compliance* and a due date, and the retention label matches none of them and carries no due date. It is now matched **by type**, because the label is deliberately generic. Two near-identical filters exist (`mwHighProfile`, `psHighProfile`); only the first was changed — `psHighProfile` feeds a dash whose `row()` carries **no taps at all**, not even for the inbound-decision precedent, so adding a row there would be a dead end. **`property-spine-app/retained_inquiry_dom.test.js` — 23/23 in a real browser**, lifting the REAL `mwHighProfile`, `obRow` and `openRetainedInquiry` out of `index.html` (not copies), asserting the row's `onclick`, the message, contact, source and candidates on screen, `elementFromPoint` **visibility** (rendered is not visible), that the queue row itself discloses no contact details, and that no-session / failed-read / unverifiable-link each say so honestly with no fixture. Registered in `verify_all.sh`. **(3) THE CONVERSATIONAL CLAIM, DEMONSTRATED.** `tests/proofs/conversational_consistency.db.js` — **23/23**, registered. *One operator, two rails:* the same question through the **real website endpoint** `POST /operator/ask-spine/message` over a real socket and the **real staff SMS handler** `staffGovernedRead.run` — both reach the same `askSpineAnswer.answer` and return **byte-identical `grounded_on`** (`{leasing_knowledge:'ESTABLISHED', topics:['leasing_faq'], scope:'property_wide'}`) and the same approved wording. **No model is involved**: `leasingKnowledge.answer()` takes a db and no anthropic client, so this is one governed read, not two similar sentences. An operator entitled to the property but **not to leasing** is refused identically on both rails and neither refusal leaks what it withheld; an unestablished topic is the same honest gap on both. *Prospect:* preferences are written through the canonical `recordPersonFact`, read back from the database, and carried by the **real `resolveTurnContext`** both live turn paths call — “What about furnished?” carries `unit_type=2BR`, `move_month=August`, `budget=$2,500` with provenance and still reaches the amenities shelf, while “How do packages work?” carries **none** of them and does **not** become a pricing search, with a control proving the flags come from the words and not the stored profile. The staff rail discloses no prospect preference. **Fixtures are labelled `TEST FIXTURE` in their own text**, on a synthetic property, with synthetic identities; no real property's knowledge was read or written. **Two guessed things were caught by running it:** `recordPersonFact` takes camelCase (`personId`/`attrKey`), and “what is our pet policy” resolves to topic **`leasing_faq`**, not a `pets` key — the first fixture set produced an empty shelf, which would have “proven” consistency by both rails agreeing on **nothing**. ⚠ **BOUNDARIES STATED:** the SMS side is entered **one layer below the Twilio webhook** — signature check, provider ack and line resolution are NOT exercised; the prospect side proves recorded preferences reach the turn, **not** a full agent turn with the model. **Unit:** identity 33/33 · shared path 65/65 · resolver 78/78 · `draft_stale_source_proof` 40/40 · 14 of 16 source-governance gates (same 2 pre-existing failures) · unit failure set identical to baseline. **⚠ SUPERSEDED BY THE 2026-09-17 DEPLOYMENT ROW.** (Was: migration 200 unreleased and production unread from here (the proxy refuses `property-spine-api.onrender.com`, HTTP 000 / CONNECT 403), so `EXPECTED_LEDGER_CEILING` must be read from the live ledger before any release. **No production write, no number action, no message to any real prospect.** |
| 116 | **HARDENED · PROVEN + MUTATION-CHECKED, 2026-09-17 — an adversarial pass on the two preceding 2026-09-17 retention rows found four real weak spots. All four are fixed and each fix is proven by a test that goes red without it.** **(1) THE REFUSAL COULD NOT SURVIVE ITS OWN RECOVERY FAILING.** `pool.connect()` for the retention sat **outside** the try. A pool that is exhausted or a database that is unreachable — precisely when a retention fails — threw from there, **replaced** the `person_identity_conflicted` error on its way out, and handed the caller a generic **500 “Could not capture the lead.”** instead of the 409 and the honest not-saved receipt. The whole pessimistic-receipt design (introduced by the 2026-09-17 retention row) was **decorative in the one failure mode it exists for.** Now `let c2 = null` inside the try, with `if (c2)` guarding rollback and release. Proven by injecting a real failure at the seam the code uses — the FIRST connect is the intake transaction and must succeed, the SECOND is the recovery — and mutation-checked: reverting it returns `500 Could not capture the lead`. **(2) A WIDELY-SHARED NUMBER DISCLOSED EVERYONE ON IT.** `rows.map(...)` was unbounded, so a corporate or family line wrote **every** matching person's id into the retained record and returned **every** name to the operator. Capped at `MAX_CONFLICT_CANDIDATES = 10`, with the TRUE total carried as `candidate_total` and the operator told `candidates_truncated` — §5: a truncated list shown as the whole list is a confident wrong answer, and a twelve-way match is a **shared line**, a different problem from two duplicate records. Proven with 12 synthetic people on one number; mutation-checked (reverting discloses all 12 names). **(3) MIGRATION 200 WOULD HAVE TAKEN A LONG EXCLUSIVE LOCK ON THE BUSIEST TABLE.** `add constraint ... check` scans the whole table under ACCESS EXCLUSIVE to prove compliance, and `comm_events` holds every message ever sent or received. Now `not valid` (lock held only long enough to record it) then `validate constraint` (scan under SHARE UPDATE EXCLUSIVE, which blocks neither reads nor writes). Enforcement for new and updated rows is identical either way. Re-applied through the real chain builder: ceiling 200, no skips. **(4) THE APP PROOF HAD THE EXACT GAP CLAUDE.md WARNS ABOUT.** Every assertion injected the lifted functions into a bare page — proving what they DO and **nothing** about whether the shipped page exposes them. The queue row calls `openRetainedInquiry` through an inline `onclick`; nested inside any of that file's ~100 IIFEs the row would throw, the lead would still be unreachable, and **every assertion would have stayed green** (the recorded precedent: a proof once called a toast function that was not on `window`, silently skipped, and reported the channel broken). The test now loads the REAL page and asks it, with the shipped `openInboundDecision` as a **control** so the check is shown to discriminate. Mutation-checked: wrapping the panel in an IIFE turns it red while the control still passes. **CHECKED AND FOUND SOUND, recorded so it is not re-litigated:** the retention is atomic with its review task (one transaction, so a failed obligation cannot leave an orphan inquiry); `/demo/intake` resolves its property through `resolveDemoPropertyRow` and **ignores any client `property_id`**, so a demo refusal cannot retain into a real property — the perimeter holds, and it holds because the existing route already enforced it. **Also:** the proof's teardown now drops the temporary CHECK §6 adds, so an abort between add and drop cannot poison later runs on the same database; the dead `e.captureFailed` flag (set, never read) is gone. **`leasing_identity_conflict_http.db.js` 54/54** (was 46) · **`retained_inquiry_dom.test.js` 25/25** (was 23) · conversational consistency 23/23 · identity 33/33 · shared path 65/65 · resolver 78/78 · `draft_stale_source_proof` 40/40 · 14 of 16 source-governance gates (same 2 pre-existing) · unit failure set identical to baseline. ****⚠ SUPERSEDED BY THE 2026-09-17 DEPLOYMENT ROW — RELEASED AND DEPLOYED.** (Was: migration 200 unreleased, production unread from this session.) No production write, no number action, no message to any real prospect.** |
| 117 | **DEPLOYED · PRODUCTION-VERIFIED, 2026-09-17 13:38 UTC — the 2026-09-16/17 leasing-conversation and retention rows are live. Migration 200 released first, then the code, both confirmed by direct observation rather than by a tool reporting success.** **THE RELEASE, IN ORDER.** Production was read before anything was changed: `/health` reported `commit_short: ea00250`, `resolved_from: render_env`, up since 2026-09-16 21:25 UTC. The ledger read returned **ceiling 199, 187 rows** — which matched the owner's independent Neon review of 2026-09-17 00:02 exactly. **That pair is a fingerprint**, and it is what confirmed the right branch and that nothing had advanced; the branch label in the console was not trusted for it. `EXPECTED_LEDGER_CEILING` was therefore **read, never assumed** — the 199 recorded in the 2026-09-16 property-line-identity row was a stale number from a different session and was not reused on that basis. **GIT-SIDE CHECK BEFORE THE DEPLOY:** `ea00250` was verified to be an **ancestor** of the candidate, so the deploy was a forward move and nothing live was reverted — 11 commits, and `200_unresolved_inquiry_evidence.sql` the **only** migration between them. **MIGRATION 200 APPLIED** to the production branch (`neondb`) at ~13:33 UTC. ⚠ It was applied as **SQL in the Neon console**, not through `migrations/migrate.js`: this session had no `DATABASE_URL`, no Neon credential and no Render key, and the proxy refuses `property-spine-api.onrender.com` (CONNECT 403), so the release was the owner's to run and the browser was the only surface available to him. The runner's own ledger format was matched exactly — `version='200'`, `name='unresolved_inquiry_evidence'` (migrate.js does `file.slice(4,-4)`, and the check on startup is **by version**, not name). What that path skips is the `EXPECTED_LEDGER_CEILING` guard, which was satisfied in substance by reading the ledger in the same editor immediately before. **VERIFIED, NOT ASSUMED.** “Committed successfully” is the editor reporting a transaction closed, not evidence the ledger row landed — a dropped `insert` would leave the objects present, the ledger at 199, and the next deploy failing on startup while everything else looked fine. One query checked all five facts: **ceiling 200 · ledger_rows 188 · column 1 · constraint 1 · index 1**. **THEN THE CODE.** API `ecfc9af4` deployed by pinned commit with auto-deploy disabled; `/health` reports `commit_short: ecfc9af`, `resolved_from: render_env`, `ok: true`, started **2026-09-17 13:38:19 UTC**. **The startup is itself independent proof of the ledger:** `prestart` runs `migrate.js` in verify-only mode and **refuses to start** naming any pending file, so a service that started is a service whose schema the code agrees with. Operator app `2e8199a` deployed alongside it — **owner-reported, not verified from here** (a static site exposes no build identity and this session cannot reach it). **WHAT CHANGED FOR PEOPLE TODAY, at the right altitude:** the contraction fix and the shared subject classifier mean a contracted property question now reaches the governed read on **both** rails, not just the web; the context resolver narrows retrieval to the shelf a question needs; and a follow-up uses preferences the prospect already gave instead of asking again. The identity-conflict retention is **live but should essentially never fire** — 0 duplicate canonical-phone groups across 997 persons — so it is a guard, not a repair, and the **104 legacy-phone-only records** are where it will first bite. **UNCHANGED AND STILL UNTESTED: delivery.** A configured line remains no evidence of provider control, registration, consent or arrival. No number was bought, ported, reassigned or republished, and no message was sent to any real prospect at any point in this release. **ROLLBACK, if ever needed:** re-deploy `ea00250`; the column may stay, since nothing else reads it. |
| 118 | **RESOLVED IN SOURCE · FIRST RED OBSERVED IN CI, REPRODUCED AND FALSIFIED LOCALLY, 2026-09-18 — a merge-only broken gate: the harness comment-stripper read `/operator/*` in server.js prose as the start of a block comment and deleted 569 lines of the file it was scanning.** CI run **668** on the integration merge `d202d632` went red in 78 seconds at `tests/unit/shared_conversational_path.test.js`: *"FAIL server.js injects the SAME action the SMS path uses"* (64 passed, 1 failed). **THE ASSERTION WAS FALSE AND THE WIRING WAS FINE.** `server.js:430` carries `conversationalApplicationAction: () => __staffLeasingAction` and is **byte-identical** on the deployed line, the integration line and the merge — verified on all three before touching anything. **THE MECHANISM.** The test's `code()` helper stripped comments in two chained passes, **block comments first**: `.replace(/\/\*[\s\S]*?\*\//g,"").replace(/\/\/.*$/gm,"")`. server.js documents its own routing in prose, and that prose contains `/operator/*`, `/admin/*`, `/tenant/*` — **19 occurrences, 4 of them in trailing `//` comments**. The `/*` inside `/operator/*` at line 87 opened a comment the regex closed at the next real `*/`, at line 656. **36,013 characters — 569 of 841 lines — vanished, the wiring line among them.** The stripped text kept 264 lines of 841. **WHY NEITHER SIDE FAILED ALONE, which is the part worth remembering.** The deployed line's server.js contains **no `*/` at all**, so the fake opener never closed and nothing was stripped: the test passed and the defect was invisible. The integration line carries a block comment that ends in `*/` (the lease-handoff note). Merging gave the fake opener a closer. **A merge can create a failure that is present on neither parent**, and no amount of reading either branch alone would have found it. CI run 664 was green with the deployed code, migration 200 and this same app pin — the only new variable was the integration line's own content. **THE FIX, and why one pass rather than reordering.** All eight harness files that scan server.js now strip in a **single alternation**: `/\/\*[\s\S]*?\*\/|\/\/[^\n]*/g`. At a `//` the block branch cannot match, so the line branch consumes the whole comment **and the `/*` inside it**. Reordering to line-comments-first was tried and **rejected by measurement**: it still failed, because four of the fake openers sit in *trailing* comments that the `^\s*//` variants never touched, and because `/* x // y */` on one line would then lose its terminator. Each site carries a comment saying the order is load-bearing, so the next identifier sweep does not split it back. **FALSIFIED BOTH DIRECTIONS on the merged tree:** before the fix 64 passed / 1 failed, after it 65 passed / 0 failed, with the only change being the stripper. **RESIDUAL, NAMED RATHER THAN PAPERED OVER:** this is still a regex and not a tokenizer. A `/*` inside a **string or regex literal** would fool it exactly the same way. server.js has none today — every one of the 19 occurrences is inside a comment — but that is a fact about today's source, not a guarantee. **NOT CHASED, recorded:** the same two-pass stripper exists at **35 further sites** under `tests/` and `tools/` (43 total, 8 fixed). They are not currently wrong because they do not scan a file whose prose contains `/*`; they are one prose comment away from being wrong, and the durable fix is one shared helper rather than 43 copies. Also observed and **not** caused by this work: `tests/scenarios/slice9_status_read_drift_guard.js` reports 63 passed / 2 failed in this sandbox both before and after the fix, and `tests/e2e/proof_boundary.test.js` cannot bind `::` here (`EAFNOSUPPORT`) — both environmental to this container, both green in CI. | `tests/unit/shared_conversational_path.test.js` (the long account lives in its `code()` helper); `tests/unit/property_line_identity.test.js`; `tests/gates/gate_activation_dormant.js`; `tests/gates/gate_ci_path_ssl.js`; `tests/proofs/operator_language_proof.js`; `tests/proofs/work_proof_photo_proof.js`; `tests/proofs/slice9_route_retirement_proof.js`; `tests/scenarios/slice9_status_read_drift_guard.js` |
| 119 | **FOUND AND STOPPED AT THE MERGE BOUNDARY, 2026-09-18 — `claude-opus/deal-reconciliation-20260915` commits `node_modules` as a SYMLINK, and git refused the merge rather than let it land.** Commit `eec7c0f4` on that branch tracks `node_modules` as mode `120000` — a symlink blob whose content is the absolute path `/home/user/property-spine-api/node_modules`. `node_modules/` is line 1 of `.gitignore`, so this was committed past an ignore rule, almost certainly by a `git add -A` in a worktree where the path was a convenience symlink. **THE MERGE WOULD NOT START:** *"The following untracked working tree files would be overwritten by merge: node_modules"* — which is the hazard demonstrating itself, because a real `node_modules` is exactly what every checkout has there. In CI the checkout lives at `/home/runner/work/...`, so the tracked symlink would land **dangling**, pointing at a path that does not exist on that machine, where `npm ci` expects a directory. **RESOLVED for the integration line:** merged the five docs and dropped the path (`git rm --cached node_modules`) inside the merge commit. Nothing under `src/`, `migrations/` or `server.js` was involved. **NOT RESOLVED on the source branch** — it is still tracked there, so the next thread that merges that branch meets the same refusal. That is the branch owner's to remove, and it is a one-line `git rm --cached`. **METHOD NOTE worth more than the defect:** the first scope measurement of both docs-only branches was WRONG. `git diff HEAD <branch>` listed 86 and 107 files including migrations and `server.js`, which reads as a large product merge. Those branches simply LACK the integration content: that diff describes what they would REVERT, not what they add. Measured against `git merge-base`, each adds only docs. **Diff a merge against its base, never against your own head.** | `.gitignore:1`; `git ls-tree origin/claude-opus/deal-reconciliation-20260915 node_modules` → `120000 blob f8b1fe27`; merge commit on `claude/main-integration-20260918` |
| 120 | **VERIFIED IN SOURCE, NOT FIXED, 2026-09-18 — `GET /deals` is reachable by anything holding the shared operator key and cannot say WHO asked.** Reported by `claude-opus/deal-reconciliation-20260915` (`docs/handoffs/new-hp/deal-reconciliation/PICKER_TRACE.md`) and **re-verified here in the merged source rather than taken on the lane's word.** `src/surfaces/property_surface.js:31` declares `router.get("/deals", async (_req, res) => {` and the module is mounted bare at `server.js:497` — `app.use('/', propertySurfaceModule({ pool }))` — with no middleware in the mount. Tracing the global gate at `server.js:213`: `/deals` is not in `PUBLIC_EXACT` or `PUBLIC_PREFIXES`, and is not `/operator/*`, team-access, `/deal-setup/*`, `/admin/*` or `/org/*`, so it falls through to the `x-operator-key` comparison. **SO IT IS NOT UNAUTHENTICATED — it is 401 without the key.** The defect is narrower and worth stating precisely: the key is a *shared bearer* credential, so the route has **no per-user identity and no property scope**. The handler's parameter is `_req` — underscored, deliberately unused — so it **cannot** read a staff session even if one were sent. The scoping is absent by construction, not by oversight of a single line. This is the class the repo already records: three of the four historical property-creation doors *"could not have answered who created a property: their authority was a shared bearer key."* It is also the distinction CLAUDE.md draws — **authentication answers WHO may call a tool, not WHERE its output may land** — and a deal picker's output is a list of deals, which is precisely a scoping question. **CONTRAST, which is what makes this a defect rather than a design:** every sibling route in `src/onboarding/deal_setup.js` (`/deal-setup/deals`, `:dealId`, `/properties`, `/available-properties`, `/source`, `/activation`) carries `requireHuman`, and the write doors additionally carry `rejectBodyActor`. The same noun is governed on one path and key-only on the other. **NOT FIXED HERE, deliberately.** Changing the authority on a route the operator app calls is app-first (Open Ruling 2): the app must send the session alongside the key before the API may require it, and this session did not confirm which picker the app actually lands on — the lane itself records that as unsettled. **NOT EXERCISED:** no HTTP call was made against this route from here, in any environment. The rung is source-reading only. | `src/surfaces/property_surface.js:31`; `server.js:213,497`; `src/onboarding/deal_setup.js:142-333`; `docs/handoffs/new-hp/deal-reconciliation/PICKER_TRACE.md` |
| 121 | **BUILT · FALSIFIED LOCALLY (pure-function rung; NOT exercised against a database or HTTP), 2026-09-18 — an unestablished leasing grain silently became 'by the unit' in six places, and on a by-the-bed building that does not shrink a number, it deletes the vacancy.** Migration 026 gave `properties.leasing_basis` three values and its own comment names the third: *"'unknown' is the honest default ... Declared basis also lets the system flag declared-vs-observed mismatches instead of silently guessing from row patterns."* Six readers then used a **two-way branch over a three-value column** — `leasing_basis === "bed" ? "bed" : "unit"` at `snapshot_loader.js:927`, `activation_service.js:266` (preview) and `:330` (apply), `source_home_identity_review.js:98`; plus `coalesce(leasing_basis,'unit')` at `activation_service.js:488`, which never even fired because the column is NOT NULL DEFAULT 'unknown'. **WHY IT IS NOT AN OFF-BY-ONE.** Under grain 'unit', `stableSpaceLabel()` returns `"(whole unit)"` for every row, so the guard that catches an unidentifiable position (`snapshot_loader.js:1032`, `label === "(bed)"`) **never executes** — no discrepancy, no refusal, control falls to the final `else` which inserts a whole-unit space. Numerator and denominator collapse **together**: a three-bed unit holding two residents and one empty bed becomes one occupied unit, and the empty bed does not become miscounted, it **stops existing**. Skyline is 160 beds in 72 units; Greenery is 105 beds in 64 units. **THE FIX:** one resolver, `src/tenancy/leasing_grain.js`, is now the single reading of the column; an explicitly supplied basis still establishes it (Deal Setup asks "by unit or by bed?"), and when nobody has answered all five governed sites refuse with copy naming the next step — *"This property has not been established as leasing by bed or by unit. Choose the property grain before establishing this rent roll."* **A report parameter is not an answer:** `Summarize By = Unit` is how Yardi arranged one export and a by-the-bed building emits it happily, so nothing here is derived from source layout. `deal_intake.js:410` is **deliberately unchanged** — its `["unit","bed"].includes()` validates a human's explicit answer before storing it, which is the correct use. **PROOF:** `tests/unit/leasing_grain_not_established.test.js`, registered in `verify_all.sh`. Witness on this tree's parent exits **1** (7 pass, 1 fail on the source gate); successor exits **0** at **8/8**. The source gate is narrow on purpose — branching on an *already-resolved* grain is correct and appears throughout these files, so only raw reads of the column or of the caller's unvalidated option are banned; a gate that fails on correct code gets deleted by the third person who hits it. **⚠ RUNG, STATED:** this is pure-function and source-scan evidence only. **No database, no HTTP, no browser.** The claim that an unestablished grain would have produced 72 and 64 positions follows from reading three functions; **it has not been observed.** The cheap way to observe it is a scratch database with `leasing_basis='unknown'` and a position count. **⚠ UNVERIFIED AND LOAD-BEARING:** the live `leasing_basis` of Skyline and Greenery was **not read** — no database access from this session. If either is not `bed`, that is a stop condition for establishing its rent roll, and one query settles it. | `src/tenancy/leasing_grain.js` (new); `src/shared/snapshot_loader.js:927`; `src/onboarding/activation_service.js:266,330,488,519`; `src/onboarding/source_home_identity_review.js:98`; `migrations/026_leasing_basis.sql`; `tests/unit/leasing_grain_not_established.test.js` |
| 122 | **BUILT · FALSIFIED LOCALLY (pure-function rung; NOT exercised against a database or HTTP), 2026-09-18 — "today" on the canonical dated reads was the SERVER's UTC day, so after ~8pm in Philadelphia the rent roll answered for tomorrow.** `dated_positions.js:777` defaulted an absent `as_of` with `new Date().toISOString().slice(0,10)`, and `rent_roll_canonical.js:65` compared `dp.as_of` against that same UTC day to decide whether a read was "today". UTC leads `America/New_York` by 4–5 hours. **Measured:** the instant `2026-08-02T01:30:00Z` is `2026-08-02` in UTC and `2026-08-01` in Philadelphia — 9:30pm on turnover night, the one night of the year when the largest number of student beds change hands. The wrong day lands on lease commencements, lease expirations, notice dates and the 1 August turnover. It also **dropped every resident balance**, because `currentRentRoll` renders balances only when the read is today, and after 8pm a read genuinely of today was judged historical. **NOTHING NEEDED INVENTING.** Migration 123 has recorded `properties.operating_timezone` since it landed, with a validating trigger and an audit table; `src/shared/property_timezone.js` already resolved it. **The dated reads simply never asked.** `propertyOperatingToday()` now joins the two and the read reports `as_of_basis` and `as_of_timezone`, so a payload that disagrees with the board late in the evening is answerable instead of merely puzzling. An explicit `as_of` is untouched and reports basis `explicit`. **NOT A REFUSAL, DELIBERATELY:** a property with no configured zone still gets the UTC day, labelled `utc_fallback`; so does one whose stored zone `Intl` rejects, and one whose timezone lookup throws. Withholding today's rent roll from every property that has not set a zone is a larger harm than the off-by-one, and the label makes the gap visible without taking the read away. **PROOF:** `tests/unit/property_operating_today.test.js`, **8/8**, registered in `verify_all.sh`. Witness on the parent exits 1 — but **six of those failures are only that the helper does not exist there**, which is a weak witness, so the suite also carries one assertion that discriminates on BEHAVIOUR and holds on any tree: the old expression applied to the turnover instant yields `2026-08-02` while Philadelphia is still on `2026-08-01`. Zones behind UTC (`America/Los_Angeles`) and ahead of it (`Europe/Berlin`) are both covered, so the fix is not quietly Eastern-only. **⚠ RUNG, STATED:** pure-function and source-scan only. **No database, no HTTP, no browser.** The fallback paths are exercised through stub pools, not a real `properties` row, and **no property's actual `operating_timezone` was read** — if Skyline and Greenery have none configured, both still take the UTC day and the fix changes nothing for them until a zone is set. **NOT CHASED, recorded:** other surfaces compute their own "today"; only these two canonical dated reads were changed. | `src/shared/property_timezone.js`; `src/tenancy/dated_positions.js:777`; `src/surfaces/rent_roll_canonical.js:65`; `migrations/123_property_operating_timezone.sql`; `tests/unit/property_operating_today.test.js` |
| 123 | **BUILT · PROVEN AGAINST A REAL SOURCE DOCUMENT (adapter + mapper exercised end-to-end; NOT exercised against a database or HTTP), 2026-09-18 — every rent roll states its own totals, and the adapter found that footer only in order to THROW IT AWAY.** `rent_roll_source_adapter.js` detected the `Total: <property>` row solely as a stop condition, so nothing downstream could compare what Spine extracted against what the file says about itself. Without that, **"all 104 rows parsed cleanly" was the entire evidence that a 105-bed building had been read** — individual rows looking valid is not evidence the SET is complete, which is the trap where green is a claim about what was measured. `parseRentRollSource` now retains `source_declared_totals` (bed count, market rent, actual rent, deposits, balances, square footage) and `prepareSource` reconciles against them **before anything can be established**, refusing (`source_totals_mismatch`, 409) rather than warning. **TWO RECONCILIATIONS, NEVER CONFLATED:** (1) *source extraction* — did Spine reproduce what this file says; (2) *canonical position* — do its claims attach to established positions. **This is only (1).** The footer comes out of the same Yardi report as the detail rows, so agreement proves **extraction fidelity and never truth about the building** — Greenery's own August roll shows unit 107 Room2 occupied under lease dates that ended in **2024**, and reproducing that exactly is the parser doing its job; the canonical dated read is what exposes the contradiction afterwards. What makes the footer worth having is that a layout read wrongly essentially never lands on the source's own totals by accident, so it catches subtotal rows read as residents, a skipped section, duplicated rows, a mis-mapped column and a future Yardi layout change — at the one moment the catch is cheap. **That generality is the point: it protects the NEXT layout, not the two files in hand.** **Money compares as integer cents** — 113,500.00 summed across 105 IEEE-754 doubles is not reliably 113,500.00, and a checksum that invents its own mismatches gets deleted. **Bed count compares against the CURRENT section only:** a footer bed count is the building's inventory, and Greenery's roll carries 14 future applicants holding no bed, so counting them would manufacture a mismatch out of a correctly read file. A layout stating no totals is **unchecked**, which is not a failed check. **PROOF:** `tests/unit/rent_roll_source_totals.test.js`, **8/8**, registered in `verify_all.sh`, over `tests/fixtures/rent_roll/greenery_1325_2026-08-31.csv` — **all 105 rows of The Greenery's August 2026 rent roll and that report's own footer, the document the lender received.** It reconciles on all seven stated figures (bed count 105 · market $113,500.00 · actual $101,200.00 · deposits $107,146.00 · other $0.00 · balance −$3,096.72 · sqft 0). Removing one resident row yields *"bed count: the report states 105 but Spine read 104"* plus four money mismatches; Skyline's hierarchical `Summarize By = Unit` export fails the bed count outright. **⚠ RUNG, STATED:** the adapter and mapper are exercised for real over real bytes, but `prepareSource`'s refusal is **wired, not exercised** — reaching it needs a retained artifact and a database, so **no HTTP and no browser**. **⚠ THE FIXTURE IS A RECONSTRUCTION:** the August rolls arrive as PDFs and the adapter accepts CSV/XLSX/XLS only, so the fixture was rebuilt from a hand extraction of the PDF. Its 105 rows reconcile to the PDF's own footer to the cent, which is strong evidence the extraction is faithful — but it is not the byte stream Yardi emitted. **Native by-room xlsx exports of exactly this shape DO exist** (`_Accounting/5.0 GPR/2025/08.2025/RentRoll08_1325.xlsx`, `RentRoll07_1417.xlsx` at 07/31/2026); August 2026 simply was not archived as one. | `src/onboarding/rent_roll_source_adapter.js`; `src/onboarding/rent_roll_source_reconciliation.js` (new); `src/onboarding/source_home_identity_review.js:111`; `tests/unit/rent_roll_source_totals.test.js`; `tests/fixtures/rent_roll/greenery_1325_2026-08-31.csv` |
| 124 | **⛔ RELEASE ORDERING — APP FIRST, NOT A PREFERENCE. FOUND BY ADVERSARIAL REVIEW OF ROW 121'S OWN CHANGE, 2026-09-18.** Row 121 makes the API refuse a rent roll on a property whose leasing grain is not established. Reviewing my own change for what it would do to a live operator found two things that turn that refusal from a guard into a trap. **(1) THE APP WAS TEACHING THE WRONG ANSWER.** `property-spine-app/index.html` rendered `<select id="dsPropBasis"><option value="">— unit —</option>…` — **the default option was labelled "— unit —" and sent nothing.** An operator who never touched it READ THE WORD "unit" ON SCREEN while the property was stored `leasing_basis='unknown'`. The screen and the database disagreed and the screen was the more convincing of the two. Before row 121 the API coerced 'unknown'→'unit', which happened to *match the label* — so the operator's wrong belief was consistent with the behaviour, and nothing anywhere contradicted it. **This is the most likely way Skyline or Greenery came to hold 'unknown', and it is a UI that implements exactly the silent guess migration 026 was written to prevent.** **(2) NOTHING IN THE APP COULD SET THE BASIS AFTER CREATION.** Traced every Deal Setup call: `POST /deal-setup/deals/:id/properties/new` is the **only** one that sends `leasing_basis`; `…/activation`, `…/read-source` and `…/establish` send `{}`, rows and artifact only. So a property created through that dropdown could never be corrected from the app — and after row 121 could never be activated either. **Deploying the API alone would strand every such property at a refusal with no control anywhere in the product to answer it.** **RESOLVED APP-SIDE FIRST** on `property-spine-app` `c8fa9e5` (branch `claude/leasing-basis-asked-not-assumed-20260918`): the default is an explicit `Choose…`, the options say what the choice MEANS ("rent attaches to the door" / "to each bedroom"), the field is required, Add refuses a blank, and the rent-roll upload screen asks the same question **when and only when** the property has no established grain, carrying the answer on `read-source` — **a body field that route already accepted** (`deal_setup.js:381`), so no API change was needed for it. An already-established property is neither asked again nor able to be overruled by the caller. `leasing_basis_asked_app.test.js` **17/17**; witness on unmodified `index.html` **5 passed / 12 failed**; whole app suite **35/35**; all 21 inline script blocks parse. **THE ORDERING IS THE RULING, and CLAUDE.md already froze it (Open Ruling 2):** *the new API may require the compatibility app; never the reverse.* **Release app `c8fa9e5` BEFORE the API branch carrying rows 121–123.** Reversing that order does not fail loudly — it produces a working screen that refuses every rent roll with no way forward, which is worse than a crash because it looks like the product simply does not work. **NOT VERIFIED:** neither change has been exercised together through real HTTP or a browser; the app proof is source-plus-behaviour over lifted functions with a stubbed DOM, not the shipped page under `elementFromPoint`. **STILL UNREAD:** the live `leasing_basis` of Skyline and Greenery. If either is already `bed`, none of this ordering bites them and the trap is only for the next property onboarded. | `property-spine-app/index.html` (`dsPropBasis`, `dsAddProperty`, `dsUploadRentRoll`, the setup render); `property-spine-app/leasing_basis_asked_app.test.js`; `src/onboarding/deal_setup.js:381`; CLAUDE.md "Before you change, gate or remove a route" / Open Ruling 2 |
| 125 | **PROVEN (real PostgreSQL 16 + real migration chain + real HTTP over a real socket), 2026-09-18 — rows 121–123 move off the pure-function rung, and the run immediately found a defect of mine that every test on this branch had reported as green.** **THE RUNTIME, built rather than assumed.** No `DATABASE_URL` and no Neon reachable from here, but `/usr/lib/postgresql/16/bin` is present: a cluster was initialised under an unprivileged user (`initdb` refuses to run as root), started on loopback:55433, given a self-signed cert so `ssl = on` matches the developer shape `gate_ci_path_ssl.js` describes, and admitted through the repo's own `tests/e2e/proof_boundary.js create` — which minted the owned database `spine_proof_f3bd1724885edcb4b0b8331f` and its nonce manifest. ⚠ Earlier threads recorded that `proof_boundary create` **cannot run in this container** because `portFree` binds `::`. That is now **stale**: the IPv6 fallback added for exactly this case fires on `EAFNOSUPPORT` and the create succeeded. Then `tests/e2e/apply_migrations.sh` built the schema from the real chain — **188 ledger rows, ceiling 200, zero skips**, which is the same fingerprint production reported on 2026-09-17 — and `server.js` ran against it, `/health` reporting `commit_short: 638c280`. **THE DEFECT, and it is mine.** `datedPropertyPositions` called `propertyOperatingToday` and **nothing imported it**. Every dated read — canonical rent roll, availability, the operator Rent Roll screen — threw `ReferenceError` on its first meaningful line, and it had been that way for five commits. **HOW IT SURVIVED, which is worth more than the fix.** The patch that inserted the call guarded itself with `assert s != o and "propertyOperatingToday" in s`. That was TRUE **because the call had just been inserted** — it proved the string existed somewhere in the file and nothing about the binding. **A check that cannot distinguish the state it is checking for from the state it is checking against is not a check.** And the unit suite agreed with it: `property_operating_today.test.js` scanned the two canonical reads AS TEXT for the same identifier, so 8/8 green against a file that could not execute. This is the repo's own *"green is a claim about what was measured"* rule, broken by me three commits after citing it. **THE CORRECTION IS NOT JUST THE REQUIRE.** The suite now drives the real `datedPropertyPositions` with a stub pool that answers the timezone query and throws a sentinel; reaching the sentinel proves control passed the call with the binding resolved. **Falsified against the exact defect: deleting the require turns the new test red while the old source scan stays green** — which is the demonstration that the scan could not tell the two states apart. Every symbol this branch introduced was then re-checked for an actual binding across all six edited files, by execution rather than by grep. **THE PROOFS, on real Postgres + real HTTP:** `current_rent_roll_reconciliation` **78 passed** · `source_home_identity_review` **54 passed** (this is the one that exercises `prepareSource`, so rows 121 and 123's refusals are no longer merely wired) · `import_retirement_resolution` **24 passed** · `canonical_onboarding_ledger` **16 passed** — **172 assertions**. `ledger_grain_reconciliation` ran 12 and passed 10; both failures are `ENOENT` on an uploaded `RentRoll07_1417.xlsx` under `/root/.claude/uploads/` from a previous session, and the failing assertion is *"the real July 31 workbook is present"*, which never reaches this branch's code. **⚠ WHAT IS STILL NOT PROVEN:** no browser rung was run, so the app change recorded in row 124 remains source-plus-behaviour over lifted functions; the refusals are proven to fire from `prepareSource` but were not driven from the operator UI; and the live `leasing_basis` of Skyline and Greenery is **still unread**. | `tests/e2e/proof_boundary.js` (IPv6 fallback, now demonstrated); `tests/e2e/apply_migrations.sh`; `src/tenancy/dated_positions.js:38-44`; `tests/unit/property_operating_today.test.js`; `tests/proofs/{current_rent_roll_reconciliation,source_home_identity_review,import_retirement_resolution,canonical_onboarding_ledger}.db.js` |
| 126 | **⛔ PROVEN BROKEN OVER REAL HTTP, NOT CAUSED BY THIS BRANCH, 2026-09-18 — the Deal Setup rent-roll upload cannot complete in the shipped app. Every upload 409s, on every property, whatever its leasing grain.** **THE API'S CONTRACT IS THREE STEPS:** upload the artifact → `POST /deal-setup/activations/:id/preview-source` (read-only; returns `rows_read`, the identity groups, and a **`source_token`**) → `POST .../read-source` carrying that token **plus `inventory_decisions`** → `…/establish`. `ingestRentRoll` calls `homeIdentity.resolveDecisions`, whose first act is `if (!source_token || source_token !== plan.source_token) throw refusal(409, "source_review_stale", …)` (`source_home_identity_review.js:366`) — **unconditional**, with no branch for a first read. **THE APP DOES TWO STEPS:** upload → `read-source` with `{rows, source_artifact_id, source_as_of_date}` → `establish`. Measured on `property-spine-app` **origin/main** (not this thread's branch): occurrences of `preview-source` **0**, `source_token` **0**, `inventory_decisions` **0**, `source_home_review_required` **0**, any identity-review surface **0**. The app has no awareness that the review exists. **PROVEN, not read:** against real PostgreSQL 16 with the real chain (188 rows / ceiling 200) and the real server over a real socket, with the property's `leasing_basis` **set to `bed` at creation specifically so this branch's new grain refusal could not fire** — `read-source` exactly as the app sends it returned **409 `source_review_stale`**, receipt *"The reviewed source or leasing basis changed. Review where its homes belong again; nothing was changed."* The same artifact through `preview-source` returned **200, rows_read 105, a source_token, 105 identity groups, `counts {needs_identity_review: 105}`.** **SO THE READ WORKS AND THE APP CANNOT REACH IT.** **WHY IT WAS INVISIBLE:** the API-side proof `source_home_identity_review.db.js` (54/54) drives preview → decisions → apply correctly, because it was written against the API's contract. Nothing exercised the APP's two-step sequence against a real server. A suite can be entirely green about a feature nobody can use — this is that, and it is the same class as the `#receipt`-under-an-overlay defect CLAUDE.md records. **ATTRIBUTION, stated because it would otherwise be assumed:** this predates rows 121–125. It was surfaced by them only because standing up a real runtime to prove the grain refusal is what first drove the app's actual request shape through the real route. **NOT FIXED HERE, deliberately.** The gap is a real feature, not a wiring slip: 105 source homes come back `needs_identity_review` on a fresh property, and the app needs a surface to resolve them plus a decision about whether "create all as new" may be one action. Auto-accepting 105 positions from a document is precisely what *"BEGINS FROM CANONICAL SPACES, never from import rows"* exists to prevent, so the interaction is the owner's call, not a unilateral one. **ALSO OBSERVED:** `previewRentRoll` returns `planReview`'s object, which does not carry the `source_totals` reconciliation row 123 added to `prepareSource`'s return — so the review screen cannot yet show *"we read your file and it adds up"*, though the refusal on mismatch still fires. | `src/onboarding/source_home_identity_review.js:366`; `src/onboarding/deal_setup.js:347` (`preview-source`), `:366` (`read-source`), `:432` (`establish`); `property-spine-app/index.html` `dsUploadRentRoll` ~30105-30160; probe transcript `/tmp/pgproof/token_probe.js` |
| 127 | **CLOSES ROW 126 · PROVEN END TO END (real PostgreSQL 16 + real migration chain + real HTTP over a real socket) WITH THE ACTUAL AUGUST GREENERY RENT ROLL, 2026-09-18.** Row 126's broken upload is fixed app-side on `property-spine-app` **`121eb63`**: the screen now does the API's three steps. Upload puts the file on record; **preview-source** returns what Spine read and a `source_token` and establishes nothing; the screen shows the file, row count, as-of, grain, how many homes Spine can place itself, which columns it used and which it read and ignored; confirming hands **the server's own `suggested_decision` back, keyed, with the token** — the browser displays the classification and does not make one. A home Spine cannot place (`suggested_decision` null) blocks the confirm and is listed, because `resolveDecisions` refuses the whole apply in that case and the operator should learn it there rather than from a 409. **THE REVIEW IS KEPT ON PURPOSE.** Uploading a rent roll is the **one-time onboarding act** for a building — after it, the rent roll is built from the work Spine captures (§42.16), and migration 179 says the same thing in its own text: *"`activations` was designed for OPENING TRUTH: one activation per property"*, with a second read already refused as `setup_already_read_source`. So the 105-home review is not friction to optimise away; it is the one moment a human looks at a document and says *"yes, this is my building"*. An earlier plan to add a bulk-accept shortcut was **dropped** for that reason. **THE FULL CHAIN, MEASURED:** preview **200 · 105 rows · token issued · 105 homes proposed · nothing established** → confirm **201 · 64 units and 105 rentable positions created** → confirm 105 staged rows **105 added, 0 refused** → establish *"Lease & occupancy established as of 2026-08-31"*. **THEN THE READ.** `currentRentRoll(property, 2026-08-31)`: inventory **105** · leasable **105** · confirmed contractual occupancy **94 of 105 (89.52%)** · `contractual_rent_trusted` **101,200** · positions contributing rent **87** · `occupied_without_known_rent` **7** · vacant **10** · `occupied_terms_not_established` **1** · unresolved **0** · contested **0**. **THE DOCUMENT STATES 105 beds, 95 occupied, 10 vacant, $101,200.00 actual.** Spine reproduces the rent **to the cent** and splits the 95 into **94 it can stand behind plus 1 it cannot** — and the split is right, which is the whole product in one number. The 7 unpriced beds are the private-room buyouts where rent sits on the sibling bed (107·2, 109·2, 215·2, 401·2, 411·2, 413·2, 415·2 — same human, two resident ids); the 1 is **unit 207 Room1, Aniya Campbell, $0.00 with no sibling carrying anything**. Both patterns were identified by hand from the PDF days earlier and **Spine found them independently through the governed path**, which is the strongest evidence in this thread that the model is right rather than merely consistent. **PROOF:** `leasing_basis_asked_app.test.js` **25/25** (was 17); two assertions from the old two-step flow were wrong once the flow was right and were **corrected rather than left passing**; whole app suite **35/35**; all 21 inline script blocks parse. **⚠ NOT DONE:** no browser rung — the app evidence is behaviour over lifted functions with a stubbed DOM, so `elementFromPoint` visibility of the new review screen is **unverified**; the run used the CSV reconstruction of the August PDF, not Yardi's own bytes; and the live `leasing_basis` of the real Skyline and Greenery is **still unread**. **⚠ STILL OWED BY THE OWNER:** a tracker cutover date (§42.11). The leasing tracker is the one source that legitimately arrives repeatedly, and while it is maintained outside Spine there are two rent rolls. | `property-spine-app/index.html` (`dsUploadRentRoll`, `dsSourceHomes`, `dsConfirmSourceHomes`, `dsDiscardPreview`, the preview render); `property-spine-app/leasing_basis_asked_app.test.js`; `src/onboarding/deal_setup.js:347,366,432`; `migrations/179_activation_source_supersession.sql`; transcript `/tmp/pgproof/three_step.js` |
| 128 | **PROVEN BY EXECUTION OF THE SHIPPED FUNCTIONS · NOT BROWSER VERIFIED, 2026-09-18 — the operator's home screen showed a fabricated 0–100 "Operating Truth" score, and one of its two inputs could never fire under any data.** `operatingTruth(pid)` started at 100 and deducted for exactly two things. Executed against the app's real shapes, a signed-in operator on Greenery read **`score 100`, green dot, "The record is complete and current — nothing outstanding."** — for a property whose `leasing_basis` has still never been read and whose rent roll only became loadable at all in row 127. That is a manufactured healthy state (§5) and a **composite silence reported as health**, which §40.7 forbids in exactly these words: composite silence is health *only if every required reader returned*. **The spend branch was dead, with three independent mismatches and no visible symptom.** It called `.filter()` on `window.__BANK_EVENTS`, which `index.html:7352-7355` builds as `{dealId: {eventId: event}}` — an object, no `.filter` — inside `try{}catch(_){}`, so the `TypeError` was swallowed on **every render**; its predicate keyed on `property_id` while events carry `deal_id`; and it looked for `confirmed_by`/`confirmed_at` where `__bankEventRepo` records a human decision in `confirmation`. Proof that it was dead rather than merely unused: confirming the seeded spend item and leaving it unconfirmed both returned `100, gaps []`. The score could therefore only ever read **better** than the truth. Second finding in the same function: the NOI input reads `window.__OFFLINE_STORE[pid].mgmtRead.noi`, a **baked fixture** keyed by the production property ids, with no session gate — §19–20 fixture-behind-a-session, in a rendered number. **FIXED app-side on `property-spine-app` `a1dd5ab`.** `operatingTruth` now **counts readers**: no reader reported → `score: null`, `established: false`, `silence: 'not_established'`, and each silent reader named; the pill reads **"Not established"** with a grey dot, the score page leads with it and lists what is silent, and `psScoreRing` draws an em dash — **not a red ring at zero, because zero is a verdict and absent is not.** The spend branch reads the real nested shape and the real field, and a `test_mode` transaction may never move an operator's score. The offline fixture is a reader only when there is no session; offline preview is unchanged (Solo still 88). **RUNG, SAID PLAINLY:** `operating_truth_silence.test.js` **34/34**, lifting and *running* the real functions and evaluating the real pill expression — not scanning source, which is how row 125's defect got past me. **Falsified** by restoring the old reader into the page: **16 of 34 go red**, including every reader-behaviour assertion. Full app suite green: 36 harnesses, 1494 passed, 0 failed. **NOT browser verified, and the reason matters: no `.browser.js` proof in the app repo has ever reached the scores surface at all** (none mentions `ps-truth`, `ps-score`, or the dash renderer), so a rendered number on the operator's home screen has never had the §33 rung that this repo's own "13/13 while the browser showed the sign-in screen" lesson exists for. That rung is owed and is its own slice. | `property-spine-app/index.html` (`operatingTruth`, new `psTruthTone`, `psScoreRing`, `psScorePageContent`'s truth branch, the dash pill, the two `unknown` styles); `property-spine-app/operating_truth_silence.test.js`; `property-spine-app/index.html:7352-7392` (the real `__BANK_EVENTS` shape and `__bankEventRepo`); `index.html:6583-6591` (`__OFFLINE_STORE`); `index.html:6633` (`_rrSignedIn`); `property-spine-app/run_harnesses.sh` output |
| 129 | **CORRECTION TO ROW 118'S RECORDED FIX — MEASURED, NOT SWEPT, 2026-09-18.** Row 118 records the remedy for the comment-stripping duplication as *"one shared helper rather than 43 copies."* **That fix is wrong as written and must not be executed as a sweep.** Measuring the copies instead of counting them: they are not one thing repeated. 20 strip line comments **anywhere** on a line vs 32 that strip only at **line start**; 5 carry a `://` guard so a URL is not mistaken for a comment and 30 do not; 12 replace the comment with a **space** and 22 with the **empty string**, which can join two tokens that were separated only by the comment; and 6 strip **SQL `--`**, not JS `//`, so they are a different language. A blind consolidation would change behaviour in roughly 34 files, and it is precisely the class of change that burned this repo at migration 159, where a blunt identifier sweep renamed an API response key and not the app that read it. **The sweep is declined, deliberately, not forgotten.** Any future consolidation must first declare which of the four axes the shared helper implements and prove each caller's behaviour unchanged, or split into the several helpers the measurement actually found. | measurement over `tests/` and `src/` comment-stripping sites; row 118; CLAUDE.md *"A rename is a contract change"* |
| 130 | **MEASURED AND EXECUTED, NOT FIXED — A DECISION IS OWED, 2026-09-18. The two LARGER numbers beside row 128's Operating Truth have the same §40.7 defect, and the screen they sit on greets a HARDCODED PERSON.** Found while fixing row 128; recorded rather than fixed because the honest repair needs an API field that does not exist and a product ruling only Kameron can give. **(a) Your Score and Deal Score read 100 from an empty read.** Executed, not inferred — lifting `scoreForDeal` and `scoreForEmployee` out of `index.html` and running them with `obligationsForProperty` returning `[]`: **`Deal Score 100, teamSize 0, roster 0`** and **`Your Score 100, owned 0`**. `scoreForDeal` says it in one line: `if(!ids.length){ dealScore = 100; }`. **And the empty read is the NORMAL case for four of the six properties.** `obligationsForProperty` composes `OBLIGATION_STORE[pid]` — a seed keyed **only** to `SOLO_ID` and `SKYLINE_ID` (`index.html:11891,11914,11927`) — with `window.__LEASING_OBS[pid]`, keyed **only** to `SOLO_ID` (`index.html:6358-6359`), plus a localStorage journal the source itself labels `source:'preview_local'`. So on Greenery, Berks, Temple Nest, 1438 and 1439 both scores read a **perfect 100 with an empty roster**, and cannot distinguish *nothing is owed here* from *the obligations reader returned nothing*. Same silence collapse as row 128, one line above it in the same function block, on the two numbers rendered largest. **CONFIRMED IN CHROMIUM the same day** (row 132's rung, swept across all seven landing properties): `Deal Score` reads **100** on Greenery, Berks, Temple Nest, 1438 and 1439 — the five with no obligation seed — and **90 / 92** on Solo and Skyline, the two that have one; `Your Score` reads **100** on all seven. The screenshot says it in the product's own words: *"Hello, Mike. Keep up the perfect performance!"* over two green 100s. **(b) The dash greets a preview identity, not the signed-in operator.** `__previewEmployeeId = 'emp_mike_griffin'` (`index.html:11810`, commented *"Default viewer: Mike (for now). No toggle."*) feeds `previewEmployee()`, the `Hello, <first>` greeting, `psViewerInitials()` in the flow-shell chrome, and `scoreForEmployee(emp.id, pid)` — so **whoever signs in is greeted as Mike and scored as Mike.** The server-confirmed identity is right there and unread: `window.__psLive.sessionMeta()` returns `{user_id, property_id}` recorded by `_recordScope` from `/operator/me`. That is §21 — the browser requests, the server decides. **WHY THIS IS RECORDED AND NOT FIXED, stated so the next thread does not redo the reasoning.** (1) `sessionMeta()` carries **no display name**, so greeting the real operator needs `/operator/me` to expose one — an API change, not an app cleanup, and not inside a rent-roll slice. (2) Whether Deal Score should **refuse** on zero obligations or legitimately read 100 ("nothing has slipped") is a product ruling about what the score MEANS, and row 128's fix was defensible only because Operating Truth claims to measure *record completeness*, where absent readers are unambiguously not health. (3) The `(for now)` comment raised the possibility that the whole dash is an intended preview stage. **CORRECTED LATER THE SAME DAY, BY EVIDENCE — see row 132's runtime. IT IS NOT A PREVIEW.** The entry gate's own comment is explicit: *"Preview is NOT a peer option here. Nothing app-side runs until a server session is accepted (see egBoot)."* `egHide()` is called from **exactly one** place, and `egBoot` reaches it by exactly two: a server-`verifySession()`-confirmed session, or a local preview door that requires `__OFFLINE_MODE` **and** hostname `localhost` or `127.0.0.1` **and** `?preview=1`, which the source states *"can never mint or imitate a staff session"* and is *"unreachable on a deployed host."* Confirmed in Chromium: the app boots to the sign-in card, and only the preview door reaches the hub. **So on the deployed host the dash is a LIVE OPERATOR SURFACE, and (a) and (b) are §19–20 and §21 defects on the live path — not a staged preview.** That removes the reason for hesitating, and the browser screenshot shows what an operator sees: *"Hello, Mike. Keep up the perfect performance!"*, **Your Score 100**, **Greenery Deal Score 100**, all green, on a property with no obligations of any kind in the read. **STILL OWED FROM KAMERON, now a narrower question:** the greeting cannot be fixed app-side alone — `/operator/me` must expose a display name — so the ruling needed is whether to add that field and give both scores row 128's treatment (a score with no reader reporting is not 100), or to withdraw the two performance scores from the dash until the obligations read is live for every property. | executed extraction of `scoreForDeal`/`scoreForEmployee` from `property-spine-app/index.html`; `index.html:11891,11914,11927` (`OBLIGATION_STORE` keys); `index.html:6358-6359` (`__LEASING_OBS` keys); `index.html:11938-11946` (`obligationsForProperty`); `index.html:11810-11811` (`__previewEmployeeId`, `previewEmployee`); `index.html:7546-7555` (the journal's own `preview_local` label); `index.html:9723,8495` (`sessionMeta`, `_recordScope`); row 128 |
| 131 | **BUILT · FALSIFIED BY EXECUTING THE REAL ROUTE HANDLER (stub pool; NOT exercised against a database or HTTP), 2026-09-18 — row 121's refusal had a hole: `/management-read` never read `properties.leasing_basis` at all. It INFERRED the grain from row shape.** `src/surfaces/management_read.js:78-82` computed **`const basis = maxSpaces > 1 ? "bed" : "unit";`** — the guess migration 026 created `'unknown'` to prevent, in the migration's own words: the declared basis lets the system *"flag declared-vs-observed mismatches instead of silently guessing from row patterns."* Three consequences, the first shown by execution: **(1)** a by-the-bed building whose beds are not materialised yet has `maxSpaces === 1`, so the read labelled its counts **"units"** while the property declared **`bed`** — the falsification run prints it verbatim: *a declared 'bed' … (got unit)*. **(2)** It could never report *not established*; every property got a noun. **(3)** This route and every other reader of the grain could disagree by construction, because one asked the column and the other asked the rows. **THE FIX IS LABELLING, NOT REFUSAL — AND THAT PROPORTION WAS MEASURED, NOT ASSUMED.** `basis` appears **exactly once** in that file after the line that computed it: in the response. No total, percentage or classification depends on it. The counts are counts of **spaces** and always were. So the read still answers; what changes is that the grain now comes from the property through `src/tenancy/leasing_grain.js` (the one reading of that column), and an undeclared grain returns `basis: null`, `basis_state: 'not_established'`, `unit_label: 'spaces'` and a `basis_receipt` that names the next step, instead of borrowing a noun. `grainCountLabel()` is added to the grain module so the count noun has **three** answers like the column has three states; **`'spaces'` is not a softer `'units'`** — it is the honest noun for *we have rows but nobody has said what a leasable position IS here*. **RUNG:** `tests/unit/management_read_grain_label.test.js` **22/22**, **executing the real express handler** against a stub pool — it asserts the column is queried, *for this property*, and that the declared basis beats the row pattern **in both directions** (declared `bed` on a one-space shape; declared `unit` on a three-space shape). **Falsified** by restoring the inference: **12 of 22 go red.** All 59 source-governance gates pass. Registered in `verify_all.sh`. **BLAST RADIUS CHECKED IN THE APP, per CLAUDE.md:** `loadProfitabilityRead` (`index.html:14146`) is the only caller, it already degrades to `null`, and **nothing in the app renders this response's `basis` or `unit_label`** — so no app change is required and none was made, and the app-first ordering ruling (row 124) does not bind here. *Source can prove a consumer exists; it cannot prove one does not* — the keys are kept and the change is additive, so an out-of-repo consumer reading `basis` sees `null` only where the property genuinely has no answer. **STILL OWED:** this route's occupancy remains a SECOND definition beside `datedPropertyPositions` — the file's own comment has said so since Step 5 (*"not a replacement of the occupancy math above (that reconciliation is a later slice)"*). This row narrows the disagreement to the counts; it does not reconcile them. | `src/surfaces/management_read.js` (header contract, the grain read, `basis_state`/`basis_receipt`); `src/tenancy/leasing_grain.js` (`grainCountLabel`); `tests/unit/management_read_grain_label.test.js`; `tests/e2e/verify_all.sh:194`; `migrations/026_*` (the `'unknown'` rationale); rows 121, 124 |
| 132 | **BROWSER VERIFIED (preview-door entry; the signed-in path is NOT covered) — and the browser immediately found a defect in row 128's own fix, 2026-09-18.** Until this row **no `.browser.js` proof in the app repo had ever reached the scores surface**: nothing matched `ps-truth`, `ps-score` or the dash renderer, so three numbers on the operator's home screen had never had the §33 rung that this repo's *"13/13 while the browser showed the sign-in screen"* lesson exists for. **WHAT THE RUNG FOUND ON ITS FIRST RUN.** Chromium, real `index.html`, clicking through to The Greenery's hub: **`● OPERATING TRUTH 100`, green dot** — while `window.__psScores.truth.silent` read **`["confirmed spend"]`**. A reader that never reported, rendered as a clean bill of health. Same §40.7 violation row 128 was written to end (*composite silence is health ONLY if every required reader returned*), just quieter: not a fabricated number, a **partial one presented as complete**. **FIXED app-side `8bc1d27`:** a reading with any silent reader is **never green** — it keeps its number, drops to brass, the pill carries a `PARTIAL` marker, and the score page says in words how many readers never reported. A bad score stays red: **silence softens a clean bill, never a verdict.** **THE RUNG ITSELF:** `property-spine-app/operating_truth_pill.browser.js`, **24/24** across **two property shapes** — Greenery (100) and, re-entered from the top, Solo (88, because Solo's NOI fixture carries `missing_source`), so the pill is proven to read its property rather than render one hardcoded number. Asserts the entry gate is gone before believing anything; **enters by clicking the real landing button** (`psOpenLandingDeal('greenery')`), never by calling a render function; scopes every selector to `#hubLayer`; reads layout-aware text; and asks the **DOCUMENT** for visibility — `elementFromPoint` at the pill's centre must return the pill. No API, no database; a static file server only. **FALSIFIED TWICE, both against a COPIED tree** (the app runner's own rule 5): restoring the old tone rule turns **3** red, exactly the partial-silence assertions; injecting a `position:fixed` full-screen overlay turns **both** `elementFromPoint` assertions red — so the visibility check genuinely catches a covered element rather than decorating the output. **⛔ SCOPE, AND IT MATTERS:** entry is through `egBoot`'s local preview door (`127.0.0.1` + `?preview=1`), which the source says *"can never mint or imitate a staff session"* and is *"unreachable on a deployed host"*. This proves the pill **renders and is visible with its real data at the preview altitude**, where `_rrSignedIn()` is false and the offline NOI fixture is a legitimate reader. **It does NOT prove the signed-in path**, where that fixture is not a reader and the pill must read *Not established*. That needs a real OTP session against a real API and is a separate slice. **NOT REGISTERED IN CI YET, DELIBERATELY.** `tests/e2e/app_pin.txt` declares `2e8199a` (`claude/determined-dirac-qcvj5v`), which does not carry this file, and `app_rung_ready` fails a run whose app checkout drifts from the pin. Adding the step now would break CI. **OWED, one commit, when the app branch merges:** move the pin to the merged app SHA and add beside the existing no-database app rung at `tests/e2e/verify_all.sh:179-186` — `step "browser: operating truth pill (app $APP_PIN_SHORT)" env CHROMIUM="${CHROMIUM:-}" SPINE_API_REPO="$ROOT" node "$APP_ROOT/operating_truth_pill.browser.js"`. The proof already reads both env vars. | `property-spine-app/operating_truth_pill.browser.js`; `property-spine-app/index.html` (`psTruthTone` partial rule, the pill `PARTIAL` marker, the score page's partial lead, `.ps-truth-part`); `property-spine-app/operating_truth_silence.test.js` (34 → 44); `index.html:12502-12510` (`egBoot`'s preview door); `tests/e2e/app_pin.txt`; `tests/e2e/verify_all.sh:173-186`; row 128 |
| 133 | **MEASURED IN THE BROWSER — the Management desk's four slots are the RIGHT four, two are honest blanks by design, and the forward number was stating a falsehood. One fix made, 2026-09-18.** Answering *"does the rent roll dash reflect current occupancy / NOI and future?"* by driving the desk in Chromium (preview door) rather than reading the source alone. **THE SHAPE IS RIGHT.** `managementConditionPanel` renders exactly four governing measures — Current occupancy · Forward occupancy · Current NOI · Trending NOI — beside a Rent Roll door ("the truth file") and a **Future Rent Roll** door ("if nothing else changes"), plus Leasing Cycle and Forward Leasing. **NOI IS NOT BUILT, DELIBERATELY AND HONESTLY.** Both NOI cells are literally `cell(label, null, note)` — hardcoded null, so they *always* read `—`: *"Current financials not connected"* and, for Trending NOI, the full definition kept visible — *contracted rent + market rent on uncovered stock at a stated occupancy, less in-place expenses* — with the source saying *"the plumbing is not [settled], and nothing here estimates it in the browser."* That is §5 done right; it is also the answer to *is NOI there*: **no, and it says so.** **⛔ THE DEFECT, IN THE NUMBER THAT MATTERS MOST.** Forward occupancy was the snapshot's `projectedOccupied / denominator`, and the denominator holds **every** position — including those `_rrForwardPosition` cannot project, which it classifies `unresolved_exposed` with conflict *'Current lease end is unavailable'*. Dividing by those turns **we cannot tell** into **not occupied** — the forward form of the §42 rule that `Open` may never be `total − occupied`. Greenery's hub rendered **`FORWARD OCCUPANCY 0.0%`** against a snapshot of **unresolved 97 · uncovered 97 · conflicts 97 · denominator 97**. In student housing *0.0% contracted* reads as *nothing is leased for next year*; the truth was *nothing here can be projected yet*. **FIXED app-side `15d8bff`:** any unresolved position makes the figure a **floor** — `≥0.0% · Contracted as of 120 days · 97 of 97 unresolved, not yet projectable` — while a fully resolved read still renders plain (no `≥` on a number that earned certainty), and only `unresolved` triggers it (a vacant position is known-empty, a notice position known-ending). The per-position classification was already correct and is untouched; `uncovered = unresolved + noticeUncovered + vacantUncovered` is a **sum of positive classifications**, already §42-compliant. Proof `forward_occupancy_unresolved.test.js` **15/15** executing the real panel and reading the forward cell only; falsified against a copied tree, **6 red**; re-rendered in the browser. **WHAT THE DEMO NUMBERS ARE, SO NOBODY MISTAKES THEM FOR THE BUILDING:** at the preview altitude the desk reads `window.__RENT_ROLL_LIBRARY`, and those fixtures are **not the properties** — Greenery **97 rows** (the building is **105 beds**), **Skyline 7 rows** (it is **160 beds**), berks 31, nest 171, tn1438 30, nest1439 21 — and **six of the seven carry no date column at all** (`solo` alone has `move_in`/`lease_end`/`move_out`). That is why Greenery shows 100% / 0 vacant / $950 avg and why every horizon reads 97 unresolved: **there is no date to project from.** `window.__RENT_ROLL_TRUTH_LIBRARY` is referenced by `_rrTruthDoc` and **defined nowhere in either repo**, so that override never fires. **NOT MEASURED — THE SIGNED-IN DASH.** `rentRollFor` returns live rows or `[]` when signed in and never substitutes a fixture, and `_rrTruthDoc` returns null outright for a session (*"a signed-in surface must never fall back to the spreadsheet"*), so the live desk reads canonical dated positions, which **do** carry terms. Row 127 proved that path delivers Greenery's 105 rows and `contractual_rent_trusted 101200`. **But no rung has yet rendered the DESK against it** — so current occupancy, the 105 row spine and a real forward percentage are unverified on the live path. That is the next rung, and it needs a session + API + database. | `property-spine-app/index.html` (`managementConditionPanel`, `renderManagement`, `rentRollStats`, `rentRollForwardStats`, `_rrForwardSnapshotAt`, `_rrForwardPosition`, `rentRollFor`, `_rrTruthDoc`); `property-spine-app/forward_occupancy_unresolved.test.js`; `property-spine-app/property-spine-data.js:1274` (`__RENT_ROLL_LIBRARY` row counts and columns); browser dumps of `rentRollStats`/`rentRollForwardStats` for Greenery and Solo; rows 127, 132 |
| 134 | **⛔ PROVEN SIGNED IN (real OTP session + owned PostgreSQL 16 + real HTTP + real Chromium), 2026-09-18 — the row spine is RIGHT and current occupancy is `total − vacant`. STOP HERE.** Row 133 measured the preview altitude; this measures the **live desk**. Greenery was built through the **governed Deal Setup path only** (upload the real August CSV → preview-source → read-source → 105 proposal confirmations → establish); organization, property shell and staff identity were SQL, **no position, space, unit or lease was written by hand**. Sign-in was driven through the real gate — type the number, click Send code, read the six digits from the carrier log, Verify. The app was **never edited**: it stays sealed to its production origin and only the TRANSPORT is redirected. **WHAT IS RIGHT.** `positions_established 105, unresolved 0`; `/operator/rent-roll/units` → **rentable_positions 105, units 64**; the browser's own `rentRollFor()` → **105 rows** and `rentRollStats().residential` → **105**. The desk renders **105 units & spaces**. `_rrSignedIn()` true, `_rrTruthDoc()` **null**, and the 97-row demo fixture is present in the page and **is not the source**. Property scope is server-derived. `contractual_rent_trusted` = **$101,200**, the source document's figure exactly. **⛔ FIRST DISAGREEMENT — CURRENT OCCUPANCY.** Four answers at one instant: `/units` **95**, `/canonical` **94 contractually_occupied** (+10 vacant +1 occupied_terms_not_established), `/operator/rent-roll`'s own summary **0** (`current_occupancy_pct: 0` beside `canonical_rows: 94`), and the desk **95 of 105 = 90%**. **The desk computes `total − vacant`** — proven by executing the app's own helpers on the live rows: `browser occupied === total − vacant → TRUE`. **THE MECHANISM:** on `/operator/rent-roll`, `row.status` carries the **resident id** (`s0004577`) for **95 of 105** rows — 96 distinct values, only the 10 vacant rows hold a lifecycle word — so `_rrStatus()` can never return `current` or `notice`, everything that is not detectably vacant falls through to occupied, **and a notice position would be counted as occupied**. The server's own summary on that route reads `occupied: 0` for the same reason. One column defect, three wrong numbers; the desk's 95 is right today only because 105 − 10 coincides with `/units`. Named instance: **`1325-107 Room2`** is counted occupied while the server has no `canonical.current` lease for it. Needs Review **0** and Not Established **0** — nothing is hiding there. **SECOND FINDING — ⚠ THE NUMBERS BELOW ARE RIGHT AND THIS DIAGNOSIS IS WRONG; CORRECTED BY ROW 141.** What I wrote was *“`/operator/rent-roll/units` IS NOT A DATED READ”* — it is dated. It echoes `as_of` and returns **95/10 at every date**, while `/canonical` and `/future-facts` move together: contractually_occupied **94 → 94 → 85 → 0** — and row 141 shows `/units` moves on exactly those numbers too, under a coarser word: its `occupied` bucket sums `contractually_occupied` and `occupied_terms_not_established`, which always total 95 here, so the split moved while the headline did not. A frozen number looks exactly like a read that ignores its date at 2026-09-18 / 2026-12-01 / 2027-02-01 / 2027-08-01, with vacant **10 at every date** (Open stays a positive classification). **THE DATED TRANSITION WORKS ON THE SERVER:** position `1325-101 Room1`, lease to 2026-12-31, reads `contractually_occupied · evidence confirmed` at 2026-12-01 and `occupied_terms_not_established · uncorroborated · contributes_trusted_rent false` at 2027-02-01. **THIRD FINDING — THE BROWSER RE-DERIVES THE FORWARD ANSWER, IT DOES NOT PRESENT IT.** The signed-in app calls only `/operator/properties`, `/operator/obligations` and `/operator/rent-roll` — **never** `/future-facts` or `/canonical` — and re-aggregates per-row `canonical` blocks with its own `_rrForwardSnapshotAt`. **In fairness, the derivation is faithful:** checked against the canonical server at three further dates it AGREES every time (94/11/1 · 85/20/10 · 0/105/95). So the failure is **structural, not numerical** — two implementations of one model, the server owning per-position facts and the browser owning the aggregation. The server also states `projections_unavailable: no_governed_pricing_or_assumptions` while the desk renders a forward percentage. **NOI UNTOUCHED AND CORRECT:** both cells are `cell(label, null, note)` in source, so they read `—` with their reasons and nothing is estimated from rent-roll data. **NOTHING IN THE PRODUCT WAS CHANGED BY THIS RUNG** — per the instruction to stop at the first disagreement rather than patch toward visual agreement. One harness fix was required to run it: `tools/browser_stack.js` wrote a 502 over an already-sent response when a CORS preflight destroyed the upstream request, taking the TLS front down on the app's first cross-origin POST (app `0c8efe1`, class 3). **ORDER OF REPAIR:** fix `row.status` first — everything downstream inherits it; then point the desk at `/canonical` or `/future-facts` for the horizon it displays; then make `/units` honour `as_of` or stop accepting it. | `docs/handoffs/new-hp/signed-in-desk/RECEIPT.md` (full evidence, endpoints, tables, screenshot); `src/shared/snapshot_loader.js:1736` (`/operator/rent-roll`); `src/identity/operator.js:1463,2435,2638` (`/canonical`, `/units`, `/future-facts`); `property-spine-app/index.html` (`_rrStatus`, `_rrIsVacant`, `rentRollStats`, `_rrForwardSnapshotAt`, `managementConditionPanel`); `property-spine-app/tools/browser_stack.js`; rows 127, 132, 133 |
| 135 | **RESOLVED ON CANDIDATE · PROVEN SIGNED IN (owned PostgreSQL 16 + real OTP session + real Chromium), 2026-09-18 — every Rent Roll row was a Person Card button, including the eleven with no person.** The gate that mattered, closed before any forward work. Four defects, each confirmed by execution in a live session *before* a fix: (1) `psRrRow` (app `index.html`) emitted `<button onclick='openPersonCard({person_id:"", …})'>` for all 105 positions — including the position that renders literally "Resident not linked"; (2) `pcOpenLiveRail` read `opts.person_id || null`, so the empty string became `null` and **the rail opened anyway** — Chromium showed `__pcLiveIds = {p:null,l:null}` with nothing thrown; (3) the same click carried `unit_id` and `lease_id`, which is a relationship id standing in for identity; (4) `rrPersonArg` probed three different field names and used a **UUID-shape regex as identity authority** — shape is not identity. Now: a row is a Person Card target only when `resident.person_id` is present, otherwise it renders as `div[data-ps-identity="not_established"]` and is not clickable; `pcOpenLiveRail` refuses at the boundary with the **specific** reason first (`relationship_id_is_not_identity` before the generic `person_identity_not_established` — ordering was itself a defect I introduced and caught); `lead_id` survives as the one governed exception because the **server** resolves it; one seam `openCanonicalPersonFromRelationship` takes `person_id` mandatorily with unit/lease/space/as_of riding as context. **No schema change was needed — the canonical row already carried `resident:{person_id,name}` or `null`; that was inspected before designing anything.** Four preview-era resolvers (`resolvePersonId`, `personSourceObject`, `pcResidentRecord`, `pcRenderResidentCard`) were wrapped in **throwing stubs** in a live session and the Person Card driven end to end: **zero hits** — they are preview-only, proven by execution rather than by reading. Also removed: "Phone (verified login identity)" as a label on an unverified number; "Verified phone" is now said only when `phone_verified_at` is a recorded fact. Proofs: `person_identity_ingress.test.js` 40/40 and the coupled `person_identity_signed_in.browser.js` 14/14, both green in the **app repo's** suite. **NOT registered in the API's `verify_all.sh`** — registering it turned CI red (MODULE_NOT_FOUND) because the declared app pin is a different app lineage; see row 139, which is the blocker and names what re-registers it. Falsified: restoring `person_id \|\| null` turns "an empty person_id is REFUSED" red; re-enabling the button with no resident turns 4 red; re-probing the legacy fields turns 3 red; restoring the phone label turns 2 red. | app `index.html` (`psRrRow`, `pcOpenLiveRail`, `pcPersonRefusal`, `openCanonicalPersonFromRelationship`, `rrPersonArg`, `rrCanonicalPersonId`, `rrNameLink`, `personField`); app `person_identity_ingress.test.js`, `person_identity_signed_in.browser.js`; `tests/e2e/verify_all.sh` |
| 136 | **ROUTING INVERSION CLOSED BY ROW 156, 2026-09-19 (no handle → no Person; the 95 existing Persons are untouched). Record kept as measured:** MEASURED ON REAL GREENERY DATA, 2026-09-18 — `resident_not_linked` is ZERO, and that is the finding, not the relief.** The expectation going in was ~95 residents with no durable Person. The truth is the inverse and quieter: **95 of 95 current leases carry a linked `persons.id`, and every one of those 95 Persons has no continuity handle at all** — `phone 0/95`, `email 0/95`, `primary_e164 0/95`, `phone_verified 0/95`; `confidence 'extracted'` 95/95; one `import_batch_id`; `resolution_kind 'created'` 95/95 with `confirmed_by` set. **This is governed, not a name match:** `ingestPerson` (`src/identity/person_ingress.js`, the only `insert into persons`) reaches `createPerson` only when nothing conflicts AND no candidate exists, under a named authority — here "operator confirmation of an activation row". The inversion worth a ruling is the **routing**: *no candidate → no review → create* — the path with the **least** evidence is the one that mints a durable human. The operator confirmed a **tenancy** row; the same click carried authority to establish a **person**. Consequence, stated plainly: Greenery's Person Spine exists on paper and **cannot recognise a single resident who texts next year**, because §0 names phone as the continuity handle and there are none. **Nothing was created or repaired to improve this count** — §24 forbids creating Persons to improve coverage and §40 forbids hiding the exception; the number is the product's actual behaviour on the real August rent roll. Resident identity establishment is **not** an operator workflow today; it is the next operational project and nothing in rows 135/137 should be read as having solved it. Measured by read-only SQL against the owned establishment of the real source document; **no production data was mutated**. | `src/identity/person_ingress.js` (read); `src/onboarding/activation_service.js` (read); receipt `docs/handoffs/new-hp/person-spine-convergence/RECEIPT.md` §23 |
| 137 | **RESOLVED ON CANDIDATE · PROVEN SIGNED IN + BROWSER, 2026-09-18 — the desk's Current and Forward occupancy are now projections of the server's dated answer, and "agreement" was not accepted as proof.** Row 134 stopped at the disagreement; this closes it. **Agreement is not convergence:** the browser's own forward classifier *agreed with the canonical server at every date tested* — 2026-12-01 → 94/11/1, 2027-02-01 → 85/20/10, 2027-08-01 → 0/105/95, server and browser identical. It was faithful and it was still **a second implementation of the model**. Signed in, both headlines now come from one new reader, `psCanonicalForward()` → `/operator/rent-roll/canonical?as_of=` + `/operator/rent-roll/future-facts?as_of=`, **proven by network log rather than by the numbers matching**; it returns `null` on failure and **never falls back** to the browser model. Current occupancy stopped being `total − vacant` and now reads `94 of 105 contractually occupied · 1 with terms not established` — unknown kept distinct from both occupied and zero. **A real bug this build's own discipline caught:** §42 asks for the effective `as_of` to be *recorded*, and recording it showed the desk reading a horizon labelled "September 1" at **2026-09-01 — seventeen days in the past** — because `_rrPlanningTargets` anchored every horizon to the **source document's** as-of (2026-08-31), and drifted (before the rent roll loaded there was no meta, so the base was today; one session produced both 2027-09-01 and 2026-09-01). A forward horizon is measured from now; an import's as-of is provenance. **Permanent display ruling (§18):** all positions unresolved → `Not projectable`; zero committed with any unresolved → `Not projectable` (*a floor of zero is not a floor*); some unresolved → `≥X%` **with the unresolved count named**; none unresolved → exact `X%`. Live at Greenery's configured horizon the desk reads *"**Not projectable** — 95 of 105 positions lack sufficient dated terms as of September 1 · nothing contracted at that date"*, which is the true picture of a student building whose leases all expire in July — not "0%" and not "≥0.0%". **Related, and the same defect class as row 131:** `_rrIsSoloContext` decided student-cycle membership with `/solo\|4233\s+chestnut/i` over the property **name** — `if (property === "Solo")` in disguise, and wrong on the facts, since Greenery and Skyline are student housing, do not match, and were denied their September date (measured before the fix: targets `30 · 60 · 90 · 120`, no September). The cycle is now configuration — `properties.lease_config` → `/operator/me.leasing_cycle`, validated server-side into `{kind, anchor_month, anchor_day, label}` — where **null means NOT CONFIGURED and renders rolling horizons, never an inferred cycle**; `/auth/sms/verify` deliberately does not also publish it, so there is one source. Hostile matrix, built in the owned runtime as **one bed with two different durable Persons sharing the name "John Smith" exactly** on sequential leases: server keeps them apart (`…441` current, `…442` successor, `conflict_state: clear`) and does **not** promote a pending lease to current; the browser answers `future_leased` not `renewed`, answers `successor_identity_unresolved` when identity is unknown on both sides (the **third** answer `_rrSameResident` returning `null` requires — "Spine does not know" is not "somebody else"), answers `unresolved_exposed` for a lease with no end date rather than vacant or occupied-forever, and raises a **conflict** on two overlapping future rights instead of first-wins. NOI untouched and blank throughout. Proofs: `forward_semantics_are_the_servers.test.js` 34/34, `forward_occupancy_unresolved.test.js` 24/24 and the coupled `forward_convergence_signed_in.browser.js` 14/14, all green in the **app repo's** suite. **NOT registered in the API's `verify_all.sh`** — same blocker as row 135; see row 139. Falsified against a copied tree: name fall-through → 9 red; property-name regex → 3 red; planning base back to the import date → 3 red; `≥0.0%` restored → 4 red; "Not projectable" removed → 5 red. | app `index.html` (`psCanonicalForward`, `renderManagement`, `managementConditionPanel`, `_rrSameResident`, `_rrForwardPosition`, `_rrIsSoloContext`, `_rrLeasingCycle`, `_rrPlanningTargets`, `rentRollCanonical`, `verifySession`); `src/identity/operator.js` (`/operator/me` publishes `leasing_basis` + validated `leasing_cycle`); app `forward_semantics_are_the_servers.test.js`, `forward_occupancy_unresolved.test.js`, `forward_convergence_signed_in.browser.js`; `tests/e2e/verify_all.sh` |
| 138 | **OPEN — FOLLOW-UP REQUIRED; ITEM (1) IS NOW CLOSED BY ROW 140, 2026-09-18 — three server readers are now legacy but still reachable, and one of them is why `total − vacant` looked right for so long.** Recorded here so the convergence in rows 135/137 is not mistaken for removal; the authenticated desk no longer depends on any of them, and each is a *different* kind of problem. (1) **`/operator/rent-roll` (legacy) ships the resident id inside `row.status`.** That is what made an occupancy derived as `total − vacant` agree with the truth — the field that looks like a status is carrying identity, so any consumer that reads `status` inherits both. The app still loads this route for row detail. (2) **⚠ WRONG, CORRECTED BY ROW 141 — I claimed `/operator/rent-roll/units` is not a dated read. It is.** What is true is that it **accepts** `as_of`, echoes it back, and returns `95 / 10` at every date — 2026-12-01, 2027-02-01, 2027-08-01, all identical, while `/canonical` moves correctly at the same dates. My conclusion from that — *“a read that accepts a date it ignores is worse than one that refuses the parameter: either honour it or stop accepting it”* — rested on the wrong cause, and acting on it would have been a **refusal or a second dated implementation, both wrong**. The date was always honoured; the *word* `occupied` collapsed two tenancy states whose sum is constant. Row 141 publishes the split instead. It also folds the terms-not-established position into `occupied 95`, where `/canonical` separates 94 + 1. (3) **— PARTLY CLOSED BY ROW 146 — `management_read.js` occupancy is a second definition of occupancy**, reachable through the app's profitability panel; its *grain* inference was corrected earlier the same day (row 131) but its occupancy is **not** converged. And (4) the ingest's identity routing measured in row 136 — *no candidate → no review → create* — deserves a deliberate ruling now that its effect on a real building is measured, rather than being inherited by the next import. None of these were changed here; scope was held to the signed-in desk. | `src/shared/snapshot_loader.js` (`GET /operator/rent-roll`, legacy); `src/identity/operator.js` (`GET /operator/rent-roll/units`); `src/surfaces/rent_roll_unit_view.js` (`unitRentRoll`); `src/surfaces/management_read.js`; `src/identity/person_ingress.js` |
| 139 | **CLOSED BY ROW 154, 2026-09-19 (pin moved to the converged app `475b3e1`; the three rungs are live again). Record kept as measured:** ~~OPEN — BLOCKER,~~ MEASURED 2026-09-18 — the API's declared operator-app pin and the app the convergence build was written against are two branches of the same repo, neither containing the other, so no pin value makes CI green today.** Found by CI going red twice on my own commits (runs 693 and 694, `MODULE_NOT_FOUND` on `person_identity_ingress.test.js`), which is the right way to find it: a rung the API registers must exist **at the declared pin**, and `tests/e2e/app_pin.txt` is the only thing that says which app version the API's proofs may assume. **First reading was wrong and is worth recording as a method note:** `git merge-base` returned *nothing* between the two branches, which reads as two unrelated histories and an alarming conclusion about which app is real. The clone is **shallow** (`git rev-parse --is-shallow-repository` → true; the pin's local history was 70 commits deep ending at a graft boundary that looks exactly like a root commit). After `git fetch --unshallow`, the merge base is app `main` `c6769ba` for both. **A shallow clone makes absence of history look like absence of relationship** — check `--is-shallow-repository` before concluding anything from `merge-base`, `rev-list --max-parents=0` or a branch comparison. **The measured facts:** declared pin `2e8199a` (`claude/determined-dirac-qcvj5v`) is **116 commits** past app main; the convergence app `2bbdb63` (`claude/leasing-basis-asked-not-assumed-20260918`) is **10 commits** past app main; neither is an ancestor of the other. The pinned `index.html` contains **none** of `psCanonicalForward`, `pcPersonRefusal`, `openCanonicalPersonFromRelationship`, `_rrLeasingCycle` or the string `Not projectable` — so the three new rungs cannot pass at the pin **by construction**, not by accident of ordering. **Moving the pin was tried and rejected, with the measurement:** the pin's lineage carries ~45 proof files the convergence lineage does not, starting with `retained_inquiry_dom.test.js`, which is registered in `verify_all.sh` and passed in CI at the pin (25 assertions) — moving the pin swaps one `MODULE_NOT_FOUND` for another and trades 45 rungs for 3. Locally, with the pin moved, `verify_all.sh` printed `2bbdb63 … checkout agrees` and then failed that rung; the pin is restored to `2e8199a` and the three registrations are **commented out in place with the reason and the re-registration condition written beside them**, which returns CI to the green it had at run 692 without dropping anything that previously ran — **confirmed: CI run 696 on `05e96c59` passed the full `verify_all.sh`**, and `git diff` from run 692's commit `cffc2a2` to `05e96c59` shows **zero non-comment changes** in `verify_all.sh` with `app_pin.txt` byte-identical, so the only difference between the two green runs is commentary. **What it would take:** a trial merge of `2bbdb63` onto `2e8199a` conflicts in **6 hunks of `index.html` and no other file** (one names `dsCanBulkCreateNew`), so convergence is tractable — but it is an app-integration decision that needs its own browser re-proof, since the G2/G3 browser evidence was gathered on the pre-merge tree and a clean merge is not proof the merged app behaves the same. Deliberately **not** done here: merging 116 commits of another lineage's app work is outside a convergence build's scope and is the pending app-main integration, not a line in a test manifest. **Consequence to hold onto:** the convergence work is real and proven (rows 135–138) but it is **not in the app the API's CI exercises**, so nothing in CI defends it yet. | `tests/e2e/app_pin.txt` (unchanged, restored); `tests/e2e/verify_all.sh` (three `step` lines commented with the reason); app branches `claude/determined-dirac-qcvj5v` @ `2e8199a` and `claude/leasing-basis-asked-not-assumed-20260918` @ `2bbdb63` |
| 140 | **RESOLVED ON CANDIDATE · PROVEN (owned PostgreSQL 16 + real HTTP, witness and successor payloads), 2026-09-18 — the rent roll's `status` column was carrying the resident's identifier, and the root is one layer deeper than row 134 recorded.** Row 134's own ORDER OF REPAIR named this **first**, because everything downstream inherits it; row 137 did step 2. **TWO defects, and the second is why the first stayed silent.** `normalizeRow` read `cleanStatus(raw.status || raw.resident_raw || raw.resident_id, section)` while `cleanStatus` ended `return s || "current"` — so an unrecognised string came back **verbatim** as the status, and on a source with no status column the third fallback won: a person's source record id became the position's lifecycle state. The empty case was no better; it manufactured `current`, a status the document never stated. **THE ROOT.** `import_source_rows.raw` is stored **normalizeRow-shaped** (the writer says so in its own comment, so the read can re-normalize it), which means the same defective normalizer **wrote** the id into every stored row and then re-read it — *ingest and read are the same defect run twice*. SQL on the governed establishment: `status = resident_id = resident_raw = 's0004577'` for **all 95** leased rows. Row 134 attributed this to the read route alone; that was incomplete. **ATTRIBUTION MATTERED AND WAS NEARLY GOT WRONG.** The first version of the fix labelled those 95 `unrecognised_source_value` — which tells an operator *the rent roll said something Spine could not read*. The rent roll said nothing: **it has no status column at all**, its header is Unit · Room · Unit/Room Type · Resident · Total Beds · … and its section header is literally *"Current/Notice/Vacant Residents"*, so **NOTICE cannot be expressed in that export**. Spine put the id there. Misattributing our own defect to their document is its own defect, so a status value **equal to this row's resident_id** now reports `identifier_in_status_field`. Equality is the test because id-**shape** alone would be a guess and a vendor's status vocabulary may look however it likes — asserted both ways. **WHAT MAY NOW SUPPLY A STATUS:** `raw.status` → `source_status_column`; `raw.resident_raw`/`raw.resident` → `resident_cell_lifecycle_word` **but only when the cell holds a recognised word**, because this vendor writes VACANT/MODEL/DOWN into the Resident column and that genuinely *is* status information; `raw.resident_id` → **never, not as a fallback, not ever**; nothing recognised → `not_established`. **Vacancy detection was the regression risk and is guarded** — it is evaluated last precisely because a stale vacancy double-lets a bed (§42) — and VACANT/MODEL/DOWN in the resident cell still classify correctly. **THE SUMMARY STOPS REPORTING A NUMBER IT CANNOT KNOW.** `occupied` is `null` unless **every** position is classified, because the bare key is what a naive consumer treats as complete — the `total − vacant` class of error being repaired here. Three bases: `source_status` · `partially_established` · `not_established`, plus `status_not_established`. §18's floor travels under its own name, `occupied_at_least`, and **a floor of zero is not a floor**: with nothing recognisably occupied it is `null`, never `0`, because `0` reads as *"nothing is leased"*. Greenery reads `partially_established` — not `not_established` — because its 10 vacant positions **are** classified; getting that label wrong in the first draft of the test is why the distinction is now asserted. Also removed: `?? status` at the end of the `resident_raw` chain, the **mirror image** of this defect, a lifecycle word leaking into a resident field. **PROOF — witness and successor as payloads, not claims**, over real HTTP against the governed Greenery establishment (105 positions, 95 leases, 95 persons, `leasing_basis bed`) on an owned disposable cluster; **reads only, nothing written to the property**: status values that are resident ids **95 → 0**; distinct status values **96 → 2**; `summary.occupied` **0 → null**; `current_occupancy_pct` **0 → null**; `occupancy_basis` **— → partially_established**; `status_not_established` **— → 95**; the 95 rows' basis **— → identifier_in_status_field**. The witness independently reproduces row 134's measurement. `tests/unit/rent_roll_status_not_identity.test.js` **47/47**, registered in `verify_all.sh`, **19 of its assertions red on the unmodified tree**; every function executed rather than scanned and every assertion written to discriminate on behaviour so it holds on any tree (row 125). **All 32 registered unit rungs green.** **INGEST BEHAVIOUR IS UNCHANGED, asserted rather than assumed:** `not_established` is in neither `OCCUPIED_STATUSES` nor `NON_REVENUE`, exactly as an id was not, so what gets established is identical, and the leased row's name, source record id, rent and lease end are each checked to survive. The `if (doc)` override that reassigns `summary.occupied` was checked for null arithmetic — it reassigns before every use, and `doc` is null on the live signed-in path. **⚠ RUNG, STATED:** no browser rung; the signed-in desk no longer reads occupancy from this route (row 137), so the visible change is to the payload, not the screen. **NOT CHASED, recorded:** the stored `raw` of Greenery's 95 rows still contains the contaminated value — the new basis makes it *diagnosable* rather than repairing it, and no production or fixture data was rewritten. Three gates are red in this container and **none is mine**: `gate_person_ingress` fails 2 of 10 identically on the witness tree and is unregistered, `gate_property_name_resolution` wants a cluster on 55433, `gate_inventory_relationship_policy.db.js` requires `HARNESS_DATABASE_URL`. | `src/shared/snapshot_loader.js` (`recogniseStatus`, `cleanStatus`, `statusFromSource`, `normalizeRow`, `summarizeRows`, `STATUS_NOT_ESTABLISHED`); `tests/unit/rent_roll_status_not_identity.test.js`; `tests/e2e/verify_all.sh`; rows 134 (order of repair), 137, 138 |
| 141 | **RESOLVED ON CANDIDATE · PROVEN (owned PostgreSQL 16 + real HTTP at four dates), 2026-09-18 — ⚠ THIS CORRECTS TWO FINDINGS OF MINE. `/operator/rent-roll/units` **is** a dated read; the word `occupied` was collapsing two tenancy states whose sum never moves.** Rows 134 and 138 both record that this route *"is not a dated read"* because it returns `occupied 95 / open 10` at every `as_of`. **The numbers were measured correctly and the diagnosis was wrong**, in the most ordinary way available: *a frozen number looks exactly like a read that ignores its date*. Both rows are annotated in place rather than left standing. **WHAT IS ACTUALLY TRUE, measured through the real dated service on the governed Greenery establishment at four dates:** `datedPropertyPositions` is correct and moves — positions with a current lease go **94 → 94 → 85 → 0** across 2026-09-18 / 2026-12-01 / 2027-02-01 / 2027-08-01, and `tenancy_state` moves with it. The `occupied` **bucket** sits at 95 at every date because it sums `contractually_occupied` **and** `occupied_terms_not_established`, and on this building those two always total 95: `94+1 · 94+1 · 85+10 · 0+95`. **The split moves; the headline does not.** **HOW IT WAS LOCATED, and the step that mattered:** the route's own comment claims it is *"the same dated position service … taken at one date"*, which my measurement appeared to contradict — so instead of trusting either, I called `unitRentRoll` and `datedPropertyPositions` side by side at four dates and then called `rentRollBuckets` **directly on the correctly-dated positions**. It returned 95/10 there too. That isolated the loss to the classifier's vocabulary rather than to the route, `unitRentRoll`, or the plumbing — all three of which were innocent, and one of which I had publicly blamed. **WHY THIS IS WORSE THAN THE STALE READ I THOUGHT I HAD FOUND.** A stale read is wrong and eventually obvious. This was a **true number under a word that means something else**: at 2027-08-01 not one Greenery position has established contractual terms and the read says `occupied: 95`. A lender or owner reading that has been told the building is full when Spine cannot stand behind a single term on it. It is §40.5's truth wall in the rent roll's own vocabulary — **`occupied ≠ contractually occupied`** — the same family as `escrow funded ≠ City paid` and `filed ≠ paid`. **THE FIX KEEPS THE TALLY CONTRACT.** `rentRollBuckets` is deliberately *"tally the decision, do not re-make it"*; the bucket decision is **untouched** and `occupied` still counts exactly what it counted, so **no existing consumer or total moves** — asserted, including a row whose recorded bucket contradicts its own `tenancy_state`, where the bucket still wins. What is added is a sub-tally of a **different recorded field** the canonical position already carries: `occupied_contractual` · `occupied_terms_not_established` · `occupied_state_unknown`, surfaced in both the property totals and the per-unit summary. **`unknown` is a real answer, not padding:** the same function's own comment records that the Rent Roll once handed it **projected rows with `basis_state` dropped**, so a projection missing `tenancy_state` lands in unknown rather than being counted as having or lacking terms — `0 lack terms` would be an invented claim (§5). `notice` and any state a later build introduces land there too, rather than quietly counting as contractual. **PROOF over real HTTP at four dates** (reads only, nothing written to the property): `occupied` **95 / 95 / 95 / 95** unchanged, beside `occupied_contractual` **94 / 94 / 85 / 0**, `occupied_terms_not_established` **1 / 1 / 10 / 95**, `occupied_state_unknown` **0 / 0 / 0 / 0** — `/units` now moves on exactly the numbers `/canonical` does, and the zero unknown confirms `positionLine` carries the field. `tests/unit/rent_roll_occupied_is_not_contractual.test.js` **23/23**, registered in `verify_all.sh`, **13 of its assertions red on the unmodified tree**. **All 33 registered unit rungs green.** One assertion was rewritten before the fix because it passed **vacuously** on the witness tree — `undefined !== 95` is true — so it now requires the field to be a `number`; that is the repo's own *"green is a claim about what was measured"* rule catching me mid-slice. **⚠ RUNG, STATED:** no browser rung; the signed-in desk reads occupancy from `/canonical` (row 137), so this changes the payload and the unit-level screen's raw material, not a rendered headline. **NOT DONE:** the four other `rentRollBuckets` consumers (`tenancy_position_read.js`, `rent_roll_institutional.js`, and the two call sites inside the unit view) now receive the sub-tally but do not yet publish it — `rent_roll_institutional.js` is the one that matters next, because it is the lender-facing surface where `occupied` at a forward date is most dangerous. | `src/tenancy/dated_positions.js` (`rentRollBuckets`); `src/surfaces/rent_roll_unit_view.js` (per-unit summary + property totals); `tests/unit/rent_roll_occupied_is_not_contractual.test.js`; `tests/e2e/verify_all.sh`; corrects rows 134 and 138 |
| 142 | **DISPROVED — DO NOT REBUILD IT, 2026-09-18 (measured over real HTTP at three dates).** I named the lender-facing institutional rent roll as the next thing to fix after row 141, on the reasoning that it consumes `rentRollBuckets` and would therefore inherit the collapsed `occupied`. **It does not, and the surface is already right.** `src/surfaces/rent_roll_institutional.js` passes `confirmed_contractual_occupancy` **through from the canonical reader** rather than computing it from the bucket, reports the bucket separately as `positions_occupied_all_bases` under a name that says so, and carries `positions_occupied_terms_not_established` beside it. Its own comment names the exact trap I was going to warn about: *“It briefly read `buckets.occupied / buckets.total`. Both halves moved, in opposite directions, under a name that says CONFIRMED CONTRACTUAL … that reports the claim-backed population to a lender as contractually confirmed.”* Rows 82 and 84 did this work, and row 84 records it having been silently reverted once by an ungraded commit — which is why it was worth **measuring rather than assuming**, in either direction. **Measured on the governed Greenery establishment over real HTTP, reads only:** `confirmed_contractual_occupancy` **94 → 85 → 0** at 2026-09-18 / 2027-02-01 / 2027-08-01, beside `positions_occupied_all_bases` **95 / 95 / 95** and `positions_occupied_terms_not_established` **1 / 10 / 95**; `trusted_monthly_contractual_rent` **101200 → 91600 → null**. **The last value is the one to notice: `null`, not `0`** — at a date where no position has established terms the lender report withholds the rent rather than printing zero, which is *missing is never `$0`* holding exactly where it matters most. **No code was written for this row.** Recorded so the next thread does not spend a slice re-deriving a protection that already exists and is already proven; the sub-tally row 141 added reaches this file and it simply does not need it. | `src/surfaces/rent_roll_institutional.js` (read and exercised, unchanged); rows 80, 82, 84, 141 |
| 143 | **RESOLVED ON CANDIDATE · PROVEN (owned PostgreSQL 16, real reader at two dates), 2026-09-18 — Ask Spine's compact standing projection answered “occupied: 95” for a date at which ZERO positions had established contractual terms, and nothing anywhere in the projection said so.** Row 141 fixed the collapsing `occupied` on the Rent Roll; `readTenancyStanding` (`src/tenancy/tenancy_position_read.js`) is the **same bucket on the conversational surface**, and it is the one §40.6 projection an entitled person reaches by texting Spine — consumed by `src/agent/ask_spine_answer.js` and registered in `gate_ask_spine_readers.js`. It is **dated** (`as_of` is a parameter, not a property of the model). **Measured before any change, on the governed Greenery establishment at two dates:** `position.occupied` **95 at both**, `standing.truth_state` **ESTABLISHED at both**, and the only unknown that moved was `occupied_positions_with_no_recorded_rent` **1 → 95** — which is the **rent** axis, not the terms axis. **The terms fact was absent from the projection entirely.** **WHY THIS IS THE MOST EXPENSIVE PLACE FOR IT.** A screen commits to an altitude and a person can open the row; **a sentence cannot be opened.** §40.4 requires a fact to carry its epistemic status **in its shape**, not to be one number a wording layer is trusted to qualify — and §40 is explicit that the model gets fluency over wording and never authority over current state. Handing a composer `occupied: 95` and hoping the sentence hedges is precisely the inversion that rule forbids. **THE FIX** publishes the sub-tally row 141 already computes: `position.occupied_contractual` · `occupied_terms_not_established` · `occupied_state_unknown`, **plus** one `unknowns.occupied_positions_with_contractual_terms_not_established` drawn from the **same tally**, so the count and the unknown cannot drift apart and then be reconciled by a reader. It is a genuinely distinct axis from the two rent lines beside it: a position can have established terms and no recorded rent, and at a forward date a recorded rent with no established terms. **Proven at two dates:** `occupied_contractual` **94 → 0**, `occupied_terms_not_established` **1 → 95**, `occupied_state_unknown` **0 → 0**, unknowns terms line **1 → 95**, with `occupied` **95 → 95** unchanged so no existing consumer moves. `gate_ask_spine_readers` **161/161**; all registered unit rungs green. **⚠ OWED RULING, OBSERVED AND DELIBERATELY NOT TAKEN:** `standing.truth_state` reads **ESTABLISHED** at 2027-08-01 with zero contractual terms, because `established` in this read means *a position has a basis*, not *a position has terms*. That is a second collapsing word on the same surface, and redefining a truth state unilaterally is exactly what row 141's lesson forbids — the ruling is Kameron's and it ripples through every consumer of `truth_state`. Named here rather than quietly changed. **⚠ RUNG — WAS NOT CI-DEFENDED; NOW IT IS, SEE ROW 144.** As written at the time: The reader's DB proof `tests/proofs/tenancy_standing_read.db.js` exists, was never registered in `verify_all.sh`, and **cannot be registered as written** because it loads `RentRoll07_1417.xlsx` from an external upload absent from the repo — the same ENOENT row 125 already recorded. What would register it: rebuild that proof on the repo's own `tests/fixtures/rent_roll/greenery_1325_2026-08-31.csv`, which is a slice of its own. **What WAS closed cheaply:** `tests/unit/tenancy_ask_spine.test.js` was also unregistered, is DB-free and green, and guards §40.8's assertion that an unentitled question never reaches a read — proved by the reader never being called rather than by inspecting the answer text. It is now registered; it was defending nothing in CI. | `src/tenancy/tenancy_position_read.js` (`position`, `unknowns`); `tests/e2e/verify_all.sh` (registers `tenancy_ask_spine.test.js`); `src/agent/ask_spine_answer.js` (consumer, unchanged); `tests/gates/gate_ask_spine_readers.js`; rows 125, 141 |
| 144 | **RUNG UPGRADE · CI-DEFENDED, WITNESS 6 RED → SUCCESSOR 127/0, 2026-09-18 — row 143 is now defended by a proof CI already runs, and getting there produced TWO method findings about falsification that are worth more than the assertions.** Row 143's fix was proven on an owned Greenery establishment and defended by nothing, because this reader's own DB proof (`tests/proofs/tenancy_standing_read.db.js`) was never registered and **cannot be** — it loads `RentRoll07_1417.xlsx` from an external upload absent from the repo (the ENOENT row 125 recorded). Rather than build a new harness, the assertions went into **`tests/proofs/canonical_occupancy_holds.db.js`**, which is already registered in `verify_all.sh`, already builds a native bed-grain property, and already imports `readTenancyStanding`, `rentRollBuckets` and `gatherFacts` — *extend or connect, do not rebuild*. Run in the runner's own owned runtime (fresh `proof_boundary create` database `spine_proof_1688c38d…`, schema from the real chain to **ledger ceiling 200**, real server on loopback, `/health` reporting `2b994b4`). **⚠ METHOD FINDING 1 — MY FIRST FALSIFICATION WAS A NO-OP AND CAME BACK GREEN.** I ran `git stash -q -- src/tenancy/tenancy_position_read.js` to revert the fix, saw the confirmation echo, re-ran the proof, and got **127 passed / 0 failed** — which I was one step from recording as “the assertions hold on the parent too, so they do not discriminate” or, worse, from not noticing. The file had **already been committed**, so `git stash -- <path>` had nothing to stash, created no stash entry, and exited 0; the echo fired and the tree never changed. `git stash pop` then said *“No stash entries found”*, which is the only reason it surfaced. **A falsification that did not change the tree is not a falsification** — verify the revert by reading the source (`grep -c` for the new symbol returned **0** once `git checkout <sha>^ -- <path>` was used) rather than by trusting a command's exit code. This is row 125's rule again in a new costume: *a check that cannot distinguish the state it is checking for from the state it is checking against is not a check.* **⚠ METHOD FINDING 2 — TWO ASSERTIONS PASSED VACUOUSLY, AND THIS IS THE THIRD TIME IN ONE SITTING.** With the reader genuinely reverted the run went **4 red**, and two of the six new assertions stayed **green**: written as a bare equality, `unknowns.occupied_positions_with_contractual_terms_not_established === position.occupied_terms_not_established` compares `undefined` with `undefined`, which is true — green about two fields that do not exist. Both now require `typeof === "number"` on **each** side, and the guard is commented as load-bearing so a later simplification cannot quietly remove it. The same shape was caught twice earlier today (row 141's `undefined !== 95`, and the `typeof` guard added there), so it is a pattern in this codebase's assertion style, not a one-off slip: **an assertion comparing two fields must prove both exist.** Final falsification: **6 red on the reverted reader, 127 passed / 0 failed with it restored.** **⚠ WHAT THIS PROOF DELIBERATELY DOES NOT PIN, MEASURED NOT ASSUMED — and the first draft overclaimed it.** The section originally asserted *“past every lease end nothing is contractually occupied”* as its discriminator. Measured on the fixture: past every lease end it reports **occupied 0 · open 0 · terms_not_established 0 · NOT_ESTABLISHED** — the positions leave **both** buckets, because these are native leases with **no confirmed opening-import claim** and a lapsed native lease leaves a position with no basis at all. So that assertion would have passed **on an empty set** while claiming to test the collapse. Greenery's 95 stay occupied-without-terms precisely because a confirmed import basis holds them there. The assertion was removed and replaced with what is real here: the sub-tally's arithmetic reconciles to the coarse count at two dates, the unknowns line is the same number as the count, occupied positions at an in-term date are contractual, and — the one worth having on this fixture — **a lapsed native lease does not become occupied-without-terms**, it leaves the set and says `NOT_ESTABLISHED`. **~~STILL OWED~~ — CLOSED BY ROW 145 in the same sitting:** reproducing the Greenery collapse in CI needed a fixture with an **accepted opening claim and no lease**. It already existed in another registered proof. The `standing.truth_state` ruling from row 143 remains owed and untouched. | `tests/proofs/canonical_occupancy_holds.db.js` (registered; six new assertions); `src/tenancy/tenancy_position_read.js` (unchanged here, falsified against); rows 125, 141, 143 |
| 145 | **RESOLVED ON CANDIDATE · CI-DEFENDED · WITNESS 5 RED → SUCCESSOR 32/0, 2026-09-18 — the collapse itself is now pinned, and the fixture that pins it shows something worse than The Greenery: `occupied` went UP while contractual occupancy went to ZERO.** Row 144 closed the *arithmetic* of the occupied split under CI but recorded honestly that `canonical_occupancy_holds.db.js` **cannot exhibit the collapse** — its beds carry native leases with no confirmed opening claim, so past every lease end the positions leave both buckets (`occupied 0 · open 0 · NOT_ESTABLISHED`) and an assertion there passes on an empty set. **The missing fixture was already in the repo and already registered.** `tests/proofs/availability_uncorroborated_claim.db.js` builds beds with an **accepted occupied opening claim** plus leases that end — the claim is exactly what holds a position occupied after its lease lapses, which is why Greenery's 95 stay in the bucket. *Extend or connect, do not rebuild*, for the second row running. **MEASURED on it, through the real reader at two dates:** `2026-07-31` → **occupied 4 = 1 contractual + 3 without terms**; `2028-01-01` → **occupied 5 = 0 contractual + 5 without terms**. **The sharpest fact of this whole thread is in those two lines.** On Greenery the coarse count sat still at 95 while the split moved; here it **RISES, 4 → 5, at the exact moment contractual occupancy reaches zero.** A reader watching `occupied` alone does not merely miss a deterioration — they watch the building appear to *improve* as Spine loses the ability to prove a single term on it. That is the strongest statement available of why `occupied ≠ contractually occupied` is a truth wall (§40.5) and not a labelling preference, and it is why the split had to travel with the count on the conversational surface (§40.4) rather than be left to a wording layer. **FIVE ASSERTIONS ADDED, all in the registered proof:** the split reconciles to the coarse count and agrees with the unknowns line at both dates (both sides required to be `number`, per row 144's vacuity finding); an accepted claim without a lease is occupied **without** established terms while a leased bed beside it is contractual, so `occupied` alone cannot tell them apart; past every lease end the claims **still read occupied** and **none** is contractual — **non-vacuous, because `occupied` is 5 there, not 0**; and `occupied` rose while contractual fell to zero. **FALSIFIED PROPERLY, using the method row 144 had to learn:** the reader was reverted with `git checkout <sha>^ -- <path>` and the revert **verified by reading the source** (`grep -c` → 0) rather than by trusting a command's exit code. **5 red on the reverted reader, 32 passed / 0 failed restored.** The witness payloads are themselves the evidence: they show `occupied 4` then `occupied 5` with no field anywhere saying contractual had gone to zero. **RUNTIME:** a second owned `proof_boundary create` database, schema from the real chain to **ledger ceiling 200**, real server on loopback:3321, the proof's own 27 pre-existing assertions still green throughout; all **34** registered unit rungs green; owned databases dropped and servers stopped afterwards. **STILL OWED, unchanged:** the `standing.truth_state` ruling from row 143 — it reads `ESTABLISHED` at a date with zero contractual terms, because `established` means *has a basis* there, not *has terms*. Row 145 does not touch it; on this fixture it reads `PARTIALLY_ESTABLISHED` at both dates, which is honest for a mixed property but does not resolve the vocabulary question. | `tests/proofs/availability_uncorroborated_claim.db.js` (registered; five new assertions); `src/tenancy/tenancy_position_read.js` (unchanged here, falsified against); rows 141, 143, 144 |
| 146 | **RESOLVED ON CANDIDATE · WITNESS 10 RED → SUCCESSOR 21/0, 2026-09-18 — the fourth occupancy definition counted VACANCY AS A LEFTOVER, so a bed somebody had already signed for was advertised as the fastest revenue to recover.** Row 138 item (3) named `management_read.js` as a second definition of occupancy; this is what was actually wrong with it, and it is worse than a disagreement about a number. The loop classified `occupied = !!r.cur_lease_id && !isDownModel` and then ended **`else { vacant++; vacantList.push(...) }`** — **vacancy was everything that was not detectably occupied.** That is precisely the subtraction §42 forbids and that the canonical reader was corrected for, in its own words: *“NOT `positions.length - occupied`. That subtraction was the defect: it swept committed, contested and unreconciled beds into Open because they were not Occupied.”* **THE PRODUCT HARM, which is why this is not a labelling nit.** A bed with a future lease and no current one is **spoken for**. It landed in `vacant`, and therefore in the `focus` card this same read renders as *“N vacant beds — ~$X/mo at market”* with the detail *“Empty beds are the fastest revenue to recover”*, **and its market rent was added to `dollars`** — the figure that tells an operator how much revenue leasing those beds would recover. So the surface that directs revenue effort pointed at beds that were already let, and overstated the recoverable money by their rent. **An all-committed building raised that card with every bed in it** — measured: `vacant 2`, focus `["vacancy"]`, at a property where every bed is signed. **WHY GREENERY COULD NOT HAVE CAUGHT THIS, measured over real HTTP first:** `occupied 95 · vacant 10 · 90.5% · vacancy card “10 vacant beds — ~$9,500/mo”` — all **correct**, because Greenery has **zero** future commitments, zero needs-review and zero unestablished positions, so the remainder happened to contain only genuine vacancies. **A remainder is not wrong until something lands in it**, which is why this rung is a constructed stub-pool test driving the REAL express handler rather than a fixture read, and why the real building was measured before and after rather than reasoned about. **THE FIX** makes `committed` a positive classification — no current lease **and** a future lease → committed, never vacant, never in the vacancy card, never in the recoverable-rent total — and publishes it, so `occupied + committed + vacant + commercial + down + model === total_spaces` **by construction** and is asserted. `down`, `model` and `commercial` keep their exact prior meanings. **Greenery after the fix is byte-identical except the new `committed: 0`**, and its vacancy card is unchanged — the regression check that matters, run over real HTTP on the governed establishment. `tests/unit/management_read_vacant_is_not_a_remainder.test.js` **21/21**, registered in `verify_all.sh`, **10 of its assertions red on the unmodified reader**; all **35** registered unit rungs green; the same three container-local gates red as before and each confirmed pre-existing. **⚠ A FOURTH VACUOUS ASSERTION, caught the same way as rows 141 and 144.** The first draft looked for the vacancy list under guessed keys (`vacant_units`, `vacancies`, `vacant_list`), got `[]` because **no such key exists** — `vacantList` is reduced into `vacantMarket` and surfaced only through `focus` — and one assertion *passed* on that empty array. Both now read `focus` by name, with title and dollars assertions that cannot pass on an absent key. Four occurrences in one sitting of the same shape: **an assertion that cannot tell “absent” from “correct” is not an assertion.** **~~FOUND, NOT FIXED~~ — FIXED BY ROW 148, and my reason for deferring it was wrong:** `upcoming_pct = (occupied + commercial + futureCount) / revenueSpaces` counts a **renewal twice** — a bed with both a current and a future lease is in `occupied` and in `futureCount`, so a fully renewed building can exceed 100%. I deferred it as needing a ruling on what *upcoming* means. **That was the wrong call** — under either reading a renewed bed is one bed, so the arithmetic is fixable without the semantics. Row 148. **~~STILL OPEN from row 138 item (3)~~ — CLOSED BY ROW 147, WHICH ALSO CORRECTS THIS SENTENCE.** I wrote that this read's occupancy is a *separate derivation* that *cannot distinguish* contractually occupied from occupied-without-terms, implying the two derivations disagree. Measured: **they agree on the coarse count** (both 95 on Greenery). The gap was a **missing distinction**, not a contradiction — see row 147, including the category error in my own wording. | `src/surfaces/management_read.js` (classification loop + `occupancy.committed`); `tests/unit/management_read_vacant_is_not_a_remainder.test.js`; `tests/e2e/verify_all.sh`; rows 131, 138, 141 |
| 147 | **RESOLVED ON CANDIDATE · WITNESS 4 RED → SUCCESSOR 26/0, 2026-09-18 — row 138 item (3) is closed, and measuring it DISPROVED MY OWN FRAMING: the two occupancy derivations AGREE, and the gap was a missing distinction rather than a contradiction.** Rows 138 and 146 both describe `management_read.js` as holding *a second definition of occupancy* that *disagrees* with the canonical reader — row 146 says in as many words *“they differ by exactly one bed”*. **That was wrong, and it was wrong in the exact way this whole line of work exists to prevent.** Measured over real HTTP on the governed Greenery establishment: this surface reports `occupied 95`; the canonical dated reader reports `occupied 95` — **they agree on Greenery**, `agrees_with_canonical: true` — alongside `occupied_contractual 94` and `occupied_terms_not_established 1`. **⚠ CORRECTED THE SAME NIGHT, BY OBSERVATION: that agreement is a property of Greenery, not of the code.** On the claim-backed fixture (`availability_uncorroborated_claim.db.js`, rebuilt in an owned runtime) this surface reports `occupied 2` (rows with a `cur_lease_id`) while the canonical reader reports `occupied 4 = 1 contractual + 3 without terms` — **`agrees_with_canonical: false`**. The two derivations agree on lease-backed buildings and diverge on claim-backed ones by exactly the claim-only positions, and the field says which. Recording a Greenery observation as a general truth was *agreement is not convergence* (§42) caught on me a second time, and the consultant's ruling to leave `management_read`'s derivation in place was made on my over-general sentence — the ruling still stands on the corrected fact, because the disagreement is now published rather than silent. **My “disagreement of one bed” was me comparing `occupied` against `contractually_occupied`** — the category error `occupied ≠ contractually occupied` (§40.5) names, committed in my own description of the defect *while fixing that defect in three other files*. It is recorded this prominently because it is the argument for the truth wall: the two words are so easy to slide between that they slid inside the sentence explaining why they must not. Rows 138 and 146 are annotated in place rather than left standing. **WHAT WAS ACTUALLY MISSING** was a contractual number — this surface had none at all. **THE FIX FOLLOWS ROW 142'S PRECEDENT RATHER THAN INVENTING ONE:** the lender-facing report was measured already doing this correctly, passing the canonical figure through under a name that says CONFIRMED CONTRACTUAL beside the coarser bucket under `positions_occupied_all_bases`. So `occupancy.canonical` now carries the canonical dated block — `occupied`, `occupied_contractual`, `occupied_terms_not_established`, `occupied_state_unknown`, `open`, `activation_pending`, `needs_review`, `not_established`, and its `as_of` — beside `agrees_with_canonical`. **No existing number changed**, so nothing reading this surface moves; Greenery's own counts are byte-identical to row 146's. **THE FAILURE PATH IS THE CAREFUL PART.** The canonical read is wrapped: a throw, or a property with no established positions, yields `canonical: null` **with a named `canonical_unavailable_reason`**, and `agrees_with_canonical: null` — **never `true`**, because Spine compared nothing. `READ_FAILED` is not agreement and is not `NOT_ESTABLISHED` (§40.7), and a presentation layer must not become the thing that decides a contract is broken — the rest of the payload is served unchanged. **⚠ “Asserted on both branches” was an overclaim and is withdrawn:** the registered stub-pool test cannot serve the canonical reader's queries, so only the `null` branch executes there — measured at **zero** runs of the populated-branch assertions. The populated branch is proven over real HTTP instead: `true` on Greenery, `false` on the claim fixture. Making the populated branch reachable in the DB-free test needs a stub that answers `datedPropertyPositions`'s queries, which is owed and not done. **⚠ AND ONE MORE HARNESS LESSON:** the first version of the witness **threw** instead of reporting, because it dereferenced `o.canonical.occupied_contractual` when the block was absent. A harness error is a worse outcome than a red assertion — it stops every later check from running — so an absent block now reports red by name and the remaining assertions still execute. `tests/unit/management_read_vacant_is_not_a_remainder.test.js` **26/26**, registered; **4 red on the tree without the block**; all registered unit rungs green. | `src/surfaces/management_read.js` (canonical read + `occupancy.canonical` / `agrees_with_canonical` / `canonical_unavailable_reason`); `tests/unit/management_read_vacant_is_not_a_remainder.test.js`; corrects rows 138 and 146; follows row 142 |
| 148 | **RESOLVED ON CANDIDATE · WITNESS 3 RED → SUCCESSOR 34/0, 2026-09-18 — a fully renewed building reported 200% upcoming occupancy, and I had wrongly filed this as needing a ruling before it could be fixed.** Row 146 found that `upcoming_pct = (occupied + commercial + futureCount) / revenueSpaces` counts a **renewal twice**, because `futureCount` counts every row carrying a `fut_lease_id` including one that also has a current lease — which is precisely what a renewal is. I recorded it *“found, not fixed”* on the reasoning that changing what **upcoming** means is a product question. **That reasoning was wrong, and naming why is the point of this row: I had conflated a semantic question with an arithmetic one.** Under *either* reading of upcoming — “beds with someone in them or coming” or “beds spoken for” — **a renewed bed is ONE bed**, and a percentage above 100% of the leasable denominator is wrong under both. The semantics stay genuinely open and are still Kameron's; the double-count never needed them. **MEASURED, not reasoned:** two renewed beds → `upcoming_pct 200`; one renewal + one committed-and-empty + two open → **75%** where two of four beds are held. **THE FIX NEEDED NO NEW CONCEPT, because row 146 had already created it.** `committed` is *future without current* **by construction**, so `occupied + commercial + committed` is already the deduplicated set of held beds — a renewal sits in `occupied` and cannot also sit in `committed`. **No clamp and no `max()`**: the numerator is a set of beds counted once, which is why the cap holds for a reason rather than by truncation, and the mixed control (50%, not 100%) proves the cap was not achieved by clamping. `future_leases` deliberately keeps `futureCount` untouched — it counts **leases** and is correct as a lease count. **Greenery unchanged over real HTTP:** `occupied 95 · vacant 10 · committed 0 · current 90.5% · upcoming 90.5%`, identical to row 146, because it has no future commitments; `canonical.occupied 95 / contractual 94`, `agrees_with_canonical true`. **34/34** registered, **3 red on the unmodified arithmetic**, all **35** registered unit rungs green. **~~RECORDED, NOT FIXED~~ — FIXED BY ROW 149, and it was NOT merely prose:** `riskReason` compares `futureCount` (a lease count) against beds open-or-expiring. The same ratio drives `riskLevel`, which drives a focus card's **severity** — a published judgement, not a sentence. Worse, **row 146 introduced an inconsistency in it.** Row 149. **⚠ A FIFTH INSTANCE OF THE SAME METHOD FAULT, this time in my own tooling:** the CI watcher I armed used the shell glob `*"f0427a38"*"status=completed"*` against a two-line output, so it matched the **other** commit's completion and reported the run finished while it was still in progress. A check that cannot distinguish the state it is checking for from the state it is checking against is not a check (row 125) — which today has appeared as a vacuous assertion (141, 144, 146), a no-op falsification (144), and now a glob spanning two records. It is not a coding slip; it is the default failure mode of *verifying by pattern instead of by identity.* | `src/surfaces/management_read.js` (`upcomingHeld`); `tests/unit/management_read_vacant_is_not_a_remainder.test.js`; corrects row 146's deferral |
| 149 | **RESOLVED ON CANDIDATE · WITNESS 2 RED → SUCCESSOR 39/0, 2026-09-18 — the leasing-risk card said leasing was fine while a bed sat unlet with nothing signed for it, AND THE INCONSISTENCY WAS INTRODUCED BY MY OWN ROW 146 FIX.** Row 146 recorded `riskReason` comparing `futureCount` (a **lease** count) against beds open-or-expiring, filed as *“prose rather than a published number”*. **Both halves of that were wrong.** The same ratio drives `riskLevel`, which drives a focus card's **severity** and the line *“turn is outrunning leasing”* — a published judgement. And the defect is not the units alone: `coverage = futureCount / (vacant + expiringSoon)` **was self-cancelling while a committed bed sat inside `vacant`** — it appeared in both halves. **Giving `committed` its own bucket in row 146 took it out of the denominator and left its lease in the numerator**, so a future lease could be credited against a bed it has nothing to do with. **I broke this ratio while fixing the vacancy remainder, and did not notice until I went back for the line I had dismissed as prose.** **MEASURED:** one committed bed beside one genuinely open bed reported `risk: low` — *“1 future signed covers most of the 1 beds open or expiring soon”* — while the open bed had **nothing** signed for it. **THE FIX IS A UNITS CORRECTION, the same argument as row 148 and again needing no ruling:** the denominator is beds still needing somebody (genuinely open, or expiring within 90 days), so the numerator must be future leases **on beds in that set**. A future lease on a bed with no current lease makes that bed `committed` and removes it from the gap entirely, so it may not also be credited elsewhere; what *can* cover a bed in the gap is a **renewal** — a future lease on a bed whose current lease is expiring. That is the whole numerator, and the three prose branches now report the number actually used rather than a different one. **CONTROLS, so the fix is not just “stop counting”:** two beds expiring with one renewal signed → `watch` and *“only 1 future leases signed”* (a genuine renewal IS credited); every expiring bed renewed → `low`. Both were green before and after, which is what makes the two reds meaningful. **One date predicate now serves both `expiringSoon` and the numerator**, so the count and the coverage cannot drift about which beds are expiring. **Greenery unchanged over real HTTP** — it holds no future leases, so the old and new numerators are both **0**: `risk high · “10 beds open or expiring within 90 days but only 0 future leases signed — turn is outrunning leasing”`, with `occupied 95 · vacant 10 · committed 0 · current 90.5% · upcoming 90.5%`. Honest for a student property in mid-September whose beds turn in August. **39/39** registered, **2 red on the unmodified ratio**, all **35** registered unit rungs green. **THE LESSON, and it is the one worth keeping from this pair of rows:** a fix that moves an item between buckets can silently break every ratio those buckets feed. Row 146 changed what `vacant` means; two dependent metrics (`upcoming_pct`, `coverage`) were computed from `vacant`'s old meaning, and **neither was named in row 146's own “not fixed” note because I had not looked for them.** After reclassifying anything, enumerate what consumed the old classification — by search, not by memory. **THAT SEARCH WAS THEN RUN, and its scope is stated so the next reader knows what it covered:** every non-comment reference to `vacant` in `management_read.js`. It found two more consumers and **both are already correct**: `vacantMarket` reduces `vacantList`, which committed beds no longer enter (asserted by row 146's `dollars` check), and the `vacant === 0 && expiringSoon === 0` branch reports *“Fully leased, nothing expiring in 90 days”* for an all-committed building, which is accurate for a card about **leasing pace** — leasing there is complete, and the occupancy block beside it already says nobody has moved in (`occupied 0`, `committed 2`). **Scope not covered:** consumers of this route's payload outside this file, which is the app, and the app lineage is the separate blocker rows 139/143 name. | `src/surfaces/management_read.js` (`expiresWithin90`, `coveringFutures`, three `riskReason` branches); `tests/unit/management_read_vacant_is_not_a_remainder.test.js`; corrects row 146; **the arc of rows 140–149, the five self-corrections and the decisions owed are in `docs/handoffs/new-hp/rent-roll-repair/RECEIPT.md`** |
| 150 | **DISPROVED · DB + HTTP ON AN OWNED RUNTIME, GENERIC WHOLE-UNIT FIXTURE, 2026-09-19 — Spine does NOT yet have one closed operating loop from a tenancy ending to the next resident's occupancy; it has one truth path with three seams that break.** Audited the six seams (Rent Roll → Availability → Leasing → Commitment → Turn Priority → Work/Readiness → Move-in) by walking ONE position through the real services (`openTurnover`, `availabilityRead`, `resolveApplicationTarget`, `rankTurnPriority`, `readNextCommittedMoveIn`, `confirmTriage`, `confirmScope`, `recordWalk`, `attemptEconomicTenancyActivation`) plus the legacy `POST /turnovers/:id/ready` over real HTTP. **23 seams hold, 8 break.** HOLDS: possession-not-returned refuses; turn's expected date is the one governed future date and leasing consumes it; move-in before it refused; committed interval refused; the locked future lease reaches Turn Priority and maintenance's move-in reader with no write; severe triage on a committed unit raises `protect_next_move_in`; certification closes the turn and leaves priority; physically-ready-but-committed refused; the commitment becomes `activation_pending` then `contractually_occupied` with no reclassification. **BREAKS, ranked: (1) a governed move-out leaves NO vacancy fact** *(closed by row 151)* — `positionBasis`/`evidenceState` (`dated_positions.js:173/:646`) establish vacancy only from an opening `vacant` claim and never read the recorded move-out or the closed turn, so after the turn closes the position reads **Occupied** from the stale opening claim (unit 102) or **occupancy unknown** on a lease-only basis (unit 105) — forever. The owned Greenery copy has 95 active leases, 0 possession events, 0 turnovers: every Greenery turn will land in state 102. The rent roll also counted a vacated, turning position as `occupied` while availability said `turnover_required`. **(2) A turn slip is invisible to leasing** *(closed by row 152)* — `turnovers.ready_date` has no writer between open and close (`turnover_service.js:134`, `turnovers.js:203`, `readiness_service.js:64` are the only three); after a severe long-lead triage the row still says `expected` on the old date and a prospect can still be promised it. **(3) Three readiness truths** *(closed by row 153)* — the legacy ready route (shared operator key) closed a turn with zero certifications over HTTP; the move-in `readiness` obligation in `movein.js` is what feeds `unit_ready` into delivery and certification never feeds it. **Smallest changes recommended (none built):** read-side later-fact rule for a turnover naming the position's lease (basis `turnover_recorded_move_out`, evidence `governed_by_later_fact`), carry `last_possession_end` on the dated projection; downgrade `availability_confidence` to `incomplete` when triage reports severe/long-lead on an open turn, plus a governed re-statement of the expected date on the existing move-out door; guard the legacy ready route on a live certification and have `finishReady` satisfy the delivery `unit_ready` input. Recorded not fixed: `vacant_turning` outranks `committed_future` in the classifier so a committed turning position is labelled `turnover_required` (refusal still held); `management_read.js:379` relays the classifier's `ready`-means-no-turn default. Receipt with the full path through the files, the proof and its output: `docs/handoffs/new-hp/operating-loop-audit/`. Not run against Skyline or Greenery; no production data, phone configuration or prospect communication touched. |
| 151 | **RESOLVED ON CANDIDATE · WITNESS 10/10 (defect present) → SUCCESSOR 19/19 · REGISTERED, 2026-09-19 — a governed move-out is now a vacancy fact, closing row 150's break (1).** `positionBasis` and `evidenceState` (`src/tenancy/dated_positions.js`) could establish vacancy only from an accepted opening claim of `vacant`; a resident who left through `turnoverService.openTurnover` left a position that read **Occupied** (stale opening claim) or **occupancy unknown** (lease-only basis) once the turn closed, forever. **The fix is reads only, no schema:** `loadSpaceRows` (`src/tenancy/space_position.js`) now carries `move_out_turnover` — the latest turnover whose **outgoing lease sits on this space**; `classifyPosition` attaches it **dated** (`_move_out_turnover`: only once `opened_on <= asOf` and the named lease no longer governs the date); `movedOutByDoor()` in `dated_positions.js` treats it as a later fact — it establishes `basis_type = turnover_recorded_move_out`, governs an older `occupied` claim (`evidence_state = governed_by_later_fact`), reads `vacant` on the tenancy axis, and yields to a **newer** opening claim (as-of on/after the door) and to an `unreconciled` claim. Vacancy is still evaluated last: a current lease, a commenced lease, an unclassifiable lease, recorded possession all still win; a lease merely expiring establishes nothing; a turn-only door (no outgoing lease) establishes nothing; bed 1's move-out says nothing about bed 2. New reason code `MOVE_OUT_RECORDED_NO_LATER_BLOCKER`; `contractualTermsState` reads `not_applicable` after the door; the dated projection now carries `last_possession_end` and `move_out_turnover`. **Measured on the audit fixture after the fix:** unit 102 `open/vacant`, availability `not_ready_confirmed(severe_condition_confirmed)`; unit 105 `open/vacant`, availability `readiness_unknown(initial_inspection_pending)` — offerable once readiness is settled, never Occupied. **Proof:** `tests/proofs/vacated_position_basis.db.js` (real turnover writer, real turn closer, dated read before the door, newer-claim control on a second property), registered in `verify_all.sh`. **Regressions green on the owned runtime with the server restarted on the fixed tree:** 9 unit suites, governance suite, Ask Spine gate, `opening_claim_identity` 17/17, `opening_claim_relay_edges` 12/12, `opening_claim_unattached` 14/14, `availability_readiness_axis` 13/13, `canonical_occupancy_holds` 127/0, `availability_uncorroborated_claim` 32/0, `turnover_sibling_cache`, `availability_turn_date_scope`, `triage_work_scope`, `current_rent_roll_reconciliation` 78/0, `application_inventory_hold` 40/0, `application_to_lease_journey` 25/0, `turnover_service_proof` 12/0. **Two proofs fail identically on the unmodified tree in this reused proof database and are not regressions:** `onboarding_space_availability` (deal status `not_established` vs `current`) and `rent_roll_occupancy_correction` (47/52, `inventory_identity_review_required` on a colliding source home); CI runs both on a fresh database. **CORRECTED THE SAME NIGHT (found by re-running the audit fixture through move-in after rows 151–153):** `movedOutByDoor` governed the evidence axis even where a stronger fact had already answered the basis — a vacated bed whose incoming lease had **commenced** read basis `commenced_lease_pending_activation` while evidence said the door governed, and `rentRollExplain`'s later-fact branch then read `cur.lease_id` on a current lease that was not there: **the dated read threw**. CI 711–713 were green because no registered proof covered door + commenced lease. The predicate now yields to a contest, a current or commenced lease, an unclassifiable spanning lease and recorded possession in every reader (one predicate, one answer), the explanation no longer assumes a lease in that branch, and the case is pinned in `vacated_position_basis.db.js` (now 21/21). Full regression set re-run green after the correction; the audit fixture re-run reads 29 hold / 2 break, both the labelling notes row 150 recorded (`loop_audit_after_151_153.out`). Not run against Skyline or Greenery. |
| 152 | **RESOLVED ON CANDIDATE · WITNESS 7/7 (defect present) → SUCCESSOR 21/21 · REGISTERED, 2026-09-19 — when a turn slips, leasing sees it, closing row 150's break (2).** `turnovers.ready_date` had no writer between move-out and close, so after a walk confirmed a dead HVAC the availability row still said `expected` on the old date and `resolveApplicationTarget` kept promising a move-in on it; nothing signalled leasing unless a committed move-in already existed. **Two reads and one governed writer, no schema:** (a) `scheduleAtRisk()` is extracted from `unit_triage_service.confirmTriage` (the same severe-or-long-lead test that raises `protect_next_move_in`) and exported; `availability_read.js` carries `initial_condition`, `long_lead_count` and `confirmed_at` on the triage overlay and `ready_date_stated_at` on the turnover (move-out instant, or the latest `turn_ready_date_restated` event); `turnPlanExceeded()` says the plan is exceeded when an at-risk finding post-dates the instant the date was last stated, and `availableFrom` then keeps the date but returns `availability_confidence: incomplete`, `blocking_fact: turn_scope_exceeds_plan`, and the row's `turnover.plan_state` reads `exceeded`; the application authority's existing rule refuses (`application_ready_date_not_governed`), no new rule. (b) `turnoverService.restateExpectedReady()` and `POST /operator/units/:unitId/turn-target` (same management session gate as the move-out door) re-state the date on the open turn **with a required reason**, write a `turn_ready_date_restated` event, move the move-out obligation's `due_at`, and restore `expected` — the manager has priced the finding in; a NEW finding after that exceeds the plan again. Ordinary turn work (findings without a long lead, required items) is the plan, not a slip; a finding confirmed before the date was stated was already priced in; no walk yet stays `expected`. `ready_date` keeps one meaning per status by convention: expected while `in_progress`, actual once closed. **Proof:** `tests/proofs/turn_slip_visible_to_leasing.db.js` — real turnover, triage, availability, application authority, Turn Priority and the new door over loopback HTTP (refusals: no reason 400, anonymous 401, no open turn 409); registered in the server block of `verify_all.sh`. **One regression caught and fixed before commit:** `availability_turn_date_scope` deep-compares the served row with the in-process row and the new timestamp was a Date in one and a string in the other; it is served as ISO text. Regressions green on the owned runtime with the server restarted: 8 unit suites incl. `operator_turnover_bridge` 13/13 and `required_work_standing`, governance suite, Ask Spine gate, `availability_readiness_axis` 13/13, `canonical_occupancy_holds` 127/0, `availability_uncorroborated_claim` 32/0, `availability_turn_date_scope`, `turnover_sibling_cache`, `triage_work_scope`, `vacated_position_basis` 19/19, `application_inventory_hold` 40/0, `application_to_lease_journey` 25/0, `current_rent_roll_reconciliation` 78/0, `opening_claim_identity` 17/17, `turnover_service_proof` 12/0. **Not done here (app repo):** the availability row in the staff app labels every non-confirmed date "expected · <fact>" (`psAvRow`), so `turn_scope_exceeds_plan` will render as *expected · turn scope exceeds plan* until the app reads `availability_confidence` — a pre-existing label, now with one more fact under it; and no staff surface yet offers the re-statement door. Not run against Skyline or Greenery. |
| 153 | **RESOLVED ON CANDIDATE · WITNESS 9/9 (defect present) → SUCCESSOR 16/16 · REGISTERED, 2026-09-19 — one physical-readiness truth feeds every gate, closing row 150's break (3).** Three things could say a unit was ready: `turnovers.status` (closable by the legacy `POST /turnovers/:id/ready` behind the shared operator key on move-out photos and deposit review alone), `unit_readiness_certifications` (the one path allowed to make a unit ready), and the move-in `readiness` obligation in `movein.js` whose approval alone fed `unit_ready` into the lease's delivery gate. **Two changes, no schema:** (a) the legacy ready route refuses `READINESS_NOT_CERTIFIED` (409, with the next step in the receipt) unless the unit carries a live certification — a certified walk already closes the turn on its own, so the route can only confirm what certification established; nothing in the app or the proofs calls it, and its retirement condition is recorded in the route. (b) `makeReadinessService` takes an optional `deliveryHelper` (server.js now builds the ONE delivery helper before the readiness service and shares it with `movein.js`), and `finishReady` satisfies `unit_ready` on every open `move_in_delivery` obligation for a lease on the unit's spaces, with the certification id in the proof; keys, funds and activation stay their own gates; a certification with no incoming lease feeds nothing. **Proof:** `tests/proofs/readiness_one_truth.db.js` (real triage → scope → walk with the real delivery helper; legacy route over loopback HTTP), registered in the server block. Regressions green on the owned runtime with the server restarted on this tree: `operator_turnover_bridge` 13/13, `required_work_standing`, `tenancy_ask_spine` 43/43, governance, Ask Spine gate, `availability_readiness_axis` 13/13, `vacated_position_basis` 19/19, `turn_slip_visible_to_leasing` 21/21, `canonical_occupancy_holds` 127/0, `turnover_sibling_cache`, `triage_work_scope`, `availability_turn_date_scope`, `application_inventory_hold` 40/0, `application_to_lease_journey` 25/0, `movein_lease_on_unit` 7/0. **Seen, not chased:** `tests/proofs/readiness_certification_proof.js` (not registered in CI) fails at require time with `MODULE_NOT_FOUND` before any assertion — a stale require, pre-existing. **Not done:** the parallel move-in readiness checklist in `movein.js` still exists as a second inspection (its approval is now idempotent against the certification's feed); a revoked certification does not un-satisfy `unit_ready` — revocation already raises a re-inspection obligation. With rows 151–153 the audit fixture's remaining BREAK lines are the two labelling notes row 150 recorded (`vacant_turning` outranks `committed_future`; a lease that merely expires stays on its opening claim). Not run against Skyline or Greenery. |
| 154 | **CLOSED ON CANDIDATE · CI GREEN (run 715 on 11c0100), 2026-09-19 — the app pin has converged; row 139's blocker is closed by the existing release process, not by weakening a test.** One app commit now carries both lineages: `claude/app-convergence-20260919` @ `475b3e1` = pin `2e8199a` (116 past app main) merged with the convergence line `2bbdb63` (10 past main); the six conflicting `index.html` hunks were resolved by hand (management vars + forward block, condition-panel arguments, `dsAddProperty` basis, the pin's server-side preview flow with `leasing_basis`, the pin's identity-review functions kept, one `dsReviewBasis` select with `Choose…` when the basis is unknown) and the app's own suite is green (`run_harnesses.sh`: **77 harnesses · 2626 passed · 0 red**). **One defect the merge itself introduced, found by the browser rung and fixed before the pin moved:** the pin's canonical desk cell (`Occupied in rent roll`, a COUNT over `of N positions · as of D`) had superseded our percent cell and silently dropped G3's §19 sub-line naming the positions whose terms are not established. The count was already the server's contractually-occupied 94 (not `total − vacant` = 95) — the number had converged, the disclosure had not. The sub-line now carries `· K with terms not established` from the same canonical read; `forward_convergence_signed_in.browser.js` reads either label and the value+sub text, its three substantive checks unchanged, falsified against the unpatched panel (13/14 → 14/14). **Browser re-proofs on the merged app against the owned runtime (real Postgres 16, real HTTP, Chromium):** coupled rent-roll rung 14/14 + receipt 6/6, `retained_inquiry_dom`, `person_identity_ingress` 40/40, `forward_semantics_are_the_servers` 34/34, `forward_occupancy_unresolved` 24/24, `operating_truth_pill` 24/24, `person_identity_signed_in` 14/14, `forward_convergence_signed_in` 14/14. **API side:** `tests/e2e/app_pin.txt` → `475b3e1918b54c648d80191c3bded9a057dfc9b8` / `claude/app-convergence-20260919`; the three `step` lines parked by row 139 are restored in `verify_all.sh` in place of the 44-line explanation, and each was run exactly as the runner invokes it (no `E2E_API_ROOT`), exit 0. Nothing that ran at the old pin was dropped — the new pin is a descendant of it. **Not done, recorded:** `canonical_onboarding_review.browser.js` `verifyZeroCounts` (its `zero` phase, not registered in `verify_all.sh`) still expects a `^Current occupancy$` label with a percent value on the signed-in desk; at the pin lineage that cell has been the `Occupied in rent roll` count since app `44e0e42`, so that phase is red at the pin **by construction** and was red before this row — it is the pin's presentation change, not this merge's. Also: the deployed app (owner-reported `2e8199a`, line ~520) is now BEHIND the API's pin; the pin says what CI exercises, not what is deployed. | `tests/e2e/app_pin.txt`; `tests/e2e/verify_all.sh` (three `step` lines live); app `index.html` (`managementConditionPanel` canonical sub-line), app `forward_convergence_signed_in.browser.js`; app branch `claude/app-convergence-20260919` @ `475b3e1`; receipt `docs/handoffs/new-hp/operating-loop-audit/RECEIPT.md` |
| 155 | **BROWSER VERIFIED (owned runtime, 19/19) · APP PIN MOVED · CI GREEN (run 716 on 78369c1), 2026-09-19 — Availability hedges a date with the server's confidence word, and a slipped turn can be re-stated from the row.** Two app follow-ups to row 152. **(a)** `psAvRow` had rendered every non-confirmed date as `expected · <fact>`, collapsing the server's `expected` (a governed plan names the date) into `incomplete` (a date is stated but Spine cannot stand behind it): a slipping turn read as on plan. The row now renders the server's word — `incomplete · turn scope exceeds plan`, `expected · turnover in progress`. **(b)** `POST /operator/units/:unitId/turn-target` (row 152) had no caller in the app. A control renders beside a row with an active turnover, only for a session the server granted the management module (`Re-state ready date` when `plan_state` is `exceeded`, `Change ready date` when it holds); date and reason are required in the form and refused by the server if missing; the receipt is the availability re-read — the submit never rewrites the row locally. Registered as the named write `restateTurnTarget`, never a generic POST. **Proof ladder:** harness `availability_cutover_app.test.js` 86/86 (confidence word; the old assertion had encoded the defect — "an incomplete date is labelled expected"; entitlement gate; unit binding; required fields; receipt-by-re-read; attribute shape with a quote-bearing id). Browser: `turn_target_from_availability.browser.js` (new, app repo) on the owned runtime — its own property on the proof database, real OTP sign-in and property picker, 401 slipped / 402 holds / 403 no turn, management re-states from the row (one POST, re-read shows the new date and `expected`, an independent API read agrees, the reason is a recorded `turn_ready_date_restated` event), empty reason refused in the form, and a leasing-only session sees the row with no control: **19/19**, screenshot kept. **The browser caught what the harness passed:** the control's `onclick` carried the unit id JSON-quoted inside a double-quoted attribute, which broke the attribute — the button rendered, was visible, and did nothing. Fixed (single-quoted id, both quote kinds escaped) and pinned in the harness. A harness that reads emitted HTML cannot see a broken attribute; only the click can. **Observed, not changed:** a unit whose lease expired with no turn opened is classified `occupied` (uncorroborated opening claim) and Availability's groups list no occupied rows, so the page says "3 positions total" and lists 2 — pre-existing, same class as the 2026-09-06 silent-group note in `PS_AV_GROUPS`. **Pin:** `tests/e2e/app_pin.txt` → app `494ff1a` (same branch as row 154, three commits on). App suite 77 harnesses · 2641 · 0 red. | app `index.html` (`psAvRow`, `psAvTurnTargetHtml`, `psAvToggleTurnTarget`, `psAvSubmitTurnTarget`, `WRITE_ACTIONS.restateTurnTarget`), app `availability_cutover_app.test.js`, app `turn_target_from_availability.browser.js`; `tests/e2e/app_pin.txt`; app branch `claude/app-convergence-20260919` @ `494ff1a` |
| 156 | **RESOLVED ON CANDIDATE · WITNESS 5/5 (defect present on 78369c1) → SUCCESSOR 20/20 · REGISTERED · ASK SPINE-READABLE (row 158) · CI GREEN (run 717 on 208d87e; run 718 on the pin commit d23caec, app 1060c23), 2026-09-19 — no durable continuity handle → no durable Person (consultant ruling 3; closes row 136's routing inversion).** **The rule, at the one choke point (`person_ingress.js`):** a continuity handle is a normalised phone or an email — a name is not one, and a PMS resident id is provenance ("never Spine's key for a human", migration 177). `createPerson` refuses `NO_CONTINUITY_HANDLE` (409, sayable, names the next step) without one; `ingestPerson`, under an authority with nothing to recognise the human by, **stages** the claim with the reason (`staged_reason: no_continuity_handle`) instead of minting. Callers that already tolerated a null person (`identity/activation.js`, `snapshot_loader.js`, `seed_snapshot.js`) needed nothing. **Deal Setup (`activation_service.confirmProposal`)** now establishes the tenancy anyway: the lease is written with **no tenant** (`tenant_ids = []`), the evidence row names the lease and no person, the promoted claim's `status_reason` and the receipt say *Resident not linked: … no phone or email, so no Person was created*, and `resident_linked:false` rides on the result. Nothing needs review — there is nothing to link to yet. **A third review outcome, because the rule opened a dead end:** a reviewer facing a name-only claim that is a *different* person from every candidate could only choose `resolved_existing` or `created`, and `created` now refuses. `confirmPersonProposal(action:'created')` on a handle-less claim resolves as **`distinct_unlinked`** — confirmed_by/at set, `payload_json.identity_decision`, status stays `staged`, `resolution_kind` null (the 177 check constraint is untouched) — and `ingestPerson` honours that decision on the next confirm (no candidates re-asked), so the lease proceeds to the overlap rules unlinked (`overlapping_operative_lease`, as before). Not a new identity system: one seam, one more answer. **The name is not lost:** `space_position.loadSpaceRows` carries `claimed_name` from the promoted lease claim, `position_classifier.shapeLease` passes it, `dated_positions` projects `resident_claim: {name, source:'rent_roll', linked:false}` only when `resident` is null, `rent_roll_unit_view` exposes `resident_claim`, and the app's unit rent roll shows *Jordan Vale · not linked* (harness `rent_roll_row_semantics` 94/94). `exceptions.resident_not_linked` counts the row (§40: the exception stays visible; the name beside it is what keeps it honest, not blank). **Proofs:** `tests/proofs/person_continuity_handle.db.js` — real `openActivation → reviewedIngest → confirmProposal` on an owned database; witness on the real parent (5/5: Person minted from a name, phone 0, email 0, `resident_not_linked` 0); successor 20/20 (lease established, no Person, claim staged with reason, evidence row, receipt, canonical `resident_not_linked = 1`, row `resident` null + `resident_claim`, still contractually occupied, `distinct_unlinked` review honoured on re-ingest, phone and email each create, PMS id alone stages, a handle without an authority still stages). Registered after "canonical onboarding snapshot"; the pinned onboarding parent (e09c541) predates the preview seam, so the witness runs by hand (`PROOF_EXPECT_DEFECT=1 PROOF_BUSINESS_ROOT=<worktree at 78369c1>`). **Blast radius, measured on the owned runtime with the server restarted on this tree:** `current_rent_roll_reconciliation` 79/79 after two test-only adaptations — section D1 now asserts *accepted, not linked* (it had encoded person-from-name), and section H's recognition scenario establishes identity the way it will happen in operation (through the ingress door **with a phone**, a stand-in for "the resident texted") before the later source recognises them; `inventory_correction_hardening` 105/105 after scoping its lineage query to `target_type='lease'` (a source row now also carries a staged person claim); green unchanged: `canonical_onboarding_{source 33, ledger 16, lifecycle 10, snapshot 15}`, `mixed_grain_writer_challenge` 38, `source_home_identity_review` 54, `availability_uncorroborated_claim` 32, `canonical_occupancy_holds` 127, `opening_claim_identity` 17 (the QA seed goes through the same door and its leases are now unlinked too), `deal_setup_http` 32, `triage_work_scope`. Unregistered `deal_setup_opening_tenancy` (E1/E4 rewritten to the rule; still refuses locally on a pre-existing grain-establishment step) and `person_spine_import_audit` (stops at the ruling, CLOSED) updated. Identical on the parent, so not this change: `confirm_proposal_operative_overlap` 16/48, `onboarding_space_availability`; need a local workbook or the Skyline fixture verify_all builds: `skyline_bed_grain_activation`, `ledger_grain_reconciliation`, `staff_assisted_journey`, `tour_application_lease`. **What this means in operation, said plainly:** the real rent-roll parser maps no phone and no email, so **every resident on a new onboarding is unlinked until reached on a handle**; the rent roll shows the source's name as a claim; a Person Card exists only once identity is established — row 136's "next operational project" is now the thing that links them. Greenery's existing 95 handle-less Persons are **untouched** (§46: not repaired while proving the software) and still read linked. `resident_not_linked` is a count that will be large and true. | `src/identity/person_ingress.js` (`continuityHandle`, `NO_HANDLE_MESSAGE`, `createPerson` guard, `ingestPerson` staged branch + honoured decision, `confirmPersonProposal` distinct_unlinked), `src/onboarding/activation_service.js` (`confirmProposal` unlinked path, `resolveResidentIdentity` receipt), `src/tenancy/space_position.js` (`claimed_name`), `src/tenancy/position_classifier.js`, `src/tenancy/dated_positions.js` (`resident_claim`), `src/surfaces/rent_roll_unit_view.js`; `tests/proofs/person_continuity_handle.db.js`; `tests/e2e/verify_all.sh`; app `index.html` (`psRruDetail`, `psRruSortValue`), app `rent_roll_row_semantics.test.js` |
| 157 | **RESOLVED ON CANDIDATE · PROVEN (owned DB, 7/7; old filter fails 3) · REGISTERED · CI GREEN (run 723 on 1f2f252), 2026-09-19 — the Availability headline `expected_within_horizon` counted only `upcoming` rows.** Seen in the row-155 screenshot: "0 expected within 90 days" above two turning positions dated inside the horizon. The tally now counts every row with `availability_confidence === 'expected'` and `within_horizon`, whatever its `marketing_state` — a `turnover_required` row with a governed plan date counts; an `incomplete` date (`turn_scope_exceeds_plan`, `no_governed_turnover_duration`, …) never does, because Spine is not standing behind it. `marketable_now` unchanged. Proof `tests/proofs/availability_expected_within_horizon.db.js` (upcoming-expected counted, turnover-expected counted, turnover-incomplete not, expected outside horizon not; independent recount), registered after "availability readiness axis"; `availability_readiness_axis` 13/13, `availability_turn_date_scope`, `turn_slip_visible_to_leasing` 21/21 unchanged. Built by a delegated agent, verified and falsified here. | `src/surfaces/availability_read.js` (headline tally); `tests/proofs/availability_expected_within_horizon.db.js`; `tests/e2e/verify_all.sh` |
| 158 | **RESOLVED ON CANDIDATE · PROVEN (owned runtime; reconciliation 88/88, gate 161/161, unit 43/43) · REGISTERED · CI GREEN (run 723 on 1f2f252), 2026-09-19 — row 156 is now done as a DOMAIN: Ask Spine reads the unlinked-resident exception from the same canonical read.** `readTenancyStanding` (tenancy_position_read.js) carries `exceptions.resident_not_linked` — the identical filter over the identical `dp.positions` that `rent_roll_canonical` uses, no second query — plus a bounded (20) `resident_claims_unlinked` list of `{unit, position, name, linked:false}` relaying `resident_claim` as a CLAIM, a truth wall *a resident_claim is not a resident*, and a `does_not_establish` line naming the claim/identity distinction. `NOT_ESTABLISHED` keeps `null`, never a zero. Proven in `current_rent_roll_reconciliation.db.js` section H: screen and Ask Spine count agree on the same read, the claimed name is reachable to an entitled module with no record id in the payload, and an unentitled session gets `not_authorized` with the name nowhere in the response (model stub throws if invoked). Built by a delegated agent, verified here. **Seen, not changed:** bare "who is in 401" routes to `work`, not `tenancy` (`questionSubject` needs a tenancy term); "who lives in 401" routes correctly. Wording layer, not router — follow-up. `tenancy_standing_read.db.js` / `tenancy_ask_spine_http.db.js` refuse locally without the Skyline workbook (pre-existing). | `src/tenancy/tenancy_position_read.js`; `tests/proofs/current_rent_roll_reconciliation.db.js` |
| 160 | **PROVEN (owned database + real HTTP) 38/38 · REGISTERED · SECOND DOOR RETIRED · ⛔ THE LOOP DOES NOT CLOSE — TWO FINDINGS, 2026-09-19 — one resident walked all the way round rows 156 → 158 → 159, and the seam that breaks is the inbound text.** Three rows built three pieces and each was proven alone; three green proofs of three pieces is not a proof that the loop closes. **`tests/proofs/identity_loop_closes.db.js`** (Class 1, **38/38**, registered in `verify_all.sh` immediately after the link-resident step) walks ONE resident: a name-only rent-roll row establishes the lease **unlinked** → the SCREEN read (`rent_roll_canonical`) and the ASK SPINE read (`readTenancyStanding`) report the *same* `resident_not_linked` count off the *same* `dp.positions` → management links by phone through real HTTP → **both readers move together** → the claim is promoted with `resolution_kind` **and** `promoted_record_id` → the Person Card shows the tenancy → an entitled Ask Spine session passes authorization and an unentitled one is refused **with the resident's name nowhere in the response**. **The Anthropic client is a stub that THROWS if called**, so nothing green here is a sentence a model generated. **⛔ THE FINDING, RECORDED AS AN ASSERTION AND NOT AS A NOTE — seam 3, that resident texts the property line and Spine does not know them.** `resolveInboundSmsContext` (`src/comms/communications_boundary.js`) resolves a known resident at tier 1 through `join tenant_invites ti on ti.person_id = per.id and ti.property_id = $1 and ti.status = 'used'` — a resident who **accepted an invite**. A rent-roll resident linked by an operator (row 159) has no such row, so their first text lands as an **UNMATCHED sender**: saved on the property, `needs_human`, zero outbound. The proof asserts exactly that, and asserts the gap is exactly the invite (`tenant_invites` count for this person = 0). **NOT FIXED, deliberately:** an operator's claim *this phone is that resident* and the resident's own proof *I hold the number that accepted the invite* are different evidence classes, and widening tier 1 to accept an operator link widens who Spine will speak to as a known resident. That is a product ruling, not a bug fix, and it is outside this slice's blast radius. **So row 159's door links a resident for the rent roll and for Ask Spine, and does NOT yet make them recognised on the phone.** **⛔ THE SECOND FINDING — seam 2, the linked resident never leaves lifecycle `lead`.** The retired door below advanced the person to `lifecycle_status='tenant'` and wrote a `lifecycle_change` event. **`link_resident.js` does not** — it passes no `lifecycle_status` to `ingestPerson`, which defaults to `'lead'`. Measured on the proof database: a Person named as the tenant of an **active lease** reads `lifecycle_status='lead', leasing_stage='lead'`. **It has a consequence worth naming:** `authority_resolution.js`'s `HARD_COUNTERPARTY` set is `{tenant, resident, past_resident, applicant, vendor}` — the lifecycle values a staff context **cannot** override when deciding `person_is_not_a_counterparty`. `lead` is not in it, so the guard that exists to stop a real counterparty being granted staff authority **does not fire** for a resident linked through row 159's door. **NOT FIXED HERE, and NOT by restoring the retired door** — which had zero callers and so was never performing the advance for anyone; this is row 159's gap, surfaced. A correct fix is a ruling, not a field: on `resolved_existing` ingress does not update an existing person, so "who advances a person to tenant, and when" has to be decided. Asserted in the proof so whoever fixes it has to come here and say so. **NEGATIVE CONTROLS, all green:** a stranger's phone is not attributed to the linked resident and is kept for a human; the inbound creates **no second Person** and exactly one Person carries the handle; a handle held by two Persons refuses **409 with the candidates and writes nothing** (lease untenanted, rent-roll counts unmoved). **PART B — the second door onto "who is on this lease" is closed.** `POST /leases/:id/tenants` (`src/tenancy/lease_lifecycle_routes.js`) took an already-known `person_id` straight onto a lease with **no property scope** (the lease id alone chose the property), no staff session, no ingress, no claim promotion and no evidence row. It now answers **410** with a receipt a person can read, naming `POST /operator/leases/:leaseId/link-resident` as the next step. Two doors onto one lease is how one lease ends up with two answers about the same resident. **CALLERS MEASURED BEFORE CLOSING: zero** — every tracked file on **all 109 app remote branches** (not just `index.html`, not just the pinned branch) and this repo's `src/`, `server.js`, `tests/`, `tools/`, `migrations/`. ⚠ Source can prove a consumer exists; it cannot prove one does not — the shared operator key is held outside this repo, which is why the path answers a refusal that **names the replacement** rather than a 404 an external caller would have to guess at. **Class 3 (retired-in-place). REMOVAL CONDITION: one full release with zero calls to this path in the access logs**, then the handler and its block comment are deleted. The file's `TENANT LINKAGE` banner still described the endpoint in the present tense and was rewritten to name the live door. **MUTATION-CONFIRMED, because green is a claim about what was measured:** `410 → 404` fails the status assertion; a bare `{error:"gone"}` fails the sayable-receipt assertion; a receipt saying `person_id` / `tenant_ids` fails the no-machinery assertion; a handler that writes the tenant **and then** refuses fails both the wrote-nothing and the canonical-read-unchanged assertions. **FOUND AND NOT FIXED:** `DELETE /leases/:id/tenants`, adjacent lines in the same file, is the **same defect class** — no property scope, no session, a client-supplied `person_id` removed from `leases.tenant_ids`, zero callers by the identical search. It is **not** retired: there is no unlink door to name as a next step, and a refusal that names none is worse product than the route; gating a route with unknown external callers is exactly the `team-invites` mistake. **An unlink door through the same gates as link-resident is the next thing someone should build**, and retiring this one is part of that slice. **NOT REACHED:** no browser rung — this is a proof of the seams between canonical reads and one refusal, and nothing new renders. **No production data was touched** (§46); no identity table, no name matching, and `person_ingress`'s staging rule is untouched. Proof database: 189 migration files, 187 applied, 2 data migrations that legitimately refuse on an empty database (087, 110). | `tests/proofs/identity_loop_closes.db.js`; `tests/e2e/verify_all.sh`; `src/tenancy/lease_lifecycle_routes.js` (410 refusal, banner); observed only: `src/comms/communications_boundary.js` (tier-1 `tenant_invites` join), `src/tenancy/link_resident.js`, `src/tenancy/tenancy_position_read.js`, `src/surfaces/rent_roll_canonical.js` |
| 159 | **CI GREEN (run 725 on e5ab219, merged as d19ba08) · BUILT · PROVEN (owned database + real HTTP) 34/34 · APP SUITE GREEN (78 harnesses, 2657 passed) · **DONE AS A SCREEN, NOT AS A DOMAIN**, 2026-09-19 — the door that links a resident row 156 deliberately left unlinked.** Row 156 ruled that a rent-roll row with no phone and no email establishes the lease with **no tenant** and stages the person claim, and the rent roll shows the source's name as `resident_claim {linked:false}`. Correct, and it left an operator with no way to link that resident once they HAD a handle. **`POST /operator/leases/:leaseId/link-resident`** (`src/tenancy/link_resident.js`, Class 1) is that one door. Body `{phone?, email?, source_basis}`; property from the **session**, never the body — a body naming `property_id` or `person_id` is **refused 403**, not ignored (§21). `requireOperator` + **management** module: linking a resident resolves who is in a home. **THE PERSON IS CREATED BY `ingestPerson` AND NOWHERE ELSE** — no direct `insert into persons`; `gate_person_ingress` does not name this file. **THE ONE DESIGN CALL WORTH THE RULING: `activation_id` is deliberately NOT passed to `ingestPerson`.** With `channel:'rent_roll'` + `activation_id` + `import_source_row_id`, ingress first honours a confirmed person proposal — and a reviewer's earlier `distinct_unlinked` verdict would short-circuit this link to `person_id: null` **forever**. That verdict was reached under a specific condition: a different person from every candidate, **and nothing to recognise them by**. An operator arriving here has supplied exactly the thing that was missing; honouring the old decision would make the handle unusable, which is the opposite of what row 156 left open. Omitting it also makes “409 and write nothing” **structural** rather than remembered: `writeProposal` returns immediately without an activation_id, so a conflict writes no stray claim row. **person_ingress's staging rule is untouched**; this module never calls `confirmPersonProposal` and never writes `persons`. **ON SUCCESS:** `leases.tenant_ids=[person_id]`; the staged claim promoted with `resolution_kind` **and** `promoted_record_id` in ONE statement (migration 177's constraint); `import_source_rows.produced_person_id` set; one `resident_linked` event naming the handle **kind** and the basis — **never the handle value**, because a ledger of how identity was established must not become a second place a phone number lives. **THE CANONICAL READ FOLLOWS WITH NO READER CHANGE:** `resident_claim` is derived as `!resident && lease.claimed_name`, so linking the tenant makes the claim null and drops `resident_not_linked` by one on its own. No other reader was touched. **PROOF** `tests/proofs/link_resident.db.js` — **34/34**, registered in `verify_all.sh` after the continuity-handle step, real services + real HTTP over a real socket, fixture built through the **Deal Setup path** (three units → three unlinked leases). It proves: link by phone → created and the Person **carries the handle**; the SAME phone on a second lease → `resolved_existing`, **one** Person on two leases; a phone on two Persons → **409 with the candidates and nothing written** (lease still untenanted, claim still staged, rent roll unchanged); wrong property → **404, not 403** (a 403 would confirm the lease exists); already-tenanted → 409; no handle, blank handle, no `source_basis`, no session, client `property_id` → refused, each with a next step. Repeatable: two consecutive runs 34/34, and `person_continuity_handle` still 20/20 beside it. **APP** (`property-spine-app` `claude-opus/link-resident-app-20260919`): a **management-only** “Link resident” control on the rent-roll detail row that states the problem, through the **named write action** `linkResident` — never a generic POST — with the **rent roll re-read as the receipt**, not a toast claiming success. Module gating reuses the existing `pcLiveHasModule`; no second entitlement check was written. `link_resident_app.test.js` **12/12** *evaluates* the lifted registry entry (path, encoding, body keys) rather than grepping for it. **⚠ NOT REACHED — DONE AS A SCREEN, NOT AS A DOMAIN (§40.2):** no Ask Spine registration. `resident_not_linked` appears **only** in `rent_roll_canonical.js` and no conversational reader surfaces it, so an entitled person cannot ask *“which residents are not linked?”* and get a governed answer. The browser rung was **not run** either. **FOUND AND NOT FIXED:** (1) `gate_person_ingress` fails 2 — `src/baseline/baseline_routes.js` writes `insert into persons` outside the boundary and `server.js` is stale in its DECLARED register; **pre-existing, verified identical at the branch point**, and neither names this file. (2) `POST /leases/:id/tenants` already exists in `lease_lifecycle_routes.js` — legacy shared-key surface, attaches an **already-known** `person_id` with no property scoping, no ingress, no claim promotion. A different door, left alone, but a real adjacent duplication someone should rule on. **Classifications:** route + module + promotion Class 1; the app control and both harnesses Class 1; nothing temporary added. **No production data was touched.** Superseded on merge: row 158 (board) already makes Ask Spine read resident_not_linked and the claimed names from the same canonical read, so this door is done as a domain once merged; a link reduces that count. |
| 71 | **CANDIDATE (not released) · HTTP-PROVEN LOCALLY + BROWSER (staff shell), 2026-09-13 — governed inventory correction: review, retire and reinstate an obsolete inventory representation through the existing owner, from the existing Rent Roll page.** Branch `claude/inventory-correction-20260913` over `7cb245e` (app `e8fe8e9`). Witness on the unchanged baseline: list/review/retire answer 404 — the owner (`inventory_retirement.js`) was reachable only from proofs. New: `src/tenancy/inventory_correction.js` (review read with lineage, relationships, eligibility and a `review_token`; transactional apply and reversal that re-verify the governed override + management module from the assignment row, take custody from the unit's own row, lock the unit's SPACES `FOR UPDATE` so a concurrent lease/application/offer/event writer waits, refuse stale review / any-lease / scheduled event / open application / current offer / invitation / executed record / renewal / work / turnover / future tour / current-representation claim, all or nothing, then call `retireInventoryUnits` / `reinstateInventoryUnit`); routes `GET/POST /operator/inventory/corrections{,/review,/history,/retire,/reinstate}` in `operator.js` (reads: management module; writes: `requireGovernanceAuthority`); tenancy standing carries `unit_records_retired_from_current_inventory` + `tenancy_attached_to_retired_inventory`; the unit rent-roll view returns `retired_excluded`; app Rent Roll page gains an Inventory records panel (list, review, rationale + explicit confirmation, history, reinstatement). Reason vocabulary and meaning unchanged; no migration. Successor 82/0 on a Greenery-shaped fixture (retained legacy labels, synthetic everything else, four designated examples, 100+ left unresolved) plus an unrelated property; browser 17/17; contract test; app suite 68/2,291/0. **Not decided here:** any real Greenery record's physical identity; a narrower correction authority than the override (recorded as an open ruling). | `src/tenancy/inventory_correction.js`; `src/identity/operator.js`; `src/tenancy/tenancy_position_read.js`; `src/surfaces/rent_roll_unit_view.js`; `tests/e2e/inventory_correction.e2e.js`; `tests/e2e/inventory_correction.browser.js`; `tests/unit/inventory_correction_contract.test.js`; `tests/e2e/verify_all.sh`; `docs/handoffs/new-hp/inventory-correction/RECEIPT.md` |
| 64 | **RESOLVED ON CANDIDATE, 2026-09-06 — a confirmed source row that attached to no position was invisible everywhere, and a whole-unit placeholder beside real beds absorbed a bare-unit claim.** Three historical shapes leave a promoted current claim that no rentable position reads: a bare-unit vacancy confirmed on a multi-bed unit under the old writer, a source row naming a room the unit does not have, and a whole-unit placeholder left beside beds by a pre-materialization load. Reproduced in an owned database on `857cf34` (`tests/proofs/opening_claim_unattached.db.js`, `PROOF_EXPECT_DEFECT=1`, 12/12): the activation tally counted all four confirmed rows as established; every position on the first two units read not established with no reference to the row; the tenancy standing projection, the Rent Roll unit view and Ask Spine carried no name for them; and the placeholder counted as a position **and** read `opening_claim_vacant` on the bare-unit claim, so availability offered it beside the control. Fix, reader only: `dated_positions.unattachedOpeningClaims` reports, under the chosen baseline, the promoted and held current claims no position's basis or conflict references, by source key (`201`, `202|Room4`), bounded, no record id — same proposals the candidate subquery reads, no second store, no broadcast to N beds, no position counted; relayed to `tenancy_position_read` (`unknowns.confirmed_source_rows_not_attached_to_a_position`, `unknowns.held_source_rows_not_attached_to_a_position`, `unattached_source_rows`) and `rent_roll_unit_view` (`totals.confirmed_rows_not_attached`, `totals.held_rows_not_attached`, `unattached_source_rows`), and so to Ask Spine. `space_position`: a bare unit key answers only when the unit has exactly one position, so a placeholder beside real beds (four positions) absorbs nothing. Successor 14/14: three rows named on all three readers with keys and no id, positions still not established, control still attaches, phantom refused, no proposal rewritten. Identity proof 17/17, Ask Spine reader gate 72/72, space-availability successor, historical challenge and the five leasing suites PASS over real HTTP. **Recorded, not changed (rulings owed):** the phantom placeholder is still counted as a position (ten for nine real) — removing it is a governed inventory correction (`inventory_materialization` refuses a non-pristine placeholder by design; retirement is unit-level); the activation tally still says 4 established, which is history, and whether Deal Setup should show unattached rows beside their confirmed status is an app change. Rung: `PROVEN` (real Postgres) locally on Linux; in `verify_all.sh`; no CI run read at the time of writing. | `src/tenancy/dated_positions.js`; `src/tenancy/tenancy_position_read.js`; `src/surfaces/rent_roll_unit_view.js`; `src/tenancy/space_position.js`; `tests/proofs/opening_claim_unattached.db.js`; `tests/e2e/verify_all.sh`; `docs/handoffs/desktop-qb-20260906/availability-basis/RECEIPT.md` |
| 63 | **CANDIDATE IDENTITY PROTECTION; COMPATIBILITY BOUNDARY REMAINS.** Durable source-space lineage survives relabeling and identifies its original position. Unit-only lineage stays inside that unit: a named label selects its room; a bare key can select the sole position under the existing ledger rule. With neither unit nor space lineage, text fallback remains subject to active retirement protection. No timestamp is identity proof. The replaced-unit and relabeling defects have successor controls; shrink-to-one case D still attaches in both modes and is not a repaired drift. The regenerated successor artifact from Fable4915c9d exactly matches the independent QB identity run on the c9d2cb9 product tree after synthetic ID normalization (17 passed). Retirement reversal/replacement, named placeholders and present-null historical provenance remain open challenges. The legacy rent-roll text matcher is unchanged. | `src/tenancy/space_position.js`; `tests/proofs/opening_claim_identity.db.js`; `docs/handoffs/desktop-qb-20260906/claim-relay/RECEIPT.md` |
| 62 | **OPEN, 2026-09-06 — the leasing desk repaints itself over a workspace the operator just opened (app).** `renderDesk('leasing')` (`index.html`) paints the desk, awaits its condition and tour-schedule reads, then calls `renderDailyLeasingSurface` again. A Market & Pricing click inside that window opens the S7 workspace, which the late repaint then covers with the desk. Observed deterministically in Chromium on the second fixture of the browser proof's spaces phase: the failure screenshot showed the desk with "1 marketable now" already populated and no workspace. The proof now waits for the desk to settle before clicking, and says so; the product is unchanged. | app `index.html` (`renderDesk` / `renderDailyLeasingSurface`); app `canonical_onboarding_review.browser.js` (`deskSettled`) |
| 61 | **RESOLVED ON CANDIDATE, 2026-09-06 — canonical availability advertised positions Spine could not establish.** `marketingState` (`src/surfaces/availability_read.js`) never read `basis_state`, the Rent Roll bucket, or the `inconclusive`/`unreconciled` evidence states; with no lease, no delivered possession and no turn, a position fell through to `marketable_now` because readiness defaults to `ready` (`position_classifier.js`). The Rent Roll refused to bucket the same position ("no basis, no bucket") and Ask Spine counted it `not_established`. Reproduced through real HTTP at unchanged `77d6156`: two rooms under a historical bare-unit vacancy read Ask open 0 / marketable_now 2; a single unconfirmed room 0 / 1; the confirmed single-room control 1 / 1. Fix: `basis_state !== "established"` → `occupancy_unknown`, `evidence_state === "unreconciled"` → `evidence_unreconciled`, placed below the operative-lease, commitment, possession and turnover guards and above triage and use type; no date, blocking fact named, both states in `states` and `headline`, `basis_*` carried on the row. Proof: `tests/unit/availability_occupancy_basis.test.js` (red 2/4 before, 4/4 after; in `verify_all.sh`); `tests/proofs/onboarding_space_availability.db.js` `PROOF_HISTORICAL_READER_CHALLENGE=1` (positive witness: passes on `77d6156`, fails on the fix at `actual 0, expected 2`) and `=successor` (unknown rooms refused with identity and no date; Rent Roll unit view and Ask agree; controls: lease in force → `occupied`, future lease → `successor_pending`, held row under an established baseline → `evidence_unreconciled` / `needs_review`); app test 67/67; Chromium spaces phase on the owned server, two fixtures, PASS. Leasing clean path, hostile (16/16), reconciliation, standing and Ask Spine suites PASS on the repaired tree. **Recorded, not changed:** `availability_canonical_proof.js`, `scenarios/cross_surface_invariants.js` and `slice9_application_target_authority_proof.js` are outside CI, already red on the unmodified tree against the e2e fixture (2/2/16), and gain reds (4/2/24) that each expect a never-established fixture position to be marketable or offerable — the defect itself; they need an established basis in their fixture or retirement. Rung: `HTTP_PROVEN` and Chromium locally on Linux against an owned server; not run through the desktop wrapper; no CI run read for these commits at the time of writing. | `src/surfaces/availability_read.js`; `tests/unit/availability_occupancy_basis.test.js`; `tests/proofs/onboarding_space_availability.db.js`; `tests/e2e/verify_all.sh`; app `index.html`, `availability_cutover_app.test.js`, `canonical_onboarding_review.browser.js`; `docs/handoffs/desktop-qb-20260906/availability-basis/` |
| 60 | **CI STATUS, 2026-09-05 02:43Z — what the `verify` workflow has actually run on this branch.** `.github/workflows/verify.yml` runs `tests/e2e/verify_all.sh` on every push. Runs 374–390 completed `success` on, in order, `5ffab56`, `1e66af6`, `e35cef3`, `7170d5c`, `d2fed71`, `7b27a35`, `7d56a8b`, `dbe8771`, `a0ac76e`, `ff4b3a3`, `919c2c8`, `92b3628`, `827f81c`, `ee51c1a`, `66dfb53`, `c6b407b`, `5b74cfc` — every commit rows 39–57 and 59 name, so those rows read CI-green. `5798154` (row 58) was pushed together with `5b74cfc` and has **no run of its own**; run 390's tree contains its change, which is a statement about a superset, not a CI result for that commit. The commit carrying this row triggers its own run; nobody has read it at the time of writing. Locally: the verification parent was green at every commit above on a disposable loopback database (`127.0.0.1`, `spine_verify_*` / `spine_falsify_*`), 25 → 41 steps, browser rungs included, each database dropped afterwards. | GitHub Actions runs 374–390 on `claude/d55-defect-porting` |
| 59 | **RESOLVED IN SOURCE, 2026-09-05 — tax and utility evidence PDFs were scanned as bytes, never as documents.** `fileToText` (`src/agent/document_ingest.js`) dispatches on `file.originalname` — `.pdf` to the PDF reader, `.xlsx/.csv` to the spreadsheet reader, `.docx` to Word, anything else `buf.toString("utf8")`. The tax door (`src/asset/tax_establishment.js`, `POST /operator/asset-management/taxes/evidence`) and the utility door (`src/asset/utility_routes.js`, `POST …/utilities/evidence`) built the file as `{ filename }`, so the name was always `""`, no branch matched, and the document reader scanned the utf-8 of a compressed PDF for its labels: `available: true, found_count: 0`, *Spine found none of the labels it looks for* — for every document ever uploaded through those two doors, while the other four evidence doors (insurance, compliance ×2, contracted services) passed `originalname` and read fine. The artifact store admits only PDF for `tax_bill` and `utility_statement` (`source_artifact_service.js`), so PDF was the whole reachable case. Fix: both callers pass `originalname`; `fileToText` is unchanged. Proof: `tests/e2e/evidence_upload_name_key.e2e.js`, invoked by `tests/e2e/verify_all.sh` against the real `server.js` on a disposable loopback database, uploading two committed PDFs (`tests/e2e/fixtures/*.pdf`, four- and six-line HTML rendered by headless Chromium; their labels are not present in the raw bytes) — tax: `annual_liability 1234.56`, account, year and due date proposed; utility: provider, `current_amount_due 210.55`, `found_count ≥ 5`. Falsified against the unmodified callers on `c6b407b`: 2/6 — both documents retained, both proposals empty. Verification parent run with rows 58 and 59's changes together in one tree; each commit carries its own files. Rung: `HTTP_PROVEN` locally through the verification parent; CI (`verify`) green on this commit — run 390, row 60. | `src/asset/tax_establishment.js`; `src/asset/utility_routes.js`; `tests/e2e/evidence_upload_name_key.e2e.js`; `tests/e2e/fixtures/`; `tests/e2e/verify_all.sh` |
| 58 | **RESOLVED IN SOURCE, 2026-09-05 — every outbound staff text carried a human approval instant with no approver.** `recordOutboundText` (`src/leasing/leasing_interactions.js`), the one interaction ledger for a human's outbound text (reached from the Person Card reply door `POST /operator/leasing/conversations/:id/reply`), wrote `human_approved_at` from `human_approved_by_user_id ? now : now` — `now()` on both branches. Every row said *approved at T* beside `human_approved_by_user_id = null`, falsifying the drafted/approved split the ledger exists to keep. The agent's writer (`src/agent/agent.js`) already stamps `null` when nobody approved (`case when $5::uuid is null then null else now() end`). Fix: `? now : null`. Nothing else in the writer changes; readers of `human_approved_at` in `src/` are the two writers themselves. Proof: `tests/e2e/outbound_text_approval_instant.e2e.js`, invoked by `tests/e2e/verify_all.sh` against the real `server.js` (harness SMS transport — no phone reached) on a disposable loopback database — reply 200; ledger row names the session user as actor and sender, not AI-drafted, no approver, and `human_approved_at` null. Falsified against the unmodified writer on `c6b407b`: 3/4 — `human_approved_at: 2026-09-05T02:31:12Z` with no approver. Verification parent run with rows 58 and 59's changes together in one tree; each commit carries its own files. Rung: `HTTP_PROVEN` locally through the verification parent. CI has **no run of its own** for this commit (`5798154` was pushed together with `5b74cfc`; run 390 ran the successor's tree, which contains this change plus row 59's) — row 60. | `src/leasing/leasing_interactions.js`; `tests/e2e/outbound_text_approval_instant.e2e.js`; `tests/e2e/verify_all.sh` |
| 57 | **HARNESS CHECK, 2026-09-05 — "stale proof paths" does not reproduce on this lineage by the method tried.** A scan of every `require("…")` and `require(path.join(ROOT, "…"))` under `tests/**/*.js` (relative and `src/`-rooted paths only) resolved all but 13 strings, and each of those is fixture text — a pattern a gate searches for, a source line a scenario rewrites, a `require` string a unit test asserts is present — not a broken require (`gate_ask_spine_readers.js`, `gate_funding_boundary.js`, `ask_spine_reader_gate_falsification.js`, `staff_sms_leasing_action.test.js`). Scope stated so the next reader knows what was NOT scanned: dynamic requires, `import`, and paths built from variables. | `tests/**` (read only) |
| 56 | **EXAMINED, NOT PORTED, 2026-09-05 — donor-branch source-level fixes whose defect is not visible on this lineage from the lines alone.** The frozen donor `fd574aa` changed `application_lifecycle.js:339`, `leasing_scheduling.js:213`, `activation_service.js:358`, `governed_charge_cutover.js:313`, `communications_boundary.js:851`, `meeting_receipt_workflow.js:59/85` and an extractor owner label. The `d55` code at each of those locations was read tonight; nothing at the line reads as a defect without the donor's surrounding rationale, which was not re-derived here. They stay matrix state 3 (present only on the donor), unported and unclaimed either way — not "fixed", not "disproved". Also recorded: the demo booking door (`leasing_leads.js` `POST /demo/book`) would move a `consumed` link that carries no `booked_tour_id` into `expired`; the path is behind `DEMO_MODE` **and** `DEMO_BOOKING_ENABLED`, no such row is produced by the code that consumes links, and it was not exercised. | `src/**` (read only) |
| 55 | **RECORDED, NOT CHANGED, 2026-09-05 — the Ask Spine reader gate discovers domains in `src/asset` and `src/tenancy` only.** `tests/gates/gate_ask_spine_readers.js` (`STANDING_READ_DIRS`) scans those two directories and deliberately excludes `src/surfaces` as projections. Nine files matching its standing-read suffixes exist outside both: `applications/application_lifecycle_read.js`, `applications/application_target_read.js`, `comms/staff_governed_read.js`, `leasing/forward_leasing_read.js`, `leasing/leasing_standing_read.js`, `leasing/opportunity_lifecycle_read.js`, `leasing/renewals_read.js`, `maintenance/unit_move_in_read.js`, `money/concessions_read.js`. Whether each is a canonical domain read (and so must register, §40.2) or a projection is a per-file classification not made tonight; widening the directory list without it would either invent domains or turn the gate red on domains nobody classified. Until it is made, the gate's green is a claim about asset + tenancy, not about the governed set. | `tests/gates/gate_ask_spine_readers.js`; the nine files (read only) |
| 54 | **DISPROVED, 2026-09-05 — `GET /pricing/:propertyId/active` cannot return an arbitrary version.** `getActivePricing` (`src/money/commitment_ledger.js`) selects `where property_id = $1 and status = 'published' limit 1` with no `order by`, which reads as a coin flip. It is not one: `uq_ppv_one_published` (migration 062) is a partial unique index on `(property_id) where status = 'published'`, so at most one published version exists per property and `limit 1` never chooses. The donor's suspicion was carried on the matrix as state 1; it is state 4-disproved on this lineage. No change made. | `migrations/062_pricing_authority.sql`; `src/money/commitment_ledger.js` |
| 53 | **RESOLVED IN SOURCE, 2026-09-05 — the second pricing-authority resolver disagreed with the first about the same human.** `pricingAuthority` (`src/money/pricing_authority.js`) has two branches: given a session `user_id` it delegates to `resolveActorContext` (row 50); given a `person_id` directly — the branch `saveDraft`/`submitReview`/`publishVersion` take when a caller supplies `person_id`, and the one `tests/proofs/pricing_governance_proof.js` exercises — it selected ONE live grant by `order by effective_from desc limit 1`. A person holding a live review grant (older) and a live publish grant (newer) was denied review on this branch and granted it on the other; the same human, the same property, two answers. Fix: the same rule as row 50 — grants read `order by effective_from, id`, a verb granted when any live grant grants it, basis naming the earliest such grant; `may_edit_pricing` still honoured for prepare; role authority unchanged. Proof: `tests/proofs/pricing_authority_grants_union.db.js`, invoked by `tests/e2e/verify_all.sh` in its harness-boundary section (`HARNESS_DATABASE_URL`, refused if it matches `DATABASE_URL`) on the disposable loopback database — both verbs granted on their own grants, prepare and manage denied (manage granted only by an expired grant), and capabilities + bases identical through `person_id` and through the session user. Falsified against the unmodified parent `ee51c1a`: 3/6 — review denied on the older grant, and the two resolvers' capabilities `[false,true,true,false]` vs `[false,false,true,false]`. Rung: `PROVEN` (real Postgres, through the verification parent) — this is a library path, not an HTTP door; CI (`verify`, `.github/workflows/verify.yml`) green on this commit — row 60. | `src/money/pricing_authority.js`; `tests/proofs/pricing_authority_grants_union.db.js`; `tests/e2e/verify_all.sh` |
| 52 | **RESOLVED IN SOURCE, 2026-09-05 — `GET /operator/economics/shadow` compared every operator's property against a production property id fixed in source.** The route (`src/identity/operator.js`) called `economicShadowReport` with `other_property_id: "9e2bb96e-…"` — the real Solo property, 4233 Chestnut (`src/onboarding/deal_registry.js`) — for whoever was signed in, on whatever property. That is a Solo-special branch in a route (§22) and a read of another property's governed pricing state (`another_property` scenario: whether it has a published version) without any entitlement to that property. Fix: the comparison property is named by the caller (`?other_property_id=`) and must be one the operator is seated on (`listAuthorizedProperties`), else 403 *The comparison property must be one you are seated on. Nothing was compared.* with `acting_on`; with none named, the report runs single-sided (the service already supported `null`). `economicShadowReport` itself is unchanged; `economic_decision_room.js` never passed a comparison property. **Remaining hard-coded Solo ids, recorded not changed:** `src/surfaces/owner.js` `NEVER_DELETE` (a deletion guard listing real properties — protective, by design), `src/leasing/demo_preflight.js` `REAL_SOLO_ID` (a preflight that checks demo traffic does not land on Solo — protective), `deal_registry.js` (the registry that names it). None selects a property for an operator. Proof: `tests/e2e/shadow_other_property_entitled.e2e.js`, invoked by `tests/e2e/verify_all.sh` against the real `server.js` on a disposable loopback database — no property named → 200 with no `another_property` scenario; a property the operator is not seated on → 403 with `acting_on` and no comparisons; seated on it → 200 with the scenario. Falsified against the unmodified parent `827f81c`: 2/4 — the fixed comparison appeared unasked (`governed_state: no_published_pricing_version` for a property absent from the database) and a foreign property answered 200. No app caller at pinned app `4849545`. Rung: `HTTP_PROVEN` locally through the verification parent; CI (`verify`, `.github/workflows/verify.yml`) green on this commit — row 60. | `src/identity/operator.js`; `tests/e2e/shadow_other_property_entitled.e2e.js`; `tests/e2e/verify_all.sh` |
| 51 | **RESOLVED IN SOURCE, 2026-09-05 — a pricing term proposed without a lease term was stored as a silent 12-month decision.** `saveDraft` (`src/money/pricing_lifecycle.js`) inserted each proposed term with `lease_term_months || 12`. The publication contract already names a term without months as the blocker `invalid_lease_term`, but the draft was saved anyway with 12 in the column — a decision nobody made, shown in the draft as if proposed, and publishable as one; a fractional month (`12.5`) reached Postgres and came back as a raw 500 `22P02`. The ruling on this lineage is already taken: *12 months is not a silent default.* Fix: a term whose `lease_term_months` is not a whole number 1–36 is refused 400 `term_without_lease_term`, naming the type and what it named, *12 is not assumed. Nothing was saved* — the transaction rolls back, so no draft version exists. Terms that name their months save exactly as named. `POST /operator/pricing/draft` has no app caller at pinned app `4849545`. **Adjacent, recorded not fixed:** `pricingAuthority` (`src/money/pricing_authority.js`) still resolves a single grant by `order by effective_from desc limit 1` on its legacy `person_id`-supplied path — the same class row 50 fixed in `resolveActorContext`; every route reaches it through `user_id`, which delegates to row 50's path, so the legacy branch is reached only by a caller that supplies `person_id` directly. Proof: `tests/e2e/pricing_term_requires_months.e2e.js`, invoked by `tests/e2e/verify_all.sh` against the real `server.js` on a disposable loopback database — no months → 400 with the receipt and zero versions; 12.5 → 400; 6 → 200 stored as 6. Falsified against the unmodified parent `92b3628`: 2/6 — 200 with a draft carrying a 12-month term, and 500 `22P02` for 12.5. Rung: `HTTP_PROVEN` locally through the verification parent; CI (`verify`, `.github/workflows/verify.yml`) green on this commit — row 60. | `src/money/pricing_lifecycle.js`; `tests/e2e/pricing_term_requires_months.e2e.js`; `tests/e2e/verify_all.sh` |
| 50 | **RESOLVED IN SOURCE, 2026-09-05 — two live concession-authority grants resolved to whichever row Postgres returned first.** `resolveActorContext` (`src/identity/actor_context.js`) — THE canonical path for who the acting human is — read a person's live `concession_authority_grants` for the property with no `order by` and then took `grants[0]`. A person holding two live grants (one `may_review_pricing`, one `may_publish_public_offers`) was granted whichever verb sat on the first row and denied the other; the decision changed with heap order, and the `authority_basis` written into privileged receipts named a grant that had not granted the verb. Fix: grants are read `order by effective_from, id`; each verb is granted when ANY live grant grants it (every live grant is a valid authorization — the comment already ruled that silently revoking given authority is its own defect), and the basis names the earliest such grant. `may_prepare_pricing` keeps honouring the older `may_edit_pricing` column. Role authority (`owner`, `asset_manager`) is unchanged and still short-circuits. Proof: `tests/e2e/authority_grants_union.e2e.js`, invoked by `tests/e2e/verify_all.sh` against the real `server.js` on a disposable loopback database, through `GET /operator/authority-view` — review granted with basis `grant:<review grant>`, publish granted with basis `grant:<publish grant>`, prepare and manage denied (manage granted only by an EXPIRED grant, which grants nothing), the view listing exactly the two live grants. Falsified against the unmodified parent `919c2c8`: 5/6 — `may_publish_pricing: false, basis: null` while the publish grant was live. Consumers reading `ctx.capabilities`/`ctx.basis` (`pricing_authority.js`, `authority_resolution.js`, fee decisions, `operator.js`) receive the same shape. Rung: `HTTP_PROVEN` locally through the verification parent; CI (`verify`, `.github/workflows/verify.yml`) green on this commit — row 60. | `src/identity/actor_context.js`; `tests/e2e/authority_grants_union.e2e.js`; `tests/e2e/verify_all.sh` |
| 49 | **RESOLVED IN SOURCE, 2026-09-05 — one bank deposit could cash-prove an unbounded total of payments.** `POST /payments/:id/link-bank` (`src/money/payments.js`) ties a payment to a bank deposit, and `recomputePayment` treats any link as cash proof for the whole payment. The route checked the deposit's existence, property and sign — never how much of it earlier links had already attributed, nor whether `amount_matched` fit the payment. A 1,500.00 deposit accepted a second full 1,000.00 payment (and would have accepted a fifth); `amount_matched: 2000` was accepted on a 1,000.00 payment. Fix, one invariant: a link attributes `amount_matched` when given, else the whole payment; the sum over the deposit's non-void links plus this attribution may not exceed the deposit → 409 naming the deposit, what it already proves, what was attempted and the two ways forward (split with `amount_matched`, or a different deposit), and nothing written; `amount_matched` above the payment → 409; not positive → 400. Accepted links and the response shape are unchanged. **Adjacent, recorded not fixed:** `recomputePayment` still counts a link with a partial `amount_matched` as full cash proof for the payment's status — a partially proven payment's `cash_proven` reads the same as a fully proven one; bounding status by attributed amount changes an existing figure and is a ruling. Proof: `tests/e2e/deposit_attribution_bound.e2e.js`, invoked by `tests/e2e/verify_all.sh` against the real `server.js` on a disposable loopback database — first full link 200; second full link 409 with the receipt and `links=1`; split 500 → 200; one cent past the deposit 409; `amount_matched` 2,000 on a 1,000 payment 409; `amount_matched` 0 → 400; the untouched deposit has no links. Falsified against the unmodified parent `ff4b3a3`: 1/9 — both over-attributions answered 200 and were written. No app caller for `link-bank` at pinned app `4849545`. Rung: `HTTP_PROVEN` locally through the verification parent; CI (`verify`, `.github/workflows/verify.yml`) green on this commit — row 60. | `src/money/payments.js`; `tests/e2e/deposit_attribution_bound.e2e.js`; `tests/e2e/verify_all.sh` |
| 48 | **RESOLVED IN SOURCE, 2026-09-05 — `GET /demo/intake/health` was an anonymous diagnostic outside the demo.** The route (`src/leasing/leasing_leads.js`, marked *TEMP DIAGNOSTIC*) sits under the key-exempt `/demo/` prefix and answered anyone, on any deployment, with database reachability, the `lead_events` check-constraint definition state and the boot-time self-heal outcome — a string that can carry a raw database error message (`"FAILED: " + e.message`). Its sibling `POST /demo/intake` fails closed unless `DEMO_MODE=true`; the diagnostic did not. Fix: the same wall, first thing in the handler — outside the demo it answers 403 `The live demo is not enabled on this deployment.` and nothing else. **Class 4, still temporary:** its removal condition is the self-heal block above it (`_selfHealStatus`) being retired once every environment's ledger carries migration 055 — the diagnostic exists only to watch that heal. **Adjacent, recorded not fixed:** that self-heal block runs `alter table lead_events` from application code at boot — schema mutation outside the migration ledger, which `prestart` verify-only mode exists to forbid; retiring it is an owner ruling because it exists to rescue an un-migrated deployment. Proof: `tests/e2e/demo_intake_health_gate.e2e.js`, invoked by `tests/e2e/verify_all.sh` against the real `server.js` (booted without `DEMO_MODE`) on a disposable loopback database — 403 with the receipt and none of `database`, `lead_events_check`, `self_heal`, `build`; `POST /demo/intake` pinned to the same wall. The `DEMO_MODE=true` side is **not measured** by this parent (one server, outside the demo). Falsified against the unmodified parent `a0ac76e`: 1/4 — 200 with `database: connected` and the self-heal status. No app caller at pinned app `4849545`. Rung: `HTTP_PROVEN` locally through the verification parent; CI (`verify`, `.github/workflows/verify.yml`) green on this commit — row 60. | `src/leasing/leasing_leads.js`; `tests/e2e/demo_intake_health_gate.e2e.js`; `tests/e2e/verify_all.sh` |
| 47 | **RESOLVED IN SOURCE, 2026-09-05 — a bodiless Read AI webhook delivery was a 500 with no security receipt.** `POST /integrations/read-ai/webhook` parses its own raw body (`express.raw`, mounted ahead of `express.json`). When a POST carries no body — no `Content-Length`, no `Transfer-Encoding`, or `Content-Length: 0` with no content type — `express.raw` hands the route `{}`, and `receiveReadWebhook` (`src/meeting_evidence/meeting_evidence_service.js`) did `Buffer.from({})`, which throws `ERR_INVALID_ARG_TYPE`. The route answered 500 `processing_failed` and wrote no `meeting_webhook_security_receipts` row, so the cheapest probe to send left no trace. Fix: a non-Buffer, non-string body is an empty body (`Buffer.alloc(0)`); the delivery then reaches the governed refusal chain unchanged — with the verification environment's configuration (connection id, no signing key) that is 503 `refused_unknown_connection`, receipted with `byte_length` 0 and the empty-body sha256. Signature verification and the payload path are untouched. Proof: `tests/e2e/read_ai_webhook_empty_body.e2e.js`, invoked by `tests/e2e/verify_all.sh` against the real `server.js` on a disposable loopback database — writes the bodiless POST straight to the socket (fetch cannot omit `Content-Length`): not 500, a `refusal_status` the table's check admits, one new receipt row for the empty hash whose `refusal_status` matches the response; the `Content-Length: 0` form pinned beside it. Falsified against the unmodified parent `dbe8771`: 0/5 — 500 `processing_failed` on both forms, server log `TypeError [ERR_INVALID_ARG_TYPE] … Received an instance of Object`, no receipt. Rung: `HTTP_PROVEN` locally through the verification parent; CI (`verify`, `.github/workflows/verify.yml`) green on this commit — row 60. | `src/meeting_evidence/meeting_evidence_service.js`; `tests/e2e/read_ai_webhook_empty_body.e2e.js`; `tests/e2e/verify_all.sh` |
| 46 | **RESOLVED IN SOURCE, 2026-09-05 — `GET /operator/build` answered anyone, though documented as behind the operator gate.** The key gate in `server.js` exempts `/operator/*` on the stated premise that every route there resolves its own staff session. `/operator/build` (`src/baseline/baseline_routes.js`) resolved nothing and published the full build identity — untruncated commit, resolving variable, node version, process start — with no credentials at all. Fix: the route resolves `x-staff-session` through the `staffSessions` resolver injected in row 39; no session → 401 `No valid operator session. Sign in.` (the same refusal as the other `/operator/*` gates); the shared operator key alone is not a session. `/health` keeps its anonymous SHORT sha by design and is pinned so the two doors do not drift into each other. Proof: `tests/e2e/operator_build_gate.e2e.js`, invoked by `tests/e2e/verify_all.sh` against the real `server.js` on a disposable loopback database — no credentials 401 with no build record; operator key alone 401; unresolvable token 401; staff session 200 with `build.commit` and `resolved_from`; `/health` 200 with `commit_short` and never `commit`. Falsified against the unmodified parent `7d56a8b`: 2/6 — 200 with the full record for all three unauthenticated calls. No app caller for `/operator/build` at pinned app `4849545`. Rung: `HTTP_PROVEN` locally through the verification parent; CI (`verify`, `.github/workflows/verify.yml`) green on this commit — row 60. | `src/baseline/baseline_routes.js`; `tests/e2e/operator_build_gate.e2e.js`; `tests/e2e/verify_all.sh` |
| 45 | **RESOLVED IN SOURCE, 2026-09-05 — an org admin's roster disclosed seats on other organizations' properties.** `GET /org/me` and `GET /org/users` (`src/identity/org_admin.js`) scope USERS to `req.orgId` but joined `property_team_assignments` without asking which organization the property belongs to. A user seated on a property in another organization showed that seat to this org's admin — `assignment_id`, `property_id`, `role_key`, `allowed_modules`, `can_manage_roles`, with only `property_name` blanked — and `/org/me` counted it in `property_count`. `DELETE /org/users/:userId` already confined deactivation to the org's properties; the reads did not match it. Fix: `/org/me` counts `distinct p.id` through `properties p … and p.organization_id = $1`; `/org/users` aggregates `filter (where p.id is not null)` instead of `a.id is not null`. Response shapes unchanged. Proof: `tests/e2e/org_roster_scope.e2e.js`, invoked by `tests/e2e/verify_all.sh` against the real `server.js` on a disposable loopback database — two organizations, a staff member seated in both; `/org/me` `property_count` 1; `/org/users` lists only the org's own seat, the admin's own seat still listed, the other organization's user absent from both reads. Falsified against the unmodified parent `7b27a35`: 5/8 — `property_count` 2 and the foreign seat listed with `property_name: null`. No app caller for `/org/*` at pinned app `4849545` (`git show 4849545:index.html`). Rung: `HTTP_PROVEN` locally through the verification parent; CI (`verify`, `.github/workflows/verify.yml`) green on this commit — row 60. | `src/identity/org_admin.js`; `tests/e2e/org_roster_scope.e2e.js`; `tests/e2e/verify_all.sh` |
| 44 | **RESOLVED IN SOURCE, 2026-09-05 — a move-in could be scheduled on one unit against a lease that lives on another.** `POST /units/:id/schedule-move-in` (`src/tenancy/movein.js`, legacy operations route) wrote whatever `lease_id` the body named into `unit_events.lease_id` without asking whether that lease's space (`leases.space_id → spaces.unit_id`) is on the unit. Two consequences on the untouched parent: readiness approval of the wrong unit fed `unit_ready` into the other lease's delivery (`approveReadiness` → `deliveryHelper.satisfyDeliveryInput`), and `uq_unit_events_one_movein_per_lease` (migration 074) then refused the lease's real unit with a raw 500 `MOVE_IN_FAILED`. Fix: when a lease is named, one query resolves the unit that holds its space — no lease → 404 `LEASE_NOT_FOUND`; another unit → 409 `LEASE_NOT_ON_UNIT` naming the unit and saying nothing was scheduled. The no-lease legacy shape is unchanged. Proof: `tests/e2e/movein_lease_on_unit.e2e.js`, invoked by `tests/e2e/verify_all.sh` against the real `server.js` on a disposable loopback database — foreign lease 409 with no event written; unknown lease 404; the lease's own unit 201 with the single `move_in_scheduled` event on it; no-lease body 201. Falsified against the unmodified parent `d2fed71`: 0/7 — 201 for the foreign lease, then 500 `MOVE_IN_FAILED` for the lease's own unit. No app caller at `4849545`; `tests/arcs/movein_beat.js` calls it with the lease's own unit and is unaffected. Rung: `HTTP_PROVEN` locally through the verification parent; CI (`verify`, `.github/workflows/verify.yml`) green on this commit — row 60. | `src/tenancy/movein.js`; `tests/e2e/movein_lease_on_unit.e2e.js`; `tests/e2e/verify_all.sh` |
| 43 | **RESOLVED IN SOURCE, 2026-09-05 — a notice was written without `unit_events.space_id`, so the canonical space reader never saw it.** `POST /units/:id/notice` (`src/tenancy/notice.js`) resolves WHICH space the notice is on (a by-bed unit must name it) and then wrote the space only into the JSON payload. The canonical space reader (`src/tenancy/space_position.js`) finds a notice by the COLUMN `ue.space_id` (migration 081), so a notice succeeded — 201, receipt and all — and was invisible to availability, the future rent roll and every surface that reads `space_position`. Fix: the insert writes the resolved `t.space_id` into the column. Nothing else about the route changes. Proof: `tests/e2e/notice_space_column.e2e.js`, invoked by `tests/e2e/verify_all.sh` against the real `server.js` on a disposable loopback database — 201 with the resolved tenancy; `unit_events.space_id` equals the resolved space; the space reader's own predicate (`ue.space_id = s.id and event_type='notice_given' and status='scheduled'`) finds the notice with its move-out date. Falsified against the unmodified parent `7170d5c`: 201 with `space_id` NULL and the predicate finding nothing. No app caller at `4849545`. Rung: `HTTP_PROVEN` locally through the verification parent; CI (`verify`, `.github/workflows/verify.yml`) green on this commit — row 60. | `src/tenancy/notice.js`; `tests/e2e/notice_space_column.e2e.js`; `tests/e2e/verify_all.sh` |
| 42 | **RESOLVED IN SOURCE, 2026-09-05 — a revoked Read AI connection was re-opened by any re-post.** `POST /operator/meeting-evidence/read-ai/connection` upserts the one provider connection (`integration_connections`); its `on conflict` set `connection_status='active'` unconditionally, so a connection an administrator had revoked to stop ingress came back to life the moment any meeting-evidence user (management, asset_management or leasing module) called the route again. Fix: the update is predicated on `connection_status <> 'revoked'`; a revoked row is refused 409 `read_ai_connection_revoked` and nothing is written. **Deliberately unchanged, and CLASSIFIED:** on a re-post the ORIGINAL `authorized_by_user_id` stays on the row — delivery-finality qualification (`meeting_evidence_service.js`, *only the user who authorized this Read connection may qualify*) is bound to that user, so rewriting it on every re-post would hand that authority to whoever posted last. The donor branch's version, which recorded the re-poster as the new authorizer, is rejected for that reason. Whether a different user may re-authorize at all, and how a revoked connection is deliberately re-authorized, are owner decisions. Proof: `tests/e2e/read_ai_connection_authority.e2e.js`, invoked by `tests/e2e/verify_all.sh` against the real `server.js` (`boot.sh` now names `READ_AI_CONNECTION_ID`) on a disposable loopback database — first authorization 200; re-post against a revoked row 409 from another user and from the original authorizer, row still revoked with its metadata untouched; re-post on an active row by another user 200 with the original authorizer retained. Falsified against the unmodified parent `e35cef3`: the revoked connection answered 200 and was active again. No app caller at `4849545`. Rung: `HTTP_PROVEN` locally through the verification parent; CI (`verify`, `.github/workflows/verify.yml`) green on this commit — row 60. | `src/meeting_evidence/meeting_evidence_service.js`; `tests/e2e/read_ai_connection_authority.e2e.js`; `tests/e2e/boot.sh`; `tests/e2e/verify_all.sh` |
| 41 | **RESOLVED IN SOURCE, 2026-09-05 — four queries still read `work_orders.person_id`, a column migration 098 dropped.** 098 split it into `reported_by_person_id` and `affected_person_id` and dropped the old name. `GET /tenant/me` (`src/comms/tenant_link.js`) kept `where person_id = $1`, so EVERY resident's own view has answered 500 "Could not load your view." since 098. `src/surfaces/desks.js` kept `w.person_id` in three queries (tenant-waiting count, item list, tenant-waiting set), so the maintenance dashboard has answered "Maintenance board unreadable" with an `unavailable` headline (`column w.person_id does not exist`) and the operator-home maintenance card has shown its fallback text. Re-derived on this tree, ONE definition: the resident of a work order is the person its updates go to — `coalesce(affected_person_id, reported_by_person_id)`, the precedent `src/technician/conversation.js` already uses — so "tenant waiting" measures the person who would receive the reply; the resident's own view lists what they REPORTED or what AFFECTS their home (both of 098's relationships). Proof: `tests/e2e/work_order_person_columns.e2e.js`, invoked by `tests/e2e/verify_all.sh` against the real `server.js` on a disposable loopback database — `/tenant/me` 200 listing exactly the reported and the affected orders (not the complete one, not a neighbour's); maintenance-dashboard 200 with an `ok` headline, `tenant_waiting` counting the tenant-sourced orders with no update sent, the resident's item carrying their name; operator-home's maintenance card carrying real facts. Falsified against the unmodified parent `1e66af6`: 500, `unavailable`, fallback text. The deployed app (`4849545`) calls the maintenance dashboard and operator-home; the resident portal calls `/tenant/me`. Rung: `HTTP_PROVEN` locally through the verification parent; CI (`verify`, `.github/workflows/verify.yml`) green on this commit — row 60. | `src/comms/tenant_link.js`; `src/surfaces/desks.js`; `tests/e2e/work_order_person_columns.e2e.js`; `tests/e2e/verify_all.sh` |
| 40 | **RESOLVED IN SOURCE for the presented-session case, 2026-09-05; the key-only legacy path is CLASSIFIED, not decided.** `POST /ingest/:runId/approve` and `/promote` resolved a staff session for attribution (#39) but never asked whether that session was seated on the run's property: a session seated on ANOTHER building approved and promoted this run's units and was recorded as reviewer and promoter. Fix: when a session resolves, its `property_id` must equal the run's, else 403 `property_scope_refused` before any write; the run-not-found 404 and the body-actor 400 come first, unchanged. Proof: `tests/e2e/ingest_property_authority.e2e.js`, invoked by `tests/e2e/verify_all.sh` against the real `server.js` on a disposable loopback database — wrong-property approve and promote 403 with no candidate change and no unit created; same-property approve 200 / promote 200 with the seated user recorded; body actor 400 even from a wrongly seated session; unknown run 404 first. Falsified against the unmodified parent `5ffab561`: the wrong-property session approved (200, reviewer recorded) and promoted (200, unit created). **CLASSIFIED, awaiting an owner ruling:** with no session at all — or a presented token that does not resolve — every route in `document_ingest_routes.js` still runs on the shared operator key alone and records no actor. Eight routes: pasted-text and file ingest, run read, candidate edit, bed-group read and grouping, approve, promote. No caller in the pinned app (`4849545`); no governed consumer found in `tools/`, `docs/` or `tests/` (one proof, `deal_setup_http.db.js` H19, uses key-only only to prove the body-actor refusal). Requiring a session or retiring the door changes the route contract; that decision is the owner's, and this slice deliberately did not take it. The unresolvable-token case is asserted in the proof so it cannot drift silently into either meaning. Rung: `HTTP_PROVEN` locally through the verification parent; CI (`verify`, `.github/workflows/verify.yml`) green on this commit — row 60. | `src/agent/document_ingest_routes.js`; `tests/e2e/ingest_property_authority.e2e.js`; `tests/e2e/verify_all.sh` |
| 39 | **RESOLVED IN SOURCE, 2026-09-04 — three extracted routes referenced a session resolver they never received.** When `server.js` was decomposed on 2026-08-27, `POST /ingest/:runId/promote`, `POST /ingest/:runId/approve` (`src/agent/document_ingest_routes.js`) and `POST /properties` (`src/baseline/baseline_routes.js`) kept calling `staffSessions.resolveStaffSession(...)`, a module-level constant of `server.js` that neither factory was handed. In the two ingest routes the call ran BEFORE the handler's `try`, so the `ReferenceError` was an unhandled rejection and under Node 22 ONE call terminated the process; in `POST /properties` the `try` caught it and answered 500 where the route intends 401. Fix: `server.js` injects the one existing `staffSessions` into both factories; each factory asserts the dependency at construction (a missing binding now fails at boot, not at the first request); the two ingest resolutions moved inside their existing `try`. Every route path, status code, response shape, the global operator-key gate, the optional session and the body-actor refusal are unchanged. Proof: `tests/e2e/extracted_route_bindings.e2e.js`, invoked by `tests/e2e/verify_all.sh` against the real `server.js` on a disposable loopback database — key-no-session 401, session-no-key 401 with no mutation, key+same-property session approve 200/`approved_count` 1/`reviewed_by` recorded and promote 200/`promoted_count` 1/no skips/`promoted_by` recorded/unit linked/server healthy, an injected rejecting resolver → handled 500 on both with the process alive, construction without the binding throws. Falsified against untouched `d55dae9`: `POST /properties` answered 500 and the promote call ended the server process. **This proves actor ATTRIBUTION, not property AUTHORIZATION** — the legacy routes' property-scope policy is untouched and is a separate decision. Rung: `HTTP_PROVEN` locally through the verification parent; CI (`verify`, `.github/workflows/verify.yml`) green on this commit — row 60. The deployed app (`4849545`) does not call these three routes; any operator-key holder can. | `server.js`; `src/agent/document_ingest_routes.js`; `src/baseline/baseline_routes.js`; `tests/e2e/extracted_route_bindings.e2e.js`; `tests/e2e/verify_all.sh` |
| 1 | **`PRODUCTION_PROVEN`, 2026-08-21.** ~~The leasing agent quotes `units.market_rent` directly to prospects~~. A controlled live Skyline turn against unit `1417-102` returned exactly **"Unit 1417-102 is $850/month on a 12-month lease."** from the published 2BR term. The run recorded `pricing_direct_quote`, prompt revision `stage-a-v10`, no provider request, and no dispatched outbound event. This second deploy followed a failed live proof in which the governed price reached the prompt but the model skipped the answer; clear linked-unit rent questions now defer directly to the canonical pricing adapter rather than asking the model to restate it. | Production draft `a0059ea8-aacd-4a5c-892a-6728afcb00bb`; deployed commit `4f555c1`; `tests/agent_property_identity_proof.js` |
| 2 | **`docs/deployment.md` instructs incorrectly about the one mechanism protecting production.** Says startup *"runs any unapplied migrations."* It does not — it verifies and **refuses to start**. | `docs/deployment.md:51` vs `migrations/migrate.js` |
| 3 | **An operator screen calls routes that 404.** A whole activation flow written, never mounted. | `src/identity/activation.js`; `grep -c "identity/activation" server.js` = **0** |
| 4 | **A test defaults to hitting PRODUCTION**, with no run receipt anywhere. | `tests/full_lifecycle_arc.js:47` |
| 5 | **RESOLVED AND DEPLOYED, 2026-08-21 — Ask Spine no longer owns a second obligation reader.** `ask_spine_service.js` consumes `operator_obligations_service.attention()` and owns only the conversational projection. The canonical service owns property/module scope, overdue meaning, open-state filtering, ranking, count, and cap for both surfaces. Source contract: 35/35; Ask Spine authenticated real HTTP + real migrated Postgres: 23/23; operator obligations authenticated real HTTP + real migrated Postgres: 21/21. | `src/obligations/operator_obligations_service.js`; `tests/ask_spine_contract_proof.js`; `tests/ask_spine_db_proof.db.js`; `tests/operator_obligations_security_proof.db.js`; deployed API `7bbb23e` |
| 6 | **The §40.11 gate scans 2 of ~15 domain dirs** — `["src/asset","src/tenancy"]`. Leasing, applications, maintenance, technician, comms, obligations, money, onboarding, **and now `src/meeting_evidence/`** cannot fail it. | `tests/gate_ask_spine_readers.js:100` |
| 7 | **REOPENED, 2026-08-22.** The 2026-08-20 repair did put production back on `main`, but production has diverged again. `/health` identifies `61f99bf` on `codex/skyline-conversation-integration-20260820`; `origin/main` is `b7720b2`, 43 commits behind that runtime, and contains migrations only through 187 while the production ledger is at 189. The deployed branch includes `main`, so this is forward drift rather than a conflicting lineage, but `main` cannot reproduce the production release today. | `/health` observed 2026-08-22; `git rev-list --count origin/main..61f99bf` → 43; Neon `schema_migrations` ceiling → 189; `origin/main` migration ceiling → 187 |
| 8 | **RESOLVED, DEPLOYED, AND PRODUCTION-OBSERVED, 2026-08-21.** ~~A signed-in operator's Invite button silently fakes success.~~ The first deployed repair still passed Team through the page-wide historical-snapshot interceptor, so a valid live session rendered `Team roster unavailable. Not part of this historical snapshot.` Team roster and invite creation now use named sealed live resources on the existing staff-session client; neither can enter the snapshot path. Production rendered the live Skyline roster, accepted one canonical Leasing invite for Mike Grivna, and returned `sms_sent`. A signed-in failure remains a failure and creates no local row. | Fix first shipped in app `567d15f`, present in deployed `d45344d`; `team_access_live_boundary.test.js`; current full app suite 1,467/1,467; production Team receipt 2026-08-21 |
| 9 | **RESOLVED AND DEPLOYED, 2026-08-21.** ~~The team roster read has no property-scope check.~~ Team roster, invite, current-access and assignment-edit URLs now share the operator CORS boundary but derive identity and property from `x-staff-session`. No session is 401; a session for another property is 403; the browser operator key is not an alternate authority path. | API `7bbb23e`; `tests/team_access_session_boundary.test.js`; real HTTP/Postgres authority proof 50/50; production no-session roster read 401 |
| 10 | **Two inbound Twilio SMS webhooks, two different security postures.** `/communications/inbound-sms` documents itself as fail-closed on signature verification. `/intake/twilio` (a second, separate webhook) is gated only by a phone-number allowlist (`INTAKE_ALLOWED_NUMBERS`) — no signature check found. | `src/onboarding/intake.js:220` vs `src/comms/communications_boundary.js` |
| 11 | **`CLAUDE.md`'s own deploy description doesn't match reality.** States *"Deploys to Render on merge to main,"* which reads as automatic push-to-deploy. The actual mechanism is `deploy.sh` — a manual script calling Render's API directly, run by a human. If it's meant to be automatic, it currently isn't. | `deploy.sh` vs `CLAUDE.md`'s "Repo orientation" section |
| 12 | **RULED 2026-08-20 — ACCEPTED AS INTENDED, WITH A REVISIT TRIGGER.** ~~A real hole in published-pricing immutability~~ — `delete from properties` cascades through the freeze that direct term deletion correctly refuses. Owner's ruling: **allow it for now.** Deleting a property should delete its pricing. **Revisit trigger, stated by the owner: "when we start dealing with more real properties."** Recorded rather than closed, because the day that trigger fires, this becomes a schema change nobody will remember was a deliberate choice. | `tests/e2e/agent_pricing_wall.e2e.js` teardown comments |
| 13 | **CLAIMED, NOT STARTED — open for either party.** Four falsification tests are pinned to a hardcoded demo UUID and nothing runs them. Verified red before AND after the pricing fix, identically, by stashing. `claude/property-spine-orientation-cso2ao` claimed this then spent the time on deploy support instead. Say who's taking it before starting, to avoid duplicate work. | `tests/` — unnamed in the report, flagged for follow-up rather than fixed |
| 14 | **RESOLVED AND DEPLOYED, 2026-08-21.** ~~`terms[0]` picks the shortest — and dearest — published term when a prospect names none~~. The adapter now agrees with `effective_pricing.js:399`: one published term is quoted and stated; two or more return the published menu and ask the prospect to choose. Clear linked-unit rent questions consume that adapter response directly, so the model cannot replace either the quote or the menu. **Proof:** real-DB `tests/e2e/agent_pricing_wall.e2e.js` 22/22; controlled live Skyline one-term quote recorded under defect #1. | `src/agent/pricing_adapter.js`; `src/agent/agent.js`; deployed commit `4f555c1` |
| 14b | **RESOLVED WITH #14 — this row identified the option that was built.** Found by the closeout thread while verifying the deploy: `effective_pricing.js:399` does not merely refuse when no term is supplied, it returns `published_terms`, the sorted list of every term on the sheet, *"specifically so a caller can present the choice instead of guessing."* That was the third option — present the menu, reuse data that already exists, no schema change — and it is what shipped. Kept rather than folded into #14 because the finding was not mine and the record should say where it came from. | `src/money/effective_pricing.js:399` · implemented in `src/agent/pricing_adapter.js` |
| 15 | **RESOLVED, 2026-08-20 — a real privilege-escalation path is now closed.** `orgchart.js` could previously create `owner`/`asset_manager` roles — pricing authority (see #14) among them — through a route gated only by the shared `x-operator-key`, with **no person-level check and no actor recorded**. Landed in the same deploy as defect #1's fix (45 commits, 9 runtime files, zero migrations). Measured on a disposable DB by the reporting thread; not yet independently re-verified against production by me. Kept as a record, not deleted, per this file's own rule. | `src/surfaces/orgchart.js`, `src/identity/authority_resolution.js` — `ASSIGNABLE_ROLES = new Set(["owner", "asset_manager"])` confirmed on `main` |
| 16 | **RESOLVED AND INDEPENDENTLY VERIFIED, 2026-08-21.** The Render database credential was stale again: `/health` returned `password authentication failed for user 'neondb_owner'`. The current Neon production connection was installed in Render, commit `bcd3089` was redeployed for the repair, and `/health` returned 200 with a live database timestamp. Later deploys `556c443` and `4f555c1` also booted green against that connection. | Render deploys `dep-da43sck9v7es7395k8a0`, `dep-da448inqj5pc73b95alg`, `dep-da44cn3m8hqs73doah40`; `/health` observed directly |

**Correction history, not a new defect.** `tests/phase_zero_property_boundary.db.js`
genuinely proves, with real Postgres and real HTTP, that a second real user
without a property assignment is refused by the property-switcher
(`GET/POST /operator/properties[/select]`). The roster did lack that wall when
this correction was written; defect #9 now records its later resolution.

---

| # | Defect | Evidence |
|---|---|---|
| 17 | **159 phantom unit rows at Skyline, shaped like beds.** Alongside `1417-116` production carries separate `units` rows named `116 - A`, `116 - B`, `116 - C` — null bed, no source code, not reachable from any import row. They are why `count(*)` reports 231 units for a 72-unit property, and why the mapping tool reports 159 positions "Not configured" after a correct run. **Not a blocker and deliberately untouched**: the canonical loader sees 160, the mapping's exact-match override cannot reach them, and the e2e now carries six of them as decoys asserting exactly that. Cleaning them is a production delete with unknown FK reach — investigation first, then a decision, not a sweep. | `tools/apply_unit_type_mapping.js` production run 2026-08-20; decoys in `tests/e2e/skyline_unit_type_mapping.e2e.js` |
| 18 | **RESOLVED AND DEPLOYED FROM THE INTEGRATION BRANCH, 2026-08-21 — not yet observed through the production Ask Spine staff surface.** `economic_picture.js` preserves each term's economics. One published term can produce a flat quote; two or more return a term menu, set `lease_term_not_selected`, and withhold a combined monthly total until a term is chosen. Ask Spine consumes this same composition for published asking rent, governed charges, deposit requirements, and advertised concessions; it does not own a second pricing reader. Source contract 11/11; real session + real Express + real HTTP + real migrated Postgres 16/16; prospect price wall 22/22. | `src/money/economic_picture.js`; `src/agent/ask_spine_answer.js`; deployed commit `4f555c1` |
| 19 | **RESOLVED AND DEPLOYED FROM THE INTEGRATION BRANCH, 2026-08-22 — not yet observed through a production staff SMS turn.** The operations SMS line routes clear reads through the existing Ask Spine service and explicit post-tour/application actions through the canonical leasing services. Technician actions, attachments, field findings, and conversations about one work order remain on the technician path. Identity, property scope, module access, people, and application targets remain server-derived; questions never become writes. No new conversational pipe. Pure routing 35/35; leasing action contract 53/53; real webhook-to-exact-bed-lease journey 36/36. | `src/conversation/staff_sms_router.js`; `src/comms/staff_governed_read.js`; `src/leasing/staff_sms_action.js`; `src/comms/staff_thread.js`; `src/comms/tenantlink.js`; deployed API `61f99bf` |
| 20 | **`PRODUCTION_PROVEN`, 2026-08-21 — property identity is scoped to the active conversation.** The first controlled Skyline price turn exposed that the shared prompt still identified every property as SOLO at 4233 Chestnut; the live draft told the Skyline prospect they had the wrong building. The legacy SOLO profile is now included only for an explicitly SOLO/4233 property. A post-deploy Skyline turn returned the Skyline unit's governed price with no SOLO, Chestnut, or University City content. | Failed draft `28b0781f-6fc5-4a88-8191-2b9a2affe979`; passing draft `a0059ea8-aacd-4a5c-892a-6728afcb00bb`; `tests/agent_property_identity_proof.js` |
| 21 | **RESOLVED AND DEPLOYED, 2026-08-21 — authority grants require real two-party review.** Precondition 9 now resolves the reviewer through the canonical actor reader and requires a distinct linked `human_staff` person, active staff context covering the property, and `may_manage_concession_authority`; the reviewer person and authority basis are written to provenance. Self-review is refused even after the recipient gains property entitlement. **Not yet exercised by a production authority grant.** Pure hostile proof passed; disposable-Neon authority chain 17/17; publication 14/14; pricing wall 22/22; Economics Ask real HTTP 16/16. | `src/identity/authority_resolution.js`; `tests/e2e/authority_chain.e2e.js`; deployed commit `556c443` (also in `4f555c1`) |
| 22 | **DEPLOYED, ACTIVATION WAITING ON INVITEE VERIFICATION, 2026-08-21.** A controlled Skyline prospect asked to tour unit `1417-102`; the agent offered no slot and instead asked for move-in/move-out dates. No SMS was sent. **Owner ruling:** Property Spine's native scheduler is the authority; Acuity or another bolt-on scheduler is not the target. One canonical service now owns the weekly policy, explicit slot publication/list/block/reopen, two-hour notice, 45-day horizon, federal-holiday closures, default host, property timezone conversion, host/unit walls, exact-slot dedupe, day-level callout close/reassign, and attributable receipts. Booked tours remain visible and are reported for coverage rather than silently changed. The prospect agent and public booking page share one offerable-slot reader and one booking transaction. The staff app and staff SMS Ask Spine both read the same schedule standing; neither expands weekly hours into availability. **Owner-confirmed Skyline policy:** Monday-Friday 9-5, Saturday 10-3, Sunday/federal holidays closed, 60-minute blocks, Mike Grivna default host. **Proof:** exact slots 23/23, weekly policy/callouts 25/25, real session + HTTP 20/20, canonical booking 33/33, cross-turn agent offer/confirm 12/12, Ask Spine schedule 8/8, existing Skyline-shaped lead-to-lease real-HTTP walk 21/21, all 47 source-governance gates, and all 1,467 app assertions pass. API `61f99bf` and app `d45344d` are live; production ledger 189 and all three native scheduler tables are verified; Skyline's audited timezone is `America/New_York`. A canonical Mike Grivna Leasing invite is now active, linked to the existing person, and provider-accepted; it superseded both stale invites. Mike has not accepted it yet, so no linked user, staff context, Skyline assignment, or eligible default host exists yet. No policy or slot row was published and Skyline is not in the agent-booking allowlist. Next: Mike verifies; re-read all four identity records; publish the policy; enable Skyline booking; repeat the controlled prospect turn. | Production draft `7a00aff5-3ec2-48de-b6e0-8dfefb1680f1`; invite `47ff00c7-8847-4ef8-8b3c-bbc3334183e8`; current API `61f99bf`; deployed app `d45344d`; timezone receipt `289f0937-e1d5-4d67-81d0-cf44ec1f588c`; migrations 188-189; native tour and unified-onboarding proof suites |
| 23 | **RESOLVED IN DEPLOYED CODE; SKYLINE ACTIVATION PENDING, 2026-08-22.** The post-tour sender previously stopped at a multi-space Skyline unit, the live conversation screen used a separate prepare/copy/manual-attestation writer, and Application Review could read `company_execute_lease` without offering the action. Both staff doors now select the same server-authored exact target and call one composite send command; `space_id` survives the invitation, tenant application, lease packet, company execution, tenancy and Person Card. The tenant sees `Unit 3B · Bed B`, while a whole-unit property keeps the simple unit label. **Observed proof:** a real server and fresh disposable production-shaped Neon branch completed native 60-minute Mike slot → booking → real operations webhook → honest standing clarification → explicit post-tour capture → Mike's personal Ask Spine answer → exact Bed B application fake-SMS → tenant submission → resident signature → company signature → exact Bed B tenancy, 36/36. No carrier was reached. App suite: 38 harnesses, 1,467 assertions; the Staff Texting organization control was visually checked against production at 390px with zero page or card overflow. Production serves app `d45344d` and API `/health` identifies `61f99bf`. **Not activated:** Mike's invite remains unaccepted, Skyline is absent from the application-intent and executed-lease property perimeters, and no live application text or live tenant execution was attempted. | `application_send_command.js`; `application_target_read.js`; `applicationSubmission.js`; `staff_sms_action.js`; `operator.js`; app `followups-door.js` / `index.html`; `tests/e2e/tour_application_lease.e2e.js` |
| 24 | **PRODUCTION-EXERCISED THROUGH DELIVERY; ACCEPTANCE PENDING, RE-READ 2026-08-22.** One canonical staff invitation establishes the phone login, explicit user-to-person bridge, property-scoped staff context, person-keyed work assignment, and property-team access in the same acceptance transaction. Exact phone matches are candidates only: the manager confirmed Mike Grivna's existing person before the system wrote or sent anything. Production created one linked Leasing invite, superseded both stale rows, and the live response recorded `sms_sent`. The invite remains active and unaccepted, so the downstream identity and access records correctly do not exist. Disposable production-clone proof 50/50; identity bridge regression 44/44; staff SMS delivery 11/11. | Invite `47ff00c7-8847-4ef8-8b3c-bbc3334183e8`; migration 189; `src/identity/teamaccess.js`; `src/identity/staffbridge.js`; API `61f99bf`; app `d45344d` |
| 25 | **`PRODUCTION_PROVEN`, 2026-08-22 — 541-305-8509 IS THE ONEFIVE OPERATIONS LINE.** Staff SMS deliberately enters through one organization-level `operations` line with `staff` audience and `reply_only` outbound policy; Skyline's active property line remains correctly limited to residents and prospects. Production has one active `OneFive Management` organization and Skyline was adopted into it through the governed super-admin path, with immutable receipt `ec4cd2b5-468b-4b8f-9156-f0fd1681e563`. The deployed transfer command rechecks the live super-admin, target organization, property ownership, source line, and number uniqueness, then retires the source and activates the target in one transaction. On the owner's ruling, `+15413058509` moved from Demo ORG to OneFive: source line `be0d860b-95f5-4477-a723-a5562f2d7797` is retired with all 26 historic events preserved, and OneFive line `9b98408b-c1b3-4046-9179-c96f62842f92` is active. The production resolver returns exactly that OneFive line; Demo ORG returns `not_connected`. The attributable actor was KZ (`78375274-922a-44c5-8b61-0c285d1b9911`). Tom/Solo no longer resolve through this number. No live SMS was sent. Proof: real disposable Postgres 40/40, transfer contract 5/5, line model 61/61, reply policy 32/32, 47 source-governance gates, app 38 harnesses/1,467 assertions, and post-transfer production resolver audit. Next: Mike accepts his invite, then send the first controlled OneFive Ask Spine text. | `src/comms/communication_lines.js`; `src/identity/super_admin.js`; `tests/operations_line_transfer.test.js`; `tests/operations_line_activation.db.js`; API `61f99bf`; app `d45344d`; production re-read 2026-08-22 |
| 26 | **RESOLVED AND DEPLOYED, 2026-08-22 — live two-surface observation waits on Mike's invite acceptance.** “What should I do today?” and its pinned natural variants now use one deterministic person-scoped obligations read whether asked in the dashboard or through staff SMS. The read admits only direct `assigned_user_id`, explicit `escalates_to_user_id`, and unassigned work in the signed-in assignment's `primary_for_modules`; property and module authority remain server-derived, and role/title inference is absent. It does not call a model to reinterpret the queue. A specific work-order question and every action remain in the technician conversation. Leasing conversion birth, explicit handoff, and reopen now project their recorded owner into the same canonical obligation instead of maintaining a parallel owner field that the personal reader cannot see. Source convergence 28/28, router 35/35, disposable production-shaped Postgres personal scope 13/13, conversion rail 15/15, release/reopen 23/23, full Skyline-shaped journey 36/36, and all 47 source-governance gates passed from the exact clean runtime commit. Production `/health` identifies `61f99bf`. OneFive's operations line is active; the real dashboard/SMS comparison can occur after Mike accepts the invite and receives his server-derived OneFive/Skyline staff context. | `src/obligations/operator_obligations_service.js`; `src/agent/ask_spine_answer.js`; `src/conversation/staff_sms_router.js`; `src/leasing/leasingconversion.js`; `src/leasing/conversion_obligation_closure.js`; `tests/personal_attention_convergence.test.js`; `tests/personal_attention_convergence.db.js`; deployed API `61f99bf` |
| 27 | **RESOLVED AND DEPLOYED, 2026-08-22 — release observed in a disposable production-shaped database, not through live Skyline traffic.** The leasing conversion rail no longer keeps post-tour responsibility in a private owner field. Eligible owner selection stamps `obligations.assigned_user_id` at birth; explicit conversation handoff moves both records in one transaction; reopen restores or clears both records from the same eligibility decision. This is the concrete convergence between the tenant journey and the staff Ask Spine queue: post-tour capture records one accountable follow-up, and the signed-in staff member's existing Ask Spine answer can see it without a leasing-specific reader. Proof: conversion rail 15/15, release/reopen 23/23, full native scheduler → booking → staff SMS post-tour capture → personal Ask Spine → exact-bed application → resident/company lease → tenancy journey 36/36, all 47 clean source-governance gates. No real SMS was sent. | `src/leasing/leasingconversion.js`; `src/leasing/conversion_obligation_closure.js`; `tests/test_conversion_rail.db.js`; `tests/test_release3.db.js`; `tests/e2e/tour_application_lease.e2e.js`; deployed API `61f99bf` |
| 28 | **RESOLVED AND DEPLOYED, 2026-08-22 — production staff-SMS observation waits on Mike's invite acceptance.** Mike's post-tour text now enters the same leasing conversion plumbing as the dashboard. “Went well” records nothing and asks for one of the four canonical standings; an explicit standing delegates to `completeTour`; the exact unit/bed menu comes from the same application-target reader as the dashboard; and the exact reply delegates to the existing composite application-send command. Questions remain reads, maintenance language remains technician work, multi-person or multi-space ambiguity asks instead of choosing, and receipts distinguish a committed record from carrier acceptance. Pure contract 53/53, clean source governance 47/47, and fresh disposable real-webhook lead-to-exact-bed-tenancy proof 36/36. Production `/health` identifies `61f99bf`; OneFive's operations line is active, but Mike's invite remains unaccepted, so no live staff or application SMS is claimed. | `src/leasing/staff_sms_intent.js`; `src/leasing/staff_sms_action.js`; `src/applications/application_target_read.js`; `src/conversation/receipt.js`; `tests/staff_sms_leasing_action.test.js`; `tests/e2e/tour_application_lease.e2e.js` |
| 29 | **RESOLVED IN CODE; SKYLINE LEASE SOURCE LIVE; CONTROLLED PATH ACTIVATION PENDING, 2026-08-22.** The exact-bed rail now carries a governed intended move-in date through the staff SMS/dashboard target, invitation, public submission, application, proposed terms, and lease package. A future target is offerable only when an active turnover plan records its expected-ready date and the intended move-in is on or after it; Application Review displays the server-computed vacancy window rather than recomputing it in the browser. The mobile tenant application is versioned and validates identity, residence, income, household, move-in, pets, guarantor, certification, and delivery consent without placing SSNs in the broad application record; stale or unversioned public forms fail closed. A governing packet now retains the exact lease-template bytes and source hash, combines them with a deterministic deal-terms schedule, and binds resident signature, company signature, and the executed-lease record to one package hash; the database refuses a mismatched execution hash. This proves the implemented chain, not independent legal sufficiency. Guarantor-required official packets remain fail-closed until a guarantor signer rail exists. Proof: real webhook-to-tenancy 36/36; future-target Postgres 6/6; governing package 15/15; execution 25/25; tenant/application render 30/30; app target/setup 37/37; all 48 source-governance gates. The exact 56,074-byte Skyline DOCX is retained in production with matching source/content/configuration SHA-256 `6efa35f8b1a6412bbb54579c11a36f2c4e8bd36c00a53dcb1f6c091dde36a635`; KZ is the active configured company signer, and the generation and execution configuration gates are ready. No real application SMS or lease execution occurred. | migrations 190-191; `applicationSubmission.js`; `application_target_authority.js`; `application_target_read.js`; `leasepackets.js`; `spine_lease_execution.js`; `source_artifact_service.js`; app `index.html` / `followups-door.js`; `tests/e2e/tour_application_lease.e2e.js` |
| 30 | **“Solo on Chestnut” is serving governed Asset Management data from a demo property identity.** The populated production row is `a50fbdd0-…`, whose canonical name/address are `Property Spine Demo Building` / `1 Demo Way` but whose display name is `Solo on Chestnut`; it owns 10 compliance and 5 contracted-service records. Canonical `4233 Chestnut` (`9e2bb96e-…`) owns only one compliance record, while two additional `Solo on Chestnut` rows own none. Until the identity is adjudicated, the populated Solo Asset Management record set must not be described as canonical 4233 truth. | Authenticated production browser audit plus read-only Neon counts, 2026-08-22 |
| 31 | **The production Contracted Services Ask Spine read is semantically reachable but brittle.** “What contracted services need attention?” answered “One item” while the Asset Management screen reported six attention items and the answer itself discussed multiple engagements. “Who services the elevator and what governs the agreement?” was not classified; the domain-specific phrase “What governs the contracted service for elevator maintenance?” succeeded. | Authenticated production Ask Spine audit on the displayed Solo property, 2026-08-22 |
| 32 | **Production is ahead of GitHub `main` again.** This is the current continuation of defect #7, separated here so the live state is not buried inside its resolution history. Production API `61f99bf` is 43 commits ahead of `origin/main`; production carries migrations 188–189 that `main` lacks. | `/health` and read-only Neon ledger observed 2026-08-22; `git rev-list origin/main..61f99bf` |
| 33 | **THE LEDGER IS NOW THREE-TIERED, AND REACHING A DEPLOYABLE STATE IS NO LONGER A FAST-FORWARD.** `origin/main` **187** · production `61f99bf` **189** · the reconciled head (`027cd51` + `9746f12`) **191**. `9746f12` carries `190_application_move_in_lineage.sql` and `191_lease_instrument_source.sql`; `61f99bf` and `027cd51` are both at 189. So the reconciled head is at **191 in files against a 189 ledger**, which `prestart` refuses **in both directions** — a deploy of it fails to boot and names the pending file, and Render keeps the previous instance live so the API looks fine while the new schema is simply absent. **Reaching deployable therefore needs a deliberate release of two migrations, not a merge.** The STATE SNAPSHOT above still reads *"Migrations on deployed branch 189 → matches production"*, which is true of production and false of any branch carrying the Skyline lease work — read it as production-only until a release happens. Found while reconciling for Q5; reported, deliberately not acted on, because releasing schema is a human act with its own ceremony. | `git diff --name-only 61f99bf 9746f12 -- migrations/`; `migrations/migrate.js` verify-only mode; `docs/RELEASE_182_187_CEREMONY.md` for the shape a release takes |
| 34 | **RESOLVED IN CODE; GUARANTOR SIGNING RELEASE PENDING, 2026-08-22.** This supersedes row 29's remaining guarantor limitation. A guarantor-required governing packet now issues separate one-time resident and guarantor links into one retained lease package; each signer sees the complete package but can complete only their own controls. Either may sign first, while company execution remains blocked until every required resident-side signer has both signed and finally submitted. Application Records and the existing Ask Spine leasing standing read the same packet signer roster and progress. Guarantor identity remains packet-scoped rather than silently creating a durable Person; raw link secrets are never stored, and signer identity plus issued control structure freeze after issuance. Integrated proof: guarantor signing 40/40, governing execution 16/16, canonical lease execution 25/25, and all 48 source-governance gates. **Not deployed:** production remains at ledger 189, so migrations 190-192 require a deliberate release before this code can boot; no real signing link, SMS, or lease execution was attempted. | migration 192; `src/applications/leasepackets.js`; `src/applications/application_review.js`; `src/leasing/leasing_standing_read.js`; `tests/lease_guarantor_signing.db.js`; integrated commit `476730a` |
| 35 | **LIVE CORRECTION TO ROWS 32-34, 2026-08-22.** A later read supersedes their deployment identity and ledger counts: production `/health` identifies API `abcb28e`, and a transaction proven read-only before its first read found 179 ledger rows at ceiling **191**, with no orphan, name-conflict, or duplicate-number defect. Migrations 190-191 have therefore already been released and must not be run again. Against the guarantor branch, the only pending file is `192_lease_packet_signers.sql`; row 34's release scope is 192 alone. This correction records the newer observation and does not claim migration 192 or the guarantor code is deployed. | production `/health`; `tools/ledger_reconcile.js` against Render's current `DATABASE_URL`; observed 2026-08-22 |
| 36 | **DEPLOYED AND SCHEMA-PROVEN; REAL SIGNING JOURNEY NOT YET EXERCISED, 2026-08-22.** The reconciled Q5 lineage and row 34's guarantor signer rail are live at API `a5c0f66`; the separate resident/guarantor progress UI is live at app `83e2b67`. Migration 192 ran inside a SHA-pinned Render pre-deploy gate from an exact 191/179 start, and the temporary release command was removed immediately afterward. A fresh transaction proven read-only before its first read found ceiling 192 with 180 rows, no pending or orphan migration, all signer tables/indexes/triggers/guards present, and all four legacy tenant links backfilled with zero token or submission drift. The live app bundle contains the separate-link issue/progress controls and has no page overflow at 1280px or 390px. **Still unclaimed:** no real resident or guarantor link was issued, no SMS was sent, no lease was executed, and the authenticated Application Records presentation has not been observed with live signer data. | API deploy `dep-da4sp5740ujc739ubabg`; app deploy `dep-da4sq7rm8hqs73am3ei0`; `tools/ledger_reconcile.js`; production schema re-read 2026-08-22 |
| 37 | **CI-CONNECTED, `HTTP_PROVEN`, AND `BROWSER_VERIFIED`; PRODUCTION ACTIVATION UNCHANGED, 2026-08-22.** The full Skyline-shaped journey now begins at the canonical staff invite instead of hand-assembling an accepted identity. A real server and fresh migrated Postgres completed manager-confirmed Person -> fake invite SMS -> fake OTP -> atomic Mike identity/access/work assignment -> native tour -> operations webhook -> post-tour capture -> the exact same personal Ask Spine answer through dashboard and staff SMS -> exact-bed application -> V3 guarantor capture -> separate resident/guarantor links and submissions -> manager/company authority handoff -> company execution -> exact-bed tenancy, 50/50. Real Chromium separately completed the resident's official governing-package UI, including explicit full-name signature intent and final submission, 7/7. `verify_all.sh` now runs both and finished `ALL PROOFS PASSED`; its E2E launcher always preloads the fake SMS transport, even if the caller's shell contains carrier credentials. Two older lease proofs were aligned to the already-required signature-consent contract, and one duplicated OTP resend predicate was deleted with no behavior change. **Not claimed:** no real carrier was contacted, no live Mike/resident/guarantor action occurred, and this does not advance Skyline's production activation state. | `tests/e2e/tour_application_lease.e2e.js`; `tests/e2e/resident_signing.browser.js`; `tests/e2e/leasing_e2e_lib.js`; `tests/e2e/leasing_path.e2e.js`; `tests/e2e/boot.sh`; `tests/e2e/verify_all.sh`; `src/identity/teamaccess.js` |
| 38 | **`migrations/migrate.js` hardcodes SSL, so `--apply` cannot build a schema on a local non-SSL Postgres.** Found while building a local harness DB for the structure thread's real-Postgres proof. Production and CI SSL targets are unaffected, but a local disaster-recovery rehearsal fails before application proofs can begin. | `migrations/migrate.js:173`; `docs/current-state-build/06_WAVE3_RESULTS.md` |

## PRODUCTION-PROVEN — the whole list

| Capability | Evidence |
|---|---|
| **Work-order completion guard** | `activation_id d93b08dd-c682-46d2-acf9-78ab6b960827` · `2026-08-12T01:49:57.866Z` · 16/16 on live instance `kbtb6` · irreversible |
| **Migration release gate** | Runs on every boot via `prestart`; deployed sha enforces `EXPECTED_SHA` and refuses on mismatch |
| **Meeting Evidence webhook ingress** (narrow) | Real delivery `c5cb8d0d-…` authenticated + retained byte-for-byte for a real provider meeting. Proves ingestion, not the pipeline downstream of it |
| **Meeting Evidence binding + finality + rejection lineage** (narrow) | A real meeting bound to a real property, qualified final; three model rejections durably retained with no receipt |
| **Meeting Receipt extraction advancement** (narrow, ×2) | Two real specimen runs each cleared one more validation gate post-deploy — proves the gate moved, not that a receipt was produced |
| **Asset Management authenticated read surfaces** (narrow) | Production browser opened the overview and domain rooms for 4125 Chestnut, 4233 Chestnut and the displayed Solo property. Debt returned populated governed standing; Compliance, Utilities, Contracted Services, Insurance, Tax and Equity returned populated or explicit empty standing. No writer was exercised |
| **Ask Spine Asset Management reads** (narrow) | Production questions returned governed Debt, Equity, Utility, Compliance and explicitly phrased Contracted Services standing. Insurance remained unregistered, cross-domain composition omitted Equity, and natural elevator phrasing failed classification |

**Rows above say "narrow" on purpose.** Each is a real, specific, falsifiable
production observation — not a green test suite. None means the entire feature
works end to end. The Meeting Receipt pipeline specifically has **zero accepted
extractions and zero receipts produced**, despite three Meeting rows touching
production. Say precisely what was observed; do not round up.

Anything not explicitly marked `PRODUCTION_PROVEN` is **at best deployed, not
observed.**

---

# THE INDEX

Grouped by where the code lives — a fact, not a taxonomy. Taxonomy is deliberately
unsettled; do not invent one while adding rows.

## Asset Management — `src/asset/`

**PR-level detail below courtesy of a direct review by the engineer who worked these
PRs (Codex), 2026-08-20 — spot-checked by me: schema tables, gate scan dirs, `debt`
registry state and the migration-181 header all confirmed exactly against the
deployed commit `30cb992`. One quoted phrase (*"does not send email"*) did not
resolve against any file I could reach — likely a runtime log or PR description
outside this tree, not treated as confirmed.**

| Capability | Rung | Ask Spine | Files / note |
|---|---|---|---|
| Compliance | `PRODUCTION_PROVEN` (read path, narrow) | registered | `compliance_*.js`. `compliance_http.db.js` was reopened and its real-DB/real-router structure confirmed before this upgrade; it was not rerun. Production rendered one record for canonical 4233, 10 for the displayed Solo property, and an honest zero through Ask Spine at 4125. The Solo identity is disputed; see defect #30 |
| Utilities | `PRODUCTION_PROVEN` (empty read path only) | registered | `utility_*.js`. Production UI and Ask Spine both returned the governed unestablished standing at 4125. Automated end-to-end gap remains: `utility_http.test.js` uses a fake pool and its DB test skips the router |
| Contracted Services — canonical domain | `PRODUCTION_PROVEN` (read path, narrow) | registered | Production rendered five populated engagements for the displayed Solo property and Ask Spine returned their evidence/standing when explicitly classified. No writer was exercised; identity and answer-count defects remain (#30–31). `contracted_service_http.test.js` still uses a fake pool |
| Contracted Services — overview integration | `PRODUCTION_PROVEN` (read path, narrow) | registered | Production Asset Management overview reported five service engagements and the detailed room rendered them. No automated real-DB/real-router proof was added |
| Insurance | `PRODUCTION_PROVEN` (empty read path only) | **none** (`pending`) | Production page opened under an entitled 4125 account and honestly reported no coverage/policy. Ask Spine still does not gather Insurance |
| Tax (Philadelphia) | `PRODUCTION_PROVEN` (empty read path only) | **none** (`pending`) | Production Philadelphia Tax page opened and reported applicability unconfirmed for Real Estate Tax, BIRT, NPT and U&O. Ask Spine still does not gather Tax; clocks only, never amounts |
| Debt — canonical truth | `PRODUCTION_PROVEN` (read path, narrow) | registered (PR #113) | Production 4125 read returned observed principal `$27,745,265.77` as of 2025-08-01 separately from projected principal `$27,131,874.12` as of 2026-08-01, with rate and maturity. No write route or causal/comparison claim was exercised |
| Debt — Ask Spine reader | `PRODUCTION_PROVEN` (narrow) | registered | Production “What is the current loan balance?” preserved observed vs projected basis, date/staleness and no-payoff caveat. The older unit test is fake-service only, but this production observation moves the read path itself |
| Equity — canonical domain | `PRODUCTION_PROVEN` (empty read path only) | registered | Production 4125 page returned zero positions and ownership reconciliation not established. No populated equity position was observed |
| Equity — Ask Spine reader | `PRODUCTION_PROVEN` (empty read path only) | registered | Production “Who holds the equity in this property?” returned an explicit coverage gap rather than “nobody owns it.” No populated equity answer was observed |
| Equity — ownership reconciliation | `LOCALLY_EXERCISED`/`HTTP_PROVEN` split | registered | PR #114. Real Postgres proves a 77.57% schedule stays `INCOMPLETE` (`equity_position_falsification.db.js`); the real-HTTP test only proves the `NOT_ESTABLISHED` case, not populated reconciliation. *"`accrued_preferred_return` is always `NOT_ESTABLISHED` for every preferred position, unconditionally, for Build 1."* Governing source clause pending |
| Funding boundary wall | enforced | — | `tests/gates/gate_funding_boundary.js` — tax/insurance funding cannot cross into economics |

## Leasing lifecycle — `src/{leasing,applications,tenancy,onboarding}/`

**None of this is registered with Ask Spine** — the gate cannot see these directories.

| Capability | Rung | Files / note |
|---|---|---|
| **Deal Setup / Opening Tenancy** | `HTTP_PROVEN` + `BROWSER_VERIFIED` | `deal_setup_http.db.js` **spawns real `server.js`**, real socket, restart persistence. **Best-proven capability in the repo** |
| Lead intake | `LOCALLY_EXERCISED` | `leasing_leads.js` |
| Tours / appointment attribution | `LOCALLY_EXERCISED` | `appointment_attribution.js`, `appointment_journey.js`, `tour_outcome.js` |
| `tour_chips` · `capture_chase` · `capture_receipt` | `BUILT_BUT_DORMANT` | **no caller in `src/` or `server.js`** |
| Post-tour conversion rail | `HTTP_PROVEN` | Real server + PostgreSQL flow records the tour outcome and returns the conversion consumed by the one application-send command. The separate `conversation_owner_user_id` ruling remains outside this proof. |
| AI Leasing Strategy | `LOCALLY_EXERCISED` | ⚠ docs say *dormant*; code is wired into the live first-response path (`leasingleads.js:614`). **Unresolved** |
| Leasing Desk | `LOCALLY_EXERCISED` | `leasing_desk.js`, `leasing_desk_loader.js`. Handoff claims HTTP proof; file inspection contradicts it |
| Application submission / lifecycle | `HTTP_PROVEN` | Real public token read and versioned V3 submission persisted exact Bed B, intended move-in, and the post-tour conversion. The public write rejects stale or unversioned captured forms; protected SSN capture remains outside this broad record. **Migration 125 still does not exist** (124→126). |
| Application target authority (unit/bed/date) | `HTTP_PROVEN` | The canonical availability read returns one exact offerable space or a governed future target backed by an active turnover expected-ready date. Unit, `space_id`, and intended move-in are revalidated by the write authority and persist through the application-to-tenancy rail. |
| Proposed terms | `HTTP_PROVEN` | Confirmed terms generated the governing lease packet in the same full-path proof. |
| **Lease packet / resident + company execution** | `HTTP_PROVEN` | The packet retains the exact lease-template bytes and source hash, combines them with deterministic deal terms, and binds both signatures and the executed-lease record to one package hash. Company signing created the exact-space tenancy. This proves the implemented execution path, not independent legal sufficiency; guarantor-required official packets remain fail-closed. |
| **Executed lease intake** | `LOCALLY_EXERCISED` | **SWITCHED OFF** — 503 unless `EXECUTED_LEASE_INTAKE_ENABLED=true` **and** property allowlisted |
| Tenancy anchor | `HTTP_PROVEN` | Governing-packet resident execution followed by company execution created the lease and reread as complete from Application Review and Person Card. The outside-document confirm-term path still fails closed without executed-lease evidence. |
| **Pending tenancy creation** | **DOES NOT EXIST** | Confirm hard-codes `lease_status='active'` (`activation_service.js:696`). The phrase appears nowhere in either repo |
| **Active tenancy activation** | `DEPLOYED` | `POST /operator/leasing/leases/:leaseId/activate-tenancy`. Real, works, byte-identical at deployed sha |
| Move-in gates / economic tenancy | `LOCALLY_EXERCISED` | `movein.js`, `economic_tenancy_service.js` |
| Lease void | `LOCALLY_EXERCISED` | **No HTTP route** — ops tool only. Six stuck leases once needed raw SQL |
| Notice to vacate | `REPORTED` | *"built and never used… Live count across the whole database: 0"* |
| Dated / space position | `LOCALLY_EXERCISED` | `dated_positions.js` — *one service, four interpretations*. Most depended-on primitive |
| Renewals | `LOCALLY_EXERCISED` | `renewals_read.js`, `renewal_lifecycle.js`. **No renewal writer.** No browser proof exists |
| `followup_ladder` / `followup_runner` | `BUILT_BUT_DORMANT` | *"DORMANT. Nothing calls this yet. It decides; it cannot send."* |

## Operations — `src/{maintenance,technician,comms,obligations}/`

⚠ The obligation **engine** is `src/shared/obligation_engine.js`, **not** `src/obligations/`
(which is only the authenticated HTTP door).

| Capability | Rung | Files / note |
|---|---|---|
| **Release 0 completion guard** | **`PRODUCTION_PROVEN`** | `proof_evaluation_service.js` + migration 137/140 |
| Work order creation / lifecycle | `HTTP_PROVEN` | `work_order_service.js`. *"the REAL server.js over real HTTP"* |
| Technician SMS operations loop | `HTTP_PROVEN` + `BROWSER_VERIFIED` | `technician/conversation.js`, `lifecycle_service.js` + screenshots. On 2026-08-21 the current proof-evaluation schema was added to the disposable harness and the plain-language technician day passed 63/63 through the real inbound webhook and PostgreSQL; carrier and media fetch remain doubled. |
| Staff SMS governed reads | `DEPLOYED` + `HTTP_PROVEN` | Real inbound webhook + real PostgreSQL 77/77; Ask response and transport are doubled in this connection proof. Uses the existing Ask Spine service; canonical Economics Ask is separately real-HTTP proven 16/16. Deployed in API `7bbb23e`, but no production staff SMS turn has been observed. |
| Operator work-order actions | `HTTP_PROVEN` | `operator_actions.js` — 4 canonical writes; "Review" deliberately excluded |
| **Obligations queue + self-claim** | `HTTP_PROVEN` + `BROWSER_VERIFIED` | `GET /operator/obligations`, `POST .../claim`. **Exists — do not rebuild** |
| **Turnovers / move-out** | `HTTP_PROVEN` | `turnover_service.js`, `operator_turnover.js`. **Exists — do not rebuild** |
| Unit triage · turn scope · work acceptance · readiness | real-DB service layer, **no HTTP harness** | `unit_triage_service.js`, `unit_turn_scope_service.js`, `work_acceptance_service.js`, `readiness_service.js` |
| Communications boundary | `HTTP_PROVEN` (line layer) | **SMS RAIL FROZEN** — *"no `operations` line row at all"* |
| Tenant link (resident SMS) | `HTTP_PROVEN` | `tenantlink.js`. Real inbound webhook + fully migrated disposable PostgreSQL passed 61/61 on 2026-08-21; classifier and carrier remain doubled. |
| Move-in delivery correlation | `LOCALLY_EXERCISED` | `delivery.js` — test uses a **hand-mocked** `client.query` |
| Prospect fact capture | `LOCALLY_EXERCISED` | `prospect_capture.js` |

**OPEN:** Resident work-order creation and technician proof-gated completion are
independently HTTP-proven, but no single scenario yet carries the same resident-created
work order through assignment, technician conversation, completion, and resident reply.
No controlled live-carrier proof has run.

## Platform / core — `src/{identity,entity,money,evidence,agent,release0,surfaces}/`

| Capability | Rung | Files / note |
|---|---|---|
| Staff session / server-derived authority | `HTTP_PROVEN` | `staff_session_service.js`. *"The caller never supplies role, modules, TTL, or entitlement"* |
| Property creation | `HTTP_PROVEN` + `BROWSER_VERIFIED` | Single-path enforced by `gate_property_creation_paths.js` |
| Legal entity primitive | `HTTP_PROVEN` + `BROWSER_VERIFIED` | `legal_entity_service.js` |
| Ask Spine (slices 1–2) | `PRODUCTION_PROVEN` (selected read paths) | Real migrated-Postgres + real-router proof confirmed in `ask_spine_db_proof.db.js`. Production browser observation returned Debt, Equity, Utility, Compliance and explicitly phrased Contracted Services answers. Insurance remains pending; natural elevator wording failed; cross-domain Debt + Equity returned Debt only. `references[]` remains absent |
| Asset Management shell | `PRODUCTION_PROVEN` (authenticated read path) | `asset_management_shell.db.js` was reopened: real Postgres + real AM router + real socket. Production browser opened the door for 4125, 4233 and displayed Solo; Skyline's door was hidden by its current module assignment. Property Expenses remains capped; no writer was exercised |
| Monthly Reporting surface | `DEPLOYED` (front-end shell only) | Production rendered the reporting room, but the surface says: *"Monthly reporting endpoint pending. Front end shell is live; backend read model can plug into /monthly-reporting-dashboard."* Final PDF was blocked and operating statement, cash flow, balance sheet, trial balance, bank reconciliation and debt confirmation remained backend-pending. App `index.html` at `d45344d` |
| Money events / accounting | `HTTP_PROVEN` | `money.js`, `bankbridge.js`, `plaid.js` — lifecycle-arc harnesses only |
| Governed pricing & charges (~26 files) | mixed | Skyline pricing is published. Integration branch fixes multi-term composition and exposes current published economics to Ask Spine without a second source. Other economic classes retain their own publication state; `$99` admin fee remains **BLOCKED on one ruling**. |
| `concession_schedule_compiler` | `BUILT_BUT_DORMANT` | *"ACTIVATES NOTHING"*; `free_rent_period` *"SPECIFIED BUT NOT IMPLEMENTED"* |
| `economic_adapter` · `pricing_adapter` | `BUILT_BUT_DORMANT` | *"DARK BY CONSTRUCTION."* **See defect #1** |
| `src/identity/activation.js` | **DEAD** | Never mounted; app screen 404s. **See defect #3** |
| Slice 9 market evidence | `HTTP_PROVEN` + `BROWSER_VERIFIED` | `evidence_projection.js`. Rent Survey / Listings permanently `not_connected` |
| Conversational seams (6 pure files) | `LOCALLY_EXERCISED` | `intent.js`, `clarification.js`, `receipt.js`, `staff_sms_router.js`, `technician_intent.js`, `work_reference.js` |
| A2P legal pages | `HTTP_PROVEN` | Header records a real incident: *"BOTH campaign-required URLs returned 404 in production"* |
| Board / desks / management / portfolio reads | `HTTP_PROVEN` (mounted) | `desks.js`: *"NEVER a fake number"* |

## Recently landed — `src/{meeting_evidence,tenancy}/`, migrations 175–181

| Capability | Rung | Note |
|---|---|---|
| Meeting Evidence (Read AI webhook ingress) | **`PRODUCTION_PROVEN`** ↑ | Wave 1 found `BUILT_BUT_DORMANT`, waiting on a signing key. **Superseded** — per Codex PR #115 direct review, spot-checked against schema at `30cb992`: a real webhook delivery (`c5cb8d0d-c5ab-401b-9d38-67a75787b28c`) was authenticated and retained byte-for-byte for provider meeting `be96f9ac-5996-4271-84a2-7098801d4fc4`. Initially unbound, zero downstream writes — narrow proof, not "the feature works end to end." Migration header: *"It does NOT wire transcripts into Ask Spine."* Not registered |
| Meeting Evidence — binding, finality, rejection lineage | **`PRODUCTION_PROVEN`** (narrow) | PR #118, migration 181. The authentic meeting above was manually bound to a real property, qualified final, and three rejected model attempts were durably retained with no receipts/writes. Gate still does not scan `src/meeting_evidence/` — absent from Ask Spine by design, and the gate cannot detect that absence |
| Meeting Receipt v0 pipeline — successful generation | `DEPLOYED`, **not observed** | PR #115, migration 176. Code live in `30cb992`; three real extraction attempts entered the workflow, **all three rejected**. Extraction runs: 0 accepted. Receipts: 0. Local HTTP tests use a fake workflow/pool; DB proofs bypass HTTP entirely |
| Meeting Receipt — unresolved named owner handling | `PRODUCTION_PROVEN` (narrow) | PR #120. Post-deploy, `extractor_unresolved_label_forbidden` stopped blocking the real specimen; next attempt advanced to evidence-role validation. Does not raise the pipeline's overall rung — no person ID attaches without an exact property-person match |
| Meeting Receipt — prompt evidence invariants | `PRODUCTION_PROVEN` (narrow) | PR #122. Real prompt revision v1 advanced past prior temporal/claim-basis failures, stopped at `extractor_quote_speaker_mismatch`. Still zero accepted extractions, receipts, emails, Ask Spine access, or canonical writes |
| Meeting Receipt — server-owned evidence addressing | `LOCALLY_EXERCISED` | Active, **unmerged** branch. Runtime 80/80, extractor 69/69 per Codex — **not independently re-run by me**. Could not verify against real Postgres today (Docker unavailable to that reviewer). *"Invalid output is rejected by the server, not repaired."* Real Receipt 001 remains the missing proof |
| Person ingress boundary | `LOCALLY_EXERCISED` | Migration 177 **unreleased** |
| Person correction (anti-merge supersession) | `BUILT_BUT_DORMANT` | Retire + point, move nothing |
| External resident identity / crosswalk | `REPORTED` | **Blocks establishing opening truth** |
| **Forward Leasing ledger** | `BROWSER_VERIFIED` | **4 browser proofs survived adversarial review untouched.** Most rigorously proven area |
| Interval tenancy read · leasing cycles · Forward Rent | `BROWSER_VERIFIED` | Migration 178 unreleased |
| Current Rent Roll (operator unit-first) | `HTTP_PROVEN` | Claimed browser-verified; downgraded on review |
| Current Rent Roll (four-bucket classification) | `LOCALLY_EXERCISED` | ⚠ Claimed `PRODUCTION_PROVEN`; **refuted** — single-source, and the API's own banner contradicts the numbers |
| Inventory retirement · grain materialization · bed-grain activation | `BUILT_BUT_DORMANT` | Migrations 179/180 unreleased. *"391 positions against 160 real beds"* |
| Tenancy standing projection | `HTTP_PROVEN` | Registered with Ask Spine |

---

## Teams / Access / Roles — `src/identity/`

**The highest-value gap in the original survey, now covered.** 148 capabilities
surveyed across this and six other areas (wave 2, 2026-08-20) — full detail in
`docs/current-state-build/05_WAVE2_RESULTS.md`. Rows below are the ones that
change what you should believe about the system; everything else is in that file.

| Capability | Rung | Note |
|---|---|---|
| Staff invite creation — `POST /properties/:id/team-invites` | `HTTP_PROVEN` | Session-only human authority; property scope and granting actor are server-derived. Canonical jobs map access and work ownership together. Existing phone match requires explicit person confirmation before write/send. |
| Team invite UI — live session path | `DEPLOYED` | Defect #8 resolved. Signed-in failures stay failures; no local fake invite. Job + name + phone is the complete form. |
| OTP send — `POST /auth/sms/start` | `LOCALLY_EXERCISED` | One route, two branches (invite-accept / re-login). Honest 503 if SMS transport doesn't confirm send — tested only against a fake pool |
| OTP verify → session mint — `POST /auth/sms/verify` | `HTTP_PROVEN` | Real HTTP/Postgres proof accepts a canonical invite and atomically establishes session, bridge, context, work assignment, and property access. |
| Team roster read — `GET /properties/:id/team` | `HTTP_PROVEN` | Defect #9 resolved. Session required; cross-property session refused. Production no-session probe returns 401. |
| Current-access read — `GET /properties/:id/my-access` | `BUILT_BUT_DORMANT` | Fully implemented, has the property wall its sibling routes lack, **the app never calls it** |
| Assignment edit — `PATCH /property-team-assignments/:id` | `BUILT_BUT_DORMANT` | Implemented and session/property/manager gated; the app still never calls it. |
| Staff session mint/resolve/revoke | `HTTP_PROVEN` | *"Class 1 permanent product primitive."* The actually solid part of this whole area |
| Property switcher — `GET/POST /operator/properties[/select]` | `HTTP_PROVEN` | **Genuinely proven**, real Postgres + real HTTP, a real second user without access is refused. See the correction above defects #9-10 |
| Super-admin surface — `super_admin.js` (`/admin/*`) | `REPORTED` | Extensively used by the app; **zero automated test evidence at any rung** |
| Org-scoped admin surface — `org_admin.js` (`/org/*`) | `BUILT_BUT_DORMANT` | Fully parallel to super-admin, **entirely unreachable from the app** — nothing calls it |
| Module vocabulary mismatch — `'capital'` vs `'asset_management'` | `REPORTED` | Different grant paths use different names for what's supposed to be the same module; one silently strips the other |
| Demo session bootstrap — `POST /demo/operator-session` | `HTTP_PROVEN` | Correctly fail-closed (403/503 without config). Site map's *"RETIRE-ON-ACTIVATION"* framing **not found anywhere in source** — no such trigger exists, map was wrong on this specific point |
| Team/assignment schema (4 tables) | `DEPLOYED` | `property_team_assignments` is the one authority row; a partial unique index was added defensively in migration 070 after a live-duplicate risk |

## Newly discovered — on nobody's map until this pass

Found by the completeness critics, not by searching for something already
suspected. Full detail in `05_WAVE2_RESULTS.md`.

| What | Note |
|---|---|
| **The Decision Rail** — `src/leasing/decisions.js` | A whole governed money-decision domain (migration 059, its own obligation routing, its own approval-cap authority) sitting in `src/leasing/` where nobody would look for it under "money" |
| **AI leasing operating rules** — `ai_leasing_operating_rules` table (migration 121) | Writes governance rules that constrain **live prospect-facing AI output**, real HTTP routes, actively read on every AI-generated lease response — zero prior visibility |
| **`source_artifacts`** — migration 153 | Cross-cutting primitive, heavily used across Utilities/Compliance/Debt/Equity/onboarding, real and tested — *"exists in the database and in nobody's map"* until now |
| **Outlook/Acuity Microsoft Graph sync** | A complete external integration exists in source but is unmounted. **Owner ruling 2026-08-21: do not make this Skyline's scheduler.** Property Spine's native availability and `leasing_tours` path is the target; this external path is legacy/migration territory, not an activation dependency. |
| **Seven independent property-scoped feature flags** | Each gates a different live capability by property-UUID allowlist. Nothing in either repo enumerates the current set — only readable by checking Render directly alongside seven separate source files |
| **`TWILIO_FROM_NUMBER`** | Documented in `docs/deployment.md` as a settable env var. **Does nothing in code** — *"a mention is not a guard,"* exactly the trap this repo's own doctrine warns about |
| **10 consecutive missing migration numbers** — 141 through 149 | Between `140_post_activation_completion_guard.sql` and `150_canonical_property_creation.sql`. Not previously documented anywhere |
| **No scheduler/cron infrastructure exists in either repo** | Several capabilities (`followup_runner.js` among them) are structurally built to need a periodic trigger. None exists — confirmed absence, not oversight |

---

## Property identity — the evidence base — `tools/identity/`

Added 2026-08-22 (CC_BUILD1, slices 1-4). Read-only. **§18 class: 3** — inventory /
evidence infrastructure, outside the operator workflow. **Removal condition: none,
deliberately** (not Class 4): the ruling is one-time and this is the record of how it was
made. Full deliverable:
`docs/PROPERTY_IDENTITY_INVENTORY.md`, ending in a three-option ruling brief with
**no recommendation** — the direction is an owner ruling.

| Capability | Rung | Files / note |
|---|---|---|
| Property dependency graph (delete + rebind), from migration source | `LOCALLY_EXERCISED` | `tools/identity/property_dependency_graph.js`. No `pg`, no `DATABASE_URL`. **It is a catalog and cannot see rows** (H-1 in `IDENTITY_HYGIENE_REGISTER.md`) |
| Its falsification suite | `LOCALLY_EXERCISED` | `property_dependency_graph_falsify.js` — 24 cases. **Six deliberate breaks each went red on the correct case**, then were reverted |
| Property census SQL | `LOCALLY_EXERCISED` | `tools/identity/property_census.sql` (generated by `generate_property_census.js`). **Not run against production, deliberately.** Executed on a disposable local Postgres built from the real chain to 189, exit 0. `--capture` refuses any non-localhost `PGURL` |

**Numbers, and one correction to a figure in circulation.** 154 FKs reference
`properties`: **75 live CASCADE** (77 *declared* — two are on `scheduled_charges`,
dropped by 059), 42 RESTRICT, 29 with no `ON DELETE` clause (default NO ACTION, confirmed
to behave as RESTRICT; none deferrable), 6 SET NULL. **71 block a delete.**

**Defect #12 is proven and is one instance of a general pattern.** Deleting a property
bypasses the published-pricing freeze — but so does deleting the *pricing version*
directly, which is the discriminating test: the guard fires and reads its already-deleted
parent, so `status` is NULL and the check passes. Of 57 `BEFORE DELETE` guards, **53 raise
unconditionally and 4 read another row**. Two consequences not previously recorded:
`concession_policies` is an **unnamed sibling of #12** (same trigger function), and
**`work_order_proof_attachments` — Release 0 proof evidence — is silently destroyed by a
property delete**, proven on a disposable database.

**Two walls the FK graph cannot see.** `ai_leasing_operating_rules` and
`governed_charge_rulings` each carry an *unconditional* delete-refusal trigger **and** a
CASCADE FK. One row in either makes `delete from properties` raise rather than cascade —
proven. An FK-only reading calls these "would be destroyed"; they are the opposite.

**Rebind surface.** 78 unique constraints involve `property_id`: 37 `unique(id,
property_id)` anchors that cannot collide, 37 collidable on a shared key, and **4 that
permit one row per property, where a merge collides with certainty** —
`communication_lines`, `property_pricing_versions`, `opening_tenancy_positions`,
`deal_intake_properties`.

**`source_artifacts` is invisible to all of the above.** It has no `property_id` column
and no FK to properties (polymorphic scope pair, migration 153), so it appears in no edge
of the graph. Its binding is immutable: **no rebinding path exists** and the database
refuses to change `scope_id`. Nothing anywhere validates a document's *contents* against
the property it is filed under.

**Name resolution — the fix already exists and five callers bypass it.**
`src/identity/property_resolution_service.js` returns `ambiguous` with every candidate on
a multi-row exact-name match — *"One row resolves; more than one is ambiguous rather than
'the oldest'"* — and is live in the import/seed path (`snapshot_loader.js`,
`seed_endpoint.js`, `seed_snapshot.js`). Five sites do what it forbids:
`operator.js:195`, `demo_reset.js:80`, `leasingleads.js:900`, `leasingleads.js:1051`
(**no limit at all** — a booking authorization wall taking `rows[0]`), and
`demo_preflight.js:106`. Three rows share the name, so "oldest wins" is a coin flip.
Per §41 the missing piece is routing, not new code. Caveat: the export is named
`resolvePropertyForImport` and one new caller is an authorization wall, so the name is
scoped narrower than the callers — rename or extract a sibling. **Not fixed here.**

**Counts, stated exactly:** 154 FK references are *declared*; **152 are live** (two are on
`scheduled_charges`, dropped by 059). Live: 75 CASCADE + 71 BLOCKS + 6 SET NULL = 152.
An independent grep and the parser were **re-run after** the rename fix and agree on
154/77/42/6.

**Correction, recorded because it was published before it was checked:** an earlier
revision raised the urgency of the bypassable delete guards on the reasoning that
`tools/scale/seed_b_qa_identity.sql` carries the production property UUID and so *is* a
delete path. **It is not.** `activation_proof.js:36` reads `SCALE_DATABASE_URL` with no
`DATABASE_URL` fallback, `setup_baseline.sh:60` overwrites `DATABASE_URL` with localhost,
and `assert_isolated_environment.sql` refuses unless `current_database() = 'r0scale'`.
The inserts are `on conflict do nothing` and the harness deletes only its own fixture
tables. The §2b guard defect stands on its own; the urgency framing does not.

**Not established, and not guessed at:** every row count. No production database was
contacted. The census answers those, and a human runs it.

---

## Property identity resolution — one contract, five callers — `src/shared/`

Appended 2026-08-22 (Q5). Product code changed; no migration, no database write.

| Capability | Rung | Files / note |
|---|---|---|
| One demo-property identity module | `LOCALLY_EXERCISED` | `src/shared/demo_property_identity.js`. **Class 2.** Replacement condition: `properties.canonical_key` populated for the demo row. Noticed by `via` in its result, asserted by the gate |
| Five name lookups deleted | `LOCALLY_EXERCISED` | `operator.js`, `demo_reset.js`, `demo_preflight.js`, `leasingleads.js` ×2. All five `from properties where name` statements are gone; the gate fails if any returns |
| Booking scope wall refuses on ambiguity | `LOCALLY_EXERCISED` | `leasingleads.js`. Was `select … where name=$1` with **no limit**, taking `rows[0]`. Now refuses, logs candidates, and compares identity as a separate refusal. Mutation-tested |
| `resolvePropertyForImport` → `resolvePropertyIdentity` | `LOCALLY_EXERCISED` | Renamed in a commit of its own with no behaviour change. `resolveProperty` was unavailable — `snapshot_loader.js` and `seed_snapshot.js` define local wrappers by that name |
| Scale seeds self-guard | `LOCALLY_EXERCISED` | `tools/scale/seed_b_qa_identity.sql`, `seed_c_governed_charges.sql`. Both carry the production Demo Building UUID; all prior guards lived in the runner, so `psql -f` bypassed them. Verified in three directions |

**What was deleted, measured as decisions rather than lines:** 8 declarations of
the demo property name → **1**; 5 SQL statements resolving a property by name →
**0**; 5 call sites able to silently pick one of three same-named rows → **0**.

**Net line count is POSITIVE (+163 in `src/`), and the contract asked to be told
if so.** Counted as code with comments stripped, the five callers are **+5** —
flat. The addition is the one module (122 lines, ~65 of them the header stating
the defect, the class and the removal condition), two 35-line SQL self-guards
that are pure addition, and Slice 4 splitting one silent comparison into two
explicit refusals plus candidate logging. The duplication is gone; the volume is
not. Recorded rather than argued away.

**Not established:** whether the demo row has a `canonical_key`. That needs a
production read, which this build did not do. Until it is known, resolution falls
to the exact-name branch — which **refuses** on ambiguity rather than taking the
oldest. That improvement holds either way.

---

## NOT YET SURVEYED — do not read absence as absence

Wave 2 (2026-08-20) closed every area listed here as of the prior version of
this file. What's left is depth, not breadth:

| Area | Status |
|---|---|
| Money / pricing (42 capabilities found) | Surveyed at headline grain; full 42-row detail in `05_WAVE2_RESULTS.md`, not yet promoted into this file's index |
| `server.js` inline routes (25 found, 23 flagged) | Confirmed CLAUDE.md's own warning — a `src/`-only search would have missed a legacy Persons CRUD, a "contained not deleted" bare lease writer, and an inline AI rent-roll pipeline. Full detail in `05_WAVE2_RESULTS.md` |
| `tools/` (16 found) | Which have been actually *run* vs. written — detail in `05_WAVE2_RESULTS.md` |
| App repo doors (25 found) | Detail in `05_WAVE2_RESULTS.md` |
| Onboarding/rent-roll intake (12 found) | Detail in `05_WAVE2_RESULTS.md` |
| Management door (11 found) | Detail in `05_WAVE2_RESULTS.md` |

Re-runnable research: `docs/current-state-build/wave1_new_domains.js`,
`docs/current-state-build/wave2_coverage_gaps.js`. Full wave 2 output:
`docs/current-state-build/05_WAVE2_RESULTS.md`.

---

## ⛔ CLOSING A THREAD — DO THIS BEFORE YOU STOP

**This file goes stale the moment a thread ships something and does not say so.**
`docs/CODEBASE_STATE.md` (5 Aug) proved it — stamped one commit, silently wrong
two weeks later. The only thing preventing a repeat is the step below.

Run this before ending any thread that touched `src/`, `migrations/` or `server.js`:

```text
1. WHAT CHANGED?      git diff --stat origin/main...HEAD -- src/ migrations/ server.js
2. FOR EACH ONE:      does a row exist here?
                        yes → update its rung, files and note
                        no  → add a row, at the grain the evidence supports
3. RUNG CHECK:        did the proof rung actually MOVE? Opening a test is the only
                      way to know. A rung is never upgraded because the code looks
                      finished — only because the next rung was OBSERVED.
4. DEFECTS:           did you fix one of the KNOWN LIVE DEFECTS? Remove it.
                      Did you find a new one? Add it.
5. SNAPSHOT:          update STATE SNAPSHOT if you verified against a newer SHA.
                      Leave it alone if you did not — a false stamp is worse
                      than an old one.
6. NOTHING CHANGED?   Say so in the commit. That is a valid outcome.
```

**Paste this at the end of a thread to run the ritual:**

```
Close out this thread: update docs/CURRENT_STATE.md for anything we built,
connected, proved or disproved. Open the tests before changing any proof rung.
If nothing changed, say so rather than editing.
```

**The rule that keeps this honest:** a thread updates rows for what it *did*.
It does not re-survey the whole repo, and it does not upgrade a row it did not
personally verify.

---

## HOW TO ADD A ROW

1. **Open the test.** Do not trust the filename.
2. **Pick the rung the evidence supports**, never the one you hoped for.
3. **Cite a file path**, and a verbatim quote for any blocker.
4. **Update the snapshot** if you verified against a newer SHA.
5. `NOT_FOUND` beats a plausible guess, always.

**No row is upgraded because someone believes the product works that way.
It is upgraded only when the next proof rung has been observed.**

---

*Supporting detail — full capability tables, per-claim adversarial verification
reasoning, and the re-runnable research scripts — is in `docs/current-state-build/`.*

---

## Append-only runtime correction and lease-CI receipt — 2026-08-24

**`REPORTED` runtime correction — supersedes rows 35 and 36 for current-state
use.** The owner independently re-verified on 2026-08-24 that production is API
`61f99bf` with migration-ledger ceiling **189**. The later-looking 2026-08-22
claims in rows 35 and 36 that ceilings 191 and 192 were deployed are therefore
not current runtime truth. Those rows remain untouched as history under this
file's append-only rule. This laptop session did not contact Render or Neon and
does not claim an independent runtime observation. Treat migrations 190–192 and
the lease/guarantor working heads as unreleased until the owner performs the
deliberate reconciliation and release.

**`LOCALLY_EXERCISED` in disposable branch CI; no production rung moved.** Four
lease/guarantor proofs that existed but were invoked by no CI path are now
appended to the existing `tests/e2e/verify_all.sh` command: the pure Application
Review action contract before database setup, then the governing-instrument,
canonical-execution, and guarantor-signing database proofs after the real
migration chain and fixtures. The database proofs receive CI's disposable
`E2E_DATABASE_URL` only through their production-refusing
`HARNESS_DATABASE_URL` boundary. This is **Class 3 — test infrastructure**; it
adds no business-logic path and has no activation removal condition.

The connection was falsified, not inferred. A deliberate wrong application ID
made the Application Review proof fail while all three newly connected database
proofs still ran and passed; the parent runner printed `VERIFICATION FAILED`
and GitHub Actions recorded exit code 1 at commit `c16c34b` ([red run
124](https://github.com/kzitelli-art/property-spine-api/actions/runs/32723805211)).
The exact original test bytes were restored. At commit `b160287`, Application
Review and all three database proofs passed and the same parent printed
`ALL PROOFS PASSED` ([clean run
125](https://github.com/kzitelli-art/property-spine-api/actions/runs/32724076888)).
This continuously re-exercises existing local HTTP/browser/database evidence;
it does not establish a Render deploy, a Neon migration release, a live carrier
interaction, or a real resident/guarantor signing journey.

---

## Mike Grivna staff-invite link repair — 2026-08-24

**`REPORTED` production defect; source cause established.** Mike received the
real Skyline staff-invitation SMS, but its link answered `Missing or wrong
x-operator-key.` The invitation writer built `APP_BASE_URL + /join/{token}`.
The API had no route at that path and the global gate did not classify it as a
public door; no app branch consumed `/join/{token}` either. The existing
end-to-end proof extracted the token from the API response and posted it
directly to `/auth/sms/start` and `/auth/sms/verify`, so it proved the canonical
acceptance transaction while stepping around the link the human actually
received.

**`LOCALLY_EXERCISED` in disposable branch CI; not deployed.** Newly issued
staff invitations now name `/auth/join/{token}`, inside the already-public
`/auth/` boundary. That mobile page asks the invitee to request and enter the
six-digit code, then delegates to the existing start/verify endpoints; it owns
no OTP, identity, access, assignment, or session writer. The Skyline journey
also opens the literal returned URL over real HTTP before it may extract the
token. A dedicated real-Chromium proof starts from the exact invitation URL,
requests the fake-carrier OTP through the page, verifies through the page, and
rereads the accepted invite, person bridge, staff context, property-team
access, and leasing work assignment from disposable Postgres.

The new runner entry was falsified independently. An impossible browser
selector made only `browser: staff invite accepts` fail; the later
invite-to-guarantor journey still passed, the parent printed `VERIFICATION
FAILED`, and GitHub Actions preserved exit code 1 at `f93147b` ([red run
128](https://github.com/kzitelli-art/property-spine-api/actions/runs/32728182465)).
The exact browser-test bytes were restored at `50b6f08`, and the full parent
returned `ALL PROOFS PASSED` ([clean run
129](https://github.com/kzitelli-art/property-spine-api/actions/runs/32728457693)).

`src/identity/staff_invite_acceptance_page.js` is **Class 2 — temporary
adapter**. Removal condition: the canonical staff app has its own
browser-verified invite-token entry screen. Until this branch is deliberately
reconciled and deployed, Mike's production link remains broken; do not issue a
replacement invitation merely to send the same old URL again. After release,
create one new canonical invite because the prior link both points to the old
door and has a 72-hour expiry, then observe Mike's real acceptance and reread
the four production identity/access records before claiming activation.

### 24 Aug 2026 — staff-invite terminal-state hardening evidence stop

The canonical staff-invite start door could issue an OTP after an invitation
had become `superseded`. The verify door then refused that same invitation.
The human therefore received fresh-looking evidence from a lifecycle state
that could never complete. The same source review found inconsistent terminal
receipts between start and verify, no invitation-expiry check at verify, and a
fifth wrong code reported as retryable even though it had just locked the
invitation.

Current branch source now routes both `/auth/sms/start` and
`/auth/sms/verify` through one lifecycle classifier. Only `active` invitations
may reach OTP issuance or comparison. Accepted, revoked, superseded, expired,
locked, and unknown states stop with explicit receipts; the fifth wrong code
returns the locked receipt immediately. The existing browser proof was
extended to exercise every terminal state, preservation of the invite row,
absence of carrier sends and user provisioning, inert GET behavior, lockout,
successful acceptance, and accepted-invite replay. It remains invoked by the
existing end-to-end runner; the runner was not reordered or restructured.

**NO EXECUTION CLAIM RUNG EARNED IN THIS SESSION.** Node parsed the two changed
files and the static team-access session-boundary test passed. The local source
governance parent stopped before this proof because this laptop has no
installed `express` dependency; it printed parent exit 1 and marked the later
tests NOT RUN. Commits written through the GitHub connector did not dispatch a
branch-push Actions run, and the available browser controller stopped on an
environment error. No green execution was observed, so no deliberate red/green
falsification was attempted and no `LOCALLY_EXERCISED`, `HTTP_PROVEN`,
`BROWSER_VERIFIED`, `DEPLOYED`, or `PRODUCTION_PROVEN` claim is made. Neon,
Render, the SMS carrier, and production were not contacted.

The shared lifecycle classifier is **Class 1 — permanent primitive**. The
expanded proof is **Class 3 — test infrastructure**. The existing public
acceptance page remains **Class 2 — temporary adapter**, removable only when
the canonical staff app owns a browser-verified invite-token entry screen.
This slice does not claim to serialize concurrent invitation creation and
supersession; that transaction question needs its own observed stop and
contract before changing the writer. The forbidden second path remains a new
OTP store, status vocabulary, provisioning writer, app entry path, or
property-specific branch.

Next evidence step: dispatch the branch workflow through an authenticated
human Git push or manual Actions run; inspect the exact terminal-state browser
output; then deliberately break one lifecycle assertion, require parent exit
1, restore the exact bytes, and require a clean parent run. Append any earned
rung here afterward; do not rewrite this stopped receipt.

### 24 Aug 2026 — superseded lease-signing links fail closed

**`HTTP_PROVEN` in disposable branch CI; not deployed.** Packet versioning
already marked an issued lease package with `superseded_at` when staff created
a governed replacement, and the company-signing door already refused that old
package. The canonical public token resolver nevertheless selected resident
and guarantor links using only token hash, expiry, and non-void status. An old
link could therefore still disclose the obsolete package and accept signer
traffic after staff had replaced it.

Both the migration-192 signer-token query and its legacy resident-token
fallback now require `lease_packets.superseded_at is null`. The existing
hostile lease journey creates a real issued packet, supersedes it through the
governed new-version command, and requires the old public link to return 404
for both packet read and final submit. No token store, status, route, signing
service, migration, or runner path was added.

The evidence cycle was observed against exact GitHub SHAs. Run
[#152](https://github.com/kzitelli-art/property-spine-api/actions/runs/32755614094)
tested `426faa8`, reported the hostile proof PASS, parent exit 0, no NOT RUN,
and every later proof executed. The test stayed intact while only the current
signer query's non-superseded predicate was removed at `4877ce7`; run
[#153](https://github.com/kzitelli-art/property-spine-api/actions/runs/32757553306)
then captured `superseded resident read refused — accepted 200` and
`superseded resident submit refused — accepted 409`, reported hostile proofs
10 passed / 2 failed, and returned parent exit 1 while still executing every
later proof. The exact guarded source blob was restored at `bd747de`; run
[#154](https://github.com/kzitelli-art/property-spine-api/actions/runs/32757884687)
reported the hostile proof PASS, parent exit 0, no NOT RUN, and every later
proof executed. Passing assertion lines are suppressed by the parent runner
and are not claimed as observed output; the product-side red run establishes
that those assertions can turn the parent red.

This also corrects the preceding receipt's tooling inference that
connector-authored pushes did not dispatch Actions. GitHub's run history later
showed push runs on the exact commits; the available workflow lookup was blind
to those push events. Exact `05ef8ba` was independently repeated by manual run
[#149](https://github.com/kzitelli-art/property-spine-api/actions/runs/32754235727):
parent exit 0, all 23 steps PASS, no NOT RUN, real Postgres and Chromium, and
the staff-invite browser proof plus both later lease journeys executed. The
older missing-selector red proves that browser step reaches the parent, but it
does not by itself establish that each lifecycle assertion detects a product
regression; no broader rung is inferred from that selector break.

The canonical token resolver guard is **Class 1 — permanent primitive**. The
hostile journey extension is **Class 3 — test infrastructure**. At closeout,
the three ownership lanes remained separate: Codex changed only
`src/applications/leasepackets.js` and its hostile lease proof; CAMP remained
on standing-projection / Ask Spine files at last-reported `1eb6221`; CABIN
remained on tenancy standing/move-in reads and its named proofs at
last-reported `41c1aa6`. `tests/e2e/verify_all.sh` was not changed.

No main merge, PR, deployment, migration, app or `index.html` change, Neon or
Render contact, production read/write, or carrier action occurred. The next
bounded source question is the public V3 guarantor fact: validated captured
contact and top-level `guarantor_name` currently can disagree before packet
generation decides whether a guarantor signer is required.

Cross-branch coordination remained contained. CAMP's exact `1eb6221` full
parent baseline is red at run
[#32731669834](https://github.com/kzitelli-art/property-spine-api/actions/runs/32731669834):
the clean lease path, hostile lease proof, cross-surface reconciliation,
standing/review comparison, and resident browser signing stop on
`signature_intent_required`. CAMP correctly refused to edit or import the
Codex-owned application and lease helpers. This is recorded as integration
drift requiring an owner reconciliation ruling after the bounded branches
close, not as a source collision and not as authority to expand this slice.

### 24 Aug 2026 — public V3 guarantor identity has one submission authority

**`HTTP_PROVEN` in disposable branch CI; not deployed.** The V3 public
application validator already required a complete
`captured.guarantor_contact` when the applicant selected that a guarantor was
needed. The same request separately accepted top-level `guarantor_name`, and
the canonical application birth persisted that independent value. Packet
generation later decided whether a guarantor signer was required from
`!!lease_applications.guarantor_name`. A crafted request could therefore pass
the complete V3 capture contract while omitting or contradicting the value
that controlled the signer rail.

The public V3 door now derives the persisted guarantor name from the validated
captured contact. An omitted legacy duplicate is the normal V3 shape; a
non-empty duplicate that contradicts the captured name returns 400 before the
invitation is consumed or an application is born. V2 and internal/import
callers keep their existing contracts. No route, service, status, column,
migration, packet branch, or client-side authority was added.

The exact evidence cycle ran automatically on GitHub. Run
[#32763311993](https://github.com/kzitelli-art/property-spine-api/actions/runs/32763311993)
tested `5f5f212`, returned parent exit 0 with no NOT RUN, and the terminal
invite-to-guarantor journey plus every earlier proof passed. The test stayed
intact while only the product-side contradiction refusal was disabled at
`068b7a5`; run
[#32763661967](https://github.com/kzitelli-art/property-spine-api/actions/runs/32763661967)
stopped that journey at `a contradictory V3 guarantor name is refused before
application birth` and returned parent exit 1. Because that fail-closed
assertion stopped its journey, the subsequent omission/derivation assertions
did not execute on the red run; no broader red claim is inferred. The exact
product source blob was restored at `cf7fcade`; run
[#32763940585](https://github.com/kzitelli-art/property-spine-api/actions/runs/32763940585)
returned parent exit 0, no NOT RUN, every later proof through the terminal
guarantor journey, and `ALL PROOFS PASSED`. Successful individual assertion
lines are suppressed by the parent runner and are not quoted as observed.

The V3 public-submission authority guard is **Class 1 — permanent primitive**.
The extended real-HTTP/Postgres journey is **Class 3 — test infrastructure**.
`tests/e2e/verify_all.sh` was not changed. Separately, Codex aligned three
Codex-owned lease proof helpers on CAMP with the already-required intentional
signature contract; exact CAMP head `40f84f0` returned full-parent green in
run
[#32764307766](https://github.com/kzitelli-art/property-spine-api/actions/runs/32764307766)
with no NOT RUN. That compatibility repair was Class 3 only and changed no
CAMP product source.

No main merge, PR, deployment, migration, app or `index.html` change, Neon or
Render contact, production read/write, or carrier action occurred. Production
and Mike Grivna's real activation state are unchanged.

### 24 Aug 2026 — typed lease signature must match the named packet signer

**`HTTP_PROVEN` in disposable branch CI; not deployed.** The public lease
field writer already resolved the current packet signer from the token,
required explicit signature consent, and server-authored the resident or
packet-signer linkage. It nevertheless accepted any trimmed signature value
of two characters or more. A token holder could type a contradictory name and
Spine would mark the signature complete while attributing it to the packet's
named resident or guarantor.

Signature completion now normalizes Unicode compatibility form, surrounding
and repeated whitespace, and case for both the typed value and the signer's
frozen `display_name`. A missing signer name returns 409; a contradictory
typed name returns 400 before the field update. The normal e2e helper types the
signer name returned by the same public packet read. This is a consistency
guard, not identity verification: the packet signer name remains a claim, and
the change does not silently promote a guarantor into a durable Person.

Run
[#32764970080](https://github.com/kzitelli-art/property-spine-api/actions/runs/32764970080)
tested baseline head `4d451190`, returned parent exit 0, hostile
falsifications PASS, no NOT RUN, every later proof through the guarantor
journey, and `ALL PROOFS PASSED`. The hostile proof stayed intact while only
the product mismatch predicate was disabled at `393f9cb`; run
[#32765268066](https://github.com/kzitelli-art/property-spine-api/actions/runs/32765268066)
captured `contradictory typed signature refused — HTTP 200` and
`stored={"completed":true,"field_value":"Not Probe Tester ..."}`, reported
hostile proofs 12 passed / 1 failed, and returned parent exit 1. No NOT RUN was
reported and every later top-level proof still passed. The exact guarded
product blob was restored at `6a7754c`; run
[#32765554773](https://github.com/kzitelli-art/property-spine-api/actions/runs/32765554773)
returned parent exit 0, hostile falsifications PASS, no NOT RUN, every later
proof through the guarantor journey, and `ALL PROOFS PASSED`. Successful
individual hostile assertions are suppressed and are inferred only from the
containing fail-closed proof result; the product-side red supplies the direct
observed evidence that both the HTTP and database walls can turn the parent
red.

The signature-name consistency guard is **Class 1 — permanent primitive**.
The hostile refusal and normal helper alignment are **Class 3 — test
infrastructure**. No second signer store, identity path, route, status,
column, migration, or runner path was added.

No main merge, PR, deployment, migration, app or `index.html` change, Neon or
Render contact, production read/write, real signing action, or carrier action
occurred. Production and Mike Grivna's real activation state are unchanged.

### 24 Aug 2026 — completed lease-signer evidence is immutable and exact retries are idempotent

**`HTTP_PROVEN` in disposable branch CI; not deployed.** Migration 192 already
installed a database trigger that freezes completed lease-field evidence. The
public field-completion door nevertheless attempted the same update again when
a signer retried an already-completed field with the same value. The database
correctly rejected that write, but the human received HTTP 500 even though
nothing had changed. A client retry could therefore look like a failed signing
action after the signature had actually been retained.

The canonical field writer now recognizes an exact retry only after resolving
the current signer, requiring explicit signature intent, and applying the same
typed-name consistency check as the first submission. If the stored completed
value and normalized incoming value agree, it returns HTTP 200 with an
idempotent receipt without rewriting the field or adding a second audit event.
A conflicting repeat returns 409. The ordinary completion update remains
guarded by `completed = false`; the migration-192 trigger remains the final
database wall. No route, signer store, status, column, migration, alternate
writer, or runner path was added.

Run
[#32768335894](https://github.com/kzitelli-art/property-spine-api/actions/runs/32768335894)
tested baseline `26c98d2`, returned parent exit 0, hostile falsifications PASS,
no NOT RUN, every later proof through the guarantor journey, and `ALL PROOFS
PASSED`. The hostile assertion stayed intact while the product-side exact-retry
return was disabled at `8b8cbb7`; run
[#32768753533](https://github.com/kzitelli-art/property-spine-api/actions/runs/32768753533)
then returned parent exit 1. Its exact evidence was retry HTTP 500 with no
idempotent response while the database trigger preserved the completed row:
`completed_at`, `field_value`, and the original `session_id` were unchanged,
and the audit count stayed 1→1. This was a genuine HTTP-contract red; it was
**not** an observed evidence rewrite. No NOT RUN was reported and every later
top-level proof still passed.

The exact guarded source was restored at `b7c806f`; run
[#32769063555](https://github.com/kzitelli-art/property-spine-api/actions/runs/32769063555)
returned parent exit 0, hostile falsifications PASS, no NOT RUN, every later
proof through the terminal guarantor journey, and `ALL PROOFS PASSED`.
Successful inner retry/session/audit assertion text is suppressed by the parent
runner and is not quoted as observed.

The migration-192 completed-evidence trigger is **Class 1 — permanent evidence
primitive**. The source-level idempotency/refusal boundary is **Class 1 —
permanent primitive**. The hostile retry proof is **Class 3 — test
infrastructure**. This slice adds no temporary adapter or delete-on-activation
scaffolding.

No main merge, PR, deployment, migration, app or `index.html` change, Neon or
Render contact, production read/write, real signing action, or carrier action
occurred. Production remains API `61f99bf` at ledger 189 by the owner's last
verified runtime receipt; this branch and migrations 190–192 remain unreleased.
Mike Grivna's production activation state is unchanged.

## Two-step leasing candidate — AUTHOR and EXECUTE — 2026-09-13

Row 70. Branch `claude/two-step-leasing-20260913` (API over `24007eb`, app over
`e23a36a`). The released path (approve → confirm terms → packet → sign →
company-sign) is untouched and still proven; the two-step path coexists and
every verdict names which basis admitted a packet. The one rule change: a
submitted application bound to its applicant's acknowledged, current authored
offer is packet-eligible without approval, and the lineage record that
admits it is written as system-derived, never as a human confirmation. The
one new door: Execute, which needs approval authority AND signer standing
and records two distinct decisions in one transaction. Nothing grants Mike
more than `property_manager`. Full receipt and scrubbed evidence under
`docs/handoffs/new-hp/two-step-leasing/`. CI run 487 (`5ba2885`) passed the full
`verify_all.sh`; the browser slice ran locally against app `9ce6fc0`.

## Governed inventory correction candidate — 2026-09-13

Row 71. The retirement owner gained a staff door, not a second writer:
review, retire and reinstate live on the existing Rent Roll page and on
`/operator/inventory/corrections*`, gated by the governed override that
already exists. Eligibility is about relationships Spine holds, never
about physical reality; the person's rationale is the identity decision.
Receipt and scrubbed evidence under `docs/handoffs/new-hp/inventory-correction/`.
CI run 496 (`5be5c1c`) passed the full `verify_all.sh`; app `0450247`.

## Inventory correction hardening — 2026-09-13

Row 73. **CANDIDATE (not released) · HTTP-PROVEN LOCALLY — final relationship
and command-identity correction.** A focused review of row 72 reproduced two
source defects on an owned database: a `unit_events` row with a current direct
`unit_id` and a retired `space_id` was accepted on both INSERT and UPDATE, and
two keys differing only after character 200 silently replayed because the
command service truncated both. API `c479a9c` changes migration 197's existing
wall to inspect every populated target, preserves the tables' existing
unit/space grain rules, and makes the conflict reader report every actual
retired target exactly once. It explicitly refuses idempotency keys longer than
200 characters before creating or replaying a command. The registered
hardening proof is **104 passed, 0 failed** and the delivered correction proof
remains **85 passed, 0 failed**; the live-catalog policy gate passes
(61 references / 61 entries / 24 blocking tables / 25 triggers), the contract
test passes, and `tests/verify_source_governance.js` exited 0. Paired app
remains `0d035c8`; no production migration, deployment, or source-home-196
integration was performed. Receipt:
`docs/handoffs/new-hp/inventory-correction/FINAL_REVIEW_20260913.md`.

Row 72. The row-71 door could be raced (authority read before the lock),
bypassed (every writer but leases could attach to retired inventory),
and could double-count on reinstatement; none of that is a Greenery
finding, all of it was shown red on the candidate first. The relationship
policy is now one declaration that the review, migration 197's triggers
and a catalog gate all consume; a retired record can be revived only by
a decision, and a reinstatement is an identity decision the person makes
explicitly when the established record cannot settle it. The tenancy
standing carries the correction explanation the staff history renders,
so Ask Spine and the app say the same thing. Receipt:
`docs/handoffs/new-hp/inventory-correction/SUCCESSOR_RECEIPT.md`;
migration description `MIGRATION_197.md` beside it (no production
migration authorised). API `e65dfb2`, app `0d035c8`. CI run 500 (`87be4bd`,
the receipt commit over `e65dfb2`) passed the full `verify_all.sh` with the
gate and the hardening proof executed.

Row 73 (current rent-roll reconciliation). The current tracker is a statement about who occupies today, not
a lease and not a forward commitment, and the existing Deal Setup doors
carry it once confirm stops inventing a term for a source that has none.
On the baseline a dateless signed row became an active lease with no
start and no end, and the bed's own resident was never offered, so the
only path was a duplicate person and a refused duplicate lease. Now a
dateless row is accepted as current occupancy with contractual terms
unknown, the home's right-holders are the identity candidates, and a
recognised resident is tied to the lease in force or pending — nothing
activated, no possession recorded, the prior tenancy never ended by an
import. The first red on the actual Skyline shape (rooms classified
'unit') is a classification defect corrected by the governed mapping
tool, not a reason to relax the grain rule. The app's picker, counter,
refusal reload and candidate buttons were each unreachable and are now
five isolated blocks. Receipt:
`docs/handoffs/new-hp/current-rent-roll-reconciliation/RECEIPT.md`.
API `be9ae71`, app `4558460`; CI run 519 (`e067069`, docs over `be9ae71`)
passed the full `verify_all.sh` with the proof executed. No production
migration; nothing released.

Row 74. The release could not have happened as pinned: the wrapper that
guards migrations 195–198 was reviewed against Windows bytes for three of
its four files, and the first Linux host to run it would have refused at
195 with the ledger untouched — a safe failure, but a failed release on
the morning of a walkthrough. The owned rehearsal found it before Render
did, the correction pins bytes every host reproduces, and a CI step now
recomputes the pins so the next drift is red on the branch, not in the
deploy log. API `03550a8` over `e808199`; CI 525. Nothing released.
