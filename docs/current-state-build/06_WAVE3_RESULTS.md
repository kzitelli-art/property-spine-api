# WAVE 3 RESULTS — the final audit gaps

Surveyed against api b7720b2 2026-08-20. 58 findings across four never-covered areas plus a final critic.

## Headlines

**schema_001_119** — Migrations 001–119 create 157 tables, 132 of which appear in no audit document; four of them (bids, documents, inventory, property_controls — all from 001_baseline.sql) have zero references in src/ or server.js, and property_controls is a complete compliance/licensing schema that the Compliance domain built later in migration 168 never once mentions.

**shared_governance** — src/shared/ holds the repo's most cross-cutting primitive (obligation_engine.js, injected into 17 mount sites and HTTP-proven) sitting beside a seam that throws on require, a fixture-injection door wired to a button in the signed-in operator UI, and a DEMO_MODE boot hook that writes durable rent-roll data while bypassing the synthetic-data perimeter its own sibling routes enforce.

**test_census** — All 37 gates that any automated runner in this repo executes are database-free, so none of the 39 real-Postgres+real-HTTP tests and none of the 133 real-Postgres service tests — the entire evidentiary basis for every HTTP_PROVEN rung in CURRENT_STATE.md — is run by anything; 255 of the 292 files in tests/ are invoked by nothing at all.

**ci_reality** — CI executes 48 of the 313 .js files under tests/ (37 of 292 at the top level, 12.7%) — every one of the 68 `.db.js` real-Postgres proofs, all 94 `*_proof.js` files, and all 23 browser proofs in the app repo are never run by any automation, and `main` is reported `"protected": false`, so a red verify does not block a merge.

## Method notes (what each agent searched, and what it did NOT)

### schema_001_119
Worktree /tmp/claude-0/-home-user/35ef8d61-d4f6-594f-9fd5-4adbc648960d/scratchpad/main-api at main (b7720b2). WHAT I DID: (1) Enumerated migration files 001–119 with a seq loop — 119 files, one per number. (2) Stripped `--` comments from each file, flattened newlines, and regex-extracted `create table [if not exists] [public.]<name>` — 159 statements, 157 distinct names. Cross-checked against an unstripped extraction; the sets were identical except two comment-only false positives (`if`, `only`), which is why comment-stripping was kept. (3) Rename/drop check ran over ALL 179 migration files including 120–187, not just my range, specifically to avoid the opening_positions false-orphan trap. (4) Ownership: a Python scan over all 329 src/**/*.js files plus server.js, stripping /* */ and // comments first, matching `(from|into|update|join)\s+[public.]"?<table>"?` case-insensitively. The comment-stripping mattered — an unstripped grep wrongly credited `inventory` to two files whose only hits were prose, and wrongly cleared `mapping_memory`. (5) Writer scan: same corpus for `insert into|update|delete from <table>`, compared against tools/, tests/, seeds/, proof/. (6) Reachability: a transitive require() walk from server.js (297 of 329 src files reached). (7) Doc coverage: whole-word grep of each of the 157 names against CURRENT_STATE.md, 03_WAVE1_RESULTS.md, 05_WAVE2_RESULTS.md. WHAT I DID NOT DO: no database was queried — no row counts, no verification of the actual production ledger contents, no confirmation that any table holds data. I extracted CREATE TABLE only; I did not au

### shared_governance
Scope covered: all 21 files in /tmp/claude-0/-home-user/35ef8d61-d4f6-594f-9fd5-4adbc648960d/scratchpad/main-api/src/shared/ and the 1 file in src/governance/ (status_model.js), each opened and read at least in header+exports, with obligation_engine.js, obligation_transitions.js, database_ssl.js, synthetic_data_perimeter.js, reader_capability_contract.js, seed_endpoint.js and status_model.js read in full. For every one of the 22 I ran two repo-wide greps from the API repo root — `grep -rn \"shared/<name>\"` and a bare `grep -rn \"<name>\"` across *.js/*.json/*.html/*.sh/*.md, excluding node_modules and the file itself — so BUILT_BUT_DORMANT claims rest on a whole-repo search including server.js at the root, per CLAUDE.md's warning. I also grepped the APP worktree (/scratchpad/main-app/index.html, 29k+ lines) for callers of the routes these files own. I opened, not just named, every test I cite a rung from: obligation_completion_canonical_proof.db.js, operator_obligations_security_proof.db.js, work_order_canonical_path_proof.js, authority_mutation_containment.db.js, gate_harness_isolation.js, gate_property_creation_paths.js, gate_person_ingress.js, gate_ci_path_ssl.js, gate_ask_spine_readers.js, and checked each for a hand-built fake pool (none of the four HTTP_PROVEN citations uses one). Two runtime checks were executed with `node -e require(...)`; node_modules is NOT installed in this worktree, so the schedulingAdapterSeam break is confirmed on a repo-relative path resolution (real) while any 'Cannot find module express' result was discarded as environmental. NOT DONE: I d

### test_census
Scope: the 292 top-level .js files in main-api/tests/ at b7720b2, plus the 16 .js in tests/e2e/ for runner-reachability only. Method: (1) mechanical classification of all 292 by comment-stripped signal grep — require(\"pg\")/new Pool/new Client vs .listen(/http.createServer/supertest/spawn(server.js) vs playwright|chromium|puppeteer; (2) opened and verified 24 files spanning all five categories, including all 10 fake-pool files individually and 6 of the 39 PG+HTTP; (3) parsed package.json, .github/workflows/verify.yml, deploy.sh and tests/e2e/verify_all.sh, and extracted verify_source_governance.js's 37-gate list programmatically, then cross-tabulated against the census; (4) EXECUTED tests/verify_source_governance.js and tests/gate_harness_isolation.js live at b7720b2 and captured their real output; (5) re-executed the database-free assertions of pricing_guards_proof.js and operator_language_proof.js verbatim; (6) node --check on all 292; (7) programmatic resolution of every relative require and every repo-relative path literal. What I did NOT do: node_modules is absent and no Postgres server is reachable in this environment, so I could not RUN any test that needs express or a database — every deadness claim I marked proven is one I could demonstrate without either. I did not evaluate the 133 PG_ONLY or 39 PG+HTTP files assertion-by-assertion for staleness; I did not audit the 18 *.browser.js files in the APP repo; I did not audit tools/ except where gate_harness_isolation.js counts it. The four files behind defect #13 are named as a candidate set with reasoning, not as a c

### ci_reality
Read /tmp/.../main-api/.github/workflows/verify.yml (the ONLY file in .github — `find .github -type f` returns exactly one), traced it into tests/e2e/verify_all.sh, into tests/verify_source_governance.js (whose GATES array I enumerated programmatically), and into tests/e2e/{apply_migrations.sh,boot.sh,port_guard.sh}. Checked all 37 CI-invoked files for transitive child spawns (only migration_release_gate.test.js, the two conversation_*.test.js and ask_spine_reader_gate_falsification.js spawn anything, and none spawns another test file). Grepped the whole repo for any runner that globs tests/ — none exists. Read package.json. Fetched the real GitHub Actions run for main head b7720b2 (run 32383370056, job 96471774202) and read its log. Checked branch protection via the branches API. Inspected the app worktree for a .github dir (none) and read its run_harnesses.sh. I did NOT run any test myself, did NOT audit the internal assertion quality of the 37 executed gates (beyond noting they complete in ~4s), and did NOT check GitHub rulesets beyond the `protected` field the branches API returns.

## All findings

| Area | Severity | Finding | Evidence |
|---|---|---|---|
| schema_001_119 | notable | **Migrations 001–119 create 157 distinct tables; 132 of them (84%) are named nowhere in any of the three audit documents** 119 migration files numbered 001–119 exist, one per number. Comment-stripped extraction finds 159 `create table` statements resolving to 157 distinct table names. A whole-word grep of each name against docs/CURRENT_STATE.md, docs/current-state-build/ | /tmp/claude-0/-home-user/35ef8d61-d4f6-594f-9fd5-4adbc648960d/scratchpad/main-api/migrations/ — 119 files 001–119; extraction script output at /tmp/cl |
| schema_001_119 | notable | **(a) FOUR tables have no owning capability anywhere — zero references in src/ or server.js, all four created by 001_baseline.sql** bids, documents, inventory and property_controls have zero SQL references in src/ or server.js after stripping /* */ and // comments. Verified twice by two independent methods (SQL-context regex, then a bare whole-word scan of every src/*.js plus ser | migrations/001_baseline.sql:258 (bids), :289 (documents), :274 (inventory), :371 (property_controls); schema.sql:232, :333, :248, :343; tools/repair/s |
| schema_001_119 | notable | **property_controls is a full compliance/licensing domain sitting in the schema since migration 001, and the Compliance room built later (migration 168)** property_controls declares control_type text with the comment vocabulary `rental_license/coo/lead_safe/tax/insurance/lender_report/permit/inspection/utility/service_contract/vendor_insurance/compliance`, plus status (active/expiring/expired/pending/c | migrations/001_baseline.sql:371-392; migrations/168_compliance_canonical_truth.sql (creates compliance_items, compliance_facts, compliance_fact_eviden |
| schema_001_119 | minor | **(b) One table in 001–119 was created then dropped and rebuilt by a later migration: scheduled_charges (002 → dropped and recreated in 036)** A literal-name grep alone would not reveal this. Verbatim from the migration's PRE-EXISTING TABLE GUARD: "An earlier migration created a DIFFERENT scheduled_charges (move-in / concession shape: label, due_on, is_move_in_gate, recur_period…) with no ` | migrations/036_scheduled_charges.sql:52-59 (guard comment and `drop table if exists scheduled_charges cascade;`); migrations/002_revenue_and_supply.sq |
| schema_001_119 | informational | **(b) NO table created in 001–119 is ever renamed — the only renames in the entire migrations tree are the two in migration 159 that a prior wave alread** After stripping comments, a scan of ALL 179 migration files (not just 001–119) for `alter table [if exists] [only] <name> rename to <name>` and `drop table [if exists] <name>` returns exactly three results in total: 036's drop of scheduled_charges, a | migrations/036_scheduled_charges.sql:59; migrations/159_opening_tenancy_position.sql:52,61; comment-stripped scan across all 179 files in /tmp/claude- |
| schema_001_119 | informational | **(c) There are NO missing migration numbers in 001–119 — every number is present exactly once** A loop over `seq -w 1 119` checking for `${n}_*.sql` produced no misses, and the directory contains exactly 119 files with a numeric prefix in that range. The 001–119 band is contiguous and unduplicated. (A `000_schema_migrations.sql` bootstrap file  | /tmp/claude-0/-home-user/35ef8d61-d4f6-594f-9fd5-4adbc648960d/scratchpad/main-api/migrations/ — 119 files matching 001–119, seq loop over 001..119 pro |
| schema_001_119 | minor | **Migration gaps 138 and 139 exist and are documented nowhere in the audit — CURRENT_STATE.md records only 125 and 141–149** Extending the same sweep across 001–187 as a control, the complete set of absent numbers is: 125, 138, 139, 141, 142, 143, 144, 145, 146, 147, 148, 149. CURRENT_STATE.md records "Migration 125 does not exist (124→126)" and "10 consecutive missing mig | seq sweep 001–187 over /tmp/claude-0/-home-user/35ef8d61-d4f6-594f-9fd5-4adbc648960d/scratchpad/main-api/migrations/; `grep -rn "138\/139" docs/CURREN |
| schema_001_119 | notable | **(d) SEVEN tables have no INSERT/UPDATE/DELETE anywhere in src/ or server.js — their only writers are tools/ scripts or tests** demo_runs, demo_events, demo_attempts (migration 052) — only writer tools/demo_run_ops.js. property_unit_types (100) — only writers tools/apply_unit_type_mapping.js plus seven test files including tests/e2e/property_fixture.sql. renewal_cases (119) — | /tmp/claude-0/-home-user/35ef8d61-d4f6-594f-9fd5-4adbc648960d/scratchpad/writers.txt; tools/demo_run_ops.js; tools/apply_unit_type_mapping.js; tests/r |
| schema_001_119 | notable | **tour_outcome_prompts feeds a live operator metric that nothing in production can ever populate** src/evidence/tour_demand.js (reachable from server.js via the require graph) computes an `outcome_captured` numerator with `exists(select 1 from tour_outcome_prompts p where p.tour_id = t.id and p.responded_at is not null)` and reports `provenance: " | src/evidence/tour_demand.js:95-96 and :186; migrations/096_tour_outcome_prompts.sql:58-60; zero insert/update/delete for tour_outcome_prompts across s |
| schema_001_119 | notable | **property_channel_capabilities (094) — the durable SMS operating-authority record — is written by no production code and its sole reader is never calle** propertyHasCapability() is defined and exported in the live, server.js-reachable src/comms/communications_boundary.js. A repo-wide grep for call sites `propertyHasCapability(` returns eight hits, ALL in tests/property_capability_proof.js — zero produ | src/comms/communications_boundary.js:100-126, :135-157, :1045; migrations/094_property_channel_capabilities.sql:16, :28-30, :72-75; `grep -rn "propert |
| schema_001_119 | minor | **integration_sync_runs and integration_sync_states (migration 049) are the only tables in 001–119 whose sole source reference lives in a file server.js** A transitive require() walk from server.js reaches 297 of the 329 src/*.js files. Both tables are referenced only by src/shared/outlookAcuitySync.js, which is in the 32-file unreached set. The only file that requires it is src/shared/outlookAcuityMou | migrations/049_outlook_acuity_sync.sql; src/shared/outlookAcuitySync.js is the only src/ file matching either table name; src/shared/outlookAcuityMoun |
| schema_001_119 | notable | **Three tables have no writer anywhere in the repo because their own migration seeds them — one seeds a single hardcoded property UUID** category_report_map (008), staff_roles (095) and property_leasing_calendar (073) have zero insert/update/delete in src/, server.js, tools/, tests/, seeds/ and proof/. All three are populated by an `insert into` inside their own migration, so they are | migrations/008_category_report_map.sql:35; migrations/095_staff_roles.sql:36; migrations/073_property_leasing_calendar.sql:32-45; src/tenancy/tenancy_ |
| schema_001_119 | notable | **Two different domains own the single `vendors` table, and `create table if not exists` hid the collision until it broke every from-scratch build** vendors is created twice in 001–119: in 001_baseline.sql as a maintenance vendor (id, name, trade, phone, email, preferred, insurance_status, note) and again in 012_bank_intake.sql as a money payee (canonical_name, yardi_code, vendor_type). Migration | migrations/001_baseline.sql (maintenance vendors); migrations/012_bank_intake.sql:33-45, :43, :46, :55, :58-60 (the three `add column if not exists` l |
| schema_001_119 | notable | **mapping_memory (013) is written by live code but its stated purpose is disconnected from the parser it was built to improve** src/onboarding/public_review.js is reachable from server.js and does really write the table (`insert into mapping_memory` at line 582, with `times_seen = mapping_memory.times_seen + 1`) and read it (lines 632, 641). But the file states verbatim at li | src/onboarding/public_review.js:11,15,549-550,575,582,587,632,641; migrations/013_public_rent_roll_review.sql |
| schema_001_119 | minor | **work_order_billback_decisions (099) accepts entry_kind='dispute' but no code anywhere can write one** src/maintenance/work_order_service.js reads and derives from disputes (line 474 filters `r.entry_kind !== "dispute"`, line 476 finds `lastDispute`, line 498 sets `disputed`), but states verbatim at line 510: "There is deliberately NO recordBillbackDi | src/maintenance/work_order_service.js:466,474,476,498,510-513; migrations/099_billback_decision.sql:145-152 |
| schema_001_119 | informational | **Whether any of these tables actually hold rows in production** NOT_FOUND. No database was queried in this audit. Migration 001 and every file through 119 exists on main and CURRENT_STATE.md reports a production ledger ceiling of 187, which implies all of 001–119 are applied — but that is a report in a document,  | docs/CURRENT_STATE.md STATE SNAPSHOT: "Production ledger ceiling 187 (schema)" — no DATABASE_URL was used and no query was run in this session |
| shared_governance | notable | **src/shared/schedulingAdapterSeam.js is BROKEN, not merely dormant — it throws on require** Line 21 reads `const buildSchedulingModule = require('../leasingscheduling');`. From src/shared/ that resolves to src/leasingscheduling.js, which does not exist — the file lives at src/leasing/leasingscheduling.js (server.js:3412 requires it correctl | src/shared/schedulingAdapterSeam.js:21; src/leasing/leasingscheduling.js (exists) vs src/leasingscheduling.js (ls: No such file or directory); server. |
| shared_governance | notable | **A demo/fixture path is reachable from the signed-in operator UI — "Demo data may exist. Demo paths may not."** main-app/index.html:29135 renders `<button class="psw-add" type="button" onclick="openSnapshotPanel()" title="Load or manage portfolio data">+ Data</button>` into the property-switcher bar (renderPropSwitcher), with no role or entitlement condition o | main-app/index.html:29135 (button), :28858 (openSnapshotPanel), :28867, :28903; src/shared/seed_endpoint.js:168,206; server.js:3297 (`app.use('/', see |
| shared_governance | notable | **The DEMO_MODE boot-time auto-seed BYPASSES the synthetic data perimeter entirely** src/shared/seed_endpoint.js:160-166: `if (DEMO_MODE) { setImmediate(() => { ensureSoloQaBaseline().then(...).catch(...) }); }`. ensureSoloQaBaseline() (:122) resolves the target by exact name ("Property Spine Demo Building") and calls loadSnapshot()  | src/shared/seed_endpoint.js:160-166 (boot hook), :122-155 (ensureSoloQaBaseline), :146 (loadSnapshot call), :187 (perimeter check, HTTP path only); gr |
| shared_governance | notable | **POST /admin/seed-snapshot/skyline checks the perimeter against one resolution and then WRITES against a second, independent re-resolution** src/shared/seed_endpoint.js:184-191 resolves the skyline target via `resolveSeedTargetId(k)` (which calls resolvePropertyForImport on `pool`) and passes THAT id to syntheticTargetAllowed. It then calls `await seedOne(pool, k)` at :196 — and src/share | src/shared/seed_endpoint.js:66-74 (resolveSeedTargetId), :184-191 (check), :196 (`seedOne(pool, k)`); src/shared/seed_snapshot.js:77-85 (seedOne re-re |
| shared_governance | notable | **CORRECTION to 05_WAVE2_RESULTS.md: seed_snapshot.js IS HTTP-reachable, not "library only"** docs/current-state-build/05_WAVE2_RESULTS.md:159 reads: "src/shared/seed_snapshot.js — library only, NOT HTTP-reachable / BUILT_BUT_DORMANT". It is reachable. src/shared/seed_endpoint.js:47 does `const legacy = require("./seed_snapshot");`, :50 destr | docs/current-state-build/05_WAVE2_RESULTS.md:159; src/shared/seed_endpoint.js:47,50,168,196; src/shared/seed_snapshot.js:77,98,259; server.js:48,3297 |
| shared_governance | notable | **Two snapshot read routes accept a client-supplied id with no property-scope check — same defect class as known defect #9** src/shared/snapshot_loader.js:1580 `router.get("/snapshot/batches/:property_id", ...)` and :1591 `router.get("/snapshot/batch/:batch_id/rows", ...)`. Neither carries `requireOperator` (defined at :1446 and applied only to the two /operator/rent-roll  | src/shared/snapshot_loader.js:1580-1589, :1591-1599, :1446 (requireOperator, applied only at :1501/:1570); server.js:164,188,199-214 |
| shared_governance | notable | **GET /operator/build is documented as gated and is in fact unauthenticated** server.js:322-327: the comment reads "The full record — including the untruncated commit — behind the operator gate, for anyone reconciling a deployment against a branch." The handler is `app.get("/operator/build", (_req, res) => { res.set("Cache-Con | server.js:322-327 (route + comment), :199-214 (gate), :203, :192-193; src/shared/build_identity.js:82-92 (IDENTITY payload shape) |
| shared_governance | minor | **src/shared/no076_failclosed_check.js is an executable, write-capable test harness shipped inside src/ — declared DEAD by the repo's own gate** The file is a `#!/usr/bin/env node` harness with `const pool = new Pool({ connectionString: process.env.DATABASE_URL // "postgres://claude:claude@localhost/spinetest_no076" })`. It inserts a real property (`insert into properties (name, sms_number) v | src/shared/no076_failclosed_check.js:18-21,41-43; tests/gate_harness_isolation.js:231-234; tests/gate_property_creation_paths.js:100-119; tests/gate_p |
| shared_governance | minor | **src/shared/proof_next_action_resolver.js — a 25KB proof harness living in src/, run by the e2e shell script, whose own header names a path it is not a** Header: "proof_next_action_resolver.js — Slice 1 headless acceptance proof ... Run: node tools/proofs/proof_next_action_resolver.js" — but the file is at src/shared/. Nothing in src/ or server.js requires it; the sole invoker repo-wide is tests/e2e/v | src/shared/proof_next_action_resolver.js:1-34; tests/e2e/verify_all.sh:44; grep -rn "proof_next_action_resolver" --include=*.js --include=*.sh . → 1 h |
| shared_governance | informational | **src/shared/obligation_engine.js — the single most cross-cutting primitive in the repo; live-wired, HTTP-proven through the maintenance door, but its o** Required at server.js:265-266 and injected as constructor deps into at least 17 mount sites: maintenance (3137), unit triage (3143), turnover (3151), unit turn scope (3158), work acceptance (3171), readiness (3185), obligations actions (3251), down_u | src/shared/obligation_engine.js:30,37-38,93-98,132-148; server.js:265-266 and 17 injection sites (3137-3596); tests/work_order_canonical_path_proof.js |
| shared_governance | minor | **The harness that HTTP-proves the obligation engine is itself an unguarded, write-capable harness pointed at plain DATABASE_URL — a registered merge bl** tests/work_order_canonical_path_proof.js:51 documents `RUN: DATABASE_URL=... node tests/work_order_canonical_path_proof.js` and :61 connects with `connectionString: process.env.DATABASE_URL` — it does NOT use tests/_run_receipt.js's harnessConnection | tests/work_order_canonical_path_proof.js:51,61; tests/gate_harness_isolation.js:515-524, 642-649 (SLICE_A_BLOCKERS) |
| shared_governance | minor | **src/shared/database_ssl.js disables certificate verification for every non-local database connection** The whole module is 46 lines and its return value for any non-loopback host is `{ rejectUnauthorized: false }` (line 42). So every deployed connection — Neon included — negotiates TLS without verifying the server certificate. The header frames the mo | src/shared/database_ssl.js:30,32-43; server.js:131 and the require of "./src/shared/database_ssl"; tools/apply_unit_type_mapping.js; tests/gate_ci_pat |
| shared_governance | notable | **Neither src/shared/ nor src/governance/ is scanned by the §40.11 Ask Spine gate — widening known defect #6** tests/gate_ask_spine_readers.js:100: `const STANDING_READ_DIRS = ["src/asset", "src/tenancy"];`. CURRENT_STATE.md defect #6 already records that this scans 2 of ~15 domain dirs. Adding to it: the gate itself IMPORTS src/shared/reader_capability_contr | tests/gate_ask_spine_readers.js:100 (STANDING_READ_DIRS), :64 (imports the shared contract); src/shared/reader_capability_contract.js:3-5,26-33,36-47; |
| shared_governance | informational | **Inventory of the remaining 10 src/shared files — all live-wired, each with its direct requirer and proof** obligation_transitions.js — THE obligation type-change service; whitelists exactly two transitions (confirm_urgency->maintenance_repair, confirm_urgency->emergency_repair); required by server.js:258 and by src/maintenance/work_order_service.js:153/60 | src/shared/obligation_transitions.js:32-45,126; src/maintenance/work_order_service.js:153,161-162,607,651; src/shared/obligation_missed.js:3-9,219-225 |
| shared_governance | informational | **src/shared/synthetic_data_perimeter.js is the strongest-proven file in the directory — refusal side proven over real HTTP** Required by src/shared/snapshot_loader.js:27 and src/shared/seed_endpoint.js:38, both mounted unconditionally at server.js:3296-3297. Three fail-closed limbs (writes enabled, allowlist configured, resolved target is a member). tests/authority_mutatio | src/shared/synthetic_data_perimeter.js:56-113; src/shared/snapshot_loader.js:27,1470-1471,1488-1489; src/shared/seed_endpoint.js:38,187-190; server.js |
| test_census | critical | **255 of the 292 test files in tests/ are invoked by NOTHING, and every automated gate that does run is database-free** The repo has exactly two automated entry points. `npm test` / `npm run verify` / `deploy.sh:61` / the CI step "source governance gates" all run `tests/verify_source_governance.js`, which invokes a hardcoded list of 37 gate files. `npm run verify:all` | package.json scripts: `"pretest": "node tests/verify_source_governance.js"`, `"test": "echo \"source-governance gates passed; database proofs run sepa |
| test_census | critical | **The fake-pool category is 10 files, not 3 — seven were previously unnamed** Definition used: a real Express router mounted on a real socket (`app.listen(0)` / `http.createServer`) driven by real `fetch`, with a hand-built object passed as `pool` and no `require("pg")` anywhere in the file. All ten were opened individually an | 1. ask_spine_http_proof.js:61 `const pool = { async query(_sql, params) {...} }`, mounted at :99 `app.use("/", require("../src/agent/ask_spine.js")({  |
| test_census | notable | **Full mechanical census of the 292 top-level tests/*.js, sample-verified by opening 24 files** PG+HTTP (real Postgres AND a real router on a real socket) = 39: 34 in-process, 5 that spawn the real server.js as a child (deal_setup_http.db.js, forward_leasing_http.db.js, hotfix_future_rent_roll_guards_proof.js, slice9_evidence_http_proof.js, wor | Classifier over all 292 files with comments stripped; signals `require("pg")`/`new Pool`/`new Client` vs `.listen(`/`http.createServer`/`supertest`/`s |
| test_census | notable | **Where grep misclassified, and why name-based fake-pool detection fails in BOTH directions** (a) My first-pass fake-pool regexes (`const pool = {`, `query: async (`, `connect: async () =>`, `fakePool/stubPool/mockPool`) returned 44 files. Opening them showed 34 were pure-unit tests using legitimate service doubles that never mount a router — | technician_lifecycle_proof.db.js:101 `const shim = { query: (...a) => db.query(...a),` with `const db = new Client(...)`. rent_roll_bridge.test.js:68  |
| test_census | critical | **DEAD, PROVEN BY EXECUTION: tests/pricing_guards_proof.js has four assertions that inverted the moment the pricing fix was committed, and can never pas** The file's own design is to prove it can fail by asserting the guard is ABSENT in `git show HEAD:` — valid only while the fix sat uncommitted in a working tree. The fix is now at HEAD, so every 'HEAD (pre-fix)' assertion is permanently false. This is | tests/pricing_guards_proof.js:33 `const head = (rel) => execSync(\`git show HEAD:${rel}\`, ...)`. Re-executed at b7720b2: L47 `ok(!/bedrooms is not nu |
| test_census | critical | **DEAD, PROVEN BY EXECUTION: tests/operator_language_proof.js section 'SCOPE STAYED CLOSED' asserts a migration count that is off by 60** Three assertions bound the repo diff against a pinned SHA (62b25e8) that has had 831 files and 61 migrations land since. The file's own comment records that this exact assertion already went stale once. Database-free; permanently red. Re-executed ver | tests/operator_language_proof.js:428 `const changed = execSync("git diff --name-only 62b25e8", ...)`. Re-executed: :439-440 "exactly one migration fil |
| test_census | critical | **47 test files are pinned to a hardcoded demo property UUID; 42 of them never create it, and NOTHING in the migration chain or the e2e fixtures does ei** `a50fbdd0-3642-431e-b532-0dcd6ab8a4fe` is "Property Spine Demo Building" (src/surfaces/owner.js:171). 47 of the 292 test files hardcode it; 42 never issue an `insert into properties`, so they must find the row already present. The migration chain nev | src/leasing/demo_preflight.js:20 `const DEMO_PROPERTY_ID = "a50fbdd0-3642-431e-b532-0dcd6ab8a4fe";`; src/surfaces/owner.js:171 `"a50fbdd0-3642-431e-b5 |
| test_census | notable | **Defect #13's 'four falsification tests pinned to a hardcoded demo UUID' — strongest candidate is the four pricing_*.js files, but the set is NOT uniqu** CURRENT_STATE.md:111 leaves them unnamed ("unnamed in the report") and 00_RESUME_HERE.md:20 repeats the claim without names; I searched every doc and found no list. 42 files share the stated pattern, so the description alone does not identify four. T | docs/CURRENT_STATE.md:111 verbatim: "Four falsification tests are pinned to a hardcoded demo UUID and nothing runs them. Verified red before AND after |
| test_census | notable | **gate_funding_boundary.js prints PASS for the debt lane while scanning zero debt-funding files, because all three declared debt paths do not exist** The gate declares per-domain file lists and reads them through `readIf()`, which returns null when the file is absent rather than failing. For the `debt` domain, `DEBT_FUNDING` names two files and `DEBT_ECONOMIC` names three; three of those five do n | tests/gate_funding_boundary.js:192 `"src/asset/debt_establishment.js"`, :195 `"src/asset/debt_funding_service.js"`, :196 `"src/asset/debt_funding_read |
| test_census | notable | **The harness-isolation gate's own live output at b7720b2: 67 write-capable scripts can still point at whatever DATABASE_URL names** I ran the gate (it is database-free) and captured its classification of all 405 .js files under tests/ and tools/. My independent tests/-only measurement agrees in shape: 93 files require pg AND read process.env.DATABASE_URL with no HARNESS_DATABASE_ | Live run output: "guarded harness 75 · approved production tool 27 · unguarded WRITE-CAPABLE 67 · unguarded read-only 20 · harness-var, no same-target |
| test_census | notable | **Five test files default to the PRODUCTION API base, not one — known defect #4 names only the first** Each takes an env override but falls back to the deployed service when the variable is unset. Four of the five also read process.env.DATABASE_URL. Two are registered in the gate's PRODUCTION_APPROVED list with a stated reason ("deployment smoke — tar | tests/full_lifecycle_arc.js:47 `const API = (process.env.ARC_BASE // "https://property-spine-api.onrender.com").replace(/\/+$/, "");` (the one CURRENT |
| test_census | minor | **Four tests/e2e files exist but are not in verify_all.sh either — including a second pricing wall** CI runs 9 e2e proofs plus a conditional browser proof. Four other .js files in tests/e2e/ are invoked by nothing (a fifth, leasing_e2e_lib.js, is a library required by the others). tests/e2e/prospect_pricing_wall.e2e.js is a sibling of the agent_pric | tests/e2e/verify_all.sh `step` lines name authority_chain, skyline_unit_type_mapping, skyline_pricing_publication, agent_pricing_wall, leasing_path, l |
| test_census | notable | **verify_source_governance.js stops at the first failure, so a single environmental red hides 22 of 37 gates** Running it on this worktree, gate 15 (conversation_intent_extraction.test.js) exits 3 because the checkout is shallow, and the parent then reports '22 gate(s) NOT RUN' and exits 3. The test itself behaves correctly — it reports NOT PROVEN rather than | Live run at b7720b2: "37 gates · first failure stops the run and sets the exit code" ... "???? NOT PROVEN every extracted line appears verbatim in the |
| test_census | notable | **A whole class of git-history-dependent tests, ten files, whose assertions decay as commits land** These compare the working tree to `HEAD` or to a pinned SHA. The `HEAD` form (pricing_guards_proof.js, renewals_r1_proof.js) inverts the moment the change under test is committed. The `git diff --name-only <old-sha>` form (operator_language_proof.js, | tests/pricing_guards_proof.js:33; tests/renewals_r1_proof.js:26 and :212 (both wrapped in try/catch returning "", so a missing SHA degrades to an empt |
| test_census | informational | **No test file references a deleted module, and no test file fails to parse** Checked because 'referencing deleted modules' was one of the requested dead-test classes. It is not a live problem here. All 292 parse. Every relative `require()` target resolves. Five string literals name files that do not exist, and all five are de | `node --check` over all 292: 0 parse failures. Programmatic resolution of every `require("./.."/"../..")` after comment-stripping: 8 apparent misses,  |
| ci_reality | critical | **(a) CI executes 48 of 313 .js files under tests/ — 37 of the 292 at the top level** verify.yml contains no test logic; its only proof step is `run: ./tests/e2e/verify_all.sh`. That script runs exactly: (1) `node tests/verify_source_governance.js`, whose hardcoded GATES array names 37 files in tests/; (2) `node src/shared/proof_next_ | /tmp/.../main-api/.github/workflows/verify.yml:108-109 `- name: Run every proof` / `run: ./tests/e2e/verify_all.sh`; /tmp/.../main-api/tests/e2e/verif |
| ci_reality | notable | **(a, corroborated) The real CI run on main head b7720b2 printed exactly 18 step lines and finished the whole proof phase in 45 seconds** Run 32383370056 (job 96471774202, main, b7720b2, 2026-08-20) is the run behind every current "CI green" claim at this commit. Its log for the `Run every proof` step is complete and short: source governance gates PASS (4.0s wall clock for all 37 gates | GitHub Actions job 96471774202 log, lines timestamped 2026-08-20T15:00:03.66Z through 15:00:48.81Z; job step `Run every proof` started_at 2026-08-20T1 |
| ci_reality | critical | **(b) All 68 `.db.js` real-Postgres proofs are never executed by CI, and could not run there even if invoked** Every .db.js harness requires HARNESS_DATABASE_URL. verify.yml sets only `E2E_DATABASE_URL: postgres://postgres:spineproof@127.0.0.1:5432/spine_verify` and never sets HARNESS_DATABASE_URL, so these harnesses would refuse rather than run. They are hum | `grep -n HARNESS_DATABASE_URL` in tests/debt_routes_http.db.js:33 "ISOLATION: HARNESS_DATABASE_URL, refused if it matches DATABASE_URL.", tests/interv |
| ci_reality | critical | **(b) All 94 `*_proof.js` files in tests/ are never executed by CI** Not one file whose name ends in `_proof.js` appears in the CI-executed set. This includes the entire Slice 9 leasing-lifecycle proof corpus (slice9_lifecycle_authority_proof.js, slice9_lifecycle_concurrency_proof.js, slice9_lifecycle_read_authority_p | Computed by diffing `fs.readdirSync('tests')` against the 37-entry GATES array in tests/verify_source_governance.js: 94 filenames matching /proof/i an |
| ci_reality | notable | **(b) The two HTTP tests the audit brief flags as fake-pool are also never run by CI** utility_http.test.js and contracted_service_http.test.js — the files the assignment names as CONFIRMED fake-pool-passed-to-real-router — do not appear in the GATES array and are not invoked anywhere on the CI path. teamaccess_sms_delivery.test.js lik | GATES array at tests/verify_source_governance.js:166-169 contains only contracted_service_source_artifact.test.js and contracted_service_ask_spine.tes |
| ci_reality | critical | **(c) One gate file is skipped by CI: tests/gate_person_ingress.js** 13 files match tests/gate_*.js; 12 are in the GATES array. gate_person_ingress.js is not. Its only invoker anywhere in the repo is tests/person_ingress_hostile.db.js, which is itself a .db.js harness CI never runs — so the person-ingress boundary gat | `ls tests/gate_*.js` returns 13 files; GATES array contains 12 of them (tests/verify_source_governance.js:46,53,55,57,62,66,75,81,88,95,103,135); repo |
| ci_reality | critical | **(d) A failed CI run fails the job but does not block a merge — main is unprotected** Mechanically the failure path is sound: verify_all.sh's `step()` sets FAILED=1 on any non-zero child and the script ends `exit $FAILED`; verify.yml's `Run every proof` step has no continue-on-error, so the job goes red. A skipped browser rung is also | tests/e2e/verify_all.sh:31-34 (`step()` sets FAILED=1), :107 `exit $FAILED`, :104 `[ -n "$SKIPPED" ] && echo "  ⚠ NOT RUN: $SKIPPED — this is not a pa |
| ci_reality | notable | **(e) CI provides a real database — a postgres:16 service container, schema built from the real 189-migration chain — and 10 executed tests use it throu** verify.yml declares a `services: postgres: image: postgres:16` container with a pg_isready health check, and exports E2E_DATABASE_URL=postgres://postgres:spineproof@127.0.0.1:5432/spine_verify. verify_all.sh drops and recreates that database, then bu | verify.yml:29-48 (services.postgres, options, env E2E_DATABASE_URL); tests/e2e/verify_all.sh:47-52, :57-82; tests/e2e/apply_migrations.sh:36-72; tests |
| ci_reality | notable | **(b) CI's browser rung covers exactly one page — the resident lease-signing page — and explicitly excludes the operator shell** tests/e2e/resident_signing.browser.js is the only *.browser.js file in the entire API repo, and it is the only browser proof CI runs. It is a strong proof for what it covers: real Chromium, a real tokenized link, clicks that are then asserted against | `find . -name '*browser*' -not -path './node_modules/*'` in main-api returns exactly one file: ./tests/e2e/resident_signing.browser.js; that file's he |
| ci_reality | critical | **(b) The app repository has NO CI at all — 23 browser proofs and 34 test harnesses run only when a human runs them** /tmp/.../main-app has no .github directory. Its 23 *.browser.js proofs — including ask_spine_browser_gate.browser.js, ask_spine_e2e_browser.browser.js, ask_spine_mobile_outage.browser.js, asset_management_shell.browser.js, compliance_browser_proof.br | `ls -a` in /tmp/.../main-app shows no .github entry; `find . -name '*.yml' -o -name '*.yaml'` returns nothing; `ls *.browser.js / wc -l` = 23, `ls *.t |
| ci_reality | minor | **(d, mechanism) The 37 source gates stop at the FIRST failure — a red gate at position 3 means 34 gates are NOT RUN and were never measured** verify_source_governance.js exits on the first non-zero child rather than running the rest, printing `${notRun} gate(s) NOT RUN.` The runner is honest about this — it reports NOT RUN rather than skipping silently — but the consequence for reading a C | tests/verify_source_governance.js:219-229 (`const notRun = GATES.length - results.length;` … `process.exit(r.status)`), :188 "first failure stops the  |
| ci_reality | notable | **(b, context) The reason the .db.js corpus is un-CI-able is documented as a blocking defect, not an oversight** docs/DB_HARNESS_ISOLATION.md opens: "**Status: BLOCKING. No further `.db.js` harness runs against production Neon.**" and "Every `.db.js` harness connects to `process.env.DATABASE_URL` with no override and no guard, and each one's `tx()` helper ends  | docs/DB_HARNESS_ISOLATION.md lines 1-14; tests/verify_source_governance.js:46-47 (gate_harness_isolation.js and its `what` string); verify.yml:29-48 p |

## Final completeness critic

**Headline:** A full real-Postgres + real-server.js + real-Chromium CI suite (`.github/workflows/verify.yml` → `tests/e2e/verify_all.sh`) runs on every push and passed 17/17 on current main `b7720b2` — the strongest evidence rung in the repo, and no survey wave recorded that it exists.

**Method:** SEARCHED: every top-level file in both worktrees (main-api b7720b2, main-app c6769ba) individually; `.github/`, `.claude/`; `package.json` + `package-lock.json` in both repos (app has none); a repo-wide `process.env.X` enumeration across `src/`, `server.js`, `migrations/` compared against `.env.example` and `docs/deployment.md`; `README.md` (both repos), `migrations/README.md`, `docs/deployment.md`, `docs/domains.md`, `docs/DB_CONNECTION_INVENTORY.md`, `CLAUDE.md`; GitHub Actions run history and one full job log for property-spine-api. EXECUTED: `.claude/hooks/session_start.sh`; the staleness command CURRENT_STATE.md prints; `node tests/verify_source_governance.js`; `npm test`; four `tests/*_proof.js`; `node tests/ai_leasing_strategy_runtime_proof.js`; `git apply --check` on the root patch; `main-app/preview_build.js`; `main-app/run_harnesses.sh`; and `migrations/migrate.js` in both veri

| Severity | Finding | Evidence |
|---|---|---|
| critical | **The documented from-scratch schema build fails at migration 083 and reports a falsehood about what it did** `migrations/migrate.js --apply` — the exact command the tool's own refusal message prints — cannot build the schema from an empty database. It stops at `083_terms_review_and_packet_versioning.sql` with `duplicate key value violates unique constraint  | Ran against a fresh local PostgreSQL 16 (`postgres://root@127.0.0.1:5432/spine_freshtest`): `MIGRATION_RELEASE=1 EXPECTED_LEDGER_CEILING=000 node migr |
| critical | **A real CI pipeline exists, runs on every push, and passed 17/17 on current main — unlisted by every wave** `.github/workflows/verify.yml` provisions a PostgreSQL 16 service container and runs `tests/e2e/verify_all.sh`, the same file a developer runs. On current main `b7720b2` it passed every step: schema built from the real migration chain, three fixtures | Run id 32383370056, job 96471774202, head_sha `b7720b273fc058f9ec20aee224503202e07980cd`, completed 2026-08-20T15:00:48Z, conclusion success. Job log: |
| critical | **KNOWN LIVE DEFECT #2 is stale — the line it cites is fixed; the falsehood survives at two other lines it does not cite** Defect #2 cites `docs/deployment.md:51` for "runs any unapplied migrations." That line now reads "→ prestart: node migrations/migrate.js   (VERIFIES the schema — applies nothing)" with an explicit correction at :58. It was fixed by commit 6c975ab (20 | `docs/deployment.md:51,58,179`; `README.md:19,93`; `git show 6c975ab -- README.md` shows only the line-19 hunk; `git log 6c975ab..HEAD -- README.md` a |
| notable | **`migrations/README.md` is a migration-001/002-era document still instructing hand-run production migrations** It describes `migrate.js` as "the apply script. It looks at the files here, checks the ledger, and runs whatever hasn't run yet. Safe to run as many times as you want", gives a section "**Against PRODUCTION (only when you mean it):** DATABASE_URL=\"< | `migrations/README.md` lines ~33-56 and the "About migration 002" section; `migrations/migrate.js:171` `const APPLY = process.argv.includes("--apply") |
| notable | **51 of the 65 environment variables the code actually reads appear in no configuration template** Enumerating `process.env.X` across `src/`, `server.js` and `migrations/` yields 65 distinct names. `.env.example` documents 15 of them; `docs/deployment.md` — the doc README points to for environment variables — documents 14. Undocumented in both: ei | 65 names from `grep -rhoE "process\.env\.[A-Z0-9_]+" src/ server.js migrations/ / sort -u`; 15 from `.env.example`; `comm` against `docs/deployment.md |
| notable | **The app repo has no package.json, no CI and no deploy config, and its README documents a file that has never existed** `main-app/README.md` is titled "# Seed Adapter — README" and documents `seed_adapter.js`, `solo_4233_seed.json` and `SEED_ADAPTER_README.md` ("this file"). None of the three is in the tree, and `git log --all -- seed_adapter.js` is empty — `seed_adap | `main-app/README.md` (last touched 563e96d, 2026-08-02); `git log --oneline --all -- seed_adapter.js` → empty; existence checks for all seven config f |
| notable | **The app's deployed-build identity stamp is 128 commits stale** `main-app/build-info.js` sets `window.__PS_BUILD` and its own header states the convention: "The stamp is therefore ONE COMMIT BEHIND by construction: after each release commit, a follow-up 'stamp' commit writes that release's SHA here... The post-de | `main-app/build-info.js`; `git merge-base --is-ancestor 8352a8b4... HEAD` → yes; `git rev-list --count 8352a8b4...HEAD` → 128; `grep -c "build-info.js |
| notable | **Four test harnesses are pinned to `/workspace/property-spine-app`, a path that exists nowhere, and all four die** `tests/operator_language_proof.js:35`, `tests/work_proof_photo_proof.js:51`, `tests/release_candidate_proof.js:48` and `tests/unit_turn_page_proof.js:21` hardcode the absolute path `/workspace/property-spine-app`. Running each: all four exit 1 with ` | Ran all four from `/tmp/.../main-api`; exit codes all 1, identical ENOENT. `ls -d /workspace` → No such file or directory. Grep for `/workspace` acros |
| notable | **`npm test` runs no tests, and the gate behind it stops at the first failure leaving 22 of 37 gates unrun** `package.json`: `"test": "echo \"source-governance gates passed; database proofs run separately — see docs/DB_HARNESS_ISOLATION.md\" && exit 0"`. The script body executes no test; the only real work is `pretest` → `node tests/verify_source_governance | `package.json` scripts block; `node tests/verify_source_governance.js` → "37 gates · first failure stops the run", "???? NOT PROVEN every extracted li |
| notable | **The signed-in operator console loads two third-party CDN scripts with no subresource integrity** `main-app/index.html` carries `<script src="https://cdn.jsdelivr.net/npm/xlsx@0.18.5/dist/xlsx.full.min.js">` and `<script src="https://cdn.plaid.com/link/v2/stable/link-initialize.js">`. `grep -c "integrity="` over the whole file returns 0 — no SRI  | `grep -oE '<script src="https://[^>]*>' main-app/index.html` → the two CDN tags; `grep -c "integrity=" main-app/index.html` → 0; `du -cb index.html` p |
| minor | **README.md and docs/domains.md describe 11 of the 20 `src/` domains — including omitting the entire Asset Management door** `src/` holds 20 directories: agent, applications, asset, comms, conversation, entity, evidence, governance, identity, leasing, maintenance, meeting_evidence, money, obligations, onboarding, release0, shared, surfaces, technician, tenancy. `README.md` | `ls -d src/*/` → 20; `README.md` layout block lists agent, applications, comms, identity, leasing, maintenance, money, onboarding, shared, surfaces, t |
| minor | **`README.md:31` calls root `schema.sql` a "Snapshot of the full schema"; it covers 17 tables against 270** `schema.sql` (21 KB, last touched 2026-08-03 at commit 67442a5) contains 17 `create table` statements: bids, comm_events, documents, events, inventory, leases, ledger_entries, obligations, persons, properties, property_controls, spaces, turnovers, un | `README.md:31` "schema.sql            — Snapshot of the full schema (reference only)"; `grep -ioE "create table (if not exists )?[a-z_]+" schema.sql / |
| minor | **Two more missing migration numbers nobody has named: 138 and 139** The complete set of missing numbers between 000 and ledger ceiling 187 is 125, 138, 139, and 141–149 — twelve numbers, 176 files present. CURRENT_STATE.md records "10 consecutive missing migration numbers — 141 through 149" and, separately, 125. 138  | `ls migrations/*.sql` prefixes vs `seq 0 187`; missing = `125 138 139 141 142 143 144 145 146 147 148 149`; `ls migrations/13*.sql` jumps 137 → 140; ` |
| minor | **A duplicate pre-gate migration applier with none of the release guards still sits at the API repo root** `/migrate.js` (109 lines, committed once at 67442a5, 2026-08-03) is the old auto-applying runner: no `MIGRATION_RELEASE`, no `EXPECTED_LEDGER_CEILING`, no `EXPECTED_SHA`, no verify mode. Its own header instructs "Against production (be careful): DATA | `/migrate.js` header lines 4-27 and `:32` `const MIGRATIONS_DIR = __dirname`; `grep -n "MIGRATION_RELEASE/EXPECTED_LEDGER_CEILING/EXPECTED_SHA/--apply |
| minor | **An ops script in `src/` fetches rent-roll truth from a public raw GitHub URL that now returns 404** `src/onboarding/import_rent_roll_truth.js` is a Class 3 one-time operations script whose own header calls itself `tools/import_rent_roll_truth.js` and whose usage lines give that path — the file is not at that path, so the documented command fails. I | `src/onboarding/import_rent_roll_truth.js:2, :15-16, :23-24, :45-51`; `curl -s -o /dev/null -w "%{http_code}"` on that URL → 404; `git ls-tree -r --na |
| minor | **A 26 KB patch file at the API root no longer applies, and a 18/18-green test reads it as its evidence base** `ai-leasing-strategy-runtime-activation.patch` (26,749 bytes) is rejected by `git apply --check` against current main for `src/leasing/leasingleads.js`, `src/agent/agent.js` and `src/applications/applicationSubmission.js`. It no longer applies becaus | `git apply --check ai-leasing-strategy-runtime-activation.patch` → "error: src/leasing/leasingleads.js: patch does not apply" (and two more); `src/lea |
| minor | **`docker-compose up`, documented as a supported local path, cannot start the API on a fresh volume** `docs/deployment.md:166-187` documents `docker-compose up` as the local Postgres path. Two independent blockers, both observed. First, `migrations/migrate.js:173` hardcodes `new Client({ connectionString: url, ssl: { rejectUnauthorized: false } })`;  | `docs/deployment.md:166-187`; `docker-compose.yml` (no ssl keys); `migrations/migrate.js:173`. Observed against a real local PostgreSQL 16: over a non |
| minor | **CURRENT_STATE.md's own staleness check contradicts the SessionStart hook that reads the same file** CURRENT_STATE.md:65 instructs `git diff --name-only 77f93f5..HEAD -- src/ migrations/ server.js` and rules "Empty → every row below is current. Non-empty → rows touching those paths are *navigation only* until reverified." It returns 33 files, so by  | `docs/CURRENT_STATE.md:41,65-68`; ran the command → 33 paths (migrations 012 and 182-187, server.js, 25 files under src/); `git diff --name-only bcd30 |
| informational | **The app repo's sanctioned suite is green today: 34 harnesses, 1435 assertions, 0 failed — and 23 browser harnesses are outside it** Nobody had recorded a run result for the app suite. `./run_harnesses.sh` completes with "34 harnesses · 1435 passed · 0 failed · 0 red / suite green." These are text and DOM-string assertions over the committed `index.html` and door files — no databa | Ran `/tmp/.../main-app/run_harnesses.sh`; final lines "34 harnesses · 1435 passed · 0 failed · 0 red", "suite green."; `run_harnesses.sh:36-37` `shopt |
| informational | **CURRENT_STATE.md's `tools/` headline count of 16 understates the directory by roughly 76 files** `docs/CURRENT_STATE.md:313` says "`tools/` (16 found)". The directory holds 50 top-level entries and 92 `.js` files across 17 subdirectories (activation, data, debt, equity, harness, identity, release, release0, repair, savepoint, scale, step12, step | `docs/CURRENT_STATE.md:313`; `ls tools/ / wc -l` → 50; `find tools -name "*.js" / wc -l` → 92; `docs/current-state-build/05_WAVE2_RESULTS.md:171-183`  |
| informational | **CI's `npm ci // npm install` fallback would silently mask a lockfile desync; the Dockerfile ignores the lockfile outright** `.github/workflows/verify.yml` installs with `npm ci // npm install`, so a `package-lock.json` that stops matching `package.json` produces a green build on resolved-at-build-time versions rather than a red one. Today the lock is in sync — all 10 depe | `.github/workflows/verify.yml` step "Install dependencies": `npm ci // npm install`; `Dockerfile:7` `RUN npm install --omit=dev`; lockfile cross-check |
